{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "JHYY: Run Current File",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "cwd": "${fileDirname}",
            "console": "integratedTerminal",
            "preLaunchTask": "JHYY: Compile"
        }
    ]
}
