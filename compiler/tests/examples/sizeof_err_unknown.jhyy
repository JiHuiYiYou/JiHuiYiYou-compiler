// v1.3.3 negative test — sizeof(unknown_type) → sema error.
// resolve_type_node returns NULL → "unknown type 'UnknownType'" reported.
// v1.7.0 Stage 1: EXPECT-ERROR replaces old //EXPECT:-1.
fn main_jhyy() -> i32 {
    return sizeof(UnknownType) as i32;
}
// EXPECT-ERROR: "unknown type"