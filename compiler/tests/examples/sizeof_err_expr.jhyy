// v1.3.3 negative test — sizeof requires type name, not expression.
// parse_type rejects binary expressions → parser error.
// v1.7.0 Stage 1: EXPECT-ERROR replaces old //EXPECT:-1 (cleaner runner contract).
// EXPECT-ERROR: "expected type name"
fn main_jhyy() -> i32 {
    return sizeof(1 + 2) as i32;
}