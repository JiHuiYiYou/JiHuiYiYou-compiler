extern fn printf(fmt: *u8, val: i32) -> i32;

fn sub_one(p: *i32) {
    let x = *p - 1;
    *p = x;
}

fn main_jhyy() -> i32 {
    let mut val = 42;
    sub_one(&val);
    printf("val: %d\n", val);
    0
}
