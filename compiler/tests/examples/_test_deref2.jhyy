extern fn printf(fmt: *u8, val: i32) -> i32;

fn decrement(p: *i32) {
    let x = *p;
    let y = 1;
    let z = x - y;
    *p = z;
}

fn main_jhyy() -> i32 {
    let mut val = 42;
    decrement(&val);
    printf("val: %d\n", val);
    0
}
