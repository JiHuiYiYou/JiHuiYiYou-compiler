extern fn printf(fmt: *u8, val: i32) -> i32;

fn decrement(p: *i32) {
    *p = *p - 1;
}

fn main_jhyy() -> i32 {
    let mut x = 10;
    decrement(&x);
    printf("x after decrement: %d\n", x);
    0
}
