extern fn printf(fmt: *u8, val: i32) -> i32;

fn test(p: *i32) -> i32 {
    let x = *p + 1;
    x
}

fn main_jhyy() -> i32 {
    let mut val = 42;
    let r = test(&val);
    printf("r: %d\n", r);
    0
}
