extern fn puts(s: *u8) -> i32;
extern fn printf(fmt: *u8, val: i32) -> i32;

fn main_jhyy() -> i32 {
    let mut seed = 12345;
    seed = (seed * 1103515245 + 12345) & 0x7fffffff;
    printf("rand: %d\n", seed);
    0
}
