// EXPECT: 42
// v1.3.7: pattern binding `Some(v) => v` extracts payload

type Option = enum { Some(i32), None }

fn main_jhyy() -> i32 {
    match Option::Some(42) {
        Option::Some(v) => v,
        Option::None    => 0,
    }
}
