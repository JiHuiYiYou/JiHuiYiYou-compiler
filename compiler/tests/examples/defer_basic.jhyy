// SKIP: v1.8 — defer codegen silent crash (W-059)
// EXPECT: 0
// v1.7.3 patch A1 (deferred to v1.8): defer 基本触发测试 (v1.3.6 commit 169759c
// ship 后 0 test 缺漏). 验证函数返回时 defer 触发。
//
// v1.7.3 ship 期间发现: defer codegen 路径 silent crash (EXIT=0 但不产出 .il).
// v1.3.6 ship 时 0 test 验证 accept path, bug 一直未发现. W-059 🟡 DEFERRED v1.8.
// 本 test 暂保留为 v1.8 真修后启用 (regress skip).

extern fn sink(v: i32) -> i32;

fn main_jhyy() -> i32 {
    let mut log: i32 = 0;
    defer sink(log);
    log = 42;
    return 0;
}