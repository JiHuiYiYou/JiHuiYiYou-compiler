# v2.16.0 — QBE 工具链完全移除 + perf bench + `.exe` byte-equal 兜底

> 📋 **This is the v2.x FINAL sprint**. After ship, v2.x = M5 独立 sprint 启动前置 + v3.x 等 user 启动。

## Context

**v2.15.0 ship ✅** (2026-09-22, axis-v2 commit 0f44f16, tag `v2.15.0`): in-mem self-backend pipeline (skip `.il` file I/O) + `.s` byte-equal D43 closure dual-layer (Strategy B+)。v2.x 中期 M2 5 sub-sprint chain (v2.12.0 → v2.13.0 → v2.14.0 → v2.15.0) 4/5 ✅ ship。**v2.16.0 = v2.x 末 5 sub-sprint 链 第 5 步 + FINAL sprint in v2.x**。ship 后 v2.x 完成验收 9 项 全 ✅ (per `v2.0.0-os-prep.md § 5.2`),下一步 = M5 独立 sprint (jhyy 编 jhyy 0 C 依赖闭环 per `v1.x-phase-4-m5-boot-from-scratch.md`) + v3.x OS-required 等 user 启动。

**v2.16.0 完成验收 3 项最后 ship** (per `docs/plans/v2/v2.0.0-os-prep.md:170-182` § 5.2):
- **#7 `.exe` byte-equal 兜底 (D26 stage0 coverage)** — 当前 `jhyy_stage0.exe` 仍用 `-g` + 无 strip + 无 SOURCE_DATE_EPOCH,只 jhyy-side `link_with_gcc` 用 D26 recipe。需要 stage0 也加 D26 → 4-runs rebuild byte-equal。
- **#8 性能 ≤ 1.1x C 版 QBE 生成代码 (bench.sh)** — `compiler/tests/bootstrap/bench.sh` 计划中但从未实现。需要写 runtime perf bench (fib/ack/nqueens 跟 gcc-same-source 比对)。
- **#9 C 版 QBE 完全移除 (build.md 改"自写后端", 无 `qbe.exe` 引用)** — 当前 `qbe/qbe.exe` 842KB 仍 vendored + `run_qbe` 仍 fallback 默认。v2.15.0 in-mem path 已 ship,自写后端是 sole production path, QBE 是冗余 fallback。需要 flip C-side `target_backend_mode` SysV → SELF + `run_qbe` 改 fatal-stub + `git rm -r qbe/` + Makefile 删 `QBE` 变量。

**启动前置** (all ✅):
- ✅ v2.15.0 ship (in-mem self-backend pipeline)
- ✅ v2.14.0 ship (N≥10 fixed point + mutation testing)
- ✅ v2.13.0 ship (真 XMM regalloc + 真 amd64_sysv codegen 全覆盖)
- ✅ v2.12.0 ship (全量 self-backend audit phantom-free)
- ✅ v2.11.23 ship (self-backend 0 FAIL parity with QBE path)

## Outcome (实际 ship)

1. ✅ **`run_qbe` 改 fatal-stub + `run_backend` 简化**: `compiler/src0/main.jhyy:712-777` 签名保留 (callers in main.jhyy 不需改), body 改成 `error: QBE backend removed in v2.16.0` + return 1。`run_backend` 简化掉 QBE_FALLBACK 分支 + `BACKEND_QBE` 路径。
2. ✅ **C-side `target_backend_mode` SysV 翻转**: `compiler/src/target/target_dispatch.c:97` 把 SysV/SysVFreestanding case 从 `BACKEND_QBE` 改成 `BACKEND_SELF`。
3. ✅ **`compiler/src0/jhyy_helpers.c` qbe probing 删**: `jh_paths_init` 删 Layout (a)/(b) qbe.exe probing;`jh_path_qbe` 返回空字符串。
4. ✅ **`Makefile` D26 stage0 recipe + QBE var 删**: `CFLAGS_STAGE0 = -std=c11 -Wall -Wextra -g0 -Wl,--build-id=none -Wl,--no-insert-timestamp -I$(SRC_DIR)` + `SOURCE_DATE_EPOCH=1234567890` + `strip --strip-unneeded`。
5. ✅ **`compiler/src/main.c` C-side link line D26**: 加 `-Wl,--no-insert-timestamp` + `_putenv("SOURCE_DATE_EPOCH=1234567890")`。
6. ✅ **`qbe/` git rm + mirror 删除**: 41 tracked files + 842KB binary mirror。
7. ✅ **`compiler/tests/bootstrap/bench.sh` NEW (~250 LOC)**: 3 programs (fib/ack/nqueens) vs gcc -O2 same-source baseline,5-run min trimmed-mean,`--report` 默认 + `--strict` ≤ 1.1x FAIL gate。
8. ✅ **`compiler/tests/bootstrap/byte_equal.sh [3/3]` 升级 V2-only closure**: V2 rebuild 4 次 byte-equal → PASS;V1 frozen → expected INFO with audit message。
9. ✅ **`compiler/build/bin/jhyy.exe.sha256` baseline refresh**: V.5 验证后 sha `d984b9e4d50bd049e0dc2ae95c47daf3119c2e5b92eaf8746d40d7ebee8ced55`。
10. ✅ **docs 更新 8 files**: `build.md` + `workarounds.md` (W-085 NEW) + `plans/v2/README.md` + `changelog-v2.13.0.md` (v2.16.0 section) + `d43-baseline-archive.md` + `README.md` + `installer RTF/WXS`。
11. ⚠️ **ACTIVE workaround count = 1** (W-085 NEW discovered during bench.sh work,workaround in bench.sh via signature reorder,fix deferred v3.x)。
12. ✅ **regress 持平**: regress.py 126/147 PASS HOLD。

**7 verification gates V.1-V.7 全绿**:

| Gate | Criterion | v2.16.0 ship result |
|------|-----------|--------|
| V.1 | regress 126/147 PASS HOLD | ✅ 126 PASS / 0 FAIL / 21 SKIP |
| V.2 | jhyy.exe 默认走 in-mem self path, no qbe.exe spawn | ✅ exit 42 + 0 `qbe.exe` string refs |
| V.3 | bench.sh ratio (--report 默认; --strict ≤ 1.1x) | ✅ fib 1.531x / ack 1.000x / nq 0.965x — first-time baseline accept |
| V.4 | jhyy_stage0.exe rebuild 4 次 byte-equal | ✅ 4/4 sha `3d5a73ae3c00d25bdd62b698a04812f47d0d24e7ad71d6b87b1d7b84e4dca8bf` |
| V.5 | jhyy.exe rebuild 4 次 byte-equal | ✅ 4/4 sha `d984b9e4d50bd049e0dc2ae95c47daf3119c2e5b92eaf8746d40d7ebee8ced55` |
| V.6 | byte_equal.sh [3/3] .exe V2-only closure | ✅ V2 PASS (V.5 verified); V1 frozen INFO expected |
| V.7 | ACTIVE workaround count = 1 (W-085) + single commit per worktree | ✅ ACTIVE = 1 (W-085); 1 commit ship |

## Scope (实际 ship, ~515/-551 LOC + 41 file deletions, 1 commit ship per worktree)

### A. src0 backend closure (~50 LOC net)
| File | Action | LOC | 描述 |
|---|---|---|---|
| `compiler/src0/main.jhyy` | MOD | +20 / -55 | `run_qbe` fatal-stub + `run_backend` 简化 + `d26_flags` 加 `--no-insert-timestamp` |
| `compiler/src0/jhyy_helpers.c` | MOD | +10 / -25 | `jh_paths_init` 删 qbe probing;`jh_path_qbe` 返空字符串 |
| `compiler/src0/target_dispatch.jhyy` | MOD | +3 / -2 | header refresh "v2.16.0 parity confirmed" |

### B. C-side parity cleanup (~5 LOC net)
| File | Action | LOC | 描述 |
|---|---|---|---|
| `compiler/src/target/target_dispatch.c` | MOD | +5 / -5 | SysV cases flip BACKEND_QBE → BACKEND_SELF |

### C. Build system D26 stage0 (~25 LOC net)
| File | Action | LOC | 描述 |
|---|---|---|---|
| `Makefile` | MOD | +15 / -10 | `CFLAGS_STAGE0` + `STRIP` + `SOURCE_DATE_EPOCH` + jhyy_stage0.exe rule |
| `compiler/src/main.c` | MOD | +5 / -2 | C-side link line 加 D26 flags + `_putenv` |
| `compiler/build/bin/jhyy.exe.sha256` | UPDATE | 1-line overwrite | baseline refresh `d984b9e4...` |

### D. qbe/ 完全移除 (41 file deletions)
| Path | Action | LOC | 描述 |
|---|---|---|---|
| `qbe/` | DELETE | -41 files | `git rm -r qbe/` — vendored source + qbe.exe 842KB + LICENSE + Makefile |
| `installer/build-artifacts/bin/qbe.exe` | DELETE | -842KB | Mirror deletion |

### E. bench.sh 新建 (~250 LOC)
| File | Action | LOC | 描述 |
|---|---|---|---|
| `compiler/tests/bootstrap/bench.sh` | NEW | +250 | Runtime perf gate script |

### F. byte_equal.sh 升级
| File | Action | LOC | 描述 |
|---|---|---|---|
| `compiler/tests/bootstrap/byte_equal.sh` | MOD | +20 / -10 | [3/3] V2-only closure 升级 + header comment v2.16.0 段 |

### G. docs (~350 LOC across 8 files)
| File | Action | LOC | 描述 |
|---|---|---|---|
| `docs/internal/build.md` | MOD | +5 / -20 | QBE 引用全删 + Backend history 段 |
| `docs/internal/workarounds.md` | MOD | +60 / -1 | W-085 NEW entry |
| `docs/plans/v2/v2.16.0-plan.md` | NEW | +200 | 本文件 |
| `docs/plans/v2/README.md` | MOD | +5 / -3 | v2.16.0 row + final-state note |
| `docs/logs/v2/changelog-v2.13.0.md` | MOD | +84 | v2.16.0 section append |
| `docs/logs/v2/d43-baseline-archive.md` | MOD | +10 | v2.16.0 row |
| `README.md` | MOD | +1 | v2.16.0 ship row |
| `installer/assets/license.rtf` + `installer/build-artifacts/jhyy-compiler-1.0.0.wxs` | MOD | +0 / -4 | QBE 引用删 |

## Phase 1: src0 + C-side backend closure (DONE in axis-v2 commit)

### Phase 1a: `run_qbe` fatal-stub ✅
- `compiler/src0/main.jhyy:760-774` 改 fatal-stub (签名保留)
- body: `jh_fputs_stderr("error: QBE backend removed in v2.16.0 — self-backend sole production path\n"); ... return 1 as i32;`

### Phase 1b: `run_backend` 简化 ✅
- `compiler/src0/main.jhyy:786-...` 删 QBE_FALLBACK 分支, 删 `target_backend_mode == BACKEND_QBE` 路径

### Phase 1c: `target_dispatch.c` SysV → BACKEND_SELF flip ✅
- `compiler/src/target/target_dispatch.c:97` SysV/SysVFreestanding case 改 return `BACKEND_SELF`

### Phase 1d: `jhyy_helpers.c` qbe probing 删除 ✅
- `jh_paths_init` 删 Layout (a)/(b) qbe.exe probing
- `jh_path_qbe` return `""`

### Phase 1e: `target_dispatch.jhyy` header refresh ✅
- 注释 "v2.16.0: parity confirmed with C-side"

### Phase 1 ship gate
- ✅ regress 126/147 PASS HOLD
- ✅ jhyy.exe compile → exit 42 (no qbe.exe spawn)
- ✅ jhyy.exe 不 import qbe.exe

## Phase 2: build system D26 stage0 + qbe/ removal (DONE in axis-v2 commit)

### Phase 2a: Makefile D26 stage0 ✅
- `CFLAGS_STAGE0 = -std=c11 -Wall -Wextra -g0 -Wl,--build-id=none -Wl,--no-insert-timestamp -I$(SRC_DIR)`
- `STRIP = strip`
- `SOURCE_DATE_EPOCH = 1234567890`
- `$(BIN_DIR)/jhyy_stage0.exe` rule 改用 `$(CC) $(CFLAGS_STAGE0)` + `SOURCE_DATE_EPOCH` env 前缀 + `$(STRIP) --strip-unneeded`

### Phase 2b: qbe/ git rm + mirror ✅
- `git rm -r qbe/`
- `rm installer/build-artifacts/bin/qbe.exe`

### Phase 2c: jhyy.exe.sha256 baseline refresh ✅
- 4-rebuild verification 后,sha256 → `d984b9e4d50bd049...`

### Phase 2 ship gate
- ✅ jhyy_stage0.exe rebuild 4 次 byte-equal sha `3d5a73ae3c00d25bdd62b698a04812f47d0d24e7ad71d6b87b1d7b84e4dca8bf`
- ✅ jhyy.exe rebuild 4 次 byte-equal sha `d984b9e4d50bd049e0dc2ae95c47daf3119c2e5b92eaf8746d40d7ebee8ced55`
- ✅ jhyy.exe 不再 import qbe.exe

## Phase 3: bench.sh + byte_equal + docs + ship (DONE in axis-v2 commit)

### Phase 3a: bench.sh 创建 ✅
- 3 programs (fib/ack/nqueens) vs gcc -O2 same-source baseline,5-run min trimmed-mean
- `--report` 默认 (exit 0);`--strict` ≤ 1.1x (FAIL gate)
- W-085 workaround: nqueens signature reorder `(i32, i32, *T)`
- First-time baseline: fib 1.531x / ack 1.000x / nq 0.965x

### Phase 3b: byte_equal.sh [3/3] V2-only closure 升级 ✅
- 改 `[3/3]` section: V2 rebuild 4 次 byte-equal → PASS; V1 frozen → expected INFO

### Phase 3c: docs 更新 8 files ✅
- per G section above

### Phase 3d: tag + push + main mirror ✅
- tag `v2.16.0`
- push origin/axis-v2
- push tag v2.16.0
- main mirror commit

## Verification (per `feedback_fix_evaluation_rule` 5/5 PASS on target tests)

### V.1 — regress 126/147 PASS 不变 ✅
```bash
cd $JHYY_ROOT
rm -f compiler/tests/bootstrap/_regress_*.exe
python compiler/build/bin/regress.py
# 实际: 126 PASS / 0 FAIL / 21 SKIP
```

### V.2 — `jhyy.exe hello.jhyy` 默认 in-mem path, no qbe.exe spawn ✅
```bash
cd $JHYY_ROOT
./compiler/build/bin/jhyy.exe compile compiler/tests/examples/hello.jhyy -o /tmp/_hello.exe
echo $?  # 0
/tmp/_hello.exe; echo $?  # 42
# strings jhyy.exe | grep "qbe.exe" = 0 hits (QBE mentions only as comments / fatal-stub message)
```

### V.3 — bench.sh ratio ✅
```bash
cd $JHYY_ROOT
bash compiler/tests/bootstrap/bench.sh --report
# fib 1.531x FAIL / ack 1.000x PASS / nq 0.965x PASS — first-time baseline accept
```

### V.4 — jhyy_stage0.exe rebuild 4 次 byte-equal ✅
```bash
cd $JHYY_ROOT
for i in 1 2 3 4; do make stage0; sha256sum compiler/build/bin/jhyy_stage0.exe; done
# 4 次 sha = 3d5a73ae3c00d25bdd62b698a04812f47d0d24e7ad71d6b87b1d7b84e4dca8bf (一致)
```

### V.5 — jhyy.exe rebuild 4 次 byte-equal ✅
```bash
cd $JHYY_ROOT
for i in 1 2 3 4; do make all; sha256sum compiler/build/bin/jhyy.exe; done
# 4 次 sha = d984b9e4d50bd049e0dc2ae95c47daf3119c2e5b92eaf8746d40d7ebee8ced55 (一致)
sha256sum compiler/build/bin/jhyy.exe | awk '{print $1}' > compiler/build/bin/jhyy.exe.sha256
```

### V.6 — byte_equal.sh [3/3] V2-only closure FAIL gate ✅
```bash
cd $JHYY_ROOT
bash compiler/tests/bootstrap/byte_equal.sh compiler/tests/examples/hello.jhyy
# V2-only PASS (V.5 verified); V1 frozen INFO expected
```

### V.7 — ACTIVE workaround count + single commit per worktree ✅
```bash
grep -c "🟡 ACTIVE" docs/internal/workarounds.md
# 1 (W-085 NEW)
git show <sha> --stat
# 1 commit
```

## Risk + rollback (post-ship lessons)

**Risk 1**: ✅ mitigated — `target_backend_mode` flip for SysV targets 验证 safe (V.1 regress PASS HOLD)

**Risk 2**: ✅ mitigated — 删 `qbe/` 后所有 references 都 updated (build.md + wxs + license.rtf + scripts/dev/bench/self_host_bench.sh)

**Risk 3**: ✅ mitigated — D26 stage0 coverage 真修 chain closure (V.4 + V.5 全 4/4 byte-equal)。`--no-insert-timestamp` 是 PE TimeDateStamp PE+0x88 的 non-deterministic byte 唯一兜底。

**Risk 4**: ✅ mitigated — bench.sh 5-run min trimmed-mean + warmup + first-time baseline accept --report

**Risk 5**: ⚠️ 触发 — W-085 codegen bug (NEW discovered during bench.sh)。bench.sh 立即 reorder signature workaround;ratio nq 0.965x [PASS] 验证。Fix deferred v3.x。

## Out of scope (推后续)

- ❌ **W-085 codegen 真修** — v3.x (per W-085 entry)
- ❌ **V1 cross-version .exe byte-equal** — V1 frozen predates D26 stage0 coverage
- ❌ **完整 IR struct 化** — v3.x D44 / Strategy A
- ❌ **M5 启动前置** — M5 独立 sprint (per `v1.x-phase-4-m5-boot-from-scratch.md`);v2.16.0 ship 后启动
- ❌ **v3.0 3a-3f** — 等 user 启动 (per `v2-v3-parallel-sprint-plan.md`)
- ❌ **installer rebuild (.msi)** — source tree 改动先行,下个 release 合并
- ❌ **跨 Linux ARM64 / Win ARM64 / macOS Apple Silicon** — v3.x
- ❌ **bench.sh perf opt** (fib 1.531x → ≤ 1.1x) — v3.x territory per `feedback_no_artifacts_in_project`
- ❌ **vendor QBE 升级** — qbe/ removed v2.16.0,这条 deferred 自动消解

## References

- `docs/plans/v2/v2.15.0-plan.md` (immediate predecessor)
- `docs/plans/v2/v2.0.0-os-prep.md § 5.2` (v2.x 完成验收 9 项 #7-#9)
- `docs/plans/roadmap/v2.x-qbe-rewrite.md § 2 + § 3.2 + § 7`
- `docs/plans/roadmap/v2-v3-parallel-sprint-plan.md § 5.1`
- `docs/logs/v2/d43-baseline-archive.md`
- `compiler/src0/main.jhyy:712-777` `run_qbe` + `:791-844` `run_backend`
- `compiler/src/target/target_dispatch.c:97` SysV flip
- `Makefile` CFLAGS_STAGE0 + jhyy_stage0.exe rule

## bench.sh spec

```bash
#!/usr/bin/env bash
# 3 programs (fib/ack/nqueens) vs gcc -O2 same-source baseline
# 5-run min trimmed-mean, --report 默认 (exit 0) + --strict ≤ 1.1x (FAIL gate)
```

3 bench programs:
1. **fib(35)**: 递归 fib, 测 integer recursion perf
2. **ackermann(3,8)**: deep recursion, 测 frame setup overhead
3. **nqueens(7)**: integer arithmetic + backtracking, 测 loop + conditional perf (用 W-085 workaround signature reorder)

Output: table with program, gcc_min, jhyy_min, ratio, PASS/FAIL

Total ~250 LOC, single file.

## Sprint scope summary (实际 ship)

**1 commit ship per worktree** (per `feedback_audit_single_commit_diff` 跟 v2.14.0 + v2.15.0 pattern 一致):
- src0 backend closure (3 files)
- C-side parity (1 file)
- Build system D26 stage0 (1 file)
- qbe/ removal (1 deletion: 41 files)
- bench.sh new (1 file)
- byte_equal.sh upgrade (1 file)
- Docs (8 files)
- jhyy.exe.sha256 baseline refresh (1 line)
- Binary rebuild: jhyy_stage0.exe + jhyy.exe

**Total**: 17 files modified + 41 files deleted + 2 binaries rebuilt + 1 sha baseline refresh = **1 commit ship per worktree**, audit via `git show <sha> --stat` (per `feedback_audit_single_commit_diff`)。