# v2.13.1 → v2.13.9 — 9 ACTIVE workaround 真修 + ship 📋 planned (2026-09-19)

## Context

**v2.13.0 ship ✅** (2026-09-19, tag `v2.13.0`, commit `5ddbb2c`): 自写后端真 XMM regalloc + 真 amd64_sysv codegen 全覆盖 + amd64_win_freestanding 真 E2E OVMF 5/5 PASS,**W-074.6 PARTIAL → FULL CLOSED** (累计 W-074.6 family closed ~900+ LOC)。

**Post v2.13.0 ship 仍剩的真 ACTIVE workaround 调研 (per workarounds.md audit 2026-09-19)**:

| ID | workarounds.md 实际状态 | 备注 |
|----|-----------------------|------|
| W-074.6.1 | 🟡 ACTIVE (per workarounds.md line 5545-5549, 4 sub-bugs): (a) struct pass-by-value multi-field emit_copy 1-to-1 gap; (b) stack-slot-reuse in other emit paths (跟 v2.11.11 bit_pack 同 family extend emit_call/emit_mem); (c) extsw silent-skip (v2.11.12 已修部分, 仍残留少数 sub-bug); (d) emit_ret 不 `mov %t1 → %eax` 后 `ret` (pre-existing W-074.6 范围) | v2.13.1 真修 |
| W-074.7.9 self-backend 侧 | 🟡 ACTIVE (QBE 侧 v2.11.9 ✅ RESOLVED, self-backend 侧 仍 ACTIVE due to W-074.6 family — idiv 无 sign-extend prefix residual + remd op silent skip in self-backend emit path) | v2.13.1 真修 (跟 W-074.6.1 同 sprint,emit 联动 per user 决定) |
| W-074.8 | ⏸ DEFERRED (v2.11.15 partial fix residues, per workarounds.md line 6157-6207, 4 sub-bugs): (1) slice addr+8 (~15-25 LOC, +5 tests); (2) C.4 float imm bit-pattern (~110-170 LOC HIGH risk, +3-4 tests); (3) A2-ptr-deref flag propagation (~10-15 LOC, +2 tests); (4) C.2 cap_table test 4 16B struct 2 reg (~40 LOC, +1 test) | v2.13.2 真修 (优先 1+3 LOW risk, 2+4 HIGH risk defer v2.13.2.5 patch) |
| W-074.9 | ⏸ DEFERRED (v2.11.16 deferred items, per workarounds.md line 6211+, 3 sub-bugs): (5) dungeon_game gcc link (Win ABI 5+ arg stack fallback + `_puts` symbol decoration, ~20-40 LOC HIGH risk); (6) B-runtime big_array frame audit (~30 LOC MED risk); (7) top_level_let_mut_types multi-global cg_mod_global_register growth (~30 LOC) | v2.13.3 真修 |
| W-073 | 🟡 ACTIVE (codegen_amd64_run 函数体内层 silent no-op → 0-byte .s; v2.11.0 ship 后仍 ACTIVE — 真根因未被 W-068/W-074 真修覆盖) | v2.13.4 真修 (per user 决定:改真活儿, RCA emit 函数体内层 silent-no-op + 1-2 file 真修) |
| W-058 | 🟡 DEFERRED v2.x (vendor QBE 不支持 `remd`/`rems`, 自研后端 emit fold 到 `fmod()` library call) | v2.13.5 真修 |
| W-057 | 🟡 DEFERRED v2.x (UTF-8 3/4-byte codepoint 显式 lex reject — vendor QBE codepoint folding 缺, 自研后端已自主) | v2.13.6 真修 |

**已 CLOSED 不重做** (per audit):
- **W-074.7** phi merge gap — ✅ CLOSED in v2.11.15 Iter 2 commit `568d3aa` (line 6085), per-arm injection strategy
- **W-074.13** sub-bug 1 (for_in_slice_nested) + sub-bug 4 (big_array) — ✅ FULLY CLOSED in v2.11.23 (line 6344), slot-vs-region 架构修 Phase 1+2
- W-074.10 / W-074.11 / W-074.12 — ✅ CLOSED in v2.11.20 (line 6252/6293/6319)

**Out of scope**:
- W-022 / W-023 / W-024 (CI 环境类 PowerShell 5.1 yaml-bash PS5.1, 非功能 bug, 推 PS7 升级)
- W-055 (✅ RESOLVED in v1.7.0 Stage 2, 不是 ACTIVE)

**最终 9 minor version 序列** (per audit 修订:撤掉 v2.13.2 + v2.13.3 整 minor,因为对应 W-074.13 / W-074.7 已 CLOSED):

| Minor | 内容 (workaround ID) | LOC | Files | 难度 |
|-------|---------------------|-----|-------|------|
| **v2.13.1** | W-074.6.1 全 4 sub-bugs (a+b+c+d) + W-074.7.9 self-backend 侧 (idiv + remd emit) | ~130-310 | emit_mem + emit_binop + emit_ctrl + emit_call (4 files) | 🟠 中大 |
| **v2.13.2** | W-074.8 (4 sub-bugs residue) | ~50-120 | emit_mem + codegen.jhyy (2 files) | 🟠 中大 |
| **v2.13.3** | W-074.9 (3 sub-bugs) | ~80-150 | emit_call + emit_mem + state (3 files) | 🟠 中大 |
| **v2.13.4** | W-073 emit 函数体内层 silent-no-op RCA + 真修 | ~30-80 | codegen_amd64.jhyy + emit_ctrl + emit_call (1-3 files) | 🟡 中 |
| **v2.13.5** | W-058 fmod `remd`/`rems` (自研后端 fold) | ~30-50 | emit_binop (1 file) | 🟢 小 |
| **v2.13.6** | W-057 UTF-8 3/4-byte codepoint (lexer + 自研后端) | ~50-100 | lexer + codegen (2 files) | 🟡 中 |
| **v2.13.7** | (预留 / 合并微调) | — | — | — |
| **v2.13.8** | (预留 / 合并微调) | — | — | — |
| **v2.13.9** | (预留 / docs + ship aggregate) | — | — | — |

**总 LOC 估**: ~370-810 (6 真修项合计, 不含 v2.13.7-9 预留)
**总 wall-clock**: ~4-5 周 (按 v2.11.x 历史 ~2-3 天/minor 估, 串行 ship)

## Scope 重算逻辑 (audit 修订)

**撤掉的 2 个 minor (v2.13.2 + v2.13.3 旧 plan → 现 v2.13.7-8 预留)**:

| 撤掉的 minor | 旧 plan 内容 | 实际状态 | 处置 |
|--------------|-------------|---------|------|
| ~~v2.13.2 (旧)~~ | W-074.13 sub-bug 1+4 (for_in_slice_nested + big_array) | ✅ FULLY CLOSED in v2.11.23 (workarounds.md:6344 明说) | 撤, 不重做 |
| ~~v2.13.3 (旧)~~ | W-074.7 phi merge gap | ✅ CLOSED in v2.11.15 Iter 2 commit `568d3aa` (workarounds.md:6085 标题明说) | 撤, 不重做 |

**调整的 2 个 minor (audit 后)**:

| Minor | 旧 plan 内容 | 实际 audit 修订 | 影响 |
|-------|-------------|----------------|------|
| v2.13.1 | W-074.6.1 全 4 sub-bugs | + W-074.7.9 self-backend 侧 (idiv + remd emit) 合并 (per user 决定:emit 联动) | LOC +50-100 |
| v2.13.4 (旧 v2.13.7) | W-073 closeout (audit + status flip only) | 改真活儿 (per user 决定:RCA emit 函数体内层 silent-no-op + 1-2 file 真修) | LOC +30-80 (1-3 files) |

## 工作环境

- **Worktree**: `C:\Users\liuzhen\Desktop\coding\JiHuiYiYou-axis-v2` (branch `axis-v2`, 当前 ahead of origin/axis-v2 by 13 commits, latest `5ddbb2c` v2.13.0 ship + tag `v2.13.0`)
- **路径约束** (per `feedback_axis_vn_worktree_isolation` + `feedback_regress_py_abspath`):
  - `regress.py` 绝对路径 = `C:\Users\liuzhen\Desktop\coding\JiHuiYiYou-axis-v2\compiler\build\bin\regress.py`
  - `jhyy.exe` 绝对路径 = `C:\Users\liuzhen\Desktop\coding\JiHuiYiYou-axis-v2\compiler\build\bin\jhyy.exe` (sha `3cc0c7752b04e0fd...` v2.13.0 baseline HOLD, Phase 1+ 真修后会变)
  - `fixed_point.sh` 绝对路径 = `C:\Users\liuzhen\Desktop\coding\JiHuiYiYou-axis-v2\compiler\tests\bootstrap\fixed_point.sh` (per audit #7 修订, 不是 `compiler/build/bin/fixed_point.sh`)
  - **不** cp .jhyy / jhyy.exe 进 main worktree
  - **不** merge axis-v2 → main
- **MCP 锁 main worktree** (per `feedback_mcp_jhyy_run_workspace` + `feedback_mcp_regress_timeout`): `mcp__jhyy__jhyy_run` / `jhyy_regress` / `jhyy_selfhost_check` 走 main, **不**在 axis-v2 用; regress + run + selfhost 全走 raw bash + 绝对路径
- **可调 MCP read-only tools**: `mcp__jhyy__jhyy_workarounds` / `jhyy_abi_info` / `jhyy_lang_ref` (不锁 worktree)
- **SSH push 前置** (per workspace CLAUDE.md + `feedback_ssh_key_same_shell`): `eval "$(ssh-agent -s)" && ssh-add /c/Users/liuzhen/.ssh/id_ed25519` 必须**同一** Bash 调用
- **GFW fallback** (per workspace CLAUDE.md): `git -c "credential.helper=" push "https://oauth2:${TOKEN}@github.com/JiHuiYiYou/JiHuiYiYou-compiler.git" <ref>` + 完事恢复 SSH canonical remote

## 启动前置验证 (Phase 0, 每 minor 启动前必跑)

```bash
cd /c/Users/liuzhen/Desktop/coding/JiHuiYiYou-axis-v2

# 1. v2.13.0 tag 存在 + 在 HEAD
git tag -l 'v2.13.0'              # 期望: v2.13.0
git log --oneline -1 v2.13.0     # 期望: 5ddbb2c
git status                       # 期望: working tree clean

# 2. jhyy.exe sha 匹配 v2.13.0 baseline HOLD
sha256sum compiler/build/bin/jhyy.exe
# 期望: 3cc0c7752b04e0fd... (Phase 1+ 真修后会变)

# 3. regress 三门槛 (raw bash + 绝对路径, per audit #5 修订)
python compiler/build/bin/regress.py 2>&1 | grep 'passed.*total' | tail -1
# 期望: 119/119 passed, 0 failed, 21 skipped (of 140 total) (QBE 默认路径, 21 SKIP 含 5 sysv + 其他)
python compiler/build/bin/regress.py --self-backend 2>&1 | grep 'passed.*total' | tail -1
# 期望: 120/120 passed, 0 failed, 20 skipped (of 140 total) (self-backend 路径, +1 PASS 因 cap_table_basic self-backend 通过)

# 4. ACTIVE workaround 数 audit (post v2.13.0 ship)
grep -E '(🟡 ACTIVE|🟡 DEFERRED|⏸ DEFERRED|🟡 \*\*ACTIVE|⏸ \*\*DEFERRED)' docs/internal/workarounds.md | wc -l
# 期望: ~6 ACTIVE (W-074.6.1 + W-074.7.9-self-backend + W-074.8 + W-074.9 + W-073 + W-058 + W-057)

# 5. D43 closure baseline anchor (per audit #8 修订)
# D43 closure baseline SHA 是 .il byte content SHA, 存储在 archive, 不是 git commit
# git rev-parse 会 fail — 改用 fixed_point.sh 验证 closure 状态:
bash compiler/tests/bootstrap/fixed_point.sh 2>&1 | tail -5
# 期望: v2/v3/v4/v5 → 1 unique sha (D43 closure N=5 byte-equal verified on `b743f8a541f1b14726da6861cbd4db6ec287ce1bbd4ea447bea744e2cf9f94ec`)
# baseline archive: docs/logs/v2/d43-baseline-archive.md (per commit 查阅历史 baseline SHA)

# 6. Phase 0 specific per minor (e.g. v2.13.1 启动前 verify regress 119/140 + 120/140 PASS HOLD)
```

**任一 fail → 标 BLOCKED, 不动该 minor, 先修前置**。

## Critical files (按 9 minor 聚合, per audit #6 修订)

### v2.13.1 (W-074.6.1 全 4 sub-bugs + W-074.7.9 self-backend 侧)

**emit_*.jhyy 实际只有 4 个** (per audit #6 修订):
- `compiler/src0/codegen_amd64_emit_mem.jhyy` MODIFY — struct multi-field emit_copy (W-074.6.1-a) + stack-slot-reuse `temp_slot_for_id` (W-074.6.1-b)
- `compiler/src0/codegen_amd64_emit_ctrl.jhyy` MODIFY — emit_ret 不 `mov %t1 → %eax` 后 `ret` 真修 (W-074.6.1-d)
- `compiler/src0/codegen_amd64_emit_call.jhyy` MODIFY — extsw silent-skip 实际位置 (W-074.6.1-c) + idiv sign-extend `cltd`/`cqto` prefix residual (W-074.7.9 self-backend) + remd op silent skip (W-074.7.9 self-backend)
- `compiler/src0/codegen_amd64.jhyy` MODIFY (alternative) — extsw silent-skip if 实际位置在主 dispatch (per Phase 0 grep)
- NEW `compiler/tests/examples/int_i32_to_i64_conv.jhyy` (W-074.6.1-c fixture, EXIT=expected)
- NEW `compiler/tests/examples/struct_pass_24byte.jhyy` (W-074.6.1-a fixture)
- NEW `compiler/tests/examples/stack_slot_call_3args.jhyy` (W-074.6.1-b fixture)
- NEW `compiler/tests/examples/div_remd_basic.jhyy` (W-074.7.9 self-backend 触发面)

**Phase 0 mandatory pre-flight** (per audit 修订): 跑 `grep -n 'extsw\|silent\|is_div\|is_rem' compiler/src0/codegen_amd64*.jhyy` 找 extsw + idiv + remd 实际 emit 位置 (confirm 4 个 emit_*.jhyy 中哪个有 extsw)。

### v2.13.2 (W-074.8 4 sub-bugs residue)
- `compiler/src0/codegen.jhyy` MODIFY — slice addr+8 真修 (per W-074.8 § Sub-bug 1 fix pattern: emit `add %t<slot>, 8` 构造 len_addr then 2nd storel 用 len_addr)
- `compiler/src0/codegen_amd64_emit_mem.jhyy` MODIFY — 其他 3 sub 真修 (iter 3 + iter 6 + iter 7)

### v2.13.3 (W-074.9 3 sub-bugs)
- `compiler/src0/codegen_amd64_emit_call.jhyy` MODIFY — Win ABI 5+ arg stack fallback (Sub-bug 5 Bug A)
- `compiler/src0/codegen_amd64_emit_call.jhyy` MODIFY — extern symbol `_puts` decoration (Sub-bug 5 Bug B)
- `compiler/src0/codegen_amd64_emit_mem.jhyy` MODIFY — B-runtime frame audit (Sub-bug 6)
- `compiler/src0/codegen_amd64_state.jhyy` MODIFY — multi-global cg_mod_global_register growth (Sub-bug 7)

### v2.13.4 (W-073 emit 函数体内层 silent-no-op 真修, per user 决定改真活儿)
- `compiler/src0/codegen_amd64.jhyy` RCA — 找 emit_X 函数体内层 silent-no-op 真根因 (per W-073 描述: emit 函数体看似跑完但 sb.len = 0)
- 1-2 file 真修 (具体位置 per RCA):
  - `compiler/src0/codegen_amd64_emit_ctrl.jhyy` 或 `emit_call.jhyy` 或 `emit_mem.jhyy` 找 silent no-op pattern
  - `compiler/src0/codegen_amd64_lexer.jhyy` 检查 lexer 漏 token (per W-074.5 经验)
- NEW fixture: `cap_test_sysv_self_backend.jhyy` (W-073 触发面)

### v2.13.5 (W-058 fmod)
- `compiler/src0/codegen_amd64_emit_call.jhyy` MODIFY — emit `fmod()` library call fold for `remd`/`rems` (per W-058 § 3 option 3 pattern; self-backend emit 不支持 `idiv`-style float remainder → emit `call fmod` 当 fallback)
- NEW `compiler/tests/examples/fmod_basic.jhyy` (W-058 fixture, EXIT=expected remainder)

### v2.13.6 (W-057 UTF-8 3/4-byte codepoint)
- `compiler/src0/lexer.jhyy` MODIFY — UTF-8 3/4-byte codepoint accept (NOT reject), fold 成 i32
- `compiler/src0/codegen.jhyy` MODIFY — 自研后端 codepoint emit `mov $codepoint, %t<id>`
- NEW `compiler/tests/examples/char_cjk.jhyy` (W-057 fixture)
- NEW `compiler/tests/examples/char_emoji.jhyy` (W-057 fixture)
- NEW `compiler/tests/examples/char_in_struct.jhyy` (W-057 fixture)
- NEW `compiler/tests/examples/char_in_array.jhyy` (W-057 fixture)

### v2.13.7-9 (预留)
- (留待 v2.13.1-6 实际 ship 后看是否需要)

## Reusable 现有 functions / utilities (沿用 v2.13.0 验证 infra)

- `compiler/build/bin/regress.py` (per `feedback_regress_py_abspath`) — raw bash + abspath
- `compiler/tests/bootstrap/fixed_point.sh` (v2.9.0 ship, per audit #7 修订) — D43 closure v2/v3/v4/v5 byte-equal
- `compiler/tests/bootstrap/sysv_full_regress.sh` (v2.13.0 ship) — docker gcc:12 chain V.3 gate
- `compiler/tests/bootstrap/sysv_float_cross.sh` (v2.11.19 ship) — Win/SysV float cross-check
- `scripts/dev/build/build-efi.sh` (v2.3.0 ship) — jhyy → .obj → lld-link → .efi
- `scripts/dev/test/run-ovmf.sh` (v2.13.0 ship, QEMU 10 适配) — QEMU + OVMF UEFI boot
- `mcp__jhyy__jhyy_workarounds` MCP tool — read-only, 不锁 worktree, 查 W-NNN status
- `mcp__jhyy__jhyy_abi_info` MCP tool — read-only, 查 SysV ABI §3.2.3 / Win ABI 文档

## Per-minor 执行步骤 (uniform template)

### Phase 0: 启动前置验证 (5+1 checks, BLOCKED 不启动)
详见上方 "启动前置验证" section,含 per-audit 修订 (regress 总数 140, fixed_point.sh 路径, D43 closure SHA 是 .il byte content 不是 git commit)。

### Phase 1-N: 真修 (per minor, 1 docs commit + 0-N source commits)

1. **Pre-modify read** (per minor 真修目标):
   - 读相关 codegen_amd64_emit_*.jhyy 当前结构, 找 silent-skip / 静默丢 / silent-fail pattern
   - **per audit #6 修订**: 确认 emit 实际位置 (Phase 0 grep 先确认 extsw / silent 在哪个文件)
2. **MODIFY src0 file(s)** (per minor 真修目标):
   - 按 `feedback_jhyy_no_forward_ref` call graph topology 排序加新函数
   - 每 file 改前必读 line 范围, 不跨文件瞎改
3. **NEW fixture(s)** (per minor 真修目标): 加 EXPECT-EXIT fixture
4. **Rebuild jhyy.exe** (per `feedback_make_clean_too_aggressive` 不 `make clean`):
   ```bash
   ./compiler/build/bin/jhyy.exe compile compiler/src0/main.jhyy -o compiler/build/bin/jhyy.exe.new
   mv compiler/build/bin/jhyy.exe.new compiler/build/bin/jhyy.exe
   ```
5. **Per-minor ship gate** (5/5 per `feedback_fix_evaluation_rule`):
   - V.X.1: 新 fixture × 5 → EXIT=expected
   - V.X.2: `python compiler/build/bin/regress.py` + `--self-backend` → 119/140 (QBE) + 120/140 (self-backend) HOLD (或 per minor +N 改善)
   - V.X.3: `bash compiler/tests/bootstrap/fixed_point.sh` → D43 closure N=5 PASS HOLD (除非 expected re-baseline event)
   - V.X.4: per minor specific E2E (e.g. W-058 → sysv_full_regress.sh / W-074.7.9 → big_test self-backend EXIT=57 / W-073 → cap_test_sysv self-backend EXIT=42)
6. **Sub-sprint commit** (单 commit per `feedback_audit_single_commit_diff`):
   ```bash
   git add compiler/src0/<modified_files>
   git commit -m "fix+test(v2.13.X): <description>"
   ```
7. **NOT push** (per `feedback_auto_push_after_commit` 反例: v2.x mid/end active)

### Phase docs: docs + ship (per minor, 1 docs commit)

1. **MODIFY `docs/logs/v2/changelog-v2.11.0.md`**: append v2.13.X section (per `feedback_changelog_umbrella` **不**创建 standalone `changelog-v2.13.X.md`)
2. **MODIFY `docs/plans/v2/README.md`**: 加 v2.13.X row
3. **MODIFY `docs/internal/workarounds.md`**: 真修的 W-NNN status flip ACTIVE/DEFERRED → RESOLVED/CLOSED
4. **MODIFY `docs/internal/architecture.md`**: 真修模块边界 + Last updated v2.13.X (per `feedback_doc_refactor_factcheck` 逐条 fact-check)
5. **Tag v2.13.X**:
   ```bash
   git tag -a v2.13.X -m "v2.13.X — <description>"
   ```
6. **Push origin/axis-v2 + tag push** (per workspace CLAUDE.md GFW fallback):
   ```bash
   TOKEN=$(/c/Program\ Files/GitHub\ CLI/gh.exe auth token 2>/dev/null)
   git -c "credential.helper=" push "https://oauth2:${TOKEN}@github.com/JiHuiYiYou/JiHuiYiYou-compiler.git" axis-v2
   git -c "credential.helper=" push "https://oauth2:${TOKEN}@github.com/JiHuiYiYou/JiHuiYiYou-compiler.git" v2.13.X
   git remote set-url origin git@github.com:JiHuiYiYou/JiHuiYiYou-compiler.git  # 恢复 SSH canonical
   ```
7. **Sub-sprint commit**:
   ```bash
   git add docs/...
   git commit -m "docs+changelog(v2.13.X/Ph.docs): docs + ship"
   ```
   (per `feedback_auto_push_after_commit`: commit + push 直接做, 不问 user 确认)

### Per-minor ship gates (V.X.1-V.X.4, 5/5 PASS each per `feedback_fix_evaluation_rule`)

- **V.X.1** (per minor 新 fixture): 新 fixture × 5 → EXIT=expected 每次都对
- **V.X.2** (per minor regress 持平): `python regress.py` + `--self-backend` → 119/140 (QBE) + 120/140 (self-backend) HOLD
- **V.X.3** (per minor D43 closure HOLD): `bash compiler/tests/bootstrap/fixed_point.sh` → D43 closure N=5 PASS HOLD (除非 expected re-baseline event)
- **V.X.4** (per minor specific E2E): per minor true fix 验证 (e.g. W-058 → sysv_full_regress.sh / W-074.7.9 → big_test EXIT=57 / W-073 → cap_test_sysv self-backend EXIT=42)

### Final ship gates (per minor, V.X.5 aggregate)

- V.X.5: V.X.1-V.X.4 全 PASS
- umbrella changelog 包含 V.X.1-V.X.4 + 真修 metrics delta + 工作风格 + side-effects
- workarounds.md 真修的 W-NNN status flip
- architecture.md Last updated v2.13.X + 1-line summary
- tag v2.13.X 成功 + push origin (axis-v2 + tag) + verify `git ls-remote origin v2.13.X`
- SSH canonical remote 恢复

## Risk + Rollback (uniform per minor, per `feedback_rca_first_root_cause` + `feedback_il_s_debugging_pattern`)

| Risk | Mitigation | Rollback |
|------|-----------|----------|
| **Risk 1** (per minor 真修 self-backend regress FAIL) | V.X.2 regress 持平 gate + per-minor `feedback_il_s_debugging_pattern` 读 `.il` + `.s` | `git reset --hard <prev minor tag>` |
| **Risk 2** (per minor scope 超估) | per `feedback_file_size_relaxed` ≤ 1000 LOC 警戒线, 超过拆 v2.13.7-9 缓冲 | per minor rollback (单 minor revert) |
| **Risk 3** (per minor D43 closure 变) | V.X.3 gate pre-phase + mid-phase; per `feedback_il_s_debugging_pattern` 找 main.jhyy IL path 触发点 | revert 触发的 minor commit + 调 emit 顺序 |
| **Risk 4** (per minor 撞新 W-NNN) | Phase 0 verify workaround count = 6; 撞新 W-NNN → 出 v2.13.X.y patch, **不**挡 v2.13.X ship (per v2.12.0 precedent) | revert 触发的 minor commit |
| **Risk 5** (per minor 真修 root cause 估错, FLIP 不到预期) | per `feedback_rca_first_root_cause` 撞 cluster > 30% 先 RCA + per `feedback_codegen_amd64_multifn` scope DOWN trigger | per minor rollback + 重新调研 |
| **Risk 6** (push 失败 — GFW blocks SSH) | HTTPS-with-token per workspace CLAUDE.md; `git push origin axis-v2 v2.13.X` 走 HTTPS | re-tag + re-push (idempotent) |
| **Risk 7** (v2.13.1 撞 silent-no-op 实际位置需要 RCA) | Phase 0 pre-flight grep `silent\|extsw` 找实际 emit 位置, 不假设 emit_*.jhyy 命名 | 重新 Phase 0 + 调 file list |

## Out of scope (推后续)

- ❌ N 代 fixed point 大幅度扩展 — v2.14.0
- ❌ QBE 自写(跳过 QBE IL)— v2.15.0
- ❌ QBE 工具链移除 — v2.16.0
- ❌ 性能 bench + .exe byte-equal — v2.16.0
- ❌ v3.0 3a-3f (per `v2-v3-parallel-sprint-plan.md`, 等 user 启动)
- ❌ W-022 / W-023 / W-024 (CI 环境类 PS 5.1, 非功能 bug, 推 PS7 升级)
- ❌ W-074.7 / W-074.13 sub-bug 1+4 (✅ CLOSED, 不重做)
- ❌ W-057 spec §4.4 完整 codepoint 族扩展 (本 sprint 只真修 UTF-8 3/4-byte lex accept, 不动 spec)
- ❌ 真实 big_test runtime STATUS_INTEGER_OVERFLOW 深度调研 (W-074.7.9 当前只修 self-backend 侧 idiv sign-extend + remd, 触发更深 root cause 推 v2.13.6+)

## References

### Plan 权威
- **本 plan** (执行 plan): `JiHuiYiYou-axis-v2/docs/plans/v2/v2.13.1-plan.md` (本文)
- **v2.13.0 plan** (immediate predecessor): `JiHuiYiYou-axis-v2/docs/plans/v2/v2.13.0-plan.md`
- **v2.13.0 ship 闭环**: 4-commit chain `5d405bb` + `b1ad5c3` + `d062a71` + `5ddbb2c`, tag `v2.13.0`
- **v2.12.0 plan** (audit closure): `JiHuiYiYou-axis-v2/docs/plans/v2/v2.12.0-plan.md`

### Roadmap + OS
- **v2.x 末 5 sub-sprint 链**: `JiHuiYiYou-axis-v2/docs/plans/roadmap/v2.x-qbe-rewrite.md` § 3 + 4
- **v2-v3 parallel sprint plan**: `JiHuiYiYou-axis-v2/docs/plans/roadmap/v2-v3-parallel-sprint-plan.md` § 4
- **M5 deferral 第二前置** (W-074.6 真修 + QBE 移除): `JiHuiYiYou-axis-v2/docs/plans/roadmap/v1.x-phase-4-m5-boot-from-scratch.md`
- **OS launch milestones M1-M11**: `JiHuiYiYou-axis-v2/docs/plans/v2/v2.0.0-os-prep.md` § 5.2 完成验收 #2

### Workaround 详情 (按 audit 修订)
- W-073: `docs/internal/workarounds.md` line 5172 (🟡 ACTIVE — codegen_amd64_run 函数体内层 silent-no-op)
- W-074.6.1 sub-family: `docs/internal/workarounds.md` line 5545-5549 (W-074.6 section 末尾 "W-074.6.1 sub-family 仍 ACTIVE" 4 sub-bugs: a=multi-field emit_copy 1-to-1 gap / b=stack-slot-reuse in other emit paths / c=extsw silent-skip residual / d=emit_ret 不 `mov %t1 → %eax`)
- W-074.7.9: `docs/internal/workarounds.md` line 5788 (✅ RESOLVED QBE 侧 / 🟡 ACTIVE self-backend 侧)
- W-074.8: `docs/internal/workarounds.md` line 6157 (⏸ DEFERRED 4 sub-bugs)
- W-074.9: `docs/internal/workarounds.md` line 6211 (⏸ DEFERRED 3 sub-bugs)
- W-058: `docs/internal/workarounds.md` line 3904 (🟡 DEFERRED v2.x, vendor QBE 不支持 remd/rems)
- W-057: `docs/internal/workarounds.md` line 3860 (🟡 DEFERRED v2.x, UTF-8 3/4-byte codepoint)
- **W-074.7**: `docs/internal/workarounds.md` line 6085 (**✅ CLOSED in v2.11.15 Iter 2 commit `568d3aa`** — 不重做)
- **W-074.13 sub-bug 1+4**: `docs/internal/workarounds.md` line 6344 (**✅ FULLY CLOSED in v2.11.23** — 不重做)
- W-074.10: `docs/internal/workarounds.md` line 6252 (✅ CLOSED 2026-09-17)
- W-074.11: `docs/internal/workarounds.md` line 6293 (✅ CLOSED 2026-09-17)
- W-074.12: `docs/internal/workarounds.md` line 6319 (✅ CLOSED 2026-09-17)

### D43 closure baseline (per audit #8 修订)
- D43 closure baseline SHA 是 **.il byte content SHA** (NOT git commit SHA), 存储在 `docs/logs/v2/d43-baseline-archive.md`
- 当前 active baseline (v2.13.0 Ph.2 active): `b743f8a541f1b14726da6861cbd4db6ec287ce1bbd4ea447bea744e2cf9f94ec`
- 验证 closure 状态: `bash compiler/tests/bootstrap/fixed_point.sh` (per audit #7 修订, NOT `compiler/build/bin/fixed_point.sh`)

### Memory items to apply

| Memory | Why relevant |
|--------|--------------|
| `feedback_codegen_amd64_multifn` | 每个 minor 真修都验证 multi-fn 不 silent-fail (单 fn PASS 不够) |
| `feedback_codegen_amd64_run_zerobyte` | 每个 minor src0 changes 必跑 `feedback_fix_evaluation_rule` 5/5 gate, 防 .s 截断 / 0-byte body 复发 |
| `feedback_rca_first_root_cause` | 真修 FAIL 跑前必 RCA: cluster > 30% 同根 → 找共根, 不逐 test 修 |
| `feedback_fix_evaluation_rule` | V.X.1-V.X.4 5/5 PASS — 5 consecutive runs 必 5/5 PASS |
| `feedback_audit_single_commit_diff` | 每个 minor `git show --stat <sha>` 看单 commit 改动, 不累计跨 commit diff |
| `feedback_no_date_estimates` | 本 plan 不写 "几月几月完成" — 用 sprint 序列 (v2.13.1 → v2.13.9) 表达, 不带日期 |
| `feedback_auto_push_after_commit` | 每个 minor docs+ship commit 后整体 push + tag push (默认, 不问确认 per `feedback_auto_push_after_commit` 反例不适用: v2.13.X 是 closed milestone) |
| `feedback_changelog_umbrella` | 每个 minor docs sub-step 1: **不**创建 `changelog-v2.13.X.md`, append 到 `changelog-v2.11.0.md` |
| `feedback_axis_vn_worktree_isolation` | 全程 raw bash + 绝对路径, **不** cp .jhyy / jhyy.exe 进 main worktree |
| `feedback_regress_py_abspath` | `python compiler/build/bin/regress.py` raw bash + 绝对路径, 不用 MCP `jhyy_regress` |
| `feedback_mcp_jhyy_run_workspace` | axis-v2 worktree **不**走 MCP `jhyy_run` / `jhyy_regress` / `jhyy_selfhost_check` (锁 main); 全走 raw bash + 绝对路径 |
| `feedback_make_clean_too_aggressive` | **不**用 `make clean`, 用 `jhyy.exe compile ... -o jhyy.exe.new` + `mv` 重建 |
| `feedback_file_size_relaxed` | 每个 minor ≤ 1000 LOC 警戒线, 超过拆 v2.13.7-9 缓冲 |
| `feedback_il_s_debugging_pattern` | 每个 minor 真修 FAIL → 读 `.il` + `.s` 找 silent-skip / 静默丢 pattern |
| `feedback_jhyy_no_forward_ref` | 加新函数前必按 call graph topology 排序 (被调用的先定义), 防 cascading error |
| `feedback_plans_per_version` | v2.13.1 plan file 已存在 (本文), 跟 v2.11.x plan 同 structure |
| `feedback_regress_clean_count` | 每个 minor ship 前 `rm _regress_*.exe` 清 stale artifact, FRESH total 才能写入 changelog, 勿带上一 session 数字 |
| `feedback_ssh_key_same_shell` | SSH push 前 `eval "$(ssh-agent -s)" && ssh-add /c/Users/liuzhen/.ssh/id_ed25519` 必须**同一** Bash 调用 |
| `feedback_memory_selectivity` | 每个 minor 发现新 general rule → 写 memory; sprint-specific 协调结果 → workarounds.md 或 commit message |
| `feedback_doc_refactor_factcheck` | docs 重构前逐条 fact-check (本 plan 已按 audit 修订: scope + 4 处文件/路径/regress 数字 + D43 SHA 措辞 + line ref 全去掉换成 section name) |

## Audit trail (per user 2026-09-19 audit feedback)

**8 个 audit point 全修订**:

| # | 类别 | 原 plan | 修订后 |
|---|------|---------|--------|
| 1 | scope | v2.13.3 = W-074.7 phi merge gap 待修 | W-074.7 ✅ CLOSED in v2.11.15 (workarounds.md:6085), v2.13.3 整 minor 撤掉 |
| 2 | scope | v2.13.2 = W-074.13 sub-bug 1+4 待修 | W-074.13 sub-bug 1+4 ✅ CLOSED in v2.11.23 (workarounds.md:6344), v2.13.2 整 minor 撤掉 |
| 3 | scope | v2.13.7 (旧) = W-073 closeout audit-only | per user 决定改真活儿: v2.13.4 = RCA emit 函数体内层 silent-no-op + 1-2 file 真修 |
| 4 | scope | v2.13.4 (旧) = W-074.7.9 QBE + self-backend 双修 | per user 决定: W-074.7.9 self-backend 侧 合并 v2.13.1 (emit 联动) |
| 5 | regress 数字 | 119/139 + 0 FAIL + 20 SKIP | 实际 119/140 (QBE) + 120/140 (self-backend) + 21 SKIP / 20 SKIP, 总数 140 |
| 6 | 文件名 | `codegen_amd64_emit_binop.jhyy` | 不存在, emit_*.jhyy 实际只有 _mem / _call / _ctrl / _sse; extsw 在 emit_call 或 codegen_amd64.jhyy 主文件, Phase 0 grep 确认 |
| 7 | 路径 | `compiler/build/bin/fixed_point.sh` | 实际 `compiler/tests/bootstrap/fixed_point.sh` |
| 8 | D43 SHA | `git rev-parse b743f8a5` | SHA 是 .il byte content, NOT git commit; 验证用 `bash compiler/tests/bootstrap/fixed_point.sh`; archive: `docs/logs/v2/d43-baseline-archive.md` |

**Line ref -2 偏移处理**: 全部 workarounds.md line ref 在本 plan 中改为"section 标题 + line"格式 (per audit 修订:避免行号漂移), 改成见 "Workaround 详情" section 列的标题描述 + 章节 line 范围。
