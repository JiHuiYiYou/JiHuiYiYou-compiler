# v2.13.6 — MCP 5-state enum + worktree-aware default path

## Context

v2.13.5 workarounds.md refactor 把状态收成 5 态 (ACTIVE / RESOLVED / SUPERSEDED / DEFERRED / INVALID) + Backfilled / Filed-by schema。但 `mcp__jhyy__jhyy_workarounds` 的解析器没跟上,仍用旧 3 态 substring match,导致:

1. **W-051 误分类**: entry 实际 RESOLVED 但 body 写 "强标 ACTIVE 不解决任何 active 问题",substring match 把它同时塞进 ACTIVE filter 和 DEFERRED filter
2. **DEFERRED / INVALID 完全不被识别**: 新 5 态的两个状态 0 计数,user 查 "active、defer还有哪几条" 拿到 MCP 错答
3. **worktree isolation 缺失**: MCP server 从 main worktree 路径启动 (per `.claude.json`),用户 cwd 在 axis-v2 时 MCP 读 main 的 stale data。axis-v2 v2.13.5 ship 后 MCP 仍显示 09-16 数据

User 反馈 (2026-09-20): "先看下mcp,你是没改还是改完了服务没生效" → 定位到 MCP 完全没改 (per v2.13.5 plan out-of-scope:"MCP enum 更新 → v2.13.6 mini (MCP server 独立 effort)")。

## Schema 设计

### Fix A — 5-state enum + 严格 token match

`_STATUS_RE = re.compile(r"[^\w]*?(?P<status>ACTIVE|RESOLVED|SUPERSEDED|DEFERRED|INVALID)\b", re.IGNORECASE)`

`_parse_status(status_str) -> "ACTIVE" | "RESOLVED" | "SUPERSEDED" | "DEFERRED" | "INVALID" | "UNKNOWN"`

支持格式:
- Post-v2.13.5: `ACTIVE since 2026-08-15 (v1.5.6) — caption`
- Pre-v2.13.5 emoji-prefix: `✅ RESOLVED ...` / `🟡 DEFERRED v2.x` / `⏸ DEFERRED v2.x` / `❌ INVALID`
- Inline: `ACTIVE (dormant)` / `ACTIVE (PowerShell 5.1...)` / `RESOLVED`

`status` filter 改严格 first-token match (不再 substring),免误分类。返回新增 `deferred_count` / `invalid_count` / `unknown_count` 字段。

### Fix B — worktree-aware default path

`_detect_root() -> Path`:
1. 默认 = `Path(__file__).resolve().parents[1]` (旧行为,script-derived)
2. Override: 若 `os.getcwd()` 是 git worktree 且其 `git rev-parse --show-toplevel` ≠ script root 且 `toplevel/docs/internal/workarounds.md` 存在 → 用 cwd 的 toplevel
3. Fallback: non-git dir / git error → script root

`_default_path() -> Path`: 在 `search()` 内每次调用 (= runtime-effective,不需 MCP restart)

### Why 函数级调用而非模块级常量

`_detect_root()` / `_default_path()` 走函数级 (NOT `JHYY_ROOT = _detect_root()` cache at module load)。原因: MCP server 是 long-running subprocess,模块级 cache 在 import 时一次定值,用户在 session 内 `cd` 到其他 worktree 后 MCP 不会跟随。函数级调用让每次 tool call 都重新探测,user cd 后立即生效。

Fix A 的常量 (`_STATUS_WORDS` / `_STATUS_RE`) 是模块级但被 `_parse_status()` / `search()` 引用 — Python import 时 cache,所以 Fix A 需要 MCP restart 才能生效。Fix B 函数级则 runtime-effective。

## Edit 策略

**Phase A — axis-v2 active dev (主 commit)**:
1. `mcp-jhyy/jhyy_workarounds.py` 改 5 态 + worktree detect (~50 LOC 改)
2. `mcp-jhyy/server.py` jhyy_workarounds tool docstring 更新 (~10 LOC 改, mention 5-state + worktree)
3. `mcp-jhyy/tests/test_workarounds.py` 加 DEFERRED test + 改 ACTIVE test 注释 (~15 LOC 改)
4. `docs/plans/v2/v2.13.6-plan.md` (本 plan)
5. `docs/logs/v2/changelog-v2.11.0.md` append v2.13.6 section
6. `README.md` add v2.13.6 row
7. `git commit` 单 commit, tag v2.13.6, push via HTTPS-with-token

**Phase B — main mirror (parallel commit, no delay)**:
- `cp axis-v2/mcp-jhyy/{jhyy_workarounds.py,server.py,test_workarounds.py} main/mcp-jhyy/`
- `git commit` 单 commit "v2.13.6 mirror: MCP 5-state enum + worktree-aware default path" (per `0cadfba docs: formalize C-side freeze decision (cancel v2.11.21 mirror)` precedent)
- 不动 main 的 docs/internal/workarounds.md (那仍是 09-16 stale — 等 axis-v2 → main merge 拉新)

## Critical files

| 文件 | 操作 | LOC |
|------|------|-----|
| `mcp-jhyy/jhyy_workarounds.py` | MODIFY | +60 −15 |
| `mcp-jhyy/server.py` | MODIFY | +12 −5 (仅 docstring) |
| `mcp-jhyy/tests/test_workarounds.py` | MODIFY | +18 −4 |
| `docs/plans/v2/v2.13.6-plan.md` | NEW | +~80 |
| `docs/logs/v2/changelog-v2.11.0.md` | MODIFY | +~30 (append section) |
| `README.md` | MODIFY | +1 row |

## Out of scope (推后续 sprint)

- ❌ **4 UNKNOWN entries** (W-001 / W-002 / W-006 / W-025 empty status) → v2.13.7 mini (data quality,补登 status + 6-field schema)
- ❌ **W-081 sub-bug 2 (phi merge) + sub-bug 3** 真修 → v2.14.0 (per v2.13.5 plan)
- ❌ **W-057 lexer 放宽真修** → v2.13.6 mini 紧接 ship 后启动 (~10 LOC)
- ❌ **W-058 fmod emit 路径** → v2.13.7 mini (~30-60 LOC)
- ❌ **其他 MCP tools (jhyy_compile / jhyy_run / etc) worktree-aware** → 后续 sprint (本次只改 workarounds)
- ❌ **server.py 改 importlib.reload() 让 Fix A 也 runtime-effective** → 后续 sprint (本次需 /restart Claude Code)

## Verification gates

| Gate | PASS criterion |
|------|----------------|
| V.0 | `_parse_status` 9 个 sample (含 ✅/🟢/🟡/⏸/❌ emoji) 全对 |
| V.1 | `search("W-051", status="ACTIVE")` matches=0 (旧 bug 修复) |
| V.2 | `search("W-051", status="RESOLVED")` matches=1 |
| V.3 | `search("W-")` 全 counts: active=0, deferred=2 (W-057+W-058), unknown=4 |
| V.4 | `search("W-074.7")` matches=0 (renumbered to W-081) |
| V.5 | `_detect_root()` 在 axis-v2 cwd → axis-v2 path |
| V.6 | 单 commit `git show <sha> --stat`: 6 files modified |
| V.7 | main mirror commit `git show <sha> --stat`: 3 files modified (mcp-jhyy only) |
| V.8 | tag v2.13.6 push 成功 |

## Risk + Rollback

| Risk | Mitigation | Rollback |
|------|-----------|----------|
| **R1 — Python module-level cache 让 Fix A 不生效** | docstring 注明 "需 /restart Claude Code 让 MCP 重启加载新代码";Fix B 函数级调用 runtime-effective | `git revert <sha>` + /restart |
| **R2 — cwd 不在 git repo 导致 subprocess 抛错** | `try/except (subprocess.CalledProcessError, FileNotFoundError, OSError)` fallback 到 script root | _detect_root() 默认分支已覆盖 |
| **R3 — main worktree 用户被新 MCP 行为打断** | Mirror commit 跟 axis-v2 同步 push,user /restart 后一致生效 | revert main mirror commit |
| **R4 — Worktree path 探测对 symlink / WSL 路径行为不一致** | 用 `Path(...).resolve()` 规范化两端 (script root + toplevel);仅在两端不相等且 toplevel docs/internal/workarounds.md 存在时切换 | 不切换 = 旧行为 |
| **R5 — GFW 墙 github.com** | HTTPS-with-token HTTP/1.1 forced push (per `feedback_git_ssh_key_same_shell` + prior v2.11.12 tag push lesson) | tag 本地保留,可手动 retry |

## References

### Plan
- **本 plan**: `JiHuiYiYou-axis-v2/docs/plans/v2/v2.13.6-plan.md`
- **v2.13.5 plan (immediate predecessor)**: `JiHuiYiYou-axis-v2/docs/plans/v2/v2.13.5-plan.md` (✅ shipped 2026-09-20, commit `295d483`)

### Code refs
- `mcp-jhyy/jhyy_workarounds.py:131-203` — 旧 `search()` 3 态 substring match
- `mcp-jhyy/jhyy_workarounds.py:18-19` — 旧 `JHYY_ROOT` / `DEFAULT_PATH` module-level cache
- `mcp-jhyy/server.py:384-405` — 旧 `jhyy_workarounds` tool (mention 3 states only)
- `mcp-jhyy/tests/test_workarounds.py:46-60` — 旧 substring test

### Memory items to apply
- `feedback_plans_per_version` — v2.13.6 = own plan file (本 plan)
- `feedback_changelog_umbrella` — v2.x 单 umbrella changelog, NOT standalone `changelog-v2.13.6.md`
- `feedback_no_date_estimates` — no "几月几月完成", 用 sprint sequence
- `feedback_axis_vn_worktree_isolation` — Fix B 设计核心; mirror commit 是例外 (per `0cadfba`)
- `feedback_regress_clean_count` — 本次无 regress (MCP server 改, no compiler src 改)
- `feedback_ssh_key_same_shell` — `eval "$(ssh-agent -s)" && ssh-add ~/.ssh/id_ed25519` 同 Bash 调用
- `feedback_commit_coauthor` — `Co-Authored-By: MiniMax-M3 <noreply@MiniMax>`
- `feedback_no_traditional_chinese` — simplified Chinese only
- `feedback_audit_single_commit_diff` — 单 commit per worktree, 用 `git show <sha>` 验证
- `feedback_no_artifacts_in_project` — plan / changelog 进项目仓,临时调试脚本不进

## Plan honesty note (scope 明确)

**Scope 限定**: v2.13.6 = MCP server tooling fix,不真改 src0/。3 file MCP code change (~80 LOC) + docs (plan + changelog + README) + mirror commit on main。
- 6 files touched in axis-v2 commit
- 3 files touched in main mirror commit (mcp-jhyy only)
- 真 ACTIVE workaround 数从 MCP 错答的 4 → 真答的 0;DEFERRED 从 0 → 2 (W-057 + W-058)
- W-074.7 → W-081 renumber 反映在 MCP 查询 (新 MCP 读 axis-v2 的 09-20 workarounds.md)
- /restart Claude Code 是 MCP 加载新代码唯一路径 (Python module cache)

**不做 src 真修** per RCA-first + Fix A/B 边界清晰 (MCP tooling scope,不混合 compiler fix)。W-057 真修紧接 v2.13.6 ship 启动 v2.13.6.1 mini (或 v2.13.7 mini,W-058 一起)。
