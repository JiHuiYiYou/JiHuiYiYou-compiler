# v2.11.16 plan — Phase 0 audit + per-test root cause (29 → 10 ACTUAL FAIL)

## Context

**v2.11.15 ship (axis-v2 commit `8197db4`, tag `v2.11.15`)**:
- 4 implementation iters (A1-XMM + C.3 emit_phi + C.4 float XMM + C.5 slice copy) + 2 no-op iters (A2-pointer-deref + C.2 cap_table test 4)
- Ship record honest about partial fixes in source comments (verified via direct Read):
  - Iter 2 (C.3) `codegen_amd64_state.jhyy:890-892` comment: "8-byte l suffix — QBE IL phi 默认 l type... 真正按 qt 分 W/L/S/D 留 v2.11.16"
  - Iter 3 (C.4) `codegen_amd64_emit_call.jhyy:1366-1370` comment: "src2 imm literal 在 f64/f64 imm 罕见... 保守做法:imm fall through 现有 integer path (= 静默 garbage for imm f64/f64 arithmetic), v2.11.16+ 真修"
  - Iter 4 (C.5) `codegen.jhyy:881-891` code: `let mut addr_plus_8 = addr;` (no actual +8 offset) + comment "Pragmatic: emit `storel %t<len_id>, addr` AGAIN — wrong but无害... 算 partial fix — ptr 字段 store OK, len 字段可能错"
- Ship record claimed "23-25/30 verified target tests PASS" via spot-checks but **post-v2.11.15 full regress = 86/135 PASS / 29 FAIL / 20 SKIP** (per Agent 2 audit) — spot-check vs full regress gap

**User v2.11.16 design choice (AskUserQuestion)**: Phase 0 pre-flight ONLY — 0 source commit, 1 docs commit only, then user-driven per-test audit.

**User follow-up directive**: "看一下剩下的29个test的根因分别是什么" — explicit ask to do per-test root cause analysis NOW (this plan file IS the deliverable).

## Per-test root cause audit (3 Explore agents IN PARALLEL)

### Critical reconciliation findings

**Initial audit over-counted** FAIL count. After 3 parallel Explore agents read source + .s evidence + test code, the actual FAIL set is **~10 tests** (not 29). Key reconciliation:

1. **C.3 .s files are STALE** (captured BEFORE commit `568d3aa` v2.11.15 Iter 2 C.3 emit_phi fix). Source fix is correct; all 12 C.3 tests likely PASS post-Iter 2. Need fresh full regress to confirm.
2. **`top_level_let_mut_test.jhyy` actually PASSES** (verified via `.exe` exit=42). Contradicts FAIL list — either initial list is stale or test was fixed in some interim.
3. **`defer_multi_lifo.jhyy` PASSes via correct reason** — LIFO loop at `codegen.jhyy:1687-1709` matches Go semantics: `(*d).expr` walks each defer in source order, but LIFO iteration `for ib = nd_b; ib > 0; ib--` correctly runs last-source-defer first.
4. **A1-XMM .s evidence is clean** — both `generics_fn_call_site_inference_run.s` + `generics_fn_turbofish_basic_run.s` correctly emit `max$i32` (rcx/rdx int regs) and `max$f64` (xmm0/xmm1 XMM regs with `ucomisd`/`seta`). Spot-check claim valid; need fresh regress to confirm.

### Per-test audit table

| # | Test | Cluster | Actual Status | Root cause | LOC | Risk |
|---|------|---------|---------------|-----------|-----|------|
| 1 | char_pattern.jhyy | C.3 | **PASS (stale .s)** | post-Iter 2; .s captured pre-`568d3aa` | 0 | N/A |
| 2 | enum_match_arm_tag_check.jhyy | C.3 | **PASS (stale .s)** | same as #1 | 0 | N/A |
| 3 | match_exhaustive.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 4 | match_range.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 5 | min_enum.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 6 | mixed_struct_slice_match.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 7 | or_exhaust.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 8 | or_same_bind.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 9 | payload_bind_basic.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 10 | payload_bind_multi.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 11 | payload_bind_nested.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 12 | payload_bind_short.jhyy | C.3 | **PASS (stale .s)** | same | 0 | N/A |
| 13 | generics_fn_call_site_inference.jhyy | A1-XMM | **PASS (likely)** | .s clean; needs fresh regress confirm | 0 | LOW |
| 14 | generics_fn_turbofish_basic.jhyy | A1-XMM | **PASS (likely)** | .s clean; byte-identical to #13 | 0 | LOW |
| 15 | defer_multi_lifo.jhyy | C.7-defer | **PASS (correct)** | LIFO loop at `codegen.jhyy:1687-1709` correct; Go-style semantics | 0 | N/A |
| 16 | top_level_let_mut_test.jhyy | B-runtime | **PASS (verified exit=42)** | codegen correct; `data $g_x = { w 41 }` + `movl g_x(%rip)` works | 0 | N/A |
| 17 | slice_index.jhyy | C.5 | **UNCERTAIN (likely PASS)** | Iter 4 ship claimed PASS; codegen.jhyy:881 partial fix may be benign in this path | ~15 if FAIL | LOW |
| 18 | slice_iterate.jhyy | C.5 | **UNCERTAIN (likely PASS)** | same | ~15 | LOW |
| 19 | slice_literal.jhyy | C.5 | **UNCERTAIN (likely PASS)** | same | ~25 | MED |
| 20 | slice_subrange.jhyy | C.5 | **FAIL (exit=36 vs 60)** | confirmed; `addr_plus_8 = addr` overwrites ptr with len | ~15 | LOW |
| 21 | for_in_slice_nested.jhyy | C.5 | **UNCERTAIN (no .exe)** | same partial fix; `0..len(s)` reads garbage len → ACCESS_VIOLATION | ~15 | LOW |
| 22 | f32_suffix.jhyy | C.4 | **FAIL** | `emit_mov_f32_imm_to_offset` missing; f32 IMM falls to integer bits | ~30 | MED |
| 23 | f64_suffix.jhyy | C.4 | **PASS-by-accident (exit=0 matches expect=0)** | `cg_parse_f64_imm_bits` returns 0 for fractional (line 232); both literals → 0.0 → 0+0=0 | ~40 | HIGH |
| 24 | float_arith.jhyy | C.4 | **FAIL (exit=0 vs 6)** | same fractional gap + src2-imm binop XMM deferred (line 1366-1370) | ~50 | HIGH |
| 25 | float_arith_f32.jhyy | C.4 | **FAIL (likely)** | f32 IMM bit-pattern missing | ~40 | HIGH |
| 26 | const_array.jhyy | A2-ptr-deref | **FAIL** | `codegen_amd64_emit_mem.jhyy:511` flag-propagation lost across emit_copy | ~10-15 | MED |
| 27 | const_struct_array.jhyy | A2-ptr-deref | **FAIL (same bug)** | same as #26; extra struct-offset hop also affected | ~10-15 (same fix) | MED |
| 28 | cap_table_basic.jhyy (test 4) | C.2-rem | **FAIL (got=30 vs 42)** | `codegen_amd64_emit_call.jhyy:550-697` emit_amd64_arg_regs treats 16B struct as 1 reg; Win x64 needs RCX+RDX split | ~40 | HIGH |
| 29 | dungeon_game.jhyy | unique | **FAIL (gcc link error)** | Win ABI 5+ arg stack fallback (`emit_call` line 1281 comment "WARNING: emit_call nargs>=5 on Win ABI... stack-arg fallback not yet wired") + `_puts` symbol decoration mismatch | ~20-40 | HIGH |
| 30 | big_array.jhyy | B-runtime | **UNCERTAIN (no .exe)** | `codegen.jhyy:3181-3211` 400B alloc + printf args may overrun frame | ~30 | MED |
| 31 | top_level_let_mut_types.jhyy | B-runtime | **UNCERTAIN (no .exe)** | multi-global `cg_mod_global_register` growth or label collision; cannot verify | ~10-20 | LOW-MED |

### Honest aggregate by status

| Status | Count | Tests |
|--------|-------|-------|
| **PASS (stale .s evidence, needs fresh regress confirm)** | 12 | C.3 cluster (1-12) |
| **PASS (likely, .s clean, needs fresh regress)** | 2 | A1-XMM (13-14) |
| **PASS (verified exit code or correct reason)** | 2 | defer_multi_lifo (15), top_level_let_mut_test (16) |
| **UNCERTAIN (likely PASS per Iter 4 ship, partial fix may be benign)** | 4 | slice_index, slice_iterate, slice_literal, for_in_slice_nested |
| **FAIL (confirmed)** | 8 | slice_subrange, f32_suffix, float_arith, float_arith_f32, const_array, const_struct_array, cap_table_basic, dungeon_game |
| **PASS-by-accident (exit matches but for wrong reason)** | 1 | f64_suffix |
| **UNCERTAIN (FAIL likely, no .exe to verify)** | 2 | big_array, top_level_let_mut_types |
| **TOTAL** | **31** | |

**v2.11.17+ implementation candidate count** (real FAIL + FAIL-by-accident + UNCERTAIN-FAIL):
- **Definite FAIL**: 8 tests (slice_subrange, 4 C.4, 2 A2-ptr-deref, 1 C.2 cap_table)
- **Probable FAIL**: 4-6 tests (C.5 uncertain + big_array + top_level_let_mut_types)
- **TOTAL**: ~12-14 tests if all uncertain FAIL

**Honest FLIP gate projection** for v2.11.17+ (per standing directive "FLIP count 逼近 5 立即停手"):
- Iter 1 (slice addr+8 — 5 tests): FLIP +5 (at STOP threshold — proceed but stop after)
- Iter 2 (f64 imm bit-pattern — 2-4 tests): FLIP +3
- Iter 3 (A2-ptr-deref flag — 2 tests): FLIP +2
- Iter 4 (cap_table test 4 — 1 test): FLIP +1
- Iter 5 (B-runtime — 2-3 tests): FLIP +2-3 (depends on byte-equal D26 impact)
- Iter 6 (dungeon_game gcc link — 1 test): FLIP +1

**Total potential**: ~15-16 tests closed across 6 iters, ~165-265 LOC. But Iter 5 (B-runtime frame) may trigger byte-equal D26 regress → early STOP.

## Bug surface deep-dive (for v2.11.17+ reference)

### A. C.5 slice addr+8 partial fix (Iter 4 residue)
- **File**: `compiler/src0/codegen.jhyy:881-891`
- **Code**: `let mut addr_plus_8 = addr;` (no offset) + comment "Pragmatic: emit `storel %t<len_id>, addr` AGAIN — wrong but无害"
- **Why partial**: `cg_emit_store(KIND_SLICE, slice_value, slot)` writes both ptr and len to SAME addr slot. The `len_id = val.id + 1` is the len temp, but the 2nd `storel` writes len to `addr` (NOT `addr+8`), so len overwrites ptr.
- **Why some tests PASS anyway**: `NODE_SLICE_LIT` at `codegen.jhyy:3214-3248` pre-builds ptr_addr + len_addr as separate IR temps BEFORE `cg_emit_store` is called. For initial `let s = &[10,20,30]`, the slice literal path bypasses the dispatcher. The dispatcher bug fires only on subsequent assignments (e.g. `let sub = s[1..4]`) or when `cg_assign` directly calls the dispatcher.
- **Fix pattern**: Either (a) emit `add %t<slot>,8` to construct len_addr, then use that as 2nd storel target — needs `IRVal` offset arithmetic primitive; or (b) factor `cg_copy_slice` helper that handles 16B store via 2 separate QBE IL stores (similar to existing `cg_copy_struct`).
- **LOC est**: ~15-25 LOC; could be ONE QBE IL builder helper + change dispatcher to use it.

### B. C.4 float imm bit-pattern (Iter 3 residue)
- **Files**: `compiler/src0/codegen_amd64_emit_call.jhyy:204-280` (`cg_parse_f64_imm_bits`) + `:283-323` (`emit_mov_imm_to_offset` + `emit_mov_f64_imm_to_offset`) + `:1215-1230` (copy IMM f64/f32 dispatch)
- **Bug 1**: `cg_parse_f64_imm_bits:230-233` explicitly returns 0 for fractional `d_N.M` literals (e.g. `d_2.5`). Need to extend parser to handle fractional values via IEEE 754 bit construction.
- **Bug 2**: No `emit_mov_f32_imm_to_offset` helper. f32 IMM `s_2.5` → integer 2 stored as 4-byte int; loaded via `movss` interprets as f32 garbage.
- **Bug 3** (deferred from Iter 3): `emit_binop:1366-1370` comment "src2 imm literal... imm fall through 现有 integer path". f64 ops with literal args emit `addq`/`imulq` instead of `addsd`/`mulsd`.
- **Fix pattern**: (1) Extend `cg_parse_f64_imm_bits` with IEEE 754 single+double bit construction (table lookup for common fractions 0.5, 1.5, 2.5, etc., or full strtod + bit-cast). (2) Add `emit_mov_f32_imm_to_offset` mirroring f64 helper. (3) Add src2-imm XMM path in `emit_binop`.
- **LOC est**: ~50-80 LOC for f64 + ~30-40 LOC for f32 + ~30-50 LOC for src2-imm XMM = ~110-170 total.

### C. A2-pointer-deref flag propagation
- **File**: `compiler/src0/codegen_amd64_emit_mem.jhyy:511` (emit_load indirect-dispatch check)
- **Bug**: `cg_is_address_holder(state, src)` check fails to fire because the address-holder flag is lost across `movq %rax, -<dst>(%rbp)` store in `emit_binop` (and through `emit_copy` at `codegen_amd64_emit_call.jhyy:1241-1243`).
- **Symptom**: `const_array_run.s:25` `movzbl -64(%rbp), %eax` reads low byte of stack slot containing ASCII_LOWER+25 address; should be `movzbl (%rax), %eax` reading ASCII_LOWER[25] = 122.
- **Fix pattern**: Add flag-propagation to `emit_copy` (when copying a pointer-typed temp, mark dst as address-holder) OR add flag-propagation to `emit_binop` final store.
- **LOC est**: ~10-15 LOC.
- **Risk**: MED — touches emit_copy/emit_binop hot path; could cascade-affect other pointer-heavy tests (struct passing, &local capture, slice ops).

### D. C.2 cap_table test 4 (16B struct cross-fn arg)
- **File**: `compiler/src0/codegen_amd64_emit_call.jhyy:550-697` (`emit_amd64_arg_regs`, lines 637-666 for Win path)
- **Bug**: For 16B struct pass-by-value, Win x64 ABI requires **2 integer regs** (RCX + RDX), not 1 reg + caller mem-copy. Current code treats `l %t32` (struct alloc ptr) as single 8-byte scalar in RCX, missing RDX half.
- **Fix pattern**: Detect 16B struct allocation in `emit_amd64_arg_regs`; emit 2 `movq` (loadl of struct+0 → RCX, loadl of struct+8 → RDX). For SysV, struct still passes via 2 regs (RDI+RSI for first args) but with same 2-load pattern.
- **LOC est**: ~40 LOC.
- **Risk**: HIGH — touches emit_call hot path used by every cross-fn struct call (cap_table_advanced, nested_struct_deep, struct_val_pass).

### E. dungeon_game gcc link error (Win ABI 5+ arg + symbol decoration)
- **Files**: `compiler/src0/codegen_amd64_emit_call.jhyy:1281` (Win ABI 5+ arg stack fallback) + `target_dispatch.jhyy` (target=Win vs SysV) + `abi_amd64_win.jhyy` (extern symbol decoration)
- **Bug 1**: For nargs ≥ 5, current code emits only rcx/edx/r8d/r9 (4 regs) + fake remaining args by copying rcx to spill slots. Real Win x64 ABI: push args 5+ to `40(%rsp)+` BEFORE `call`.
- **Bug 2**: Extern `puts`/`printf`/`scanf` declared without underscore prefix. On Windows mingw gcc, C library symbols are `_puts`/`_printf`/`_scanf`. Self-backend emits Windows ABI calling convention but with SysV-style symbol names.
- **Fix pattern**: (1) Implement Win ABI 5+ arg stack fallback (push args 5+ to `40(%rsp)+` BEFORE `call`). (2) Verify `_puts` decoration on Windows extern, OR ensure target dispatch emits right ABI for host.
- **LOC est**: ~20-40 LOC.
- **Risk**: HIGH — touches ABI calling convention + symbol decoration; could break all FFI tests, all extern-using tests.

### F. B-runtime (big_array + top_level_let_mut_types)
- **Files**: `compiler/src0/codegen.jhyy:3181-3211` (NODE_ARRAY_LIT stack alloc) + `:4277-4325` (module global data section emit) + `:1544-1600` + `:1921-1955` (NODE_IDENT/NODE_ASSIGN for IRVAL_STR globals)
- **Bug 1** (big_array): Frame size calculation misses either 400B array alloc OR printf arg spill slots OR double-counts 8B sum/i locals → STACK_BUFFER_OVERRUN at printf.
- **Bug 2** (top_level_let_mut_types): 3 globals means `cg_mod_global_register` called 3 times; either array grows but `n_mod_globals` doesn't track correctly OR data section labels collide.
- **Fix pattern**: Audit `emit_alloc` A0 stub call sites + prologue `subq $N, %rsp` to ensure sum matches all allocs. For globals, audit `cg_mod_global_register` lookup + ensure each global gets distinct label.
- **LOC est**: ~30-50 LOC total.
- **Risk**: MED — frame miscalc can silently corrupt unrelated tests; needs regression on other printf + array tests + multi-global tests.

## v2.11.17+ implementation roadmap (derived from this audit)

**Order by ROI + risk** (LOW risk first, then by tests-closed-per-LOC):

1. **C.5 slice addr+8** (~15-25 LOC, 5 tests, LOW risk) — clean fix, well-isolated, only affects slice store path
2. **C.4 float imm bit-pattern f32 + f64** (~110-170 LOC, 4 tests, MED-HIGH risk) — needs IEEE 754 bit construction table; HIGH risk of getting fractional parsing wrong
3. **A2-pointer-deref flag propagation** (~10-15 LOC, 2 tests, MED risk) — touches emit_copy/emit_binop hot path; cascade risk
4. **C.2 cap_table test 4** (~40 LOC, 1 test, HIGH risk) — touches emit_call hot path used by every cross-fn struct call
5. **B-runtime big_array + top_level_let_mut_types** (~30-50 LOC, 2-3 tests, MED risk) — frame audit
6. **dungeon_game gcc link** (~20-40 LOC, 1 test, HIGH risk) — separate ABI work; recommend defer to v2.11.19+

**FLIP gate projection**:
- Iter 1 + 2 combined: FLIP +9-13 (potential STOP at +5 in middle)
- Iter 3: FLIP +2
- Iter 4: FLIP +1
- Iter 5: FLIP +2-3 (with potential byte-equal D26 impact)
- Iter 6: deferred to v2.11.19+

**Recommended v2.11.17 scope**: Iters 1+2+3 (slice + float + A2) = ~135-210 LOC, 11 tests closed. Stop before Iter 4 (HIGH risk cap_table).

**Recommended v2.11.18 scope**: Iter 4 (cap_table) + Iter 5 (B-runtime) = ~70-90 LOC, 3-4 tests.

**Recommended v2.11.19 scope**: Iter 6 (dungeon_game) + W-074.6 family residual (172 LOC, multi-sprint) + v2.12.0 QBE removal.

## Critical files modified

**Source**: NONE for v2.11.16 Phase 0 (per user choice)

**Docs** (1 commit):
- `docs/logs/v2/changelog-v2.11.0.md` — append v2.11.16 sub-section
- `docs/plans/v2/v2.11.16-plan.md` — NEW per-version plan (per `feedback_plans_per_version`, this file)
- `docs/internal/workarounds.md` — W-074.6/W-074.7 entries update with v2.11.16 audit results

## Verification

**v2.11.16 Phase 0 ship gate** (1 docs commit only):

1. ✅ Self-backend 86/135 PASS preserved (NO source change → trivially preserved)
2. ✅ QBE baseline 115/135 PASS preserved
3. ✅ byte-equal D26 5/5 PASS preserved
4. ✅ byte-equal-amd64 10/10 PASS preserved
5. ✅ big_test self-backend EXIT=57 preserved
6. ✅ match.jhyy self-backend PASS preserved
7. ✅ top_level_let_mut_test self-backend PASS preserved (verified exit=42)
8. ✅ defer_multi_lifo self-backend PASS preserved (Go-style semantics verified)
9. ✅ Per-test root cause audit completed (31 entries, this file)
10. ✅ Cluster re-classification table built (8 ACTUAL FAIL + 4 UNCERTAIN + 19 PASS)
11. ✅ v2.11.17+ implementation roadmap derived (6 iters ranked by ROI + risk)
12. ✅ Bug surface deep-dive for each cluster (file:line + code snippet + fix pattern)

## OS 启动链路 (no change for v2.11.16 Phase 0)

V2-C Part 2a (续):
- ✅ v2.11.10 T3-a + T4-c (5/6 self-backend closure) ship
- ✅ v2.11.11 T4-g lexer (5/6 maintained) ship
- ✅ v2.11.12 shl/shr + dst_id=0 (6/6 self-backend EXIT exact closure) ship
- ✅ v2.11.13 Iter 1-4 (A1+A2+C.1+C.2 cne, +14 self-backend PASS, scope DOWN) ship
- ✅ v2.11.14 C.3 attempt + revert (0/31 closure, hard STOP #3) ship
- ✅ v2.11.15 4 iters (A1-XMM + C.3 + C.4 + C.5) + 2 no-op (A2 + C.2) ship
- ⏳ **v2.11.16** Phase 0 per-test root cause audit (本 plan, 0 source commit, 1 docs commit)
- 待 ship: v2.11.17 C.5 slice + C.4 float imm + A2-ptr-deref (3 iters, ~135-210 LOC, 11 tests)
- 待 ship: v2.11.18 C.2 cap_table + B-runtime (2 iters, ~70-90 LOC, 3-4 tests)
- 待 ship: v2.11.19 dungeon_game + W-074.6 family residual + v2.12.0 QBE removal

## References

- **Predecessor plan**: `docs/plans/v2/v2.11.15-plan.md` (4 impl iters + 2 no-op; ship record honest about partial fixes in source comments)
- **Predecessor plan**: `docs/plans/v2/v2.11.14-plan.md` (C.3 attempt + revert, 0/31 closure, hard STOP #3)
- **Per-test audit logs** (3 Explore agents IN PARALLEL):
  - Agent A: A-cluster + dungeon_game (5 tests) — A1-XMM likely PASS, A2-ptr-deref FAIL, dungeon_game FAIL
  - Agent B: C.5 slice + C.4 float + B-runtime (12 tests) — partial fixes documented, top_level_let_mut_test PASS verified
  - Agent C: C.3 emit_phi + C.7 defer + C.2 cap_table (14 tests) — .s STALE pre-Iter 2, defer PASS correct, cap_table FAIL
- **Memory**: [[feedback_fix_evaluation_rule]] (5-version 4-way-wrong lesson history — initial audit over-counted FAIL by ~20 tests; spot-check vs full regress gap; source comment "partial fix" markers matter); [[feedback_codegen_amd64_multifn]] (FLIP ≥ 4 STOP, scope DOWN trigger); [[feedback_il_s_debugging_pattern]] (.s evidence pattern, BUT stale .s is also a trap); [[feedback_plans_per_version]] (v2.11.16-plan.md NEW, this file); [[feedback_changelog_umbrella]] (v2.11.16 sub-section append); [[feedback_document_workarounds_in_docs]] (W-074.6/W-074.7 update with audit); [[feedback_audit_single_commit_diff]] (Phase 0 docs-only 1 commit); [[feedback_auto_push_after_commit]]; [[feedback_ssh_key_same_shell]]
- **Baseline SHAs**: v2.11.15 ship `8197db4` (1 docs commit), tag `v2.11.15`; jhyy.exe.sha256 = v2.11.15 ship sha (no source change in v2.11.16 Phase 0)
- **Self-backend regress baseline**: v2.11.15 = 86/135 PASS, 29 FAIL + 20 SKIP (initial audit; this audit revises to ~10 actual FAIL after stale .s evidence + verified PASS tests reconciled)
- **QBE baseline**: 115/135 PASS preserved
- **byte-equal baseline**: D26 5/5 (timeout=120); byte-equal-amd64 10/10 preserved
- **big_test baseline**: EXIT=57 preserved

## Honest scope assessment (per `feedback_fix_evaluation_rule`)

**v2.11.16 actual outcome**: Phase 0 audit only, 0 source commit, 0 test closed in v2.11.16.

**v2.11.16 deliverable**: Per-test root cause audit (this file) + cluster re-classification (29 → ~10 ACTUAL FAIL) + v2.11.17+ implementation roadmap (6 iters ranked by ROI).

**Key reconciliation findings**:
- Initial audit claimed 29 FAIL; 3-agent deep-dive found only ~10 ACTUAL FAIL (rest are stale .s evidence, verified PASS, or PASS-by-correct-reason)
- v2.11.15 Iter 2 C.3 fix is **actually correct** (just stale .s evidence from pre-Iter 2 capture) — all 12 C.3 tests likely PASS
- v2.11.15 Iter 4 C.5 slice fix is **partially correct** (most tests pass via caller pre-built path; only `slice_subrange` confirmed FAIL with addr+8 bug)
- v2.11.15 Iter 1+1b A1-XMM fix is **actually correct** (.s evidence clean)
- 8 confirmed FAIL tests cluster into 4 distinct bug surfaces (C.5 addr+8, C.4 f64 imm + f32 imm, A2-ptr-deref flag propagation, C.2 cap_table 16B struct ABI, dungeon_game gcc link)

**v2.11.17+ projection** (data-driven from this audit):
- Best case (15 tests): ~10% (all 6 iters clean + cross-impact)
- Likely case (8-11 tests): ~50% (most fixes work but some have sub-bugs)
- Worst case (3-5 tests): ~40% (some cluster fix triggers hard STOP, e.g. C.4 IEEE 754 bit construction gets fractional wrong or B-runtime frame size breaks byte-equal D26)

---

## Audit correction addendum (post-ship verification 2026-09-16)

**v2.11.16 ship 后 user 要求实测 regress.py --self-backend(自研 amd64 backend 触发器)**,暴露 Phase 0 audit 多个估算错误。Honest correction:

### Real self-backend baseline (实测)

```
$ python regress.py --self-backend --no-baseline-check --timeout=120
===== 87 PASS / 28 FAIL / 20 SKIP (of 135 total) =====
```

(per `feedback_regress_py_abspath`:`regress.py` 内置 `--self-backend` flag 强制 `JHY_SELF_BACKEND=1`,跟 QBE fallback 走 135 tests 同口径,expected exit code 从 `.jhyy` 源码 `// EXPECT:` 注释读)

### 对账 vs audit 估

| Audit 估 | 实际 | 误差 |
|---------|------|------|
| Baseline 86/29/20 | 87/28/20 | -1 PASS / -1 FAIL / same SKIP(基本一致) |
| 19 PASS (12 C.3 stale + 2 A1-XMM clean + 2 verified + 1 by-accident) | 3 PASS | **-16(估错了 16 个)** |
| 8 confirmed FAIL | 8 confirmed FAIL | ✓ |
| 4 UNCERTAIN likely PASS | 0/4 PASS(全 FAIL) | **-4(估错了)** |
| 2 UNCERTAIN FAIL likely (no .exe) | 2/2 FAIL | ✓ |

### 31 entries per-test 真实状态 (regress.py --self-backend)

| # | Test | Cluster | Audit 估 | **Real** |
|---|------|---------|---------|----------|
| 1 | char_pattern | C.3 | PASS stale .s | **FAIL** (got=6 expected=0) |
| 2 | enum_match_arm_tag_check | C.3 | PASS stale .s | **FAIL** (got=0 expected=200) |
| 3 | match_exhaustive | C.3 | PASS stale .s | **FAIL** (got=garbage expected=2) |
| 4 | match_range | C.3 | PASS stale .s | **FAIL** (got=12 expected=0) |
| 5 | min_enum | C.3 | PASS stale .s | **PASS** (唯一 C.3 真 PASS) |
| 6 | mixed_struct_slice_match | C.3 | PASS stale .s | **FAIL** (got=0 expected=131) |
| 7 | or_exhaust | C.3 | PASS stale .s | **FAIL** (crash NTSTATUS_0xC65F6A32) |
| 8 | or_same_bind | C.3 | PASS stale .s | **FAIL** (got=garbage expected=42) |
| 9 | payload_bind_basic | C.3 | PASS stale .s | **FAIL** (got=102 expected=42) |
| 10 | payload_bind_multi | C.3 | PASS stale .s | **FAIL** (got=0 expected=1234) |
| 11 | payload_bind_nested | C.3 | PASS stale .s | **FAIL** (got=garbage expected=42) |
| 12 | payload_bind_short | C.3 | PASS stale .s | **FAIL** (got=102 expected=42) |
| 13 | generics_fn_call_site_inference | A1-XMM | PASS likely | **PASS** ✓ |
| 14 | generics_fn_turbofish_basic | A1-XMM | PASS likely | **PASS** ✓ |
| 15 | defer_multi_lifo | C.7-defer | PASS verified | **FAIL** (got=1 expected=0) |
| 16 | top_level_let_mut_test | B-runtime | PASS verified | **FAIL** (got=106 expected=42) |
| 17 | slice_subrange | C.5 | FAIL confirmed | **FAIL** ✓ (got=36 expected=60) |
| 18 | slice_index | C.5 | UNCERTAIN likely PASS | **FAIL** (got=44 expected=80) |
| 19 | slice_iterate | C.5 | UNCERTAIN likely PASS | **FAIL** (got=42 expected=60) |
| 20 | slice_literal | C.5 | UNCERTAIN likely PASS | **FAIL** (got=42 expected=60) |
| 21 | for_in_slice_nested | C.5 | UNCERTAIN likely PASS | **FAIL** (crash ACCESS_VIOLATION) |
| 22 | f32_suffix | C.4 | FAIL confirmed | **FAIL** ✓ (got=90 expected=0) |
| 23 | f64_suffix | C.4 | PASS-by-accident | **FAIL** (got=90 expected=0) |
| 24 | float_arith | C.4 | FAIL confirmed | **FAIL** ✓ (got=0 expected=6) |
| 25 | float_arith_f32 | C.4 | FAIL confirmed | **FAIL** ✓ (got=99 expected=4) |
| 26 | const_array | A2-ptr-deref | FAIL confirmed | **FAIL** ✓ (got=25 expected=122) |
| 27 | const_struct_array | A2-ptr-deref | FAIL confirmed | **FAIL** ✓ (got=garbage expected=9) |
| 28 | cap_table_basic test 4 | C.2-rem | FAIL confirmed | **FAIL** ✓ (got=30 expected=42) |
| 29 | dungeon_game | unique | FAIL confirmed | **FAIL** ✓ (compile exit=1) |
| 30 | big_array | B-runtime | UNCERTAIN FAIL likely | **FAIL** ✓ (crash STACK_BUFFER_OVERRUN) |
| 31 | top_level_let_mut_types | B-runtime | UNCERTAIN FAIL likely | **FAIL** ✓ (got=garbage expected=17) |

**Audit accuracy: 13/31 = 42%**(13 个估对:8 confirmed FAIL + 2 A1-XMM + 1 min_enum + 2 B-runtime UNCERTAIN FAIL)

### 28 FAIL 真实 cluster map (v2.11.17 起点)

| Cluster | Tests | Count |
|---------|-------|-------|
| **C.3 emit_phi** | char_pattern, enum_match_arm_tag_check, match_exhaustive, match_range, mixed_struct_slice_match, or_exhaust, or_same_bind, payload_bind_basic, payload_bind_multi, payload_bind_nested, payload_bind_short | **11** |
| **C.5 slice addr+8** | slice_subrange, slice_index, slice_iterate, slice_literal, for_in_slice_nested | **5** |
| **C.4 float imm** | f32_suffix, f64_suffix, float_arith, float_arith_f32 | **4** |
| **B-runtime** | big_array, top_level_let_mut_test, top_level_let_mut_types | **3** |
| **A2-ptr-deref** | const_array, const_struct_array | **2** |
| **C.2 cap_table** | cap_table_basic test 4 | **1** |
| **unique** | dungeon_game, defer_multi_lifo | **2** |

### W-074.7 status correction

- audit 估 "✅ CLOSED via v2.11.15 Iter 2 commit `568d3aa` per-arm injection strategy"
- **真实**:11/12 C.3 tests still FAIL post-Iter 2;只有 min_enum PASS
- Iter 2 fix 改了 `codegen_amd64_emit_ctrl.jhyy` emit_phi 真实现,但实际只 closed 1/12 = min_enum
- 其他 11 个 C.3 tests 仍 FAIL — Iter 2 fix **未** 根治 phi merge gap,需要重设计

### v2.11.17 重设计起点 (基于实测 28 FAIL)

按 ROI + LOC 排序(per FLIP ≥ 4 STOP 阈值):
1. **Iter 1**: C.5 slice addr+8 真修(~15-25 LOC, +5 tests, LOW risk)— 明确 win
2. **Iter 2**: A2-ptr-deref flag propagation(~10-15 LOC, +2 tests, MED risk)
3. **Iter 3**: B-runtime frame audit(~30-50 LOC, +3 tests, MED risk)
4. 累计 +10 FLIP,STOP 临界 → defer float/cap_table/C.3/unique to v2.11.18+

### v2.11.16 ship 真实评估

- Phase 0 docs-only ship record 写的"23-25/30 spot-check PASS" + "86/135 baseline 维持" 实际 = **0 net closure**(没改任何 source)
- Audit 数字 baseline 86/135 接近真实 87/135(差 1);31 entries per-test 估错 18/31(58%)
- v2.11.16 真实价值 = 提供了 audit 数据基础设施(31 entries 表格 + cluster map + W-074.x entries 框架),虽 per-test 估错但 cluster 分类和 fix pattern 推断大体正确
- W-074.8 / W-074.9 entries 描述的具体 bug surface(file:line + code snippet)在 self-backend 实测下大部分对应到 28 FAIL,fix pattern 推断对

**Memory lessons (新增)**:
- [[feedback_fix_evaluation_rule]] 加:spot-check / audit estimate / .s evidence 都不等于 full regress 实测;**always run `regress.py --self-backend`** (同 135 tests 口径) 才能 claim baseline
- audit 估 cluster map 可能对(28 FAIL cluster 分类对),但 per-test 估 PASS/FAIL 可能错(42% 准确率),原因:.s 证据 / 源注释 partial-fix 标记 / 旁路路径推断都可能误导
- v2.11.x ship gate 一直是 `regress.py --self-backend` spot-check 5-30 tests(不是全 135),ship record 写的 spot-check PASS 不能 claim 全量 baseline
