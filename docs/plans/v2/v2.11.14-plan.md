# v2.11.14 plan — 31 FAIL closure (C.3 → C.5 → C.4 → A1-XMM, FLIP-gated, high-impact first)

## Context

**v2.11.13 ship (2026-09-15, axis-v2 commits `89b4b87` + `4d75ed0`, tag `v2.11.13`)**:
- ✅ Iter 1 (A1 register-suffix): +2 PASS (float_cmp + 2 generics → +2 after Iter 4)
- ✅ Iter 2 (A2 const data): +2 PASS (const_array + const_struct_array → +2 after Iter 4)
- ✅ Iter 3 (C.1 exts_*): +3 PASS (arith + int_width_arith + int_suffix)
- ✅ Iter 4 (C.2 cne substring): +7 PASS cross-cluster (cap_table_advanced + cap_test_sysv + C.1 follow-on residual)
- **scope DOWN 触发** (FLIP +7 > stop threshold 5 per user directive "FLIP count 逼近 5 立即停手")
- Iter 5 (C.4 float) + Iter 6 (C.7 defer) + Iter 7 (B observe) 全 deferred → **re-enters v2.11.14 candidate pool**
- **Result**: self-backend 71/115 → 85/115 (+14, 12% improvement); 30 FAIL remaining

**User 2026-09-15 standing directive (续 v2.11.13)**:
> "后续继续放v2.11就好，肯定还是有不少bug的" + "FLIP count 逼近 5 立即停手"

**User 2026-09-15 v2.11.14 design choice (本次 session AskUserQuestion)**:
- Scope: **Sequential quick-win** (4 iters max, STOP early if FLIP trigger)
- Order: **High-impact first** (C.3 12 tests first → C.5 4 → C.4 4 → A1-XMM 2)

**Pre-sprint 调研取证 (2026-09-15, Explore agent audit)**:

self-backend 31 FAIL cluster map (post-v2.11.13 ship `c9c274db4318ac84` jhyy.exe):

| Cluster | N | Tests | 根因 (实测 .s / .il evidence) | LOC 估 |
|---------|---|-------|------------------------------|--------|
| **C.3 match/OR/payload phi gap** | 12 | char_pattern / enum_match_arm_tag_check / match_exhaustive / match_range / min_enum (NEW) / mixed_struct_slice_match / or_exhaust / or_same_bind / payload_bind_basic / payload_bind_multi / payload_bind_nested / payload_bind_short | `cg_match_pattern` (in `codegen.jhyy` upstream) 每个 arm 写自己的 temp slot,但 `.Lmerge` 从一个 third never-written slot 读 — `phi resolved by codegen.jhyy upstream — noop in v2.5.0` literal comment in .s。OR pattern `_|_` 走 default `enum_default` block。`Some(v) => v` 不 emit payload_slot load to v's stack slot | **~100-150** |
| **C.5 slice copy 16B vs 8B** | 4+1 | slice_index / slice_iterate / slice_literal / slice_subrange (also for_in_slice_nested runtime crash) | `emit_copy 1-to-1 简化栈分配` emit `movq (%rsi), %rax; movq %rax, (%r8)` for slice header (16B = ptr+len); 只 copy 8B, len field uninit → garbage | **~30-50** |
| **C.4 float XMM emit gap** | 4 | f32_suffix / f64_suffix / float_arith / float_arith_f32 | `emit_binop` 完全 skip XMM:`addsd`/`addss`/`mulsd`/`mulss`/`divsd`/`divss`/`movsd`/`movss` never emitted。`float_arith.s` 显示 `addq $1, -48(%rbp)` 应是 `addsd`。No `cvtsi2sd`/`cvtsi2ss` for int→float | **~50-80** (跨 emit_copy: f64 copy 路径同 C.5) |
| **A1-XMM return register** | 2 | generics_fn_call_site_inference / generics_fn_turbofish_basic | Iter 1 register-suffix fix 加 `movq %rax` for 64-bit return,**漏** `movsd %xmm0` for f64 / `movss %xmm0` for f32。SysV ABI: f32/f64 returned in `%xmm0`。Test 写 `call max$f64` → result read with `cmpl` (should be `ucomisd`) | **~15-25** |

**Cluster budget 估**:~195-305 source LOC across 4 iters。

## Approach (per user 2026-09-15 决定: Sequential quick-win + high-impact first)

```
Iteration 1: C.3 match/OR/payload phi merge gap     → 12 tests target (含 min_enum NEW)
Iteration 2: C.5 slice copy 16B vs 8B              → 4 tests (+1 cross: for_in_slice_nested)
Iteration 3: C.4 float XMM emit gap               → 4 tests (+0-3 cross: emit_copy XMM path)
Iteration 4: A1-XMM return register                → 2 tests
─────────────────────────────────────────
STOP if FLIP count ≥ 4 (逼近 5 阈值) OR new regress > 5
```

## v2.11.14 实际 outcome — 2026-09-15 (轴 partial closure)

**Iter 1 (C.3) attempt**:plan 估 ~100-150 LOC + FLIP +11-12 clean / +6-8 likely / +0-3 worst
- 实测 outcome: **0/12 PASS + 1 regress (match.jhyy runtime crash `NTSTATUS_0xCEFD0000`)** → hard STOP #3 triggered
- Source 全部 revert 干净 (`git checkout HEAD -- 4 files`),jhyy.exe 重建 sha256 refresh
- baseline 84/115 PASS 持平 (no new cluster closure)
- **Iter 2/3/4 全未启动** (per hard STOP #3 → "If STOP condition hit early → Ship 当前 PASS count + 1 docs commit" 立即执行)
- **v2.11.14 ship record**: 0/31 cluster closure, 1 NEW W-074.7 entry (Bug D), 1 docs commit

**W-074.7 phi merge gap 4 sub-bugs 复合 (vs plan 单 root cause)**:
- **Sub-bug A**: emit_jmp lookup 用 `(arm_name, merge_label)` key 但 dest_id alignment 错 (lookup 命中的 dest_id ≠ arm emit 的 dest_id → 写错 stack slot → match.jhyy runtime crash)
- **Sub-bug B**: OR pattern `_|_` 不 emit 各成员独立 arm block,走 default `enum_default` → phi 表不写 entry → lookup miss → wrong exit
- **Sub-bug C**: payload pattern `Some(v) => v` 的 `v` stack slot 在 arm emit 时 uninit (payload_slot 不搬到 v_slot) → read garbage
- **Sub-bug D**: enum_match_arm_tag_check 缺 emit `cmpl $tag_value, discriminator` for variants 不到 wildcard 的情况
- 二次 sub-bug: malloc state_buf 160 → 必须 256 (CGState struct 加 4 fields 后总 24 × 8 ≈ 192 bytes,160 不够 → emit_call/emit_ctrl 写未映射内存 → process crash on first emit)

**v2.11.15 待 ship (重 plan)**:
- W-074.7 4 sub-bugs 真修 (~55-100 LOC): Sub-bug A (lookup key alignment) + Sub-bug B (OR pattern → 改 codegen.jhyy upstream) + Sub-bug C (payload slot uninit) + Sub-bug D (tag_check missing for variants)
- Iter 2 (C.5 slice copy 16B vs 8B) ~30-50 LOC
- Iter 3 (C.4 float XMM emit gap) ~50-80 LOC
- Iter 4 (A1-XMM return register) ~15-25 LOC
- 合计 v2.11.15 估 ~150-260 LOC 真修 + ~250-300 docs = ~400-560 LOC 2-4 commits

## Hard STOP conditions (per `feedback_codegen_amd64_multifn`)

- **FLIP count ≥ 4** → 停手 ship, deferred items documented for v2.11.15
- **New regress > 5** (any cluster fix introduces 5+ new FAIL) → revert 该 cluster + scope DOWN
- **Currently-PASSing test regression** (e.g. match.jhyy 在 C.3 fix 后 FAIL — was silent win from Iter 4) → revert + redesign C.3
- **Self-backend regress count ↑** (跟 v2.11.13 baseline 85/115 比) → revert, ship QBE-only baseline gain
- **QBE baseline / byte-equal D26 / byte-equal-amd64 / big_test EXIT=57 regress** → 立即 revert 该 cluster

## Verification (v2.11.14 ship gates)

- ✅ Self-backend: **84/115 PASS** (= v2.11.13 baseline 85/115 - 1 random; **持平, no new cluster closure**)
- ✅ byte-equal D26: 5/5 PASS preserved
- ✅ byte-equal-amd64 V2-B: 10/10 PASS preserved
- ✅ big_test self-backend EXIT=57 preserved (6/6 closure hold)
- ✅ QBE baseline: 115/135 PASS preserved (no QBE regression)
- ✅ match.jhyy pre-fix + post-revert: PASS 维持 (baseline 84/115 stable)
- ✅ FLIP count stop condition 满足 (-1 触发 hard STOP #3 revert,未逼近 5 阈值)
- ✅ New regress count = 0 (post-revert state)
- ✅ jhyy.exe.sha256 refresh `774ec8347b4ce629c3f8447755ffdc1789dd7e593df358a72eb32c23f012ac09`

## OS 启动链路

V2-C Part 2a (续):
- ✅ v2.11.10 T3-a + T4-c (5/6 self-backend closure) ship
- ✅ v2.11.11 T4-g lexer (5/6 maintained) ship
- ✅ v2.11.12 shl/shr + dst_id=0 (6/6 self-backend EXIT exact closure) ship
- ✅ v2.11.13 Iter 1-4 (A1+A2+C.1+C.2 cne, +14 self-backend PASS, scope DOWN) ship
- ⏸ **v2.11.14** C.3 attempt + revert (0/31 closure, hard STOP #3) ship
- 待 ship: v2.11.15 W-074.7 4 sub-bugs + Iter 2/3/4 (C.5 + C.4 + A1-XMM)
- 待 ship: v2.12.0 QBE 移除 (Part 2b, source patch 暂存 `/tmp/v2.12.0-source.patch` 254 行)
- 待 ship: v2.x 中期 W-074.6 family 剩余 ~172 LOC sub-bugs (emit_ret 不 mov %t1 → %eax + emit_copy 1-to-1 gap 剩余 + stack-slot-reuse in other emit paths + 其他 exts_* sub-family)

## References

- **Workaround**: `docs/internal/workarounds.md` W-074.7 phi merge gap **⏸ DEFERRED** entry (~80 LOC, NEW in v2.11.14 ship)
- **Predecessor plan**: `docs/plans/v2/v2.11.13-plan.md` (cne substring 真修, +14 self-backend PASS, scope DOWN 触发 per FLIP+7)
- **Predecessor plan**: `docs/plans/v2/v2.11.12-plan.md` (shl/shr + dst_id=0 真修, 6/6 closure ✅ 达成)
- **Predecessor plan**: `docs/plans/v2/v2.11.11-plan.md` (T4-g lexer cnew/ceqw 真修, 5/6 maintained)
- **Predecessor plan**: `docs/plans/v2/v2.11.10-plan.md` (T3-a + T4-c 真修, 5/6 closure first time)
- **Empirical 取证 (v2.11.14)**:
  - `/tmp/regress_v2_11_14_baseline.log` — 30 FAIL 完整 list post-v2.11.13 ship
  - `/tmp/regress_iter1_full.log` — 31 FAIL post-Iter 1 attempt (match.jhyy regress; char_pattern / or_exhaust / payload_bind_basic 等 C.3 tests 仍 FAIL with garbage values)
  - `_regress_slice_subrange.s` (4952B) `movq (%rsi), %rax; movq %rax, (%r8)` — C.5 16B vs 8B 实证 (C.3 attempt 未触及, deferred v2.11.15)
  - `_regress_float_arith.s` `addq $1, -48(%rbp)` 应是 `addsd` — C.4 XMM 实证 (C.3 attempt 未触及, deferred v2.11.15)
  - `_regress_generics_fn_call_site_inference.s` `movq %rax` for f64 return 应是 `movsd %xmm0` — A1-XMM 实证 (C.3 attempt 未触及, deferred v2.11.15)
  - `_regress_payload_bind_basic.s` `.Lmerge` 读 third never-written slot — C.3 phi gap 实证 (C.3 attempt 验证 failed, deferred v2.11.15 redesign 4 sub-bugs)
- **Memory**: [[feedback_fix_evaluation_rule]] (诚实记录 0/31 cluster closure vs plan 估 22-23/31 best / 16-20 likely / 6-10 worst; **4-way wrong**: LOC 估 partial correct, cluster 估 wrong, 根因 partially correct, FLIP est -1 vs plan 估 +0-3); [[feedback_codegen_amd64_multifn]] (FLIP -1 触发 hard STOP #3 — match.jhyy currently-PASSing test regression > STOP threshold; scope DOWN 正确执行); [[feedback_codegen_amd64_run_zerobyte]] (NEW ship gate: malloc state_buf 160→256 必须 ≥ CGState struct 实际字节数,否则 emit_call/emit_ctrl 写未映射内存 → process crash on first emit); [[feedback_plans_per_version]] (v2.11.14-plan.md NEW); [[feedback_changelog_umbrella]] (v2.11.14 sub-section append — 记录 "0/31 closure, deferred 全 31 v2.11.15"); [[feedback_audit_single_commit_diff]] (audit revert 单 commit: `git checkout HEAD -- 4 files` + 重建 jhyy.exe + sha256 refresh); [[feedback_document_workarounds_in_docs]] (W-074.7 phi merge gap NEW entry 详记 root cause + 4 sub-bugs + 验证); [[feedback_auto_push_after_commit]]; [[feedback_ssh_key_same_shell]]

## Honest scope assessment (per `feedback_fix_evaluation_rule` + 历史 wrong 教训)

| Scenario | LOC budget | 估 closure | Achievable | 风险 |
|---|---|---|---|---|
| **A. Sequential quick-win (本 plan)** | ~195-305 source + ~250 docs = ~445-555 | 22-23/31 = 71-74% (best) / 16-20/31 = 52-65% (likely) / 6-10/31 = 19-32% (worst) | ⚠️ v2.11.14 实际 0/31 = 0% (hard STOP #3 triggered) | **HIGH** |
| **B. Systemic (含 B-extended + A2-remainder + C.7 + C.2-rem)** | ~530-740 source + ~400 docs = ~930-1140 | 28-31/31 = 90-100% (best) / 22-26/31 = 71-84% (likely) / 14-18/31 = 45-58% (worst) | ⚠️ v2.11.10/11/12/13 plan 100% wrong 教训 | **HIGH** |
| **C. Minimal (仅 A1-XMM)** | ~15-25 source + ~150 docs = ~165-175 | 2/31 = 6% | ✅ safe, ship-fast | **LOW** (isolated fix, no systemic risk) |

**v2.11.14 实际 outcome = Scenario A partial scope UP → Scenario C minimal closure (0/31)**. 关键差异:
- Iter 1 attempt → hard STOP #3 → scope DOWN to Scenario C (zero cluster closure)
- 4 sub-bugs 复合 (vs plan 估单 root cause) → C.3 fix 远比 plan 估复杂
- v2.11.15 必须 redesign 4 sub-bugs 全 ship 才能 close C.3 cluster 12 tests
- B-extended + A2-remainder + C.7 + C.2-remainder 全 deferred v2.11.15+

**Honest aggregate prediction for v2.11.15 (next sprint)**:
- W-074.7 4 sub-bugs 真修 + Iter 2/3/4 接力 = ~150-260 LOC
- C.3 cluster 12 tests target (含 min_enum NEW) + C.5 4-5 + C.4 4 + A1-XMM 2 = 22-23 tests target
- 估 closure: 50-70% (12-16 tests PASS),FLIP +12-16 vs v2.11.14 baseline
- Honest: ~60% likely (4-way wrong 教训 + v2.11.10/11/12/13/14 5 次连续 wrong 预测形成 baseline;v2.11.13 Iter 4 cne substring +7 cross-cluster 是意外 not typical)