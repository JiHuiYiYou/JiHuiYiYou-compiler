# docs/plans/v2/ — v2.x sprint 计划（QBE 自写 + 多目标 + OS 准备）

本目录放 **v2.x**（QBE 自写 + 多目标 / 自研 OS 准备）的 sprint 级计划。

**当前状态**：v2.0 阶段 (v2.0.0 → v2.4.0) ✅ 全 ship (2026-09-02 ~ 09-04, tags `v2.3.0` / `v2.4.0`);v2.x 中/末 ⏳ 未启动,等 user 启动。

## 文件清单

| 文件 | 状态 | 简介 |
|------|------|------|
| [`v2.0.0-os-prep.md`](v2.0.0-os-prep.md) | 设计文档 | OS 编写前置的语言 / 编译基础设施清单（M1-M11 节点 + P0/P1/P2 优先级）|
| [`v2.0.0任务清单 + 概要设计.md`](v2.0.0任务清单 + 概要设计.md) | **sprint 计划**(2026-09-01 user 决定) | v2.0.0 = Sprint A Stage 1: target dispatcher 起步 |
| [`v2.1.0任务清单 + 概要设计.md`](v2.1.0任务清单 + 概要设计.md) | **sprint 计划** | v2.1.0 = Sprint A Stage 2: ABI 抽离（Windows x64 + UEFI 风格）|
| [`v2.1.0详细实现方案.md`](v2.1.0详细实现方案.md) | **sprint 计划**(详细实现) | v2.1.0 = 每个 ABI 函数的具体实现 + codegen 抽离调用细节 + byte-equal 验证脚本 |
| [`v2.2.0任务清单 + 概要设计.md`](v2.2.0任务清单 + 概要设计.md) | **sprint 计划** | v2.2.0 = Sprint A Stage 3: spec 锁定（abi § 13 + lang-spec § 17-20 + build.md）|
| [`v2.3.0任务清单 + 概要设计.md`](v2.3.0任务清单 + 概要设计.md) | **sprint 计划** | v2.3.0 = Sprint B: hello-freestanding.efi 跑 OVMF（EFI struct + lld-link + QEMU 启动 + printk）|
| [`v2.4.0任务清单 + 概要设计.md`](v2.4.0任务清单 + 概要设计.md) | **sprint 计划** | v2.4.0 = Sprint C: 多目标 dispatcher + byte-equal 三件套（cross-jhyy-version 验证）|
| [`v2.5.0详细实现方案.md`](v2.5.0详细实现方案.md) | **sprint 计划**（详细实现） | v2.5.0 = codegen_amd64.jhyy skeleton 起步（self backend wired）|
| [`v2.6.8-plan.md`](v2.6.8-plan.md) | **sprint 计划** | v2.6.8 = docs hygiene D43 closure sha refresh（workarounds.md 2 处 stale `d708793c...` → `92e82554...`）|
| [`v2.7.0-plan.md`](v2.7.0-plan.md) | **sprint 计划** | v2.7.0 = amd64_sysv + amd64_sysv_freestanding 真 ABI codegen path（2 ABI modules + emit_call 拆 target + 5 sysv tests SKIP）|
| [`v2.7.1-plan.md`](v2.7.1-plan.md) | **sprint 计划** | v2.7.1 = Linux ELF runtime（crt0.S + link.ld）+ regress.py `--cross {wsl,docker,auto,none}` 实 wire + D43 HOLD |
| [`v2.7.2-plan.md`](v2.7.2-plan.md) | **sprint 计划**（✅ shipped, 2026-09-08, commit `e3dac54`,patch）| v2.7.2 = regress.py `--cross=docker` false-positive 修（exit=127 显式检查 + docker image `ubuntu:22.04 → gcc:12`）,5 LOC 1 commit |
| [`v2.8.0-plan.md`](v2.8.0-plan.md) | **sprint 计划**（✅ shipped, 2026-09-08, 2-commit chain `0dd33bb` + `84d8c61`）| v2.8.0 = M2 sysv + sysv_freestanding codegen 真实现（codegen_amd64_emit_mem/ctrl/peephole target_tag dispatch）+ D43 re-baseline `cc894329...` → `6a2f2277...` + docker wire-only chain（方案 C）+ 5 sysv tests SKIP honest（C-side TARGET_AMD64_SYSV_FREESTANDING enum pending v2.x 末）,~260 LOC 2 commits |
| [`v2.8.1-plan.md`](v2.8.1-plan.md) | **sprint 计划**（✅ shipped, 2026-09-08, commit `2d37492`, tag `v2.8.1`）| v2.8.1 = C-side target_dispatch.{c,h} mirror update（4 targets, rename STUB → SYSV preserve =2）+ C-side cg_module SYSV/SYSVFS cases + regress.py comments。~50 LOC 1 commit。**Honest discovery**: jhyy-side codegen.jhyy cg_module 仍 v2.7.0 stub-fatal for SYSV/SYSVFS → W-070 NEW, 留 v2.8.2 / v2.x 末 (M4 launch 硬前置) |
| [`v2.8.2-plan.md`](v2.8.2-plan.md) | **sprint 计划**（✅ Phase 1 ship, 2026-09-08, commit `8b4d43d`）| v2.8.2 = W-070 真修：jhyy-side codegen.jhyy cg_module 真 emit SysV QBE IL。10 dispatch sites in cg_func/cg_expr + CGContext target_tag field (144→152 bytes) + cg_module fallthrough restructure。~120 LOC 1 commit。**M4 launch 硬前置彻底解锁**。5 sysv tests end-to-end 真跑 PASS requires Ubuntu WSL host (user-level verify) |
| [`v2.8.3-plan.md`](v2.8.3-plan.md) | **sprint 计划**（✅ Phase 1 ship, 2026-09-08, 2-commit chain, this plan 验证 5/5 sysv PASS gate + scope creep fix）| v2.8.3 = docker gcc chain infra 补完: `--no-link` flag (cmd_compile + --help) + regress.py docker branch 2-stage subprocess rewrite (Stage 1: Windows-side jhyy --no-link → .s; Stage 2: docker gcc:12 chain → ELF → 跑) + driver logic fix (r.returncode 不再用, EXIT:N-only PASS check) + 2 stale fixture fix (sysv_abi_test / sysv_vararg_basic extern → local inline fn) + scope creep: _DOCKER_BIN abs path fallback (per feedback_gh_cli_path pattern)。~75 LOC source + 125 LOC docs, 2 commits。**M4 launch 验证完整化 (5/5 sysv PASS via docker)** |
| [`v2.9.0-plan.md`](v2.9.0-plan.md) | **sprint 计划**（V2-C Part 1, ✅ shipped, 2026-09-08, tag `v2.9.0`, 2-commit chain `623f3f8` src0 revert + `ad6459f` verification harness）| v2.9.0 = N≥3 selfhost fixed point 验算 + verification harness（`fixed_point.sh` + `fixed_point_summary.py` + regress.py `--fixed-point` + cap_test.jhyy 跨代一致）。~290 LOC sprint + src0 revert 3100 LOC deletions。**M5 deferral 第二前置 Part 1**（verification 闭环, N=3 ✅ PASS + N=4/N=5 informational PASS）|
| [`v2.10.0-plan.md`](v2.10.0-plan.md) | **sprint 计划**（V2-C Part 2+3, 🚫 **RENUMBERED → v2.12.0**, 2026-09-09 user 决定, per W-074 codegen_amd64_run 0-byte .s blocker）| 原 v2.10.0 因 W-073 + W-074 双 bug 暴露 (regress 5/5 FAIL on `JHY_SELF_BACKEND=1` path) renumber 为 [v2.12.0](v2.12.0-plan.md)。**v2.11.0** = codegen_amd64_run 真修 (per [`v2.11.0-plan.md`](v2.11.0-plan.md), W-073 + W-074 trace + fix + regress.py `--self-backend` flag),ship 在 v2.12.0 之前 |
| [`v2.11.0-plan.md`](v2.11.0-plan.md) | **sprint 计划**（V2-C Part 2a, ✅ shipped 2026-09-10 on commit 25dfb00 + tag v2.11.0, 串行 v2.9.0 + v2.11.1 之间）| v2.11.0 = codegen_amd64_run 真修 (W-073 verification gap closure + W-074 0-byte .s 根因定位 + regress.py `--self-backend` flag + 5/5 QBE PASS + self-backend 不 crash + stderr warning)。~140 LOC 2 commits。**M5 deferral 第二前置 Part 2a**。|
| [`v2.11.1-plan.md`](v2.11.1-plan.md) | **sprint 计划**（V2-C Part 2a-补, ✅ shipped 2026-09-10 on axis-v2 + tag v2.11.1, 串行 v2.11.0 + v2.11.2 之间）| v2.11.1 = W-074.5 lexer gap closure + 7 真修延伸 (dbgfile/dbgloc/{/} + ILTOK_DIRECTIVE + parse_and_emit noop + csltw lexer + next_token_call args consume + struct offset 真修 + emit cast 真修) + scope DOWN ship gate (5/5 QBE PASS + 1/5 self-backend hello PASS per [[feedback_codegen_amd64_multifn]])。~390 LOC 3 commits (tag v2.11.0 + source + docs)。**M5 deferral 第二前置 Part 2a-补** (lexer gap closure 闭 → W-074.6 multi-func self-backend 留 v2.x 中期 per [[feedback_codegen_amd64_run_zerobyte]])。|
| [`v2.11.2-plan.md`](v2.11.2-plan.md) | **sprint 计划**（V2-C Part 2a-后, ✅ shipped 2026-09-11 on axis-v2 + tag v2.11.2, 串行 v2.11.1 + v2.11.3 之间）| v2.11.2 = W-074.6 multi-func self-backend crash/hang closure (Tier 1+2+3 全 + Tier 5 token cap + per-fn frame state reset + global-max variant + 3 个 temp offset fn 统一 + Rule 3 DISABLED) + scope DOWN ship gate per user 决定 (5/5 QBE PASS + 5/5 self-backend terminate + EXIT ≥3/5 match hello/fib_renamed/struct_val_pass)。~420 LOC 2 commits。**M5 deferral 第二前置 Part 2a-后** (W-074.6 PARTIAL closure, W-074.7 EXIT exact 留 v2.11.3 per [[feedback_codegen_amd64_multifn]])。|
| [`v2.11.3-plan.md`](v2.11.3-plan.md) | **sprint 计划**（V2-C Part 2a-后-补, ✅ shipped 2026-09-11 on axis-v2 + tag v2.11.3, 串行 v2.11.2 + v2.12.0 之间）| v2.11.3 = W-074.7 self-backend EXIT exact (T4-h 真修 + cur_fn_idx init/reset + T4-c/T4-b/T4-g scope DOWN deferral) + scope DOWN ship gate (5/5 QBE PASS + 5/5 self-backend terminate + EXIT exact 2/5 hello=42/struct_val_pass=35; fib_renamed=32 lit imm operand bug / nested_struct_deep=wrong / big_test=link error pre-existing 重复 `.Lloop_body` label in codegen.jhyy, deferred v2.11.4+ / v2.x 中期)。~25 LOC source 真修 + ~140 LOC 防御性字段/注释 + ~120 LOC docs = ~285 LOC 2 commits。**M5 deferral 第二前置 Part 2a-后-补 PARTIAL** (T4-h 真修 closure, big_test link error 留 v2.x 中期 codegen.jhyy fix)。|
| [`v2.11.4-plan.md`](v2.11.4-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补, ✅ shipped 2026-09-12 on axis-v2 + tag v2.11.4, 串行 v2.11.3 + v2.12.0 之间）| v2.11.4 = W-074.7 T4-b `emit_binop` src1/src2 parse 真修 (fib_renamed EXIT 32 → 40 = 832040 mod 256 真修)。~30 LOC source 真修 (cg_find_byte helper + emit_binop parse + 7 op branch imm support)。**M5 deferral 第二前置 Part 2a-后-补-补** (T4-b fib_renamed 真修 closure; struct_val_pass EXIT=102 pre-existing loadw/alloc bug + nested_struct_deep EXIT=127 pre-existing multi-arg/loadw bug + big_test link error pre-existing codegen.jhyy 重复 `.Lloop_body` label 仍待 v2.x 中期独立 sprint 排查/真修; 不在 v2.11.4 scope per user)。|
| [`v2.11.5-plan.md`](v2.11.5-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补, ✅ shipped 2026-09-12 on axis-v2 + tag v2.11.5, 串行 v2.11.4 + v2.11.6 之间）| v2.11.5 = W-074.7 alloc-tracking 真修 (`cg_offset_for_temp` 跟 `cg_alloc_slot` 两套方案统一 via `CGState.temp_slot_for_id` 256-entry array + extern bridge 跟 regalloc 同 pattern + `emit_alloc` record slot)。修了 nested_struct_deep NTSTATUS_0xDE09A8E7 crash + struct_val_pass garbage exit。~111 LOC source + ~80 LOC docs = ~190 LOC 2 commits。**fix_evaluation_rule 诚实记录**:实际 EXIT match 2/5 (跟 v2.11.4 baseline 持平, hello=42 / fib_renamed=832040), EXIT exact 仍 deferred v2.11.6 因为 emit_store pointer semantics gap (alloc-result %tN 当 stack slot VALUE 处理, 真语义要 `lea -OFF(%rbp), %rax` 间接地址, L4 § 3.5 E2 注释确认的 v2.5.0 简化边界)。**M5 deferral 第二前置 Part 2a-后-补-补** (撞 crash 修复, EXIT exact 留 v2.11.6)。|
| [`v2.11.6-plan.md`](v2.11.6-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补, ✅ shipped 2026-09-12 on axis-v2 + tag v2.11.6, 串行 v2.11.5 + v2.11.7 之间）| v2.11.6 = W-074.7.5 peephole max_lines 真修 (4096 → 65536) + 配套 arena def_size 8 MB bump + W-074.7.6 block-name uniquify (`_b<count>` suffix 拼 `.L<name>` via CGState.block_name_uniq 256-entry table + FNV-like hash + per-fn zero reset + record/lookup helpers)。**scope UP 决策**: 原 plan ~25 LOC scope DOWN (block-name uniquify only), 调研后发现 peephole max_lines cap 才是真根因 (big_test 4090 行 .s 在 cap 4096 fold 时 truncated, 第 4091 行起 drop → main_jhyy 永不写出 → link fail), per 2026-09-12 user 决定 scope UP 真修真根因。~158 LOC source + ~80 LOC docs = ~240 LOC 2 commits。**fix_evaluation_rule 诚实记录**: **5/5 link 真达成** ✅ (big_test 此前 v2.11.5 + 所有之前版本都 link fail), 但 EXIT match 仍 3/5 (跟 v2.11.5 baseline 持平) — struct_val_pass EXIT=6 (≠ 35 EXPECT) / nested_struct_deep EXIT=35 (≠ 22 EXPECT) 都因 emit_store pointer semantics gap, deferred v2.11.7; big_test runtime STATUS_INTEGER_OVERFLOW 0xC0000095 是 separate deeper bug, deferred v2.11.8+ scope 待调研。**M5 deferral 第二前置 Part 2a-后-补-补-补** (link 真修 closure, EXIT exact 留 v2.11.7)。**显式证伪 [[feedback_codegen_amd64_run_zerobyte]]**: N≥3 .il byte-equal 不验 .s, peephole cap bug 不能 catch — big_test N=3 .il byte-equal 但 .s 在 t_bit_and body 中部截断 → main_jhyy 不见 → link fail。|
| [`v2.11.7-plan.md`](v2.11.7-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补, ✅ shipped 2026-09-12 on axis-v2 + tag v2.11.7, 串行 v2.11.6 + v2.11.8 之间）| v2.11.7 = **调研 + scope DOWN defer** (~0 LOC source change, ~120 LOC docs)。原 plan 假设真根因是 "emit_store 把 alloc-result pointer 当 stack slot VALUE 处理", scope UP CGState 大改 (新 `temp_is_alloc_pointer` 256-entry bitmap + emit_load/emit_store/emit_copy TEMP/emit_call arg load 全 dispatch, ~190 LOC) 实施后 regress 53/115 → 40/115 (13 测试 regress)。**调研发现真根因更深**: 4 emit path 真修只对 alloc-result pointer 有效, **derived address (e.g. `add %t6, 0` 的 result temp 装的是 runtime 计算的 address, 不是 stack slot) 才是真根因**。Codegen 1-to-1 栈栈搬运假设 (L4 § 3.5 E2 简化边界) 在 `storew val, derived_addr` 跟 `loadw derived_addr` 形态破, 需 `movl %eax, (%raddr)` 间接写。per 2026-09-12 user 决定 scope DOWN — defer 真修 v2.11.7, 启动 v2.11.8 调研 full derived-address tracking (bitmap 扩到 flag 任何 temp holding derived address, ~250-350 LOC 估)。**W-074.7.7 emit_store pointer semantics = INVALID closure** (调研结论); **W-074.7.8 derived-address tracking = NEW (v2.11.8 待 ship)**。**fix_evaluation_rule 诚实记录**: 实际 EXIT match = 3/5 (跟 v2.11.6 baseline 持平 — **没新修, 因为调研发现真根因更深, scope UP 不充分**); 不强宣 "4/5 EXIT exact closure"。**M5 deferral 第二前置 Part 2a-后-补-补-补-补** (调研 + scope DOWN defer, 真修留 v2.11.8 derived-address tracking)。|
| [`v2.11.8-plan.md`](v2.11.8-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补, ✅ shipped 2026-09-13 on axis-v2 + tag v2.11.8, 串行 v2.11.7 + v2.11.9+ 之间）| v2.11.8 = **W-074.7.8 derived-address tracking 真修 (FULL scope, ~319 LOC source + ~30 docs = ~349 LOC 2 commits)**。Bitmap flag 任何 temp holding derived address (alloc-result + binop add/sub on pointer + copy of address-holder); emit_load/store/loadsub 改 full dispatch (slot vs indirect `mov<size> (%r8), %reg` via %r8 scratch); emit_binop 产生 derived-address 时 flag result; emit_copy propagate flag (TEMP + FNARG paths); emit_alloc 用 lea+mov 写 alloc'd 地址到 pointer-slot (formula offset, NOT region — **self-referential slot bug 真修**)。**关键真修**: 1) self-referential slot bug (v2.11.5 design 让 pointer-slot == region, lea+mov 自我覆盖; 真修 SKIP `cg_record_temp_slot`, pointer-slot 走 formula `-(32+t*8)` Win); 2) FNARG flag propagate (l-typed fnarg = struct param pointer 走 FNARG path 不 flag propagate, struct_val_pass EXIT=6→35 真修)。**fix_evaluation_rule 诚实记录**: **4/5 EXIT exact closure** ✅ (hello=42 / fib_renamed=40 = 832040 mod 256 / struct_val_pass=35 / nested_struct_deep=22 / struct_val_assign=30); big_test runtime STATUS_INTEGER_OVERFLOW 0xC0000095 仍 deferred v2.11.9+ (W-074.7.9 NEW, separate deeper bug 跟 derived-address 无关); 不强宣 "5/5 EXIT closure"。self-backend regress +2 flips (56→58, 远低于 +5 scope DOWN trigger per [[feedback_codegen_amd64_multifn]]); QBE fallback 115/115 PASS preserved; byte-equal-amd64 10/10 preserved; D43 closure v1↔v2 .il sha HOLD; **NEW ship gate**: main_jhyy.s = 12 行 / 207 bytes ≥ baseline (no truncation, 防 v2.11.6-style 截断 bug 回归 per [[feedback_codegen_amd64_run_zerobyte]])。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补** (W-074.7.8 PARTIAL closure 4/5 EXIT exact; W-074.7.9 big_test runtime deferred v2.11.9+)。|
| [`v2.11.9-plan.md`](v2.11.9-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补, ✅ shipped 2026-09-13 on axis-v2 + tag v2.11.9, 串行 v2.11.8 + v2.11.9a+ 之间）| v2.11.9 = **W-074.7.9 big_test runtime QBE side 真修 (~18 LOC source + ~90 LOC docs = ~108 LOC 2 commits)**。**2 个独立 root cause 真修** (per plan mode Explore agent 调研): 1) `idiv` emit 无 sign-extend prefix (`is_div` branch at `codegen_amd64_emit_call.jhyy:1068-1080`, x86 `idivl` semantics dividend = `(%edx << 32) \| %eax` → `%edx` 残留 → x86 #DE fault → Windows STATUS_INTEGER_DIVIDE_BY_ZERO 0xC000008C, wrapped 0xC0000095 per process config; 真修 add `\tcltd\n` / `\tcqto\n` prefix mirror `is_mod`); 2) Lexer 不识 QBE `rem` keyword (`next_token_binop` dispatcher + main `next_token` `'r'` → ret/rem disambiguate, QBE `%` operator emits `rem` (signed remainder, per QBE spec §6.3) → `next_token` returns -1 → `lex_il` 静默 skip → rem-result SSA values 永远 missing from .s; 真修 add `rem` branch in `next_token_binop` + `'r'` → ret/rem dispatch in main `next_token`)。Bonus `is_rem` flag + dispatch (`emit_binop`: `cg_find_sub` check for "rem" 3-char substring + dispatch branch mirror `is_mod`)。**fix_evaluation_rule 诚实记录 scope DOWN per user 决定**: **QBE side 5/5 EXIT exact closure 真修达成** ✅ (hello=42 / big_test=**12345** 真修 closure / struct_val_pass=35 / fib_renamed=832040 / nested_struct_deep=22 / struct_val_assign=30, 9 `rem` ops 正确 emit cltd+idivl + movq %rdx, %rax); self-backend side 仍 🟡 ACTIVE due to **W-074.6 family** (extsw silent-skip + multi-func body 0-byte + emit_ret 不 mov %t1 → %eax 等 pre-existing bugs, 已知范围 ~500+ LOC 多 sprint, **显式 deferred v2.x 中期 per W-074.6 ACTIVE state**)。QBE fallback 115/115 regress PASS preserved + self-backend 1/5 hello PASS preserved (per W-074.6 baseline, multi-func closure deferred v2.x 中期); D43 closure v1↔v2 .il sha HOLD (v2/v3/v4/v5 sha `42580b87...` post-fix, re-baseline per D43 closure rule 因 rem emit 微变); byte-equal-amd64 10/10 preserved; fixed_point N≥3 PASS preserved; **NEW ship gate audit grep** (per [[feedback_codegen_amd64_run_zerobyte]]): self-backend big_test.s `cltd` = 14 (5 div + 9 rem) + `idivl` = 14 + `movq %rdx, %rax` = 9 (was 0 pre-fix) + `.s` 行数 8727 ≥ 100 bytes + `main_jhyy.s` non-empty; jhyy.exe.sha256 refresh `9ef7f49734ef99d7...`。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补** (W-074.7.9 QBE side CLOSURE; W-074.6 family self-backend 仍 ACTIVE, v2.x 中期独立 sprint 真修; 不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动)。|
| [`v2.11.10-plan.md`](v2.11.10-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补-补, ✅ shipped 2026-09-13 on axis-v2 + tag v2.11.10, 串行 v2.11.9 + v2.11.10a+ 之间）| v2.11.10 = **W-074.6 T3-a per-fn frame size + T4-c multi-arg FN_ARG 真修 (~155 LOC source + ~200 docs = ~355 LOC 2 commits)**。T3-a closure: 两阶段 pre-scan (`cg_compute_per_fn_max_temps` Phase 1 扫 fn_starts, Phase 2 per-fn max temp id) + CGState 加 fn_starts/per_fn_max/fn_count 3 字段 + emit_func_header 改读 `per_fn_max[cur_fn_idx]` 替换 v2.11.2 global-max variant (1 个 fn = 15488 bytes frame → 递归函数 SIGSEGV 真修)。T4-c closure (scope UP 配套 T3-a): lexer `next_token_func_header` 改 dup 完整 header text (`function w $name(args)`) 替代 v2.11.0-2.11.9 只 dup name; emit_func_header 加 `cg_state_set_fn_header` populate cur_fn_header_text + extract name from full header for `.globl` + label; emit_copy FNARG 路径替换 hardcode `reg_rcx_for_qt(dst_qt)` → `cg_find_arg_idx` + `reg_rax_for_qt_idx(dst_qt, idx, target_tag)` (Win x64 arg 0..3 = %rcx/%rdx/%r8/%r9, SysV = rdi/rsi/rdx/rcx via target_tag)。**fix_evaluation_rule 诚实记录**: **5/6 self-backend EXIT exact closure** ✅ (hello=42 / struct_val_pass=35 / fib_renamed=40 / nested_struct_deep=22 / struct_val_assign=30), 跟 v2.11.9 baseline 一致但 different bug fixed (T3-a 真修 verified: gcd `subq $600, %rsp` vs 旧 15488; T4-c 真修 verified: gcd body `movl %ecx` + `movl %edx` vs 旧 都 %ecx)。**5/5 self-backend closure NOT achieved** — gdb 取证 big_test 在 T3-a+T4-c 真修后 SIGFPE at `gcd+158 idivl -576(%rbp)` (T4-g: emit_binop cnew silent-skip + emit_jnz loop body entry silent-skip — pre-existing W-074.6 family bug, NOT in v2.11.10 plan scope)。v2.11.10 plan 文档 "5/5 100% 可达" 预测 wrong — per [[feedback_codegen_amd64_multifn]] scope UP trigger: T4-g 暴露后 scope DOWN 5/6 closure 接受 (跟 v2.11.9 baseline 一致不触发 +5 FLIP trigger)。**T4-g 真修 deferred v2.11.10a+ (~30-50 LOC 估)**。Stage 2 N=4 jhyy 编 jhyy closure PASS; QBE fallback 115/115 PASS preserved; byte-equal-amd64 10/10 preserved; fixed_point N≥3 PASS preserved; **NEW ship gate audit** (per [[feedback_codegen_amd64_run_zerobyte]]): `objdump -d big_test_run.exe | grep sub.*\$0x` distinct frame size > 5 (per-fn 优化生效) + `grep subq \$15488` = 0 + main_jhyy.s non-empty; jhyy.exe.sha256 refresh。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补-补** (W-074.6 T3-a + T4-c CLOSURE; W-074.6 family 其余 sub-bug 含 T4-g 仍 ACTIVE, 5/5 self-backend closure 留 v2.11.10a+ 真修; 不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动)。|
| [`v2.11.11-plan.md`](v2.11.11-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补-补-补, ✅ shipped 2026-09-13 on axis-v2 + tag v2.11.11, 串行 v2.11.10 + v2.11.11a+ 之间）| v2.11.11 = **W-074.6 T4-g lexer cnew/ceqw silent-skip 真修 PARTIAL closure (~5 LOC source + ~200 docs = ~205 LOC 2 commits)**。T4-g 真修: stage 1 guard 加 `n1 == (110 as i32)` ('n' for cnew prefix) → cnew 不再 stage 1 reject; stage 2 guard refactor flag pattern (2 独立 if 设 `has_type_suffix` flag + 最终 if check) 接受 type suffix at idx 3 (4-char ceqw/cnew) OR idx 4 (5-char csltw/cultw/...),**绕开 codegen short-circuit OR workaround 的 nested-OR bug** (首次尝试 nested OR 触发 QBE "predecessors not matched in phi" → refactor flag pattern 替代); op_str table 加 `n1 == 110 ('n') → "cnew"` branch consistency (dead store 但保持代码对称)。**fix_evaluation_rule 诚实记录**: **5/6 self-backend EXIT exact closure maintained** ✅ (hello=42 / struct_val_pass=35 / fib_renamed=40 / nested_struct_deep=22 / struct_val_assign=30), 跟 v2.11.10 baseline 持平但 different bug 真修 (T4-g 真修 verified: 65 个 4-char compare op 正确 emit = 11 sete + 54 setne in big_test_run.s + gcd Euclid loop cond check 正确 emit → 不再 SIGFPE in gcd+158)。**6/6 self-backend closure NOT achieved** — big_test 通过 gcd 后 **hang at t_bit_pack** (5min+ timeout),**新 stack-slot-reuse sub-bug 暴露** (W-074.6 family 范围内, in `compiler/src0/codegen_amd64_emit_call.jhyy` emit_binop shift op 路径): bit_pack 函数 emit `(a & 0xFF) | ((b & 0xFF) << 8) | ((c & 0xFF) << 16) | ((d & 0xFF) << 24)` 时所有 shift amounts (8/16/24) 跟 0xFF masks 写**同一个** -32(%rbp) slot → 计算错值 → check_eq 不等 → hang。QBE 后端不受影响 (QBE 走自己 allocator, EXIT=57 PASS 维持)。v2.11.11 plan 文档 "6/6 100% 可达" 预测 wrong (跟 v2.11.10 plan "5/5 100% 可达" 一样 wrong, Risk 5 警告成真) — per user 2026-09-13 pre-confirmed scope DOWN ("仅 T4-g 真修 (推荐)"): scope 限定在 T4-g lexer 真修, 6/6 closure 留 v2.11.11a+ 真修 stack-slot-reuse (~30-50 LOC 估)。Stage 2 N=4 jhyy 编 jhyy closure PASS; QBE fallback 115/115 PASS preserved; byte-equal-amd64 10/10 preserved; fixed_point N≥3 PASS preserved; **NEW ship gate audit** (per [[feedback_codegen_amd64_run_zerobyte]]): `set(ne|e)\b` count ≥ 65 + `jne .Lloop_body` count > 100 + `jmp .Lloop_end` count > 100 + `grep subq \$15488` = 0 (T3-a closure preserved) + main_jhyy.s non-empty; self-backend regress **71/115 PASS** (vs v2.11.10 baseline 58/115, **+13 PASS** 改善, T4-g fix 暴露之前 crash 在 gcd 的 tests 现在通过, FLIP count = 13 PASS improvements 非 scope DOWN trigger per [[feedback_codegen_amd64_multifn]]); jhyy.exe.sha256 refresh。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补-补-补** (W-074.6 T4-g PARTIAL closure, 6/6 self-backend closure deferred v2.11.11a+ stack-slot-reuse sub-bug; 不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动)。|
| [`v2.11.12-plan.md`](v2.11.12-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补-补-补-补, ✅ shipped 2026-09-15 on axis-v2 + tag v2.11.12, 串行 v2.11.11 + v2.11.12a+ 之间）| v2.11.12 = **W-074.6 shl/shr (5 ops and/or/xor/shl/shr) missing + emit-copy dst_id=0 真修 → 6/6 self-backend EXIT exact closure ✅ 达成 (~115 LOC source + ~250 docs = ~365 LOC 2 commits)**。**2 个独立 bugs 联动真修** (per v2.11.12 调研):(1) **Bug A — shl/shr (5 ops) missing**:lexer `next_token_binop` + main dispatcher `'s'` branch + `'a'` branch + 新 `'o'` + `'x'` branch 没有 and/or/xor/shl/shr handler → silent skip → bit_pack `shl $8/%cl` 类 op 不 emit → result 计算错;(2) **Bug B — emit_copy dst_id=0**:LHS parse path `% <ident> <ws>+ =` 在 t178 (last correct) → t180+ 之间 cursor state corruption → lex_parse_temp_id_from_ident 返回 0 → emit_copy 拿 dst_id=0 → formula `-(32 + 0*8) = -32` (Win) → 所有 copy collapse to slot -32 → bit_pack values overwrite each other → hang。**fix_evaluation_rule 诚实记录**: **6/6 self-backend EXIT exact closure ✅ 达成** (跟 v2.11.10 + v2.11.11 plan "100% 可达" 两次 wrong 教训形成对比,v2.11.12 ship record 6/6 真达成) — hello=42 / **big_test=57** (= 12345 mod 256, **was hang at t_bit_pack 5min+ timeout in v2.11.11, now PASS**) / struct_val_pass=35 / fib_renamed=40 / nested_struct_deep=22 / struct_val_assign=30。Stage 2 N=5 jhyy 编 jhyy closure PASS (D43 closure HOLD sha `9e61c42c...`); QBE fallback 115/115 PASS preserved; byte-equal-amd64 10/10 preserved; fixed_point N=3,4,5 .il byte-equal + cap_test 跨 N 代 EXIT=42 一致 preserved; **NEW ship gate audit** (per [[feedback_codegen_amd64_run_zerobyte]]): 20 shll/shrl/andl/orl/xorl emits in `_regress_big_test.s` (vs 0 pre-fix) + t_bit_pack + t_bit_unpack + t_shifts 全 PASS (was hang/crash) + bit_pack distinct slots -1424, -1432, -1440, ..., -1552 (vs all -32 collapse pre-fix); self-backend regress 69/115 (-2 vs v2.11.11 baseline 71/115, 全部 pre-existing FAILs dominate, plan target ≥75/115 NOT met — 因为 t_bit_pack / t_bit_unpack / t_shifts 不在 regress 测试 list); jhyy.exe.sha256 refresh `58a6f3a2...`。**Phase C sub-bug 修正** (本次 ship 期间 surface):(a) `%%` → `%` (sb_append_cstr 是 plain append 不是 printf-style),(b) reg path 漏 `movl` prefix → 分开 imm / reg path 各自 emit 完整 prologue。**累计 W-074.6 family closed ~325 LOC**:T3-a + T4-c + T4-g (lexer partial) + shl/shr (本次) + dst_id=0 (本次);剩余 ~175+ LOC (stack-slot-reuse in other emit paths + extsw silent-skip + emit_ret 不 mov %t1 → %eax 等) 留 v2.x 中期 V2-D。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补-补-补-补**:6/6 self-backend EXIT exact closure ✅ 达成, M5 启动仍需等剩余 W-074.6 family 子 sprint (extsw + emit_ret 等) 闭环 (per `v1.x-phase-4-m5-boot-from-scratch.md`);不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动。|
| [`v2.12.0-plan.md`](v2.12.0-plan.md) | **sprint 计划**（V2-C Part 2b+3, 待 ship, 串行 v2.11.3 后, renumbered from v2.10.0 per 2026-09-09 user 决定）| v2.12.0 = QBE 工具链完全移除 + toolchain self-contained closure（`run_qbe` 留空 stub + `qbe.exe` 删 + `qbe_wrapper.{c,h}` 删 + 自写后端 fatal-on-fail + Procmon 验证 0 qbe spawn）。Source 暂存 `/tmp/v2.12.0-source.patch` (254 行),v2.11.0 ship 后 reapplied。~320 LOC 2 commits。**M5 deferral 第二前置 Part 2b**（QBE 移除 → M5 可独立 sprint 启动）。**v2.12.0 ship gate scope DOWN per 2026-09-11 user 决定**:QBE fallback 5/5 PASS + self-backend ≥3/5 EXIT match (hello + fib_renamed + struct_val_pass),nested_struct_deep + big_test 待 v2.11.3 EXIT exact 后才完整闭合。|
| [`batch-V2-A-plan.md`](batch-V2-A-plan.md) | **sprint 计划**（batch）| V2-A batch = v2.0.0 + v2.1.0 + v2.2.0 + v2.3.0 + v2.4.0 串行 ship 链路（前 5 版本）|
| [`batch-V2-B-plan.md`](batch-V2-B-plan.md) | **sprint 计划**（batch）| V2-B batch = v2.5.0 + v2.6.x + v2.7.0 + v2.7.1 + v2.7.2 + v2.8.0（v2.x 中期 M2 自写后端 + Linux ELF infra）|

## 版本轴关系（关键）

**v2.0 阶段（v2.0.0 → v2.1.0 → v2.2.0 → v2.3.0 → v2.4.0）= 串行**（2026-09-01 user 决定）：
- 串行原因：每版在前版 baseline 上做，byte-equal 阶段性 self-equal 必须严格顺序（per D43）
- 跨版不能并行：v2.1.0 ABI 抽离依赖 v2.0.0 target dispatcher 起步；v2.2.0 spec 锁定依赖 v2.1.0 ABI 实现；v2.3.0 E2E 启动依赖 v2.2.0 spec 锁；v2.4.0 byte-equal 三件套依赖 v2.3.0 E2E 通
- v2.0 阶段 ship 后才启动 v2.x 中/末 + v3.0 3a-3f（per [`../roadmap/v2-v3-parallel-sprint-plan.md § 5.1` 路径 A](../roadmap/v2-v3-parallel-sprint-plan.md)）

**v2.x 中/末 ‖ v3.0+ = 异步并行**（2026-09-01 user 决定）：
- 不强配对：两条线各自推进，不要求 v3.0 启动跟 v2.x 中/末对齐
- 三条硬约束保留（per [`../roadmap/v2-v3-parallel-sprint-plan.md § 5.1-5.3`](../roadmap/v2-v3-parallel-sprint-plan.md)）：D27 v3.1 3g→3g.5→3g.7 串行 / v3.0 3c volatile 先 ship 再 v2.x 后端移植 volatile / v3.0 3d `#[no_std]` 软 ship（M1 launch 不依赖 per D10）

## 关联

- v2.x 长线路线图 → [`../roadmap/v2.x-qbe-rewrite.md`](../roadmap/v2.x-qbe-rewrite.md)
- v2.x ‖ v3.x 并行 sprint 调度 → [`../roadmap/v2-v3-parallel-sprint-plan.md`](../roadmap/v2-v3-parallel-sprint-plan.md)
- 上一代（v1.x 自举）→ [`../v1/`](../v1/)
- 自研 OS（独立 repo）→ `../../../jhyy_OS/`