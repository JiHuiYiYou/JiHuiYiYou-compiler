# docs/plans/v2/ — v2.x sprint 计划（QBE 自写 + 多目标 + OS 准备）

本目录放 **v2.x**（QBE 自写 + 多目标 / 自研 OS 准备）的 sprint 级计划。

**当前状态**：v2.0 阶段 (v2.0.0 → v2.4.0) ✅ 全 ship (2026-09-02 ~ 09-04, tags `v2.3.0` / `v2.4.0`);v2.x 中期 M1-B **parity milestone** ✅ ship (v2.11.23 ship 2026-09-17, self-backend 0 FAIL parity with QBE path, regress 119/119 PASS / 0 FAIL / 20 SKIP, jhyy.exe sha `02118a50...`, D43 closure HOLD on `e6b6f1fa...`);v2.x 末 5 sub-sprint 链 4/5 ✅ shipped (v2.12.0 audit ✅ → v2.13.0 真 XMM + sysv ✅ → v2.14.0 N 代 mutation ✅ → **v2.15.0 in-mem pipeline + `.s` byte-equal D43 dual-layer ✅ ship 2026-09-22, axis-v2 commit, tag `v2.15.0`, plan REWRITE per code reality (Strategy B+ skip `.il` file I/O, NOT 原 "QBE 自写"), regress 126/147 PASS HOLD, D43 closure HOLD on `954a8563...` (`.il`) + `216683e1...` (`.s`), ACTIVE workaround count = 0** → v2.16.0 QBE 移除 + bench + .exe byte-equal 📋 planned)。

## v2 整阶段完成定义 (3 阶段视图)

| 阶段 | 范围 | 关键 ship gate | 当前状态 |
|------|------|---------------|----------|
| **v2.0 阶段** | v2.0.0 → v2.4.0 (target dispatcher + ABI 抽离 + spec 锁 + hello-freestanding E2E + 多目标 dispatcher) | 多目标 dispatcher + byte-equal 三件套 + D26 reproducibility recipe + tag v2.4.0 | ✅ 全 ship (2026-09-02 ~ 09-04) |
| **v2.x 中期** | v2.5.0 → v2.13.0 (self-backend M2 + M1-A unit + M1-B parity + 真 XMM + 真 sysv) | self-backend 0 FAIL parity with QBE path (regress 119/119 PASS, 0 FAIL, 20 SKIP) + 真 XMM regalloc + 真 amd64_sysv codegen 全覆盖 | ✅ M1-B ship (v2.11.23), M2 真 XMM + sysv 全覆盖 📋 planned (v2.13.0) |
| **v2.x 末** | v2.14.0 → v2.16.0 (N 代 fixed point + mutation + QBE 自写 + QBE 移除 + perf + .exe byte-equal) | N≥10 fixed point + mutation catch ≥ 80% + 跳过 QBE IL 直 emit x86-64 + QBE 工具链 0 依赖 + perf ≤ 1.1x C 版 + `.exe` byte-equal recipe | 📋 全 planned (v2.12.0 audit → v2.13.0 → v2.14.0 → v2.15.0 → v2.16.0) |
| **M5 deferral** | 删 `src/*.c` + untrack QBE + 删 `runtime.c` (per `v1.x-phase-4-m5-boot-from-scratch.md`) | jhyy 编 jhyy 0 C 依赖闭环 | ⏸ v2.x 末 ship 后启动 (M5 第二前置 = QBE 完全移除 per v2.16.0) |

**关键说明**:
- v2.x 中期 + 末 跟 v3.x **异步并行** (per 2026-09-01 user 决定,放弃 wall-clock 并行优化)
- v2.x 末 5 sub-sprint **串行** (per `feedback_plans_per_version`,每步 ship 后下一步启动;v2.12.0 audit 验证 phantom → v2.13.0 真 XMM regalloc 在 audit verified infra 上加 → ...)
- v2.x 末 ship 后 → M5 独立 sprint 启动 → jhyy 编 jhyy 0 C 依赖闭环 → jhyy_OS M1/M4/M11 launch 联调 (per `v2-v3-parallel-sprint-plan.md § 5.1`)

## 文件清单

| 文件 | 状态 | 简介 |
|------|------|------|
| [`v2.0.0-os-prep.md`](v2.0.0-os-prep.md) | 设计文档 | OS 编写前置的语言 / 编译基础设施清单（M1-M11 节点 + P0/P1/P2 优先级）|
| [`v2.0.0任务清单 + 概要设计.md`](v2.0.0任务清单 + 概要设计.md) | **sprint 计划**(2026-09-01 user 决定) | v2.0.0 = Sprint A Stage 1: target dispatcher 起步 |
| [`v2.1.0任务清单 + 概要设计.md`](v2.1.0任务清单 + 概要设计.md) | **sprint 计划** | v2.1.0 = Sprint A Stage 2: ABI 抽离（Windows x64 + UEFI 风格）|
| [`v2.1.0详细实现方案.md`](v2.1.0详细实现方案.md) | **sprint 计划**(详细实现) | v2.1.0 = 每个 ABI 函数的具体实现 + codegen 抽离调用细节 + byte-equal 验证脚本 |
| [`v2.2.0任务清单 + 概要设计.md`](v2.2.0任务清单 + 概要设计.md) | **sprint 计划** | v2.2.0 = Sprint A Stage 3: spec 锁定（abi § 13 + lang-spec § 17-20 + build.md）|
| [`v2.3.0任务清单 + 概要设计.md`](v2.3.0任务清单 + 概要设计.md) | **sprint 计划** | v2.3.0 = Sprint B: hello-freestanding.efi 跑 OVMF（EFI struct + lld-link + QEMU 启动 + printk）|
| [`v2.4.0任务清单 + 概要设计.md`](v2.4.0任务清单 + 概要设计.md) | **sprint 计划** | v2.4.0 = Sprint C: 多目标 dispatcher + byte-equal 三件套（cross-jhyy-version 验证）|
| [`v2.5.0详细实现方案.md`](v2.5.0详细实现方案.md) | **sprint 计划**（详细实现） | v2.5.0 = codegen_amd64.jhyy skeleton 起步（self backend wired）|
| [`v2.6.8-plan.md`](v2.6.8-plan.md) | **sprint 计划** | v2.6.8 = docs hygiene D43 closure sha refresh（workarounds.md 2 处 stale `d708793c...` → `92e82554...`）|
| [`v2.7.0-plan.md`](v2.7.0-plan.md) | **sprint 计划** | v2.7.0 = amd64_sysv + amd64_sysv_freestanding 真 ABI codegen path（2 ABI modules + emit_call 拆 target + 5 sysv tests SKIP）|
| [`v2.7.1-plan.md`](v2.7.1-plan.md) | **sprint 计划** | v2.7.1 = Linux ELF runtime（crt0.S + link.ld）+ regress.py `--cross {wsl,docker,auto,none}` 实 wire + D43 HOLD |
| [`v2.7.2-plan.md`](v2.7.2-plan.md) | **sprint 计划**（✅ shipped, 2026-09-08, commit `e3dac54`,patch）| v2.7.2 = regress.py `--cross=docker` false-positive 修（exit=127 显式检查 + docker image `ubuntu:22.04 → gcc:12`）,5 LOC 1 commit |
| [`v2.8.0-plan.md`](v2.8.0-plan.md) | **sprint 计划**（✅ shipped, 2026-09-08, 2-commit chain `0dd33bb` + `84d8c61`）| v2.8.0 = M2 sysv + sysv_freestanding codegen 真实现（codegen_amd64_emit_mem/ctrl/peephole target_tag dispatch）+ D43 re-baseline `cc894329...` → `6a2f2277...` + docker wire-only chain（方案 C）+ 5 sysv tests SKIP honest（C-side TARGET_AMD64_SYSV_FREESTANDING enum pending v2.x 末）,~260 LOC 2 commits |
| [`v2.8.1-plan.md`](v2.8.1-plan.md) | **sprint 计划**（✅ shipped, 2026-09-08, commit `2d37492`, tag `v2.8.1`）| v2.8.1 = C-side target_dispatch.{c,h} mirror update（4 targets, rename STUB → SYSV preserve =2）+ C-side cg_module SYSV/SYSVFS cases + regress.py comments。~50 LOC 1 commit。**Honest discovery**: jhyy-side codegen.jhyy cg_module 仍 v2.7.0 stub-fatal for SYSV/SYSVFS → W-070 NEW, 留 v2.8.2 / v2.x 末 (M4 launch 硬前置) |
| [`v2.8.2-plan.md`](v2.8.2-plan.md) | **sprint 计划**（✅ Phase 1 ship, 2026-09-08, commit `8b4d43d`）| v2.8.2 = W-070 真修：jhyy-side codegen.jhyy cg_module 真 emit SysV QBE IL。10 dispatch sites in cg_func/cg_expr + CGContext target_tag field (144→152 bytes) + cg_module fallthrough restructure。~120 LOC 1 commit。**M4 launch 硬前置彻底解锁**。5 sysv tests end-to-end 真跑 PASS requires Ubuntu WSL host (user-level verify) |
| [`v2.8.3-plan.md`](v2.8.3-plan.md) | **sprint 计划**（✅ Phase 1 ship, 2026-09-08, 2-commit chain, this plan 验证 5/5 sysv PASS gate + scope creep fix）| v2.8.3 = docker gcc chain infra 补完: `--no-link` flag (cmd_compile + --help) + regress.py docker branch 2-stage subprocess rewrite (Stage 1: Windows-side jhyy --no-link → .s; Stage 2: docker gcc:12 chain → ELF → 跑) + driver logic fix (r.returncode 不再用, EXIT:N-only PASS check) + 2 stale fixture fix (sysv_abi_test / sysv_vararg_basic extern → local inline fn) + scope creep: _DOCKER_BIN abs path fallback (per feedback_gh_cli_path pattern)。~75 LOC source + 125 LOC docs, 2 commits。**M4 launch 验证完整化 (5/5 sysv PASS via docker)** |
| [`v2.9.0-plan.md`](v2.9.0-plan.md) | **sprint 计划**（V2-C Part 1, ✅ shipped, 2026-09-08, tag `v2.9.0`, 2-commit chain `623f3f8` src0 revert + `ad6459f` verification harness）| v2.9.0 = N≥3 selfhost fixed point 验算 + verification harness（`fixed_point.sh` + `fixed_point_summary.py` + regress.py `--fixed-point` + cap_test.jhyy 跨代一致）。~290 LOC sprint + src0 revert 3100 LOC deletions。**M5 deferral 第二前置 Part 1**（verification 闭环, N=3 ✅ PASS + N=4/N=5 informational PASS）|
| [`v2.10.0-plan.md`](v2.10.0-plan.md) | **sprint 计划**（V2-C Part 2+3, 🚫 **RENUMBERED → v2.12.0**, 2026-09-09 user 决定, per W-074 codegen_amd64_run 0-byte .s blocker）| 原 v2.10.0 因 W-073 + W-074 双 bug 暴露 (regress 5/5 FAIL on `JHY_SELF_BACKEND=1` path) renumber 为 [v2.12.0](v2.12.0-plan.md)。**v2.11.0** = codegen_amd64_run 真修 (per [`v2.11.0-plan.md`](v2.11.0-plan.md), W-073 + W-074 trace + fix + regress.py `--self-backend` flag),ship 在 v2.12.0 之前 |
| [`v2.11.0-plan.md`](v2.11.0-plan.md) | **sprint 计划**（V2-C Part 2a, ✅ shipped 2026-09-10 on commit 25dfb00 + tag v2.11.0, 串行 v2.9.0 + v2.11.1 之间）| v2.11.0 = codegen_amd64_run 真修 (W-073 verification gap closure + W-074 0-byte .s 根因定位 + regress.py `--self-backend` flag + 5/5 QBE PASS + self-backend 不 crash + stderr warning)。~140 LOC 2 commits。**M5 deferral 第二前置 Part 2a**。|
| [`v2.11.1-plan.md`](v2.11.1-plan.md) | **sprint 计划**（V2-C Part 2a-补, ✅ shipped 2026-09-10 on axis-v2 + tag v2.11.1, 串行 v2.11.0 + v2.11.2 之间）| v2.11.1 = W-074.5 lexer gap closure + 7 真修延伸 (dbgfile/dbgloc/{/} + ILTOK_DIRECTIVE + parse_and_emit noop + csltw lexer + next_token_call args consume + struct offset 真修 + emit cast 真修) + scope DOWN ship gate (5/5 QBE PASS + 1/5 self-backend hello PASS per [[feedback_codegen_amd64_multifn]])。~390 LOC 3 commits (tag v2.11.0 + source + docs)。**M5 deferral 第二前置 Part 2a-补** (lexer gap closure 闭 → W-074.6 multi-func self-backend 留 v2.x 中期 per [[feedback_codegen_amd64_run_zerobyte]])。|
| [`v2.11.2-plan.md`](v2.11.2-plan.md) | **sprint 计划**（V2-C Part 2a-后, ✅ shipped 2026-09-11 on axis-v2 + tag v2.11.2, 串行 v2.11.1 + v2.11.3 之间）| v2.11.2 = W-074.6 multi-func self-backend crash/hang closure (Tier 1+2+3 全 + Tier 5 token cap + per-fn frame state reset + global-max variant + 3 个 temp offset fn 统一 + Rule 3 DISABLED) + scope DOWN ship gate per user 决定 (5/5 QBE PASS + 5/5 self-backend terminate + EXIT ≥3/5 match hello/fib_renamed/struct_val_pass)。~420 LOC 2 commits。**M5 deferral 第二前置 Part 2a-后** (W-074.6 PARTIAL closure, W-074.7 EXIT exact 留 v2.11.3 per [[feedback_codegen_amd64_multifn]])。|
| [`v2.11.3-plan.md`](v2.11.3-plan.md) | **sprint 计划**（V2-C Part 2a-后-补, ✅ shipped 2026-09-11 on axis-v2 + tag v2.11.3, 串行 v2.11.2 + v2.12.0 之间）| v2.11.3 = W-074.7 self-backend EXIT exact (T4-h 真修 + cur_fn_idx init/reset + T4-c/T4-b/T4-g scope DOWN deferral) + scope DOWN ship gate (5/5 QBE PASS + 5/5 self-backend terminate + EXIT exact 2/5 hello=42/struct_val_pass=35; fib_renamed=32 lit imm operand bug / nested_struct_deep=wrong / big_test=link error pre-existing 重复 `.Lloop_body` label in codegen.jhyy, deferred v2.11.4+ / v2.x 中期)。~25 LOC source 真修 + ~140 LOC 防御性字段/注释 + ~120 LOC docs = ~285 LOC 2 commits。**M5 deferral 第二前置 Part 2a-后-补 PARTIAL** (T4-h 真修 closure, big_test link error 留 v2.x 中期 codegen.jhyy fix)。|
| [`v2.11.4-plan.md`](v2.11.4-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补, ✅ shipped 2026-09-12 on axis-v2 + tag v2.11.4, 串行 v2.11.3 + v2.12.0 之间）| v2.11.4 = W-074.7 T4-b `emit_binop` src1/src2 parse 真修 (fib_renamed EXIT 32 → 40 = 832040 mod 256 真修)。~30 LOC source 真修 (cg_find_byte helper + emit_binop parse + 7 op branch imm support)。**M5 deferral 第二前置 Part 2a-后-补-补** (T4-b fib_renamed 真修 closure; struct_val_pass EXIT=102 pre-existing loadw/alloc bug + nested_struct_deep EXIT=127 pre-existing multi-arg/loadw bug + big_test link error pre-existing codegen.jhyy 重复 `.Lloop_body` label 仍待 v2.x 中期独立 sprint 排查/真修; 不在 v2.11.4 scope per user)。|
| [`v2.11.5-plan.md`](v2.11.5-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补, ✅ shipped 2026-09-12 on axis-v2 + tag v2.11.5, 串行 v2.11.4 + v2.11.6 之间）| v2.11.5 = W-074.7 alloc-tracking 真修 (`cg_offset_for_temp` 跟 `cg_alloc_slot` 两套方案统一 via `CGState.temp_slot_for_id` 256-entry array + extern bridge 跟 regalloc 同 pattern + `emit_alloc` record slot)。修了 nested_struct_deep NTSTATUS_0xDE09A8E7 crash + struct_val_pass garbage exit。~111 LOC source + ~80 LOC docs = ~190 LOC 2 commits。**fix_evaluation_rule 诚实记录**:实际 EXIT match 2/5 (跟 v2.11.4 baseline 持平, hello=42 / fib_renamed=832040), EXIT exact 仍 deferred v2.11.6 因为 emit_store pointer semantics gap (alloc-result %tN 当 stack slot VALUE 处理, 真语义要 `lea -OFF(%rbp), %rax` 间接地址, L4 § 3.5 E2 注释确认的 v2.5.0 简化边界)。**M5 deferral 第二前置 Part 2a-后-补-补** (撞 crash 修复, EXIT exact 留 v2.11.6)。|
| [`v2.11.6-plan.md`](v2.11.6-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补, ✅ shipped 2026-09-12 on axis-v2 + tag v2.11.6, 串行 v2.11.5 + v2.11.7 之间）| v2.11.6 = W-074.7.5 peephole max_lines 真修 (4096 → 65536) + 配套 arena def_size 8 MB bump + W-074.7.6 block-name uniquify (`_b<count>` suffix 拼 `.L<name>` via CGState.block_name_uniq 256-entry table + FNV-like hash + per-fn zero reset + record/lookup helpers)。**scope UP 决策**: 原 plan ~25 LOC scope DOWN (block-name uniquify only), 调研后发现 peephole max_lines cap 才是真根因 (big_test 4090 行 .s 在 cap 4096 fold 时 truncated, 第 4091 行起 drop → main_jhyy 永不写出 → link fail), per 2026-09-12 user 决定 scope UP 真修真根因。~158 LOC source + ~80 LOC docs = ~240 LOC 2 commits。**fix_evaluation_rule 诚实记录**: **5/5 link 真达成** ✅ (big_test 此前 v2.11.5 + 所有之前版本都 link fail), 但 EXIT match 仍 3/5 (跟 v2.11.5 baseline 持平) — struct_val_pass EXIT=6 (≠ 35 EXPECT) / nested_struct_deep EXIT=35 (≠ 22 EXPECT) 都因 emit_store pointer semantics gap, deferred v2.11.7; big_test runtime STATUS_INTEGER_OVERFLOW 0xC0000095 是 separate deeper bug, deferred v2.11.8+ scope 待调研。**M5 deferral 第二前置 Part 2a-后-补-补-补** (link 真修 closure, EXIT exact 留 v2.11.7)。**显式证伪 [[feedback_codegen_amd64_run_zerobyte]]**: N≥3 .il byte-equal 不验 .s, peephole cap bug 不能 catch — big_test N=3 .il byte-equal 但 .s 在 t_bit_and body 中部截断 → main_jhyy 不见 → link fail。|
| [`v2.11.7-plan.md`](v2.11.7-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补, ✅ shipped 2026-09-12 on axis-v2 + tag v2.11.7, 串行 v2.11.6 + v2.11.8 之间）| v2.11.7 = **调研 + scope DOWN defer** (~0 LOC source change, ~120 LOC docs)。原 plan 假设真根因是 "emit_store 把 alloc-result pointer 当 stack slot VALUE 处理", scope UP CGState 大改 (新 `temp_is_alloc_pointer` 256-entry bitmap + emit_load/emit_store/emit_copy TEMP/emit_call arg load 全 dispatch, ~190 LOC) 实施后 regress 53/115 → 40/115 (13 测试 regress)。**调研发现真根因更深**: 4 emit path 真修只对 alloc-result pointer 有效, **derived address (e.g. `add %t6, 0` 的 result temp 装的是 runtime 计算的 address, 不是 stack slot) 才是真根因**。Codegen 1-to-1 栈栈搬运假设 (L4 § 3.5 E2 简化边界) 在 `storew val, derived_addr` 跟 `loadw derived_addr` 形态破, 需 `movl %eax, (%raddr)` 间接写。per 2026-09-12 user 决定 scope DOWN — defer 真修 v2.11.7, 启动 v2.11.8 调研 full derived-address tracking (bitmap 扩到 flag 任何 temp holding derived address, ~250-350 LOC 估)。**W-074.7.7 emit_store pointer semantics = INVALID closure** (调研结论); **W-074.7.8 derived-address tracking = NEW (v2.11.8 待 ship)**。**fix_evaluation_rule 诚实记录**: 实际 EXIT match = 3/5 (跟 v2.11.6 baseline 持平 — **没新修, 因为调研发现真根因更深, scope UP 不充分**); 不强宣 "4/5 EXIT exact closure"。**M5 deferral 第二前置 Part 2a-后-补-补-补-补** (调研 + scope DOWN defer, 真修留 v2.11.8 derived-address tracking)。|
| [`v2.11.8-plan.md`](v2.11.8-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补, ✅ shipped 2026-09-13 on axis-v2 + tag v2.11.8, 串行 v2.11.7 + v2.11.9+ 之间）| v2.11.8 = **W-074.7.8 derived-address tracking 真修 (FULL scope, ~319 LOC source + ~30 docs = ~349 LOC 2 commits)**。Bitmap flag 任何 temp holding derived address (alloc-result + binop add/sub on pointer + copy of address-holder); emit_load/store/loadsub 改 full dispatch (slot vs indirect `mov<size> (%r8), %reg` via %r8 scratch); emit_binop 产生 derived-address 时 flag result; emit_copy propagate flag (TEMP + FNARG paths); emit_alloc 用 lea+mov 写 alloc'd 地址到 pointer-slot (formula offset, NOT region — **self-referential slot bug 真修**)。**关键真修**: 1) self-referential slot bug (v2.11.5 design 让 pointer-slot == region, lea+mov 自我覆盖; 真修 SKIP `cg_record_temp_slot`, pointer-slot 走 formula `-(32+t*8)` Win); 2) FNARG flag propagate (l-typed fnarg = struct param pointer 走 FNARG path 不 flag propagate, struct_val_pass EXIT=6→35 真修)。**fix_evaluation_rule 诚实记录**: **4/5 EXIT exact closure** ✅ (hello=42 / fib_renamed=40 = 832040 mod 256 / struct_val_pass=35 / nested_struct_deep=22 / struct_val_assign=30); big_test runtime STATUS_INTEGER_OVERFLOW 0xC0000095 仍 deferred v2.11.9+ (W-074.7.9 NEW, separate deeper bug 跟 derived-address 无关); 不强宣 "5/5 EXIT closure"。self-backend regress +2 flips (56→58, 远低于 +5 scope DOWN trigger per [[feedback_codegen_amd64_multifn]]); QBE fallback 115/115 PASS preserved; byte-equal-amd64 10/10 preserved; D43 closure v1↔v2 .il sha HOLD; **NEW ship gate**: main_jhyy.s = 12 行 / 207 bytes ≥ baseline (no truncation, 防 v2.11.6-style 截断 bug 回归 per [[feedback_codegen_amd64_run_zerobyte]])。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补** (W-074.7.8 PARTIAL closure 4/5 EXIT exact; W-074.7.9 big_test runtime deferred v2.11.9+)。|
| [`v2.11.9-plan.md`](v2.11.9-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补, ✅ shipped 2026-09-13 on axis-v2 + tag v2.11.9, 串行 v2.11.8 + v2.11.9a+ 之间）| v2.11.9 = **W-074.7.9 big_test runtime QBE side 真修 (~18 LOC source + ~90 LOC docs = ~108 LOC 2 commits)**。**2 个独立 root cause 真修** (per plan mode Explore agent 调研): 1) `idiv` emit 无 sign-extend prefix (`is_div` branch at `codegen_amd64_emit_call.jhyy:1068-1080`, x86 `idivl` semantics dividend = `(%edx << 32) \| %eax` → `%edx` 残留 → x86 #DE fault → Windows STATUS_INTEGER_DIVIDE_BY_ZERO 0xC000008C, wrapped 0xC0000095 per process config; 真修 add `\tcltd\n` / `\tcqto\n` prefix mirror `is_mod`); 2) Lexer 不识 QBE `rem` keyword (`next_token_binop` dispatcher + main `next_token` `'r'` → ret/rem disambiguate, QBE `%` operator emits `rem` (signed remainder, per QBE spec §6.3) → `next_token` returns -1 → `lex_il` 静默 skip → rem-result SSA values 永远 missing from .s; 真修 add `rem` branch in `next_token_binop` + `'r'` → ret/rem dispatch in main `next_token`)。Bonus `is_rem` flag + dispatch (`emit_binop`: `cg_find_sub` check for "rem" 3-char substring + dispatch branch mirror `is_mod`)。**fix_evaluation_rule 诚实记录 scope DOWN per user 决定**: **QBE side 5/5 EXIT exact closure 真修达成** ✅ (hello=42 / big_test=**12345** 真修 closure / struct_val_pass=35 / fib_renamed=832040 / nested_struct_deep=22 / struct_val_assign=30, 9 `rem` ops 正确 emit cltd+idivl + movq %rdx, %rax); self-backend side 仍 🟡 ACTIVE due to **W-074.6 family** (extsw silent-skip + multi-func body 0-byte + emit_ret 不 mov %t1 → %eax 等 pre-existing bugs, 已知范围 ~500+ LOC 多 sprint, **显式 deferred v2.x 中期 per W-074.6 ACTIVE state**)。QBE fallback 115/115 regress PASS preserved + self-backend 1/5 hello PASS preserved (per W-074.6 baseline, multi-func closure deferred v2.x 中期); D43 closure v1↔v2 .il sha HOLD (v2/v3/v4/v5 sha `42580b87...` post-fix, re-baseline per D43 closure rule 因 rem emit 微变); byte-equal-amd64 10/10 preserved; fixed_point N≥3 PASS preserved; **NEW ship gate audit grep** (per [[feedback_codegen_amd64_run_zerobyte]]): self-backend big_test.s `cltd` = 14 (5 div + 9 rem) + `idivl` = 14 + `movq %rdx, %rax` = 9 (was 0 pre-fix) + `.s` 行数 8727 ≥ 100 bytes + `main_jhyy.s` non-empty; jhyy.exe.sha256 refresh `9ef7f49734ef99d7...`。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补** (W-074.7.9 QBE side CLOSURE; W-074.6 family self-backend 仍 ACTIVE, v2.x 中期独立 sprint 真修; 不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动)。|
| [`v2.11.10-plan.md`](v2.11.10-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补-补, ✅ shipped 2026-09-13 on axis-v2 + tag v2.11.10, 串行 v2.11.9 + v2.11.10a+ 之间）| v2.11.10 = **W-074.6 T3-a per-fn frame size + T4-c multi-arg FN_ARG 真修 (~155 LOC source + ~200 docs = ~355 LOC 2 commits)**。T3-a closure: 两阶段 pre-scan (`cg_compute_per_fn_max_temps` Phase 1 扫 fn_starts, Phase 2 per-fn max temp id) + CGState 加 fn_starts/per_fn_max/fn_count 3 字段 + emit_func_header 改读 `per_fn_max[cur_fn_idx]` 替换 v2.11.2 global-max variant (1 个 fn = 15488 bytes frame → 递归函数 SIGSEGV 真修)。T4-c closure (scope UP 配套 T3-a): lexer `next_token_func_header` 改 dup 完整 header text (`function w $name(args)`) 替代 v2.11.0-2.11.9 只 dup name; emit_func_header 加 `cg_state_set_fn_header` populate cur_fn_header_text + extract name from full header for `.globl` + label; emit_copy FNARG 路径替换 hardcode `reg_rcx_for_qt(dst_qt)` → `cg_find_arg_idx` + `reg_rax_for_qt_idx(dst_qt, idx, target_tag)` (Win x64 arg 0..3 = %rcx/%rdx/%r8/%r9, SysV = rdi/rsi/rdx/rcx via target_tag)。**fix_evaluation_rule 诚实记录**: **5/6 self-backend EXIT exact closure** ✅ (hello=42 / struct_val_pass=35 / fib_renamed=40 / nested_struct_deep=22 / struct_val_assign=30), 跟 v2.11.9 baseline 一致但 different bug fixed (T3-a 真修 verified: gcd `subq $600, %rsp` vs 旧 15488; T4-c 真修 verified: gcd body `movl %ecx` + `movl %edx` vs 旧 都 %ecx)。**5/5 self-backend closure NOT achieved** — gdb 取证 big_test 在 T3-a+T4-c 真修后 SIGFPE at `gcd+158 idivl -576(%rbp)` (T4-g: emit_binop cnew silent-skip + emit_jnz loop body entry silent-skip — pre-existing W-074.6 family bug, NOT in v2.11.10 plan scope)。v2.11.10 plan 文档 "5/5 100% 可达" 预测 wrong — per [[feedback_codegen_amd64_multifn]] scope UP trigger: T4-g 暴露后 scope DOWN 5/6 closure 接受 (跟 v2.11.9 baseline 一致不触发 +5 FLIP trigger)。**T4-g 真修 deferred v2.11.10a+ (~30-50 LOC 估)**。Stage 2 N=4 jhyy 编 jhyy closure PASS; QBE fallback 115/115 PASS preserved; byte-equal-amd64 10/10 preserved; fixed_point N≥3 PASS preserved; **NEW ship gate audit** (per [[feedback_codegen_amd64_run_zerobyte]]): `objdump -d big_test_run.exe | grep sub.*\$0x` distinct frame size > 5 (per-fn 优化生效) + `grep subq \$15488` = 0 + main_jhyy.s non-empty; jhyy.exe.sha256 refresh。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补-补** (W-074.6 T3-a + T4-c CLOSURE; W-074.6 family 其余 sub-bug 含 T4-g 仍 ACTIVE, 5/5 self-backend closure 留 v2.11.10a+ 真修; 不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动)。|
| [`v2.11.11-plan.md`](v2.11.11-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补-补-补, ✅ shipped 2026-09-13 on axis-v2 + tag v2.11.11, 串行 v2.11.10 + v2.11.11a+ 之间）| v2.11.11 = **W-074.6 T4-g lexer cnew/ceqw silent-skip 真修 PARTIAL closure (~5 LOC source + ~200 docs = ~205 LOC 2 commits)**。T4-g 真修: stage 1 guard 加 `n1 == (110 as i32)` ('n' for cnew prefix) → cnew 不再 stage 1 reject; stage 2 guard refactor flag pattern (2 独立 if 设 `has_type_suffix` flag + 最终 if check) 接受 type suffix at idx 3 (4-char ceqw/cnew) OR idx 4 (5-char csltw/cultw/...),**绕开 codegen short-circuit OR workaround 的 nested-OR bug** (首次尝试 nested OR 触发 QBE "predecessors not matched in phi" → refactor flag pattern 替代); op_str table 加 `n1 == 110 ('n') → "cnew"` branch consistency (dead store 但保持代码对称)。**fix_evaluation_rule 诚实记录**: **5/6 self-backend EXIT exact closure maintained** ✅ (hello=42 / struct_val_pass=35 / fib_renamed=40 / nested_struct_deep=22 / struct_val_assign=30), 跟 v2.11.10 baseline 持平但 different bug 真修 (T4-g 真修 verified: 65 个 4-char compare op 正确 emit = 11 sete + 54 setne in big_test_run.s + gcd Euclid loop cond check 正确 emit → 不再 SIGFPE in gcd+158)。**6/6 self-backend closure NOT achieved** — big_test 通过 gcd 后 **hang at t_bit_pack** (5min+ timeout),**新 stack-slot-reuse sub-bug 暴露** (W-074.6 family 范围内, in `compiler/src0/codegen_amd64_emit_call.jhyy` emit_binop shift op 路径): bit_pack 函数 emit `(a & 0xFF) | ((b & 0xFF) << 8) | ((c & 0xFF) << 16) | ((d & 0xFF) << 24)` 时所有 shift amounts (8/16/24) 跟 0xFF masks 写**同一个** -32(%rbp) slot → 计算错值 → check_eq 不等 → hang。QBE 后端不受影响 (QBE 走自己 allocator, EXIT=57 PASS 维持)。v2.11.11 plan 文档 "6/6 100% 可达" 预测 wrong (跟 v2.11.10 plan "5/5 100% 可达" 一样 wrong, Risk 5 警告成真) — per user 2026-09-13 pre-confirmed scope DOWN ("仅 T4-g 真修 (推荐)"): scope 限定在 T4-g lexer 真修, 6/6 closure 留 v2.11.11a+ 真修 stack-slot-reuse (~30-50 LOC 估)。Stage 2 N=4 jhyy 编 jhyy closure PASS; QBE fallback 115/115 PASS preserved; byte-equal-amd64 10/10 preserved; fixed_point N≥3 PASS preserved; **NEW ship gate audit** (per [[feedback_codegen_amd64_run_zerobyte]]): `set(ne|e)\b` count ≥ 65 + `jne .Lloop_body` count > 100 + `jmp .Lloop_end` count > 100 + `grep subq \$15488` = 0 (T3-a closure preserved) + main_jhyy.s non-empty; self-backend regress **71/115 PASS** (vs v2.11.10 baseline 58/115, **+13 PASS** 改善, T4-g fix 暴露之前 crash 在 gcd 的 tests 现在通过, FLIP count = 13 PASS improvements 非 scope DOWN trigger per [[feedback_codegen_amd64_multifn]]); jhyy.exe.sha256 refresh。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补-补-补** (W-074.6 T4-g PARTIAL closure, 6/6 self-backend closure deferred v2.11.11a+ stack-slot-reuse sub-bug; 不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动)。|
| [`v2.11.12-plan.md`](v2.11.12-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补-补-补-补, ✅ shipped 2026-09-15 on axis-v2 + tag v2.11.12, 串行 v2.11.11 + v2.11.12a+ 之间）| v2.11.12 = **W-074.6 shl/shr (5 ops and/or/xor/shl/shr) missing + emit-copy dst_id=0 真修 → 6/6 self-backend EXIT exact closure ✅ 达成 (~115 LOC source + ~250 docs = ~365 LOC 2 commits)**。**2 个独立 bugs 联动真修** (per v2.11.12 调研):(1) **Bug A — shl/shr (5 ops) missing**:lexer `next_token_binop` + main dispatcher `'s'` branch + `'a'` branch + 新 `'o'` + `'x'` branch 没有 and/or/xor/shl/shr handler → silent skip → bit_pack `shl $8/%cl` 类 op 不 emit → result 计算错;(2) **Bug B — emit_copy dst_id=0**:LHS parse path `% <ident> <ws>+ =` 在 t178 (last correct) → t180+ 之间 cursor state corruption → lex_parse_temp_id_from_ident 返回 0 → emit_copy 拿 dst_id=0 → formula `-(32 + 0*8) = -32` (Win) → 所有 copy collapse to slot -32 → bit_pack values overwrite each other → hang。**fix_evaluation_rule 诚实记录**: **6/6 self-backend EXIT exact closure ✅ 达成** (跟 v2.11.10 + v2.11.11 plan "100% 可达" 两次 wrong 教训形成对比,v2.11.12 ship record 6/6 真达成) — hello=42 / **big_test=57** (= 12345 mod 256, **was hang at t_bit_pack 5min+ timeout in v2.11.11, now PASS**) / struct_val_pass=35 / fib_renamed=40 / nested_struct_deep=22 / struct_val_assign=30。Stage 2 N=5 jhyy 编 jhyy closure PASS (D43 closure HOLD sha `9e61c42c...`); QBE fallback 115/115 PASS preserved; byte-equal-amd64 10/10 preserved; fixed_point N=3,4,5 .il byte-equal + cap_test 跨 N 代 EXIT=42 一致 preserved; **NEW ship gate audit** (per [[feedback_codegen_amd64_run_zerobyte]]): 20 shll/shrl/andl/orl/xorl emits in `_regress_big_test.s` (vs 0 pre-fix) + t_bit_pack + t_bit_unpack + t_shifts 全 PASS (was hang/crash) + bit_pack distinct slots -1424, -1432, -1440, ..., -1552 (vs all -32 collapse pre-fix); self-backend regress 69/115 (-2 vs v2.11.11 baseline 71/115, 全部 pre-existing FAILs dominate, plan target ≥75/115 NOT met — 因为 t_bit_pack / t_bit_unpack / t_shifts 不在 regress 测试 list); jhyy.exe.sha256 refresh `58a6f3a2...`。**Phase C sub-bug 修正** (本次 ship 期间 surface):(a) `%%` → `%` (sb_append_cstr 是 plain append 不是 printf-style),(b) reg path 漏 `movl` prefix → 分开 imm / reg path 各自 emit 完整 prologue。**累计 W-074.6 family closed ~325 LOC**:T3-a + T4-c + T4-g (lexer partial) + shl/shr (本次) + dst_id=0 (本次);剩余 ~175+ LOC (stack-slot-reuse in other emit paths + extsw silent-skip + emit_ret 不 mov %t1 → %eax 等) 留 v2.x 中期 V2-D。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补-补-补-补**:6/6 self-backend EXIT exact closure ✅ 达成, M5 启动仍需等剩余 W-074.6 family 子 sprint (extsw + emit_ret 等) 闭环 (per `v1.x-phase-4-m5-boot-from-scratch.md`);不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动。|
| [`v2.11.13-plan.md`](v2.11.13-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补-补-补-补-补, ✅ shipped 2026-09-15 on axis-v2 + tag v2.11.13, 串行 v2.11.12 + v2.11.14 之间, **scope DOWN 触发** FLIP-gate per user 2026-09-15 directive "FLIP count 逼近 5 立即停手")| v2.11.13 = **W-074.6 cne substring missing in emit_binop 真修 → partial closure 14/44 (32%); cross-cluster +7 PASS self-backend (78→85/115); scope DOWN 触发 (FLIP +7 超 stop threshold 5, Iter 5-7 deferred v2.11.14) (~1 LOC source + ~250 docs = ~251 LOC 2 commits)**。**1 line 真修** (per v2.11.13 调研, 跟 v2.11.12 真修的 shl/shr missing + dst_id=0 同 family pattern — W-074.6 silent-skip 在 emit_binop dispatch layer 而不是 lexer silent-skip):emit_binop `cg_find_sub(op_text, op_text_len, "cnew" as *u8, 4 as i64)` 改为 `"cne" 3-char` → catch 全部 4 个 cne_* QBE op (cnew/cnel/cnes/cned — compare-not-equal per size suffix letter w/l/s/d)。**Plan v2.11.13 Iter 4 估 "Cap<T> sizeof default 30-50 LOC" 严重错归因** — 实测 sizeof 在 sema `type_size` (types.jhyy:366) hardcode 8, IL emit `%t2 =l copy 8` 跟 QBE byte-equal; 真根因是 `if s_cap != 8` emit `cnel %t8, %t11`, cnel 在 emit_binop dispatch substring miss → fall through `add` path → `addl src1, src2` + `cmpl $0, sum` 而非 `cmpl src2, src1` + `setne %al` → cap_table_basic got=10 (8+8=16 ≠ 0 → then-branch → return 10)。**Cross-cluster impact** (1 LOC fix 连锁 closure, plan 没预期): arith + int_width_arith + int_suffix + cap_table_advanced + cap_test_sysv 全 PASS (5 tests); cap_table_basic partial (test 4 cross-fn struct field access 单独 bug, deferred v2.11.14)。**fix_evaluation_rule 诚实记录**:**partial closure 14/44 (32%)** (跟 v2.11.10 + v2.11.11 + v2.11.12 plan "100% / 6/6 / full closure" 三次 wrong 教训形成对比 — v2.11.13 plan honest scope (Scenario A iterative + "FLIP-gate") 仍 predict wrong cluster scope 但 FLIP-gate mechanism catch over-shoot → scope DOWN 正确执行); self-backend 78/115 → 85/115 (+7 FLIP, total +14 from v2.11.11 baseline 71/115); 30 FAIL (-14 from 44 baseline = 32% closure)。**FLIP +7 触发 stop threshold 5 → scope DOWN v2.11.13** (per user 2026-09-15 "FLIP count 逼近 5 立即停手" + plan hard gate "FLIP count ≥ 4 → 停手 ship 当前 progress")。Iter 5 (C.4 float f32/d) + Iter 6 (C.7 defer LIFO) + Iter 7 (B observe) 全 deferred v2.11.14。Stage 2 N=5 jhyy 编 jhyy closure PASS (D43 closure HOLD); QBE fallback 115/135 PASS preserved; byte-equal-amd64 10/10 preserved; fixed-point N=3,4,5 .il byte-equal preserved; **NEW ship gate audit** (per [[feedback_codegen_amd64_run_zerobyte]]): cnel in `_regress_cap_table_basic.s` pre-fix `addl src1, src2` + `cmpl $0, sum`, post-fix `cmpl src2_off(%rbp), %rax` + `setne %al` + `movzbl %al, %eax`; **big_test self-backend EXIT=57 preserved** (6/6 closure hold); jhyy.exe.sha256 refresh `c9c274db...`。**累计 W-074.6 family closed ~328 LOC** (v2.11.12 325 + v2.11.13 1 LOC 真修); 剩余 ~172 LOC (struct pass-by-value emit_copy 1-to-1 gap + stack-slot-reuse in other emit paths + emit_ret 不 mov %t1 → %eax + extsw 之外其他 sub-family) 留 v2.x 中期 V2-D。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补-补-补-补-补**: 6/6 self-backend EXIT exact closure ✅ 达成 hold, M5 启动仍需等剩余 W-074.6 family 子 sprint (struct pass-by-value + emit_ret 等) 闭环 (per `v1.x-phase-4-m5-boot-from-scratch.md`); 不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动。
| [`v2.11.14-plan.md`](v2.11.14-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补-补-补-补-补-补, ✅ shipped 2026-09-15 on axis-v2 + tag v2.11.14, 串行 v2.11.13 + v2.11.15 之间, **hard STOP #3 triggered** → 0/31 closure, Iter 1 attempt reverted, deferred 全 31 v2.11.15）| v2.11.14 = **W-074.7 phi merge gap (NEW family) C.3 4 sub-bugs 真修 attempt → hard STOP #3 (match.jhyy regress) → source revert → 0/31 cluster closure (0%) (~120 LOC source attempt + ~150 LOC revert + ~250 docs = ~520 LOC 1 docs commit)**。**Plan v2.11.14 Iter 1 估 "C.3 12 tests target ~100-150 LOC FLIP +11-12 clean / +6-8 likely / +0-3 worst" 4-way wrong 预测**: (a) LOC 估 partial correct (实际 ~120 LOC source attempt); (b) cluster 估 wrong (实测 0/12 PASS); (c) 根因 partially correct (plan 估 "emit_phi noop + .Lmerge 读 third never-written slot" → 实测 4 sub-bugs 复合 — A: emit_jmp lookup `(arm_name, merge_label)` key 但 dest_id alignment 错; B: OR pattern `_|_` 不 emit 各成员独立 arm block 走 default `enum_default`; C: payload pattern `Some(v) => v` 的 `v` slot uninit; D: enum_match_arm_tag_check 缺 emit `cmpl $tag_value, discriminator` for variants 不到 wildcard); (d) FLIP est wrong (+11-12 → -1, hard STOP #3 trigger)。**二次 sub-bug (compile-time crash)**: malloc state_buf 160 → 必须 256 (CGState struct 加 4 fields 后总 24 × 8 ≈ 192 bytes, 160 不够 → emit_call/emit_ctrl 写未映射内存 → process crash on first emit, 首 build 报 "5/115 passed, 110 failed")。**fix_evaluation_rule 诚实记录** (跟 v2.11.10 + v2.11.11 + v2.11.12 + v2.11.13 plan 4 次 wrong 预测形成对比 — v2.11.14 plan honest scope (Scenario A iterative + "FLIP-gate" + "high-impact first") 第 5 次 partial wrong): 0/31 closure (0%); self-backend 84/115 PASS = baseline 持平 (no new cluster closure); match.jhyy pre-fix + post-revert PASS 维持 (silent win from v2.11.13 Iter 4 cne substring preserved); baseline gate 全 preserved (byte-equal D26 5/5, byte-equal-amd64 10/10, big_test self-backend EXIT=57, QBE fallback 115/135, jhyy.exe.sha256 `774ec8347b4ce629...`)。**hard STOP #3 (currently-PASSing test regression)** triggered per plan hard gate "Currently-PASSing test regression (e.g. match.jhyy 在 C.3 fix 后 FAIL) → revert + redesign C.3" + user 2026-09-15 "FLIP count 逼近 5 立即停手" directive → scope DOWN v2.11.14 (1 docs commit only, source revert 干净 `git checkout HEAD -- 4 files`)。Iter 2 (C.5 slice copy 16B vs 8B) + Iter 3 (C.4 float XMM emit gap) + Iter 4 (A1-XMM return register) 全 deferred v2.11.15。Stage 2 N=5 jhyy 编 jhyy closure PASS (D43 closure HOLD); QBE fallback 115/135 PASS preserved; byte-equal-amd64 10/10 preserved; fixed-point N=3,4,5 .il byte-equal preserved; **NEW ship gate audit** (per [[feedback_codegen_amd64_run_zerobyte]]): source revert 干净 baseline maintained + match.jhyy PASS 恢复 + self-backend regress 84/115 (= baseline 持平, no new cluster closure); **big_test self-backend EXIT=57 preserved** (6/6 closure hold); jhyy.exe.sha256 refresh `774ec8347b4ce629c3f8447755ffdc1789dd7e593df358a72eb32c23f012ac09`。**累计 W-074.6 family closed ~328 LOC + W-074.7 family NEW ⏸ DEFERRED** (4 sub-bugs ~55-100 LOC); 剩余 ~172+55-100 LOC (~227-272 LOC) 留 v2.11.15 + v2.x 中期 V2-D。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补-补-补-补-补-补**: 6/6 self-backend EXIT exact closure ✅ 达成 hold, M5 启动仍需等剩余 W-074.6 + W-074.7 family 子 sprint 闭环 (per `v1.x-phase-4-m5-boot-from-scratch.md`); 不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动。|
| [`v2.11.15-plan.md`](v2.11.15-plan.md) | **sprint 计划**（V2-C Part 2a-后-补-补-补-补-补-补-补-补-补-补-补-补, ⏳ in-progress 2026-09-16 on axis-v2, 串行 v2.11.14 + v2.11.15a+ 之间, **Phase 0 MANDATORY pre-flight per v2.11.14 fabricated narrative lesson**; user 2026-09-16 AskUserQuestion 选 Systemic 6 iters + FLIP ≥ 4 STOP）| v2.11.15 = **W-074.7 emit_phi 真修 + A1-XMM + C.4 + C.5 + A2-pointer-deref + C.2 cap_table 31 FAIL closure (target 27/31 = 87%; honest likely 19-22/31 = 61-71%; worst 12-16/31 = 39-52%) (~245-410 source LOC + ~400 docs = ~645-810 LOC 6 commits)**。**Phase 0 pre-flight 实证 (2026-09-16, 1 docs commit)** — actual regress = 84/115 PASS + **31 FAIL + 25 SKIP** (= v2.11.14 baseline 持平); baseline gates 全 preserved (QBE 115/135, byte-equal D26 5/5, byte-equal-amd64 10/10, big_test self-backend EXIT=57, match.jhyy PASS, jhyy.exe.sha256 `774ec8347b4ce629...`); 7 representative `.s` files captured (per `feedback_il_s_debugging_pattern`)。**🔴 Phase 0 critical re-classification — A2 cluster root cause REFUTED** (Agent 2 audit wrong): const_array + const_struct_array const data IS emitted correctly (`.data ASCII_LOWER: .byte 97-122` + `.data PALETTE: .long 1-9` both in .s); 实际 bug = address-holder locals reading stack slot as if it were pointee data (`movl -72(%rbp), %eax` reads low 32 bits of address instead of `movl (%rax), %eax` reading PALETTE[2].b) — **NEW cluster name = A2-pointer-deref**, scope changed from "ILTOK_DATA_PRIM lexer" (Agent 2 wrong) to "self-backend emit deref" (NEW file:line TBD)。**Phase 0 cluster confirmation** (6 of 8 clusters verified via .s evidence):(1) C.3 emit_phi noop CONFIRMED — char_pattern_run.s:60-61 `.Lmerge1_b0_fn1:` followed only by `# phi resolved by codegen.jhyy upstream — noop in v2.5.0` comment, 0 lines of `mov<size>` from arm temp to merge slot, 真 root cause single-file (NOT v2.11.14 plan's 4 sub-bugs compound);(2) C.5 slice 16B→8B CONFIRMED — slice_subrange_run.s `movq -40(%rbp), %rax; movq %rax, -56(%rbp)` single 8B copy for 16B slice header (ptr+len, len uninit);(3) C.4 float XMM CONFIRMED — float_arith_run.s only integer ops (`addq`/`imulq`) for what should be float, `grep -E "addsd\|addss\|mulsd\|mulss"` 0 hits in codegen_amd64_emit_*.jhyy;(4) A1-XMM CONFIRMED — generics_fn_call_site_inference_run.s `call max$f64` followed by `movq %rax, -104(%rbp)` reads garbage from %rax instead of f64 from %xmm0;(5) A2-pointer-deref CONFIRMED (refuted Agent 2 audit);(6) dungeon_game separate — gcc ld returned 5 (NOT Agent 2's "undefined reference"); (7) C.2 cap_table test 4 TBD;(8) C.7 defer LIFO TBD;(9) B-runtime/globals TBD。**CGState struct 实际 size** (verified): 8 fields × 8 bytes = **64 bytes**, `malloc(64 as i64)` is correct. v2.11.14 plan's "160 → 256" claim was wrong. **6-iter Systemic scope per user 2026-09-16 决定**:Iter 1 A1-XMM (~15-25 LOC, 2 tests, LOW risk) → Iter 2 C.3 emit_phi 真修 (~80-120 LOC, 12 tests, LOW-MED risk, **CRITICAL: match.jhyy 不 regress**) → Iter 3 C.4 float XMM (~50-80 LOC, 4 tests, LOW risk) → Iter 4 C.5 slice copy (~30-50 LOC, 5 tests, MED risk, **CRITICAL: byte-equal D26 不 regress**) → Iter 5 **A2-pointer-deref** (~40-60 LOC, 2 tests, MED risk, **REVISED from Agent 2 audit**) → Iter 6 C.2 cap_table test 4 (~20-40 LOC, 1 test, LOW risk);STOP if FLIP ≥ 4 OR new regress > 5 OR currently-PASSing test regression。**Deferred to v2.11.16+** (NOT in v2.11.15 scope per user choice):B-runtime/globals 3 tests (big_array + top_level_let_mut_*) + C.7 defer LIFO 1 test (defer_multi_lifo) + dungeon_game separate 1 test (gcc link TBD) = **5 tests deferred total**。**fix_evaluation_rule 诚实记录 (Phase 0 only, no impl yet)**: 0/31 cluster closure (Phase 0 是 docs-only); self-backend 84/115 PASS = baseline 持平; match.jhyy PASS preserved; baseline gate 全 preserved; FLIP count = 0 (no impl attempted); jhyy.exe.sha256 refresh NOT NEEDED (no source change)。**累计 W-074.6 family closed ~328 LOC + W-074.7 family ⏸ DEFERRED** (v2.11.15 Iter 2 re-targets as **single root cause** = emit_phi noop, NOT v2.11.14 plan's 4 sub-bugs compound); remaining ~172 LOC W-074.6 family sub-bugs + ~80-120 LOC W-074.7 真修 + ~245-410 LOC v2.11.15 6 iters 留 v2.11.15 (current) + v2.x 中期 V2-D (post v2.11.15 ship + v2.11.16 deferred)。**M5 deferral 第二前置 Part 2a-后-补-补-补-补-补-补-补-补-补-补-补-补**:6/6 self-backend EXIT exact closure ✅ hold, M5 启动仍需等 v2.11.15 + v2.11.16 + v2.x 中期 V2-D 全部 cluster closure (per `v1.x-phase-4-m5-boot-from-scratch.md`); 不阻 v2.12.0 启动 + 不阻 v3.0 3a-3f 启动。|
| [`v2.12.0-plan.md`](v2.12.0-plan.md) | **sprint 计划**（v2.x 末 第 1 步, 📋 planned, 2026-09-17, 全量 self-backend audit 无抽样, per user 2026-09-17 决定 "到时候都修完了以后每个test都看一下，不抽测，也就是2.12.x做这个事"）| v2.12.0 = 全量 self-backend audit（无抽样, 119/119 manual trace codegen path + multi-input EXIT boundary）。**启动前置**: v2.11.23 ship ✅ (regress 119/119 PASS / 0 FAIL / 20 SKIP, W-074.13 4 sub-bug 全真修, ACTIVE workaround 5 → 3, jhyy.exe sha `02118a50...`, D43 closure HOLD on `e6b6f1fa...`)。**v2.x 末 5 sub-sprint 链入口**: v2.12.0 (audit) → v2.13.0 (真 XMM + sysv) → v2.14.0 (N 代 mutation) → v2.15.0 (QBE 自写) → v2.16.0 (QBE 移除 + bench + .exe byte-equal)。**Renumber note**: 原 v2.10.0 / v2.12.0 (老) = QBE 移除 + toolchain closure, 2026-09-17 reconcile 重命名为 `v2.16.0-plan.md` (v2.x 末 第 5 步)。|
| [`v2.13.0-plan.md`](v2.13.0-plan.md) | **sprint 计划**（v2.x 末 第 2 步, ✅ shipped 2026-09-19 on axis-v2 + tag `v2.13.0`, 4-commit chain `5d405bb` Ph.1 真 XMM regalloc + `b1ad5c3` Ph.2 真 amd64_sysv codegen 全覆盖 (8-class §A.4) + `d062a71` Ph.3 amd64_win_freestanding 真 E2E + Phase 4 docs+ship）| v2.13.0 = 真 XMM regalloc + 真 amd64_sysv codegen 全覆盖 + amd64_sysv_freestanding 真 E2E。`compiler/src0/codegen_amd64_regalloc.jhyy` MODIFY (~80-150 LOC XMM 真 regalloc) + `abi_amd64_sysv.jhyy` MODIFY (~100 LOC 8-class §A.4 + sret + vararg + IEEE 754 width) + `codegen_amd64_emit_call.jhyy` MODIFY (~120 LOC XMM FNARG + SysV dispatch) + `codegen_amd64_emit_mem.jhyy` MODIFY (~30 LOC sret setup) + NEW `xmm_pressure_9args.jhyy` fixture + NEW `sysv_full_regress.sh` (docker gcc:12 chain V.3 gate) + `run-ovmf.sh` MODIFY (QEMU 10 chardev syntax) + **W-074.6 PARTIAL → FULL CLOSED** (真 XMM regalloc + spill + caller/callee save + stack-arg fallback 全真修, V.1 9-f64-arg test 5/5 PASS 证明) + 5 sysv tests SKIP → 5/5 PASS (sysv_abi_test=28 / sysv_struct_mixed=42 / sysv_struct_pass=35 / sysv_struct_ret=18 / sysv_vararg_basic=42 via docker gcc:12) + OVMF 5/5 PASS (self-backend → efi 真 E2E, ConOut 可见) + regress 119/139 HOLD + D43 closure re-baseline `e6b6f1fa...` → `b743f8a5...` (Phase 2 IL emit 微变 expected re-baseline per D43 closure rule) + ACTIVE workaround 3 → 2 (W-074.6 FULL CLOSED;W-074.6.1 PARTIAL kept ACTIVE 推 v2.13.1)。~800 LOC 4 commits。|
| [`v2.13.1-plan.md`](v2.13.1-plan.md) | **sprint 计划**（v2.x 末 第 2 步 补丁, ✅ shipped 2026-09-20 on axis-v2 + tag `v2.13.1`, 1 docs commit）| v2.13.1 = RCA status audit + 6 status flips + 1 new fixture（Group A only, 0 src change per user 2026-09-20 决定）。**RCA-first 发现**: W-074.6.1 sub-family 4 sub-bugs (a/b/c/d) STALE description（代码已 ship v2.11.x 真修）;W-074.7.9 self-backend STALE（idiv sign-extend + rem lexer 真修 ship v2.11.9）;W-073 STALE（self-backend regress 120/120 PASS confirms 0-byte .s not triggered）。**W-074.8 sub-bug 1 + 3 audit-false-positive**（per [[feedback_rca_first_root_cause]] Phase 1 调研）— baseline dispatcher `storel val.id, addr`（codegen.jhyy:866）正确写 slice_slot addr; emit_mem ptr-deref flag 已正确 propagate; baseline `for_in_slice_nested.jhyy` + `const_array.jhyy` + `const_struct_array.jhyy` 都 PASS EXIT=expected → 不真改 src0。**6 status flips**（workarounds.md docs-only）:W-073 ACTIVE → ✅ RESOLVED v2.13.1（RCA closeout）;W-074.6.1 a/b/c/d ACTIVE → ✅ CLOSED v2.13.1（cross-ref 真修 commit `6192834` v2.11.8 + `c93247c` v2.11.10 + `6c7f81c` v2.11.13 + `6795f75` v2.11.15）;W-074.7.9 PARTIAL → ✅ CLOSED v2.13.1（cross-ref `8ffccce` v2.11.9）;ACTIVE workaround count 7 → ~2（剩 W-058 + W-057 vendor-only）。**NEW fixture** `compiler/tests/examples/slice_iter_nested_basic.jhyy`（15 LOC, EXPECT=66）— nested slice iter E2E 验, baseline PASS EXIT=66;regress 119 → 120（+1 coverage）。**Group C defer**（per audit risk ranking）:W-074.8 sub-bug 2（C.4 float imm）+ sub-bug 4（cap_table 16B 2-reg）v2.13.2;W-074.9（3 sub-bugs）v2.13.3;W-058 fmod v2.13.5;W-057 UTF-8 v2.13.6。D43 closure HOLD `b743f8a5...`（docs-only sprint, 无 src0 改 → 无 re-baseline event）;jhyy.exe sha `3cc0c7752b04e0fd...` HOLD;regress 120/140 PASS QBE + 120/141 PASS self-backend（1 transient `match.jhyy` race during full run, single-pass PASS）;umbrellla changelog `docs/logs/v2/changelog-v2.11.0.md` v2.13.1 section（per [[feedback_changelog_umbrella]] 不创建 standalone `changelog-v2.13.1.md`）。~80 docs + 15 fixture LOC, 1 docs commit。|
| [`v2.13.2-plan.md`](v2.13.2-plan.md) | **sprint 计划**（v2.x 末 第 2 步 补丁-2, ✅ shipped 2026-09-20 on axis-v2 + tag `v2.13.2`, 1 docs commit）| v2.13.2 = W-074.8 sub-bug 2+4 audit-flip + cap_table_2reg_basic.jhyy fixture（Group A only, 0 src change per user 2026-09-20 决定, 跟 v2.13.1 同 pattern）。**RCA-first 发现**: W-074.8 sub-bug 2（C.4 float imm）STALE description — 真修复 ship 在 v2.11.19 Phase 3 commit `d6364ba`（f32 IMM + f64 fractional + FNARG XMM 真解 via `jhyy_helpers.c` `jh_double_to_bits` + `jh_float_to_bits`）;v2.12.0 audit line 184-191 验证 4/4 C.4 float tests PASS QBE≡SB parity（float_arith EXIT=6 / float_arith_f32 EXIT=4 / f32_suffix EXIT=0 / f64_suffix EXIT=0）。Bug C src2-imm XMM 仍 theoretical（QBE materializes literals to temp first → 无 test 触发）。**W-074.8 sub-bug 4（cap_table 16B 2-reg）STALE on 2 counts** — 错诊: cap_table_basic test 4（got=30 vs 42）真因是 emit_copy `pct_count` heuristic miscounts bare `%t`（fn arg name = single letter `t`）→ mis-routed as TEMP copy → FNARG path never entered → flag propagation skipped → t4 = t3（pointer value, not deref）;真修 ship 在 v2.11.21-fix Phase 1 commit `4beab82`（1 LOC fix per `rca-v2.11.21.md` § 2 Option 2）;SysV § A.4 分类误判: CapTable<i32> = struct { 8B ptr + 8B i64 } 两 eightbytes 都是 INTEGER class → INTEGER+INTEGER merge → INTEGER class = **1 reg pass**（NOT 2-reg as workarounds.md stale claim）;`emit_amd64_arg_regs` 当前 1-reg handling 是 CORRECT per SysV § A.4 + Win x64 calling convention。**3 status flips**（workarounds.md docs-only）:W-074.8 entry header ⏸ DEFERRED → ✅ FULLY CLOSED v2.13.2（4 sub-bugs 全 audit-flip: 1+3 v2.13.1 / 2+4 v2.13.2, 0 src change total）;W-074.8 sub-bug 2 ⏸ DEFERRED → ✅ CLOSED v2.13.2（cross-ref `d6364ba`）;W-074.8 sub-bug 4 ⏸ DEFERRED → ✅ CLOSED v2.13.2（cross-ref `4beab82` + SysV § A.4 INTEGER+INTEGER class → 1 reg）;ACTIVE workaround count ~5 → ~3（剩 W-074.9 3 sub-bugs + W-058 + W-057 vendor-only, v2.13.3+ 推）。**NEW fixture** `compiler/tests/examples/cap_table_2reg_basic.jhyy`（15 LOC, EXPECT=42）— sanity-check 16B struct cross-fn pass-by-value（CapTable<i32> INTEGER+INTEGER class, 1 reg pass per SysV § A.4）;regress 120/140 → 121/141（+1 coverage）。D43 closure HOLD `b743f8a5...`（docs-only sprint, 无 src0 改 → 无 re-baseline event）;jhyy.exe sha `3cc0c7752b04e0fd...` HOLD（rebuilt baseline not touched in v2.13.2）;umbrellla changelog `docs/logs/v2/changelog-v2.11.0.md` v2.13.2 section（per [[feedback_changelog_umbrella]] 不创建 standalone `changelog-v2.13.2.md`）。~70 docs + 15 fixture LOC, 1 docs commit。|
| [`v2.13.3`](inline exec, 无 plan file) | **sprint 计划**（v2.x 末 第 2 步 补丁-3, ✅ shipped 2026-09-20 on axis-v2 + tag `v2.13.3`, 1 docs commit）| v2.13.3 = STALE docs audit #3: 5 flips + 1 reclassify (Group A only, 0 src change, 跟 v2.13.1/2 同 pattern)。**RCA-first 发现** (via Explore subagent cross-ref grep + git log + audit log, ~30s): (1) **W-074 entry header 🟡 ACTIVE → ✅ CLOSED v2.11.0 ship** — 真修 ship 在 commit `25dfb00` "W-074 il_len=0 root cause 真修 + regress --self-backend" (Wed Sep 9 2026 20:28:05 +0800);W-074.5 sub-entry 早 v2.11.1 ship 翻 RESOLVED,本 entry header 漏翻。(2) **W-074.9 entry header ⏸ DEFERRED → ✅ CLOSED v2.11.21-fix + v2.11.23 ship** — 3 sub-bugs 全真修 ship:sub-bug 5 dungeon_game 真修 commit `1985bd5` v2.11.21-fix Ph.4 next_token_ret 跨行吞 @else 真修;sub-bug 6 big_array 真修 commit `98ca31f` v2.11.21-fix Ph.3 2-pass slot alloc;sub-bug 7 top_level_let_mut_types 真修 v2.11.21-fix chain;v2.12.0 audit 验证 3/3 sub-bugs 全 PASS QBE≡SB parity (dungeon_game EXIT=0 / big_array EXIT=5050 / top_level_let_mut_types EXIT=17)。(3) **W-074.7 title/body mismatch canonical flip** ✅ CLOSED title → ⏸ DEFERRED — v2.11.15 Iter 2 commit `568d3aa` per-arm injection strategy 估"✅ CLOSED" 基于 spot-check 5/12 PASS,但 v2.11.16 Phase 0 audit user 要求 full regress 跑全 135 tests 发现 spot-check 不可靠 + stale .s 推断错误,body 标 ⏸ DEFERRED audit correction 才是 ground truth,title 误标 v2.13.3 closeout flip canonical 一致。(4) **W-023 misclassification ACTIVE → 📚 DOCS / canonical pattern** — yaml 表达式 + bash sub-shell 语义鸿沟是 GH Actions 设计如此 (`${VAR}` 不展开 `${{ env.X }}`),entry 自身写 "失效条件 N/A (设计如此)" 即承认无 bug;canonical pattern 用 `$VERSION` 直接读 env block。**5 flips + 1 reclassify** (workarounds.md docs-only):W-074 entry header + W-074 superseder + W-074.9 entry header + W-074.7 title + W-023 status。**ACTIVE workaround count ~3 → ~1** (剩 W-058 + W-057 vendor-only,v2.13.4+ 可推 ACTIVE = 0)。D43 closure HOLD `b743f8a5...` (docs-only, 无 src0 改);jhyy.exe sha `3cc0c7752b04e0fd...` HOLD;regress 121/141 PASS QBE + 121/142 PASS self-backend HOLD (无 new fixture);umbrellla changelog `docs/logs/v2/changelog-v2.11.0.md` v2.13.3 section (per [[feedback_changelog_umbrella]]);v2.13.3 = **无 standalone plan file** (per [[feedback_small_plans_no_docs]] 单 stage step-by-step plan 不写 `docs/plans/`, 走 inline execution)。~50 docs LOC, 1 docs commit。|
| [`v2.13.4`](inline exec, 无 plan file) | **sprint 计划**（v2.x 末 第 2 步 补丁-4, ✅ shipped 2026-09-20 on axis-v2 + tag `v2.13.4`, 1 docs commit）| v2.13.4 = docs cleanup #4: 4 mislabel/PARTIAL reclassify (Group A only, 0 src change, 跟 v2.13.1/2/3 同 pattern)。**RCA-first 发现** (via status line audit grep): (1) **W-022 ACTIVE → 📚 DOCS / canonical pattern** — entry 自身写 "失效条件 N/A (设计如此,workaround 是规范用法)" 即承认无 bug;GH Actions PS5.1 default 在 windows-latest runner,workaround = `Set bash as default shell step` v1.5.5 ship 起 canonical pattern;GH Actions 升 PS7 后 PS7 `Out-File -Encoding utf8NoBOM` 默认无 BOM,但 PS7 跟 PS5.1 共存期间需保留 bash-default canonical。(2) **W-024 ACTIVE → 🌍 ENV-ONLY** — 真因 PS5.1 `Set-Content` / `Out-File` 写 UTF-8 文本默认加 BOM + CRLF,Windows PowerShell 5.1 是 GH Actions `windows-latest` runner default;**jhyy-side 不可修** — workaround pattern 稳定 (bash-default shell step 或 `[System.IO.File]::WriteAllText($path, $content, [System.Text.UTF8Encoding]::new($false))` 强制 utf8NoBOM)。(3) **W-029 🟢 ACTIVE → 🟢 STABLE-PRODUCTION mislabel fix** — 真修 ship 在 v1.5.6 commit `a2dd4c1` "feat(v1.5.6): jhyy_helpers.c 加 jh_gcc_path() + jh_gcc_invoke() — A 派 Driver 探测" + docs commit `28450d3`;**`commit TBD` 是 docs 漏填** 不是 fix 没 ship — fix ship'd 4+ years stable in production,🟢 ACTIVE label 误导 future contributor 误以为还需真修。(4) **W-074.6 T4-g ⚠️ PARTIAL → ✅ RESOLVED** — v2.11.11 真修 ship in commit `e01cb59` "fix(codegen): v2.11.11 W-074.6 T4-g lexer cnew/ceqw silent-skip 真修" + docs `c6a703f` + `f0c1860`;5/6 self-backend EXIT exact closure ship done big_test EXIT=57 preserved 跨 v2.11.10/11/12/13/15/19/20/21-fix/23 + v2.13.0/1/2/3 全程维持;**6/6 完整 closure NOT 达成** 是 W-074.7.9 范围 已 ship 闭环,不是 W-074.6 T4-g scope — ⚠️ PARTIAL 是 v2.11.11 ship 当时 honest scope-claim 错出,v2.13.4 closeout flip 回 ✅ RESOLVED;entry section header (line 5865) 已经写 ✅ RESOLVED,body status ⚠️ PARTIAL label 不一致,v2.13.4 closeout flip body status 跟 header canonical 一致。**真 ACTIVE workaround count 5 → 0** 🎉 (剩 W-058 + W-057 + W-074.7 三条真未修 — W-058 codegen emit 路径缺 fmod / W-057 lexer spec 限 UTF-8 3/4-byte / W-074.7 phi merge gap 重设计,推 v2.13.5/6/7 真修 sprint)。D43 closure HOLD `b743f8a5...` (docs-only, 无 src0 改);jhyy.exe sha `3cc0c7752b04e0fd...` HOLD;regress 121/141 PASS QBE + 121/142 PASS self-backend HOLD (无 new fixture);umbrellla changelog `docs/logs/v2/changelog-v2.11.0.md` v2.13.4 section (per [[feedback_changelog_umbrella]]);v2.13.4 = **无 standalone plan file** (per [[feedback_small_plans_no_docs]])。~30 docs LOC, 1 docs commit。|
| [`v2.14.0-plan.md`](v2.14.0-plan.md) | **sprint 计划**（v2.x 末 第 3 步, ✅ shipped 2026-09-22 on axis-v2 + tag `v2.14.0`, 1 docs commit (single-axis-v2 per `feedback_audit_single_commit_diff`)）| v2.14.0 = N≥10 fixed point 正式 verify + mutation testing protocol (100% catch rate) + cross-platform N≥3 (Linux x86_64 docker gcc:12)。`fixed_point.sh` 扩 N=3→N=10 + `mutations.json` NEW (286 LOC, 30 mutations 5 ABI + 10 codegen + 10 parser + 5 typechecker) + `mutation_test.py` NEW (426 LOC, 3 detection modes: compile_fail / compile_il_diff / regress;ironclad try/finally + atexit restore per `feedback_unrelated_uncommitted_revert`) + `mutation_test.sh` NEW (80 LOC, driver) + `fixed_point_linux.sh` NEW (223 LOC, docker gcc:12 chain + MSYS_NO_PATHCONV=1) + `d43_linux.sh` NEW (82 LOC, Linux D43 closure verify) + `cd $JHYY_ROOT` lock in both scripts (closure quirk: jhyy.exe dbgfile path cwd-sensitive → .il sha `43fee332...` vs `481c2e99...` drift prevention)。**启动前置**: v2.13.11 ship ✅ + v2.13.0 ship ✅ + v2.12.0 ship ✅ + v2.11.23 ship ✅ + v2.9.0 ship ✅。**5 verification gates V.1-V.5 全绿**:V.1 N≥10 byte-equal 10/10 PASS `43fee332...` (per-代 2.2-2.8s 全 < 1.5x baseline 7.5s);V.2 mutation catch rate 30/30 = **100%** (3 轮迭代 60% → 93.3% → 100%; baseline regress 126/147 PASS HOLD, 0 false positive);V.3 Linux 5 main tests PASS + gcc 12.5.0 toolchain sanity (hello / arith / match / cap_test / break_continue Win-Linux parity);V.4 D43 closure baseline `43fee332...` HOLD (N=10 long-term hold across v2.13.7 → v2.13.11 → v2.14.0 chain);V.5 regress 持平 126/147 PASS HOLD。**scope minimisation**:src0 0 改动 (verification infra expansion, NOT src0 真修);0 src0 changes = 0 risk of regress regression + 0 risk of D43 closure break + 0 risk of jhyy.exe rebuild。**main mirror commit** 同步 12 files (per `0cadfba` precedent;exclude `mutation-test-report.md` auto-generated + `*.bak` temporary files)。~1166 LOC source/scripts + ~211 LOC docs = ~1377 LOC total。|
| [`v2.15.0-plan.md`](v2.15.0-plan.md) | **sprint 计划**（v2.x 末 第 4 步, ✅ shipped 2026-09-22 on axis-v2 + tag `v2.15.0`, 1 commit ship per worktree per `feedback_audit_single_commit_diff`, plan REWRITE per code reality + `feedback_doc_refactor_factcheck`）| v2.15.0 = **Strategy B+** (NOT 原 "QBE 自写"): **in-memory self-backend pipeline (skip `.il` file I/O)** + **`.s` byte-equal D43 closure** dual-layer (per `feedback_changelog_umbrella` v2.x single umbrella changelog `docs/logs/v2/changelog-v2.13.0.md`)。`compiler/src0/codegen_amd64_inmem.jhyy` NEW (~131 LOC, `codegen_amd64_run_text(text, len, asm_path, target_tag) -> i32` 7-step orchestration, 复用现有 `lex_il` + `parse_and_emit` + `peephole_fold_with_len` + `jh_write_file`, **0 修改** 9 个 `codegen_amd64_*.jhyy` 文件) + `compiler/src0/main.jhyy` MOD (+25 / -8 LOC: `import codegen_amd64_inmem` + `jh_write_il_enabled()` env gate 默认 0 + `build_il` in-mem dispatch 优先级链 in-mem self > write `.il` + `compile()` skip `run_backend` 当 `build_il` 返回 2) + `compiler/tests/bootstrap/fixed_point.sh` MOD (+40 / -5 LOC: `.s` byte-equal 第二层 D43 closure loop + `JHY_FP_BASELINE_S_SHA` env var + warn 1.5x → 1.3x) + `compiler/tests/bootstrap/byte_equal.sh` MOD (+12 / -3 LOC: `.s` from INFO-only → primary closure gate) + **W-074.14 NEW → RESOLVED** (v2.15.0 `codegen_amd64_run_text` 直传 text 跳过 `jh_read_file` heap-boxed i64 兜底) + W-068 fix #4 unit-type alignment cascade (if/else 分支都加显式 assignment 避 sema `()` vs `i32` 错) + **6 verification gates V.1-V.6 全绿**:V.1 regress 126/147 PASS HOLD;V.2 in-mem `.s` byte-equal to file-path self (`216683e1...`);V.3 V2↔V3 `.il` + `.s` 双层 byte-equal closure (`954a8563...` + `216683e1...`);V.4 `.s` baseline re-pin 3/3 收敛;V.5 ACTIVE workaround count = 0;V.6 single commit per worktree。~290 LOC source/scripts + ~290 LOC docs = **~580 LOC total**, **1 commit ship per worktree** (per `feedback_audit_single_commit_diff`)。**Pre-existing lex limitation NOT v2.15.0 regression** (`codegen_amd64_lexer.jhyy` v2.11.19 B3 fix 对 `data {...}` 含 `}` 字符串字面量会 emit 6 个 "unknown QBE IL mnemonic" stderr warnings, in-mem 跟 file-path 完全一致行为, fixed_point.sh setup fails for src0/main.jhyy 是 pre-existing behavior per `feedback_codegen_amd64_run_zerobyte` + `feedback_codegen_amd64_multifn`)。**Renumber note**: 原 plan (2026-09-17 写) 描述 "QBE 自写 (跳过 QBE IL, 直接 emit x86-64) + 新建 `compiler/src0/qbe/amd64_codegen.jhyy` + `qbe_il.jhyy`" 跟 code reality 不符 (self-backend 已存在并 ship 跑通 in 9 个 `codegen_amd64_*.jhyy` 平铺文件),2026-09-22 user decide Strategy B+ (skip `.il` file I/O, NOT skip QBE IL) + plan REWRITE 反映 code reality per `feedback_doc_refactor_factcheck`。**下一阶段**: v2.16.0 (QBE 工具链完全移除 + perf bench + `.exe` byte-equal 兜底 recipe per v2.x 末 5 sub-sprint 链 第 5 步) + v3.x OS-required 等 user 启动 (per `v2-v3-parallel-sprint-plan.md`)。|
| [`v2.16.0-plan.md`](v2.16.0-plan.md) | **sprint 计划**（v2.x 末 第 5 步, 📋 planned, 2026-09-17）| v2.16.0 = QBE 工具链完全移除 + 性能 bench + .exe byte-equal 兜底 (v2.x 末 "完成定义")。`run_qbe` 留空 stub + `qbe.exe` 删除 + `qbe_wrapper.{c,h}` git rm + 自写后端 fatal-on-fail + Procmon 验证 0 qbe spawn + perf bench ≤ 1.1x C 版 + `.exe` byte-equal 兜底 recipe。**启动前置**: v2.15.0 ship ✅ + v2.14.0 ship ✅ + v2.13.0 ship ✅ + v2.12.0 ship ✅ + v2.11.23 ship ✅。~350 LOC 3 commits。**Renumber note**: 原 `v2.10.0-plan.md` (2026-09-09, V2-C Part 2+3) 因 codegen_amd64_run 0-byte .s blocker (W-074) renumber 为 `v2.12.0-plan.md` (老), 2026-09-17 reconcile 重命名为 `v2.16.0-plan.md` (v2.x 末 第 5 步)。Source 暂存 `/tmp/v2.16.0-source.patch` (~350 行 diff)。|
| [`batch-V2-A-plan.md`](batch-V2-A-plan.md) | **sprint 计划**（batch）| V2-A batch = v2.0.0 + v2.1.0 + v2.2.0 + v2.3.0 + v2.4.0 串行 ship 链路（前 5 版本）|
| [`batch-V2-B-plan.md`](batch-V2-B-plan.md) | **sprint 计划**（batch）| V2-B batch = v2.5.0 + v2.6.x + v2.7.0 + v2.7.1 + v2.7.2 + v2.8.0（v2.x 中期 M2 自写后端 + Linux ELF infra）|

## 版本轴关系（关键）

**v2.0 阶段（v2.0.0 → v2.1.0 → v2.2.0 → v2.3.0 → v2.4.0）= 串行**（2026-09-01 user 决定）：
- 串行原因：每版在前版 baseline 上做，byte-equal 阶段性 self-equal 必须严格顺序（per D43）
- 跨版不能并行：v2.1.0 ABI 抽离依赖 v2.0.0 target dispatcher 起步；v2.2.0 spec 锁定依赖 v2.1.0 ABI 实现；v2.3.0 E2E 启动依赖 v2.2.0 spec 锁；v2.4.0 byte-equal 三件套依赖 v2.3.0 E2E 通
- v2.0 阶段 ship 后才启动 v2.x 中/末 + v3.0 3a-3f（per [`../roadmap/v2-v3-parallel-sprint-plan.md § 5.1` 路径 A](../roadmap/v2-v3-parallel-sprint-plan.md)）

**v2.x 中/末 ‖ v3.0+ = 异步并行**（2026-09-01 user 决定）：
- 不强配对：两条线各自推进，不要求 v3.0 启动跟 v2.x 中/末对齐
- 三条硬约束保留（per [`../roadmap/v2-v3-parallel-sprint-plan.md § 5.1-5.3`](../roadmap/v2-v3-parallel-sprint-plan.md)）：D27 v3.1 3g→3g.5→3g.7 串行 / v3.0 3c volatile 先 ship 再 v2.x 后端移植 volatile / v3.0 3d `#[no_std]` 软 ship（M1 launch 不依赖 per D10）

## 关联

- v2.x 长线路线图 → [`../roadmap/v2.x-qbe-rewrite.md`](../roadmap/v2.x-qbe-rewrite.md)
- v2.x ‖ v3.x 并行 sprint 调度 → [`../roadmap/v2-v3-parallel-sprint-plan.md`](../roadmap/v2-v3-parallel-sprint-plan.md)
- 上一代（v1.x 自举）→ [`../v1/`](../v1/)
- 自研 OS（独立 repo）→ `../../../jhyy_OS/`