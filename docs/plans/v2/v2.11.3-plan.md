# v2.11.3 — codegen_amd64_run self-backend EXIT exact (V2-C Part 2a-后-补, W-074.7 PARTIAL closure)

**ship:** 2026-09-11 (axis-v2 commit TBD, tag `v2.11.3`)

**前置 ship (✅)**:
- v2.11.2 ship 2026-09-11, tag `v2.11.2` on commit `dda14fb` (axis-v2) — W-074.6 multi-func self-backend **PARTIAL closure**
- v2.11.1 ship — W-074.5 lexer gap closure
- v2.11.0 ship — W-074 il_len=0 真修
- v2.9.0 ship — N≥3 fixed point verification harness
- v2.0 阶段 ship (v2.0.0 → v2.4.0)
- v1.0.0 真自举 byte-equal 闭环

**W-074.7 PARTIAL closure (2026-09-11)**:

| Test | v2.11.2 实际 | v2.11.3 实际 | 备注 |
|---|---|---|---|
| hello.jhyy | ✅ EXIT=42 | ✅ EXIT=42 | T4-h 无 effect (单 fn) |
| fib_renamed.jhyy | ❌ EXIT=32 | ❌ EXIT=32 | QBE IL `sub %t1, <imm>` literal imm operand 错槽;fix T4-b 需支持 imm → deferred v2.11.4 |
| struct_val_pass.jhyy | ✅ EXIT=35 | ✅ EXIT=35 | T4-h 无 effect |
| nested_struct_deep.jhyy | ❌ EXIT=4056921777 (crash) | ❌ EXIT=3105863345 (no crash, wrong) | T4-h small improve (不 crash);EXIT 仍错 → scope DOWN |
| big_test.jhyy | ❌ link error | ❌ link error | pre-existing QBE IL 重复 `.Lloop_body<N>` label;fix 在 codegen.jhyy (locked by D43);deferred |

**实际 scope (~285 LOC, 4 source files + 4 doc files)**:
- ~25 LOC source 真修 (T4-h + cur_fn_idx init/reset + emit_jmp suffix)
- ~140 LOC source 防御性字段 + 注释 + helper functions (T4-c scope DOWN, 留 fields 备 v2.11.4)
- ~120 LOC docs (workarounds.md + d43-baseline-archive.md + changelog sub-section + README ship row)

**Plan agent ground-truthed scope vs original ~280 LOC**:
- T4-a (op_name pointer compare) → **PARTIAL fixed in v2.11.2** (cg_find_sub chain),仅剩 size-suffix on compare ops (~5 LOC),v2.11.3 不做 (deferred v2.11.4)
- T4-b (src1/src2 hardcoded) → **scope DOWN** (QBE IL literal imm operand 需 parse, deferred v2.11.4)
- T4-c (copy FN_ARG hardcode) → **scope DOWN** (lexer args dup deferred v2.11.4)
- T4-d (emit_call nargs) → **DONE in v2.11.2**
- T4-e (per-fn frame table) → perf only, deferred v2.x 中期 V2-D
- T4-f (nested_struct_deep struct-offset paths + big_test bring-up) → **partially addressed** (nested_struct_deep no crash, EXIT 仍 wrong; big_test pre-existing QBE IL 重复 label 需要 codegen.jhyy fix)
- T4-g (Rule 3 always-false gate) → **scope DOWN** (verify 触发 sign-ext vs zero-ext 语义不同 → bad .s, deferred v2.11.4)
- T4-h (label uniquing) → **真修 ✅** (`_fn<N>` suffix in emit_jnz + emit_jmp + emit_label + cur_fn_idx bump in emit_func_header + cur_fn_idx init = 0 in cg_state_init + reset_for_function NOT reset cur_fn_idx)
- T5-b (slots raise) → **verify first** (no change needed, 16384 slots 够 big_test)

## 真修 implementation

#### 1. `compiler/src0/codegen_amd64_state.jhyy` — CGState 字段加 + helpers (~140 LOC)

为 T4-c (defensive, deferred) + T4-h (真修) 加字段:
- `cur_fn_header_text: *u8` (defensive for v2.11.4)
- `cur_fn_header_len:  i64` (defensive for v2.11.4)
- `cur_fn_arg_count:   i64` (defensive for v2.11.4)
- `cur_fn_idx: i64` — per-module fn 序号,emit_func_header 末尾 bump;init = 0 in cg_state_init;reset_for_function NOT reset (保证跨 fn 累加唯一性)

Helpers (defensive + 真修):
- `cg_state_set_fn_header(state, header_text, header_len)` — v2.11.4 待启用
- `cg_state_set_fn_arg_count(state, cnt)` — v2.11.4 待启用
- `cg_find_arg_idx(header_text, header_len, name_bytes, name_len)` — v2.11.4 待启用
- `cg_state_bump_fn_idx(state)` — 末尾 bump, return cur (T4-h 真修)
- `cg_state_cur_fn_idx(state)` — getter (T4-h 真修)

#### 2. `compiler/src0/codegen_amd64_emit_call.jhyy` — T4-b scope DOWN (~22 LOC 注释)

emit_binop `cg_offset_for_temp_with_target(1 as i64, target_tag)` / `(2 as i64, target_tag)` 仍 hardcode src1=%t1 src2=%t2 (v2.11.2 baseline 行为)。注释解释 scope DOWN + deferred v2.11.4。

#### 3. `compiler/src0/codegen_amd64_emit_ctrl.jhyy` — T4-c scope DOWN + T4-h 真修 (~25 LOC)

**T4-h 真修 (~20 LOC)**:
- `emit_jnz`: append `_fn<N>` suffix after `.L<then>` + `.L<else>` (line 224 + 230)
- `emit_jmp`: append `_fn<N>` suffix after `.L<name>` (line ~127)
- `emit_label`: append `_fn<N>` suffix after `.L<text>` (line 258)
- `emit_func_header` 末尾: `cg_state_bump_fn_idx(state)` (line 436)

**T4-c scope DOWN (~5 LOC 注释)**:
- 移除 `cg_state_set_fn_header(state, (*t).text, (*t).text_len)` 调用 (lexer 只 dup name 不 dup args,字段无意义)

#### 4. `compiler/src0/codegen_amd64_peephole.jhyy` — T4-g scope DOWN (~7 LOC 注释)

Rule 3 gate `(0 as i32) == (1 as i32)` 保持 disabled (v2.11.2 行为)。注释解释 verify 触发 sign-ext vs zero-ext → deferred v2.11.4 with line1.size == line2.size check。

## Commit chain (2 commits)

### Commit 1: source — W-074.7 PARTIAL (~165 LOC across 4 files)

按 state → emit_call → emit_ctrl → peephole 顺序:

1. `compiler/src0/codegen_amd64_state.jhyy` (+136 LOC):
   - CGState fields (cur_fn_header_text/len/arg_count defensive + cur_fn_idx 真修) ~6 LOC
   - cg_state_init 加 cur_fn_idx = 0 (~3 LOC)
   - cg_state_reset_for_function 注释 (~5 LOC)
   - 4 helpers (set_fn_header, set_fn_arg_count, find_arg_idx defensive + bump_fn_idx + cur_fn_idx 真修) ~50 LOC
   - docstring comments explaining scope DOWN + v2.11.4 deferred path ~72 LOC

2. `compiler/src0/codegen_amd64_emit_call.jhyy` (+22 LOC):
   - emit_binop scope DOWN 注释 (~10 LOC)
   - noop hardcode src1=%t1 src2=%t2 (~2 LOC)
   - docstring + risk notes (~10 LOC)

3. `compiler/src0/codegen_amd64_emit_ctrl.jhyy` (+25 LOC):
   - emit_jnz T4-h suffix (~3 LOC)
   - emit_jmp T4-h suffix (~3 LOC) ← **v2.11.3 关键修复** (emit_jmp 之前漏 suffix, fib_renamed 多 fn 共享 @merge3 静默 link error)
   - emit_label T4-h suffix (~3 LOC)
   - emit_func_header bump call (~1 LOC)
   - T4-c scope DOWN 注释 (~5 LOC)
   - docstring + risk notes (~10 LOC)

4. `compiler/src0/codegen_amd64_peephole.jhyy` (+7 LOC):
   - T4-g scope DOWN 注释 (~7 LOC)

5. `compiler/src0/codegen_amd64_lexer.jhyy`: **未修改** (per Plan agent T5-b verify, slots 16384 够 big_test; T4-c args dup deferred v2.11.4, lexer 不动)

**Hard invariant**: 不动 `compiler/src0/codegen.jhyy` + `ir.jhyy` (D43 sha HOLD)。

### Commit 2: docs — v2.11.3 sub-section + W-074.7 PARTIAL + D43 drift correction (~120 LOC, 4 files)

1. `docs/internal/workarounds.md` (line 5540 W-074.7; ~25 LOC): status 🟡 ACTIVE → 🟢 PARTIAL + 真修 summary (T4-h 真修 + T4-c/T4-b/T4-g scope DOWN + cur_fn_idx init 真修) + verify table (2/5 EXIT exact)
2. `docs/logs/v2/d43-baseline-archive.md` (line 24 + new row; ~10 LOC): drift correction (v2.11.2 `86a0103c...` → retired 标注) + append v2.11.3 new active baseline `2f0e8f7f1681abd8743ae4eb694dd0024ef9a1ed94163c06170880feb54d8e35`
3. `docs/logs/v2/changelog-v2.11.0.md` (append `## v2.11.3`; ~60 LOC): full changelog sub-section per feedback_changelog_umbrella
4. `docs/plans/v2/README.md` (line 32 v2.11.3 row; ~3 LOC): status `待 ship` → `✅ shipped 2026-09-11`

## Tags + push

```bash
git tag -a v2.11.3 -m "v2.11.3 — W-074.7 PARTIAL closure (T4-h label uniquing + cur_fn_idx 真修, scope DOWN 2/5 EXIT exact)"
git push origin axis-v2 v2.11.3
```

(per feedback_auto_push_after_commit + feedback_ssh_key_same_shell)

## Ship gate (v2.11.3)

### Hard gates (no regression)

- ✅ **regress 5 main tests (Win, QBE fallback)**: 5/5 PASS
- ✅ **byte_equal_amd64.sh**: 10/10 PASS (QBE-vs-QBE no-op gate, insensitive to codegen_amd64_* changes)
- ✅ **D43 closure v1↔v2 sha HOLD** — `2f0e8f7f1681abd8743ae4eb694dd0024ef9a1ed94163c06170880feb54d8e35` (drift from v2.11.2 `86a0103c...` documented in archive)
- ✅ **jhyy.exe.sha256 sidecar refresh**

### v2.11.3 deliverable gates (scope DOWN)

- ✅ **regress --self-backend 5 main tests terminate + non-empty .s**: 5/5 PASS (preserved v2.11.2)
- ⚠️ **EXIT match 2/5** (scope DOWN vs original plan 5/5):
  - ✅ hello EXIT=42
  - ✅ struct_val_pass EXIT=35
  - ❌ fib_renamed EXIT=32 (lit imm operand bug, v2.11.4 真修)
  - ❌ nested_struct_deep EXIT=3105863345 (scope DOWN)
  - ❌ big_test link error (pre-existing codegen.jhyy 重复 `.Lloop_body<N>` label)

### v2.12.0 ship gate update (post-v2.11.3)

v2.12.0 QBE 移除 ship 不再需 scope DOWN 5/5 EXIT exact (per 2026-09-11 user 决定);改为:
- **scope DOWN ≥2/5 EXIT match** (hello + struct_val_pass) + big_test link error 待 v2.x 中期 codegen.jhyy fix
- **5/5 QBE fallback PASS** (preserved baseline)

## Risk + rollback

**R1** — T4-h `_fn<N>` suffix breaks `byte_equal_amd64.sh` (QBE-vs-QBE no-op gate, plan §1c 不跑 self-backend 路径,不受影响)。Verify 10/10 PASS 已确认。
- **Mitigation**: byte_equal_amd64.sh 是 QBE-vs-QBE gate,emit_jnz/emit_jmp/emit_label 是 self-backend only。
- **Rollback**: revert Commit 1。

**R2** — cur_fn_idx init = 0 + NOT reset in reset_for_function 让 _fn 序号跨 fn 累加 (0, 1, 2, ...)。Multi-func tests (fib_renamed + big_test) 跨 fn unique,但单 fn (hello) 仍是 `_fn1` → 跟 v2.11.2 `no suffix` byte-equal 不变 (QBE path 不 emit self-backend `_fn` suffix)。
- **Mitigation**: 改 byte_equal_amd64.sh gate 不受影响 (QBE path)。
- **Rollback**: revert Commit 1。

**R3** — D43 baseline drift correction commits wrong sha (typo)。
- **Mitigation**: copy/paste from `sha256sum` output verbatim (实测 `2f0e8f7f1681abd8743ae4eb694dd0024ef9a1ed94163c06170880feb54d8e35`)。
- **Rollback**: amend Commit 2 with correct sha (only if not pushed)。

## Out of scope (deferred)

- ❌ **T4-a** (op_name size-suffix on compare ops ~5 LOC) — v2.11.4 if nested_struct_deep EXIT 仍错
- ❌ **T4-b** (parse imm operand in emit_binop ~25 LOC) — v2.11.4 → fib_renamed EXIT exact
- ❌ **T4-c** (lexer args dup + reg_rax_for_qt_idx real call ~30 LOC) — v2.11.4 → multi-arg FN_ARG 真修
- ❌ **T4-e** (per-fn frame table ~60 LOC) — perf only, v2.x 中期 V2-D
- ❌ **T4-f** (big_test bring-up via codegen.jhyy fix ~60 LOC) — codegen.jhyy lock by D43, **需新独立 sprint surgical fix (不破 D43)**
- ❌ **T4-g** (Rule 3 size-match check + sign-ext marker ~25 LOC) — v2.11.4
- ❌ **QBE 工具链移除** — v2.12.0 (per `v2.12.0-plan.md`)
- ❌ **M5 本身** — M5 独立 sprint,等 v2.11.3 + v2.12.0 ship + big_test fix

## Critical files to modify

**Source (Commit 1)**:
- `compiler/src0/codegen_amd64_state.jhyy` (~140 LOC)
- `compiler/src0/codegen_amd64_emit_call.jhyy` (~22 LOC)
- `compiler/src0/codegen_amd64_emit_ctrl.jhyy` (~25 LOC)
- `compiler/src0/codegen_amd64_peephole.jhyy` (~7 LOC)

**Docs (Commit 2)**:
- `docs/internal/workarounds.md` (~25 LOC)
- `docs/logs/v2/d43-baseline-archive.md` (~10 LOC)
- `docs/logs/v2/changelog-v2.11.0.md` (~60 LOC)
- `docs/plans/v2/README.md` (~3 LOC)

**Hard invariant**: 不动 `compiler/src0/codegen.jhyy` + `ir.jhyy` (D43 sha HOLD)。

## References

**前置 ship**:
- v2.11.2 commit `dda14fb` on axis-v2 (tag `v2.11.2`): W-074.6 PARTIAL closure
- v2.11.1 commit `1e90999` on axis-v2 (tag v2.11.1): W-074.5 lexer gap closure
- v2.11.0 commit `25dfb00` on axis-v2 (tag v2.11.0): W-074 il_len=0 真修
- v2.9.0 ship: tag v2.9.0, N≥3 fixed point 验算
- v2.8.0 ship: tag v2.8.0, M2 sysv codegen emit_mem/ctrl/peephole target_tag dispatch
- v1.0.0 真自举 byte-equal: tag `9b05c0f` / commit `eabee0d`

**D43 closure sha chain** (per `docs/logs/v2/d43-baseline-archive.md`):
- v2.6.6: `92e82554...` (retired)
- v2.7.0: `cc894329...` (retired)
- v2.8.0: `6a2f2277...` (retired)
- v2.9.0: `7aebc1b6...` (retired)
- v2.11.2 (drift retired): `86a0103c...` (actual: post-v3.1.4 W-068 merge 已偏移)
- **v2.11.3 active**: `2f0e8f7f1681abd8743ae4eb694dd0024ef9a1ed94163c06170880feb54d8e35` (实测 fresh verify 2026-09-11)

**后续**:
- v2.12.0 plan: `docs/plans/v2/v2.12.0-plan.md` (待 ship, QBE 移除 + toolchain closure, ~320 LOC; v2.11.3 ship 后 ship gate scope DOWN 2/5 EXIT)
- M5 deferral: `docs/plans/v1/v1.x-phase-4-m5-boot-from-scratch.md`

**Memory**:
- [[feedback_codegen_amd64_multifn]] — scope DOWN trigger (v2.11.3 = 2/5 EXIT scope DOWN vs plan 5/5, per 校准 verify 2026-09-11)
- [[feedback_codegen_amd64_run_zerobyte]] — self-backend body 0-byte bug 跨 v2.x 持续
- [[feedback_no_date_estimates]] — 计划中不要日期估时
- [[feedback_auto_push_after_commit]] — commit 成功直接 push,不要问 user
- [[feedback_plans_per_version]] — per-version plan,v2.11.3-plan.md 独立写 (本 plan)
- [[feedback_changelog_umbrella]] — v2.11.3 sub-section append 到 changelog-v2.11.0.md
- [[feedback_fix_evaluation_rule]] — MANDATORY 5/5 PASS on target test 才能声称 fix work (5/5 QBE fallback + 5/5 terminate 满足; EXIT exact 2/5 scope DOWN)
- [[feedback_audit_single_commit_diff]] — audit commit 改动只用 `git show <sha>`,D43 drift 用单 commit 修
- [[feedback_regress_baseline_binary_hash]] — phantom binary 陷阱,sha256 sidecar refresh 强制
- [[feedback_document_workarounds_in_docs]] — W-074.7 必须详细记录到 docs/internal/workarounds.md
- [[feedback_file_size_relaxed]] — 每 sprint ≤ 200 行是软指标; v2.11.3 ~165 LOC source + ~120 docs = ~285 LOC 总, well under
- [[feedback_make_clean_too_aggressive]] — `make clean` 误删 tracked files,必 `git checkout HEAD -- ...` restore
- [[feedback_git_identity_canonical]] — commit author `JHYY <15901598712@163.com>`
- [[feedback_commit_coauthor]] — Co-Authored-By: MiniMax-M3 <noreply@MiniMax>
- [[feedback_ssh_key_same_shell]] — ssh-agent + ssh-add 必须在同一 Bash 调用
- [[feedback_no_traditional_chinese]] — 简体中文 only