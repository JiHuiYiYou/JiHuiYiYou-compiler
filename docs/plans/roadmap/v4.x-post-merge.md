# v4.x: post-merge serial axis (M5 + 语言成熟化 + UX/性能 + 高级类型)

> **目标**：v2.x + v3.x axes merge 完成后,主版本轴推 v4.0.0;之后**单线串行**,把 v2/v3 期间 deferred 的工作(M5 + 所有非 OS-required 抽象 feature)清完。
> **前置**: v2.x 全 ship (V2-C v2.8.0) + v3.x 全 ship (v3.0.0..v3.3.0)
> **产出**: jhyy 编 jhyy 0 C 依赖闭环 + 完整现代语言特性
> **触发**: 2026-09-06 user 决定 — "v2,v3后面会merge，merge完主版本轴会推v4，之后就是串行了，所以像删src这种活排到v4就行"
> **状态**: ⏳ **概念阶段**(per 2026-09-06 user:"v4 现在还没出文档，但是其实有这个概念");L3 per-version plan 已建 13 个(L4 详细方案不写,per `feedback_small_plans_no_docs`)

---

## 1. v4 触发条件 / merge 决策点

### 1.1 merge 强前置(全 ✅ 后才可推 v4.0.0)

| 触发 | 状态 | 来源 |
|------|------|------|
| V2-C `v2.8.0` ship(N 代 fixed point + QBE 移除 + 工具链闭环) | ⏳ 未 ship | `[../v2/batch-V2-C-plan.md § Part 1+2+3](../v2/batch-V2-C-plan.md)` |
| V3 `v3.1.0` ship(3g `&mut` + Cap<T> 8B) | ✅ 2026-09-05 tag `d0b0f79` | `[../v3/batch-V3-C-plan.md](../v3/batch-V3-C-plan.md)` |
| V3 `v3.1.1` ship(3g.5 PhantomData) | ⏳ 未 ship | `[../v3/v3.1.1-plan.md](../v3/v3.1.1-plan.md)` |
| V3 `v3.1.2` ship(3g.7 CapTable + 跨函数 cap) | ⏳ 未 ship | `[../v3/v3.1.2-plan.md](../v3/v3.1.2-plan.md)` |
| V3 `v3.1.3` patch ship(SysV Cap<T> backport) | ⏳ 未 ship | `[../v3/v3.1.3-plan.md](../v3/v3.1.3-plan.md)` |
| V3 `v3.2.0`..`v3.2.5` ship(generics + closures + 4 个 std lib) | ⏳ 未 ship | `[../v3/v3.2.*-plan.md](../v3/)` |
| V3 `v3.3.0` ship(3h 浮点)| ⏳ 未 ship | `[../v3/v3.3.0-plan.md](../v3/v3.3.0-plan.md)` |
| user 决定 merge 时机 | ⏳ 决定中 | — |

### 1.2 merge 顺序

v2-C v2.8.0 ship 跟 v3.x 全 ship **可并行**(per `project_v2_v3_parallel_axes` + `v2-v3-parallel-sprint-plan.md § 5.1` 路径 A);merge 等两轴都 ship 完毕 + user 拍板。

### 1.3 v2 vs v3 哪些 serial / 哪些 parallel

- **pre-merge**:**v2.x ‖ v3.x 异步并行**(per `project_v2_v3_parallel_axes`);两轴各自 ship 后在 M1/M4/M11 launch 联调
- **merge 时**:1 个 merge commit + 删 axis-v2 / axis-v3 worktree/branch(per `feedback_batch_worktree_cleanup`)
- **post-merge**:**v4.x 单线串行**;不再有 parallel axis

---

## 2. v4 sprint 总览(per-version plan 已建 13 个)

| Sprint | 版本 | 主题 | 阻塞 | 跨 OS | 状态 |
|--------|------|------|------|-------|------|
| 4a | **v4.0.0** | v2+v3 merge → main axis push | V2-C v2.8.0 + V3 全 ship | — | ⏳ |
| 4b | **v4.1.0** | M5:删 `src/*.c` + untrack QBE + 删 `runtime.c`(jhyy 编 jhyy 0 C 依赖闭环)| V4.0.0 | M11 真闭环 | ⏳ |
| 4c | v4.2.0 | async/await + Future runtime | V4.1.0 | UX | ⏳ |
| 4d | v4.3.0 | lifetime 完整版 + Polonius borrow check | V4.1.0 | OS 安全 | ⏳ |
| 4e | v4.4.0 | closure 增强(move / borrow capture / generic / trait object)| V4.3.0 + V4.6.0 | UX | ⏳ |
| 4f | v4.5.0 | const generic | V4.1.0 | UX | ⏳ |
| 4g | v4.6.0 | trait objects(dyn Trait)| V4.1.0 | UX | ⏳ |
| 4h | v4.7.0 | 3k 错误恢复(parser / sema 多错误报告)| V4.1.0 | UX | ⏳ |
| 4i | v4.8.0 | 3m 基本优化 pass(常量折叠 / 死代码消除 / 代数化简)| V4.1.0 | perf | ⏳ |
| 4j | v4.9.0 | 3n 包管理器(jhyy new / build / test)| V4.1.0 | UX | ⏳ |
| 4k | v4.10.0 | HKT + specialization | V4.5.0 + V4.6.0 | UX | ⏳ |
| 4l | v4.11.0 | 并发 cap + 完整 memory model + 多架构 ABI(aarch64 / riscv64)| V4.1.0 | M11+ | ⏳ |

每 sprint 详细 scope / 验收 / 风险 / commit 节奏 → 对应 `v4.X.Y-plan.md`。

---

## 3. v4.x 跨轴关系(post-merge serial)

### 3.1 v4.x 内部顺序(per D28 + 2026-09-06 user 决定)

- **v4.0.0 merge**(机械,不动代码)→ **v4.1.0 M5**(强前置 merge)
- v4.2.0 async 跟 v4.7.0 错误恢复可并行,但**不开 worktree**(per 2026-09-06 user "顺序做的后面不用开branch") — user 决定每次只做 1 个
- v4.3.0 lifetime full → v4.4.0 closure 增强(v4.4.0 依赖 v4.3.0 Polonius)
- v4.5.0 const generic / v4.6.0 trait objects → v4.10.0 HKT + specialization
- v4.11.0 并发 cap → 跟 v4.2.0 async + v4.3.0 lifetime 集成(部分依赖)

### 3.2 v4 ↔ jhyy_OS

| OS 节点 | v4.x 触发 | 备注 |
|---------|----------|------|
| M1 launch | v4.0.0 merge 之前已 ship(per v3.x + v2.x 各 sub-sprint)| — |
| M4 launch | v4.0.0 merge 之前已 ship(per 3g + 3g.5 + 3g.7)| — |
| M11 launch | v4.0.0 merge 之前已 ship(per 3i + 3j + 3l.1-3l.4 + 3h)| — |
| OS 真闭环(自举 OS 编自己) | **v4.1.0 M5 ship 后** | jhyy 编 jhyy 0 C 依赖闭环;OS launch 阶段才暴露 |
| OS multi-core + 完整 memory model | **v4.11.0 ship 后** | M11+ polish;非 MVP 必需 |
| GUI M8d | v3.x 已 ship(per D-GUI-11 升级 3i + 3j)| v4.x 不直接涉及 |

### 3.3 v4 ↔ v2 / v3 历史 axes

- **v2.x** — V2-A (v2.5.0) / V2-B (v2.6.0 + v2.7.0) / V2-C (v2.8.0) 全 ship 完毕后,merge 进 v4.0.0;后续 v2 工作直接走 v4.x 编号(如 aarch64 / riscv64 ABI 走 v4.11.0,不另开 v2.9.x)
- **v3.x** — V3-A (v3.0.0) / V3-B (v3.0.1..v3.0.5) / V3-C (v3.1.0..v3.1.2) + 后续 v3.2.x + v3.3.0 全 ship 完毕后,merge 进 v4.0.0
- **历史 batch plan 保留**:`docs/plans/v2/batch-V2-*.md` + `docs/plans/v3/batch-V3-*.md` + `docs/plans/v3/v3.*-plan.md` 不动,作为历史锚点 + cross-ref

---

## 4. v4.x 决策锁

### 4.1 merge 时机(per 2026-09-06 user 决定)

- **user 决定 merge 时机** — 不主动推
- 触发前置全 ✅ 后 user 拍板 → 1 个 merge commit → 推 v4.0.0 tag
- axis-v2 / axis-v3 worktree + branch 删除(per `feedback_batch_worktree_cleanup`)

### 4.2 worktree 规则(post-merge)

- **v4.x 不开新 worktree**(per 2026-09-06 user:"顺序做的后面不用开branch")
- v4.x 主轴 = `main` branch(merge 后 main 是 v4.x 唯一轴)
- v4.x 每 sub-sprint ship 走:`main` direct commit + tag `v4.X.0`

### 4.3 D43 阶段性 self-equal(per `v1.x-phase-4-m5-boot-from-scratch.md`)

- v4.0.0 merge 时重新 baseline(V2-C v2.8.0 N=3 closure 已 ship → 主轴最终 baseline)
- v4.1.0 M5 ship 后 baseline = jhyy 编 jhyy 0 C 依赖闭环 + N=3+ byte-equal hold
- v4.2.0..v4.11.0 每 ship 重新 baseline(D43 hold)

### 4.4 D26 reproducibility recipe(per v2.4.0 锁)

- v4.0.0 merge 后 N=3 byte-equal 是 closure invariant
- v4.x 每 ship 走 V2-C v2.8.0 + v3.x 已建立的 reproducibility gate(同输入 .il byte-equal)

### 4.5 抽象 feature 跨 sprint 决策

- **不**写"v4.x 中" / "v4.x 末"模糊期 — 所有 v4.x feature 都编 v4.X.Y 号(per `feedback_plans_per_version`)
- 后续 v4.X.Y 编号(如 v4.12.0 / v4.13.0)按需添加,不预设 roadmap

---

## 5. v4.x 完成标准

v4.x 全部 ship = jhyy 是**功能完整现代系统编程语言**:

| 类别 | 特性 | 来源 |
|------|------|------|
| **工具链** | selfhost 闭环 + 0 C 依赖 + 多目标(amd64_win/sysv/freestanding)| v1.x + v2.x + v4.1.0 |
| **OS-required 语言** | asm / naked / volatile / no_std / link_section / barrier / `&mut` + lifetime / Cap<T> / generics / closures / std lib(mem / fmt / string / arena / io / os / vec / map / math)| v3.x + v3.3.0 |
| **M5 闭环** | 删 `src/*.c` + untrack QBE + 删 `runtime.c` | v4.1.0 |
| **语言成熟化** | async / lifetime full + Polonius / closure 增强 / const generic / trait objects | v4.2 - v4.6 |
| **UX** | 错误恢复 / 优化 pass / 包管理器 | v4.7 - v4.9 |
| **高级类型系统** | HKT + specialization / 并发 cap + memory model + 多架构 ABI | v4.10 - v4.11 |

完成后 = **现代系统编程语言 + 自举 OS 真闭环**。

---

## 6. v4.0.0 merge 流程(简)

merge 本身是机械动作(per `[../v4/v4.0.0-plan.md](../v4/v4.0.0-plan.md)`):

1. `git checkout main`
2. `git merge --no-ff axis-v2`(冲突解决;docs/ 路径冲突多,源码 `src0/*` 冲突少)
3. `git merge --no-ff axis-v3`
4. D43 重新 baseline freeze → 写进 `docs/logs/v4/changelog-v4.0.0.md`
5. `axis-v2` + `axis-v3` worktree + branch 删除(per `feedback_batch_worktree_cleanup`)
6. `git tag -a v4.0.0` + `git push origin v4.0.0`
7. `main` `CLAUDE.md` 版本轴段更新:v2/v3 并行 → "v4 串行"

merge 不开新 worktree — 1 个 commit 完成。

---

## 7. v4.x 完成 ≠ M5 完成 / OS 真闭环

v4.1.0 M5 ship 后:
- 编译器端 0 C 依赖闭环(`jhyy 编 jhyy`)
- 但 **OS 端真闭环**还要求 jhyy_OS 用 jhyy 编 OS kernel + 在裸机跑通 — 这部分在 jhyy_OS 项目,不在 v4.x scope
- v4.1.0 ship 后 OS launch 阶段才能暴露 / 验证

---

## 8. 后续 v4.X.Y 编号预留

v4.12.0+ 编号**未预设**;按需添加(每 minor version 一份 `v4.X.Y-plan.md`,per `feedback_plans_per_version`)。候选 backlog(不在 v4.0..v4.11 已编):

- 完整 8-class SysV 分类器(原 v3.x 中 → 推 v4.x)
- `&dyn Trait`(原 v4.6.0 简化为 `Box<dyn>`;完整版留)
- 完整 drop semantics(原 v3.x 中 → 推 v4.x)
- async + 多线程完整集成(原 v3.x 末 → 推 v4.x)
- 复数 / 矩阵 / 向量 std lib(原 v3.x 末 → 推 v4.x)
- SIMD 数学 std lib(原 v3.x 末 → 推 v4.x)
- 网络 std lib(原 v3.x 末 → 推 v4.x)
- 文件系统 std lib(原 v3.x 末 → 推 v4.x)
- 进程 std lib(原 v3.x 中 → 推 v4.x)
- mmap / io_uring(原 v3.x 末 → 推 v4.x)
- async trait(原 v3.x 中 → 推 v4.x)
- 链接时优化 LTO(原 v3.x 末 → 推 v4.x)
- Profile-guided optimization (PGO)(原 v3.x 末 → 推 v4.x)
- IDE / LSP 集成(原 v3.x 末 → 推 v4.x)
- Distributed cap / capability delegation 跨机器(原 v3.x 末 → 推 v4.x)
- fix-it hint / 错误代码 E0001(原 v3.x 中 → 推 v4.x)
- 多级优化 `-O2` / `-O3`(原 v3.x 末 → 推 v4.x)
- async closure(原 v3.x 中 → 推 v4.x)
- ghost / 幽灵借用(原 v3.x 中 → 推 v4.x)
- auto-vectorization(原 v3.x 末 → 推 v4.x)

需要时新加 `v4.12.0-plan.md` / `v4.13.0-plan.md` 等,不在本 doc 锁死。

---

## 9. Cross-ref

- 权威 spec:`[../abis/jhyy-lang-spec-v1.3.0.md](../abis/jhyy-lang-spec-v1.3.0.md)` + `[../abis/jhyy-abi-v1.0.0.md](../abis/jhyy-abi-v1.0.0.md)`
- v2 路线图:`[./v2.x-qbe-rewrite.md](./v2.x-qbe-rewrite.md)`(merge 后 archived)
- v3 路线图:`[./v3.x-language-expansion.md](./v3.x-language-expansion.md)`(merge 后 archived)
- OS 链路:`[../v2/v2.0.0-os-prep.md](../v2/v2.0.0-os-prep.md)`(M1-M11)
- OS 镜像:`[../../../jhyy_OS/docs/coordination.md](../../../jhyy_OS/docs/coordination.md)`
- v4 per-version plans:`[../v4/](../v4/)` 13 个 L3 docs
- M5 deferral:`[../v1/v1.x-phase-4-m5-boot-from-scratch.md](../v1/v1.x-phase-4-m5-boot-from-scratch.md)`
- v4 concept:[[project-v4-post-merge-axis]] memory
- worktree cleanup:`feedback_batch_worktree_cleanup` memory
