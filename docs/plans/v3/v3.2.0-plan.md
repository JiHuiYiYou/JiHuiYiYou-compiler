# v3.2.0 plan — 3i generics (单态化)

## Context

**状态**: ⏳ **未启动**(`v3.x-language-expansion.md § Sprint 3i` 已写设计草案,**未**写 L3 + L4 doc)。

**上游依赖**:
- ✅ V3-C `v3.1.0` + `v3.1.1` + `v3.1.2`(借用检查 stub + Cap<T> 8B + CapTable)
- ⏳ `v3.2.1` (3j closures)依赖本 sub-sprint ship — **D28 顺序锁**

**触发前置 / OS 关系**:
- **M8d launch 硬前置**(per D-GUI-11 锁 2026-09-01):compositor surface / buffer / seat interface 需 generic over T
- **M11 launch 硬前置**(per `v2.0.0-os-prep.md § 1` M11):本 sub-sprint + v3.2.1..v3.2.5 全 ship
- 3j 紧随其后:3j closures 内部依赖 3i generics(closure env 用 generic struct),per D28 顺序

**Worktree 约定**:per 2026-09-06 user → **不开新 worktree**(user "顺序做的后面不用开branch"),axis-v3 direct commit + tag `v3.2.0`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| 语法:`fn name<T, U>(...)` + `name::<T>(args)` | `compiler/src0/parser.jhyy` | ~80 行 |
| 泛型参数挂在函数/struct/enum 节点 | `compiler/src0/ast.jhyy` | ~40 行 |
| 单态化后类型检查 | `compiler/src0/sema.jhyy` (复跑) | ~150 行 |

### 新建
- `compiler/src0/monomorphize.jhyy`(~300 行)— 调用处收集具体类型 + 单态化 pass(sema 之后 / codegen 之前)
- `tests/generics_basic.jhyy` + `generics_struct.jhyy` + `generics_enum.jhyy` + `generics_multi_arg.jhyy`(~200 行 + SKIP)
- `tests/std_lib_vec_pre.jhyy`(集成,jhyy 自身源准备用 `Vec<T>`)
- `docs/abis/jhyy-lang-spec-generics-supplement-v3.2.0.md`(~250 行)
- `docs/logs/v3/changelog-v3.2.md`(append 段)

## 关键决策

- **M0 简化:不做 lifetime 泛型参数**(泛型 + lifetime 留 v3.x 中)
- **M0 简化:不做 const generic**(`const N: usize` 等,留 v4.x)
- **M0 简化:单态化在 sema 之后 / codegen 之前**(避免 monomorphize 改动 codegen 主路径)
- **不开新 worktree**(per 2026-09-06 user)
- **3j 紧随其后**:3j closures 内部依赖 3i generics,per D28 顺序

## 验收 / 5/5 ship gate

- 5+ 泛型测试 5/5 PASS(generics_basic / generics_struct / generics_enum / generics_multi_arg / std_lib_vec_pre)
- `tests/std_lib_vec_pre.jhyy`(jhyy 自身源准备用 `Vec<T>`)跑通(集成)
- D43 re-baseline → **N10**(泛型改 codegen,极可能漂移)
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` → N9 → N10 byte-equal hold

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 单态化 pass 引入 codegen 主路径漂移 | regress 退步 | monomorphize 独立模块,失败时 codegen 走单态化 fallback;test 覆盖 |
| 泛型 + 借用检查交互(泛型参数借用 T) | sema 复杂度 | M0 简化:泛型参数不借用(借用泛型留 v3.x 中) |
| jhyy 自身源迁移 Vec<T>(per D28)耗时 | ship 时间线 | ship gate 不要求 jhyy 自身迁移;M11 launch 时再做 |

## Out of scope

- lifetime 泛型参数(留 v3.x 中)
- const generic(留 v4.x)
- specialization / trait objects(留 v3.x 末)
- 完整 reborrow + lifetime 子类型(留 v3.x 中)
- 高阶类型(HKT,留 v3.x 末)
- jhyy 自身源迁移 Vec<T>(M11 launch 时做,**不**阻塞本 ship)
- 3l std lib(留 v3.2.2+)

## Commit / tag 节奏

- **Commit 1**:`feat(generics): add fn/struct/enum 泛型 + monomorphize pass (3i)`
- **Tag**:`v3.2.0`
- 写进 `docs/logs/v3/changelog-v3.2.md` 对应段(新建 umbrella)
- 通知 user:本 sub-sprint ship 后,D28 锁下一步 = v3.2.1 (3j) 可启动

## Cross-ref

- 设计草案:`[../roadmap/v3.x-language-expansion.md § Sprint 3i](../roadmap/v3.x-language-expansion.md)`
- 上游:`v3.1.2-plan.md`(CapTable + 借用 stub ship)
- 下游:`v3.2.1-plan.md`(3j closures 依赖本 sub-sprint)
- 跨 axis V2-A `codegen_amd64.jhyy`(已 ship v2.5.0,monomorphize 集成)
- D-GUI-11 锁(3i + 3j 升级 M8d):`[../../jhyy_OS/docs/coordination.md](../../jhyy_OS/docs/coordination.md)` § 3
- M8d launch gate:`[../v2/v2.0.0-os-prep.md § 1](../v2/v2.0.0-os-prep.md)`
