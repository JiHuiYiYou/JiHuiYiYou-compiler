# v3.1.2 plan — 3g.7 CapTable<T> + 跨函数 cap pass

## Context

**状态**: ⏳ **未 ship**(D27 锁:v3.1.1 ship 后启动)。

**上游依赖**:
- ✅ V3-C `v3.1.0`(3g `&mut` + NLL stub + `Cap<T>` 8 字节)ship
- ✅ V3-C `v3.1.1`(3g.5 PhantomData)ship — **D27 硬前置**
- ✅ V3-A `v3.0.0` `#[no_std]`

**触发前置 / OS 关系**:
- **M4 launch 硬前置**(per `v2.0.0-os-prep.md § 1` M4):v3.1 3g + 3g.5 + 3g.7 全 ship → 本 sub-sprint ship = **M4 launch 可启动**
- D27 锁:本 sub-sprint 必须在 v3.1.1 ship 后启动
- **V2-C v2.8.0 触发**:本 sub-sprint ship 后 V2-C v2.8.0 N 代 fixed point 才可启动(否则 N 代验算跑 `cap_test.jhyy` fail)

**Worktree 约定**:per 2026-09-06 user → **不开新 worktree**,axis-v3 direct commit + tag `v3.1.2`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| `CapTable<T>` builtin + `*Cap<T>` 裸指针类型 | `compiler/src0/parser.jhyy` | +30 行 |
| `CapTable<T>` 类型识别 + 字段访问 | `compiler/src0/sema.jhyy` | +50 行 |
| `CapTable<T>` layout:`*Cap<T>`(8B ptr)+ `len: i64`(8B)= 16B | `compiler/src0/codegen.jhyy` | +80 行 |
| 跨函数 `Cap<T>` cap pass 校验 | `compiler/src0/sema.jhyy` | +80 行 |
| `Cap<T>` 跨函数 call emit 走 64-bit register | `compiler/src0/codegen_amd64_emit_call.jhyy` | +30 行 |
| `Cap<T>` class = INTEGER(SysV 8-byte,占一个 reg) | `compiler/src0/abi_amd64_sysv.jhyy` (v2.7.0 创建) | +10 行 |

### 新建
- `tests/cap_table_basic.jhyy`(~50 行 + SKIP)— `CapTable<i32>` 创建 + 访问 + pass 跨函数
- `tests/cap_test_advanced.jhyy`(~80 行 + SKIP)— 跟 3g + 3g.5 联合验证
- `tests/cap_test_sysv.jhyy`(~30 行 + SKIP,走 `--target=amd64_sysv`)— SysV target 跨函数 `Cap<T>` reg 验证
- `docs/abis/jhyy-lang-spec-cap-table-supplement-v3.1.2.md`(~120 行)
- `docs/abis/jhyy-abi-v1.0.0.md § 13.x`(SysV Cap<T> class 章节,~30 行)
- `docs/logs/v3/changelog-v3.1.md`(append 段,~15 行)

## 关键决策

- **CapTable<T> 用裸指针 + 长度**:per c-typedef 不借用(slice 借用由 user 自管;`Cap<T>` 本身不可变)
- **Cap<T> 跨函数 cap pass 校验**:不允许跨越 volatile 引用(借用 + volatile 互斥语义复杂,留 v3.x 中)
- **D27 锁:本 sub-sprint 必须在 v3.1.1 ship 后启动**
- **不开新 worktree**(per 2026-09-06 user)

## 验收 / 5/5 ship gate

- `tests/cap_table_basic.jhyy` 5/5 PASS
- `tests/cap_test_advanced.jhyy` 5/5 PASS
- `tests/cap_test_sysv.jhyy` 5/5 PASS(走 `--target=amd64_sysv`)
- D43 re-baseline → **N8**(post-v3.1.2 merge,CapTable 改 codegen,极可能 .il 漂移)
- `regress.py --all` → 2/2 gated PASS,jhyy.exe 109/109 + 17 SKIP
- `jhyy_selfhost_check` → N7 → N8 byte-equal hold

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| CapTable 跨函数 pass 借用 vs cap pass 冲突 | sema 复杂度 | M0 简化:CapTable pass-by-value 走 8-byte pointer + 8-byte len;不做借用 |
| 跨函数 Cap<T> 跟 3c volatile 互斥(cap pass 跨 volatile 引用)| ship gate 阻塞 | sema 显式禁用 + 错误信息;留 v3.x 中完整支持 |
| Cap<T> SysV class 未填导致 V2-B v2.7.0 sysv target 集成 fail | ship gate 阻塞 | v2.7.0 ship 后 backport;**不阻塞**本 sub-sprint ship(Windows target 优先 per 2026-09-06 决策)|

## Out of scope

- CapTable 借用语义(留 v3.x 中)— 当前 CapTable 用裸指针 + 长度
- Cap<T> 跨 volatile 引用借用(留 v3.x 中)
- 完整 reborrow `&mut *x`(留 v3.x 中)
- 跨函数 borrow 完整版(留 v3.x 末)
- 多线程 cap(M4 launch 之后;留 v4.x)
- 全 C11 / Rust memory model(留 v3.x 中)
- ARM / RISC-V Cap<T> ABI(留 v3.x 中 / M11 launch 时)

## Commit / tag 节奏

- **Commit 1**:`feat(cap-table): add CapTable<T> + cross-function cap pass (3g.7)`
- **Tag**:`v3.1.2`
- 写进 `docs/logs/v3/changelog-v3.1.md` 对应段
- **跨 axis 通知**:**v3.1.2 ship 后通知 user:V2-C v2.8.0 N 代 fixed point 可启动**(per `[../v2/batch-V2-C-plan.md](../v2/batch-V2-C-plan.md)` 跨 axis 硬前置)

## Cross-ref

- 上游:`[batch-V3-C-plan.md](batch-V3-C-plan.md)`(V3-C v3.1.0 + v3.1.1 ship)+ `v3.1.1-plan.md`
- 下游:V2-C `batch-V2-C-plan.md`(N 代 fixed point 触发前置 = 本 sub-sprint ship)
- 跨 axis V2-A `codegen_amd64_emit_call.jhyy`(已 ship v2.5.0,跨函数 Cap<T> reg)
- 跨 axis V2-B `abi_amd64_sysv.jhyy`(`Cap<T>` class,触发前置 = V2-B v2.7.0 ship)
- D27 串行锁:`[../roadmap/v2-v3-parallel-sprint-plan.md § 4.4](../roadmap/v2-v3-parallel-sprint-plan.md)`
- M4 launch gate:`[../v2/v2.0.0-os-prep.md § 1 M4](../v2/v2.0.0-os-prep.md)`
- 8 字节 Cap<T> layout:`project_cap_abi_layout.md`
