# v3.1.3 plan — SysV abi_amd64_sysv.jhyy Cap<T> class backport patch

## Context

**状态**: ⏳ **未 ship**(scope-deferred,per 2026-09-06 决策:V3-C 整 batch scope **Windows target only**)。

**上游依赖**:
- ✅ V3-C `v3.1.0`(3g + `Cap<T>` 8 字节 layout Windows target)ship
- ✅ V3-C `v3.1.2`(CapTable + 跨函数 cap pass Windows target)ship
- ⏳ V2-B `v2.7.0`(`abi_amd64_sysv.jhyy` 创建)**未 ship** — **本 patch 触发前置**

**触发前置 / OS 关系**:
- M4 launch 副作用:V2-B v2.7.0 ship 后 backport SysV `Cap<T>` class,跨函数 `Cap<T>` 在 sysv target 上的 register 分配验证
- **不在 v3.1.2 scope**(per 2026-09-06 决策:Windows-only scope 优先 ship)

**Worktree 约定**:per 2026-09-06 user → **不开新 worktree**,axis-v3 direct commit + tag `v3.1.3`。

**命名冲突说明**:本 patch 用 `v3.1.3` 而非 `v3.1.2-patch`,因 v3.1.2 已 ship tag;**不**遵循 feedback `feedback_changelog_umbrella` 的"vX.Y axis 只 1 个 umbrella changelog,无 standalone patch changelog"(本 patch 只往 `changelog-v3.1.md` append 段,**不**新建 standalone file)。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| `abi_amd64_sysv.jhyy`: `Cap<T>` class = INTEGER(SysV 8-byte,占一个 reg)| `compiler/src0/abi_amd64_sysv.jhyy` (v2.7.0 创建 / 当时未填 Cap class) | +10 行 |
| 跨函数 `Cap<T>` 在 sysv target 上的 register 分配验证 | `compiler/src0/codegen_amd64_emit_call.jhyy` | ~0 行(继承 v3.1.2 emit)|
| lang-spec 增补(SysV Cap<T> class 章节)| `docs/abis/jhyy-abi-v1.0.0.md § 13.x` | +30 行 |

### 新建
- `tests/cap_test_sysv.jhyy`(~30 行 + SKIP,走 `--target=amd64_sysv`)— SysV target 跨函数 `Cap<T>` reg 验证
- `docs/logs/v3/changelog-v3.1.md`(append 段,~10 行)

## 关键决策

- **触发前置**:V2-B `v2.7.0` (`abi_amd64_sysv.jhyy`)ship 后 backport(目前 `v2.7.0` 未 ship,等 V2-B 启动)
- **不在 v3.1.2 scope**(per 2026-09-06 决策:Windows-only scope 优先 ship)
- **不开新 worktree**(per 2026-09-06 user)
- **命名冲突**:本 patch 用 `v3.1.3` 而非 `v3.1.2-patch`,因 v3.1.2 已 ship tag;只往 umbrella changelog append 段

## 验收 / 5/5 ship gate

- `tests/cap_test_sysv.jhyy` 5/5 PASS(走 `--target=amd64_sysv`)
- D43 re-baseline → **N9**(SysV Cap<T> 改 abi,可能漂移)
- `regress.py --all` → 2/2 gated PASS(109/109 + 17 SKIP 不变)
- `jhyy_selfhost_check` → N8 → N9 byte-equal hold

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| V2-B `v2.7.0` 启动延迟 → 本 patch 推迟 | 时间线漂移 | 等 V2-B ship 立即启动;本 patch 不阻塞 V3 axis 主线 |
| SysV `Cap<T>` class 8 字节布局与 ABI spec 不符 | cross-target 联调 fail | 复用 v3.1.0 Windows 验证过的 8 字节布局 + per c-typedef 锁定 |

## Out of scope

- 多架构 Cap<T> ABI(ARM / RISC-V,留 v3.x 中 / M11 launch 时)
- Cap<T> SysV class 之外其他 SysV-specific 调整(留 V2-B)
- V2-B v2.7.0 本身的 scope(属 V2 axis)

## Commit / tag 节奏

- **Commit 1**:`feat(cap-sysv): backport Cap<T> class to abi_amd64_sysv.jhyy (3g.7 patch)`
- **Tag**:`v3.1.3`
- 写进 `docs/logs/v3/changelog-v3.1.md` 对应段(append-only)
- **触发条件**:V2-B `v2.7.0` ship 后立即启动

## Cross-ref

- 上游:`v3.1.2-plan.md`(CapTable + 跨函数 cap pass Windows target ship)
- 下游:V2-B `batch-V2-C-plan.md` 跨 axis 联调(本 patch ship 后 V2-C v2.8.0 N 代 fixed point 验算可走 sysv target)
- 跨 axis V2-B:`[../v2/batch-V2-B-plan.md](../v2/batch-V2-B-plan.md)`(`v2.7.0` 创建 `abi_amd64_sysv.jhyy`,未填 Cap class)
- ABI:`[../abis/jhyy-abi-v1.0.0.md](../abis/jhyy-abi-v1.0.0.md)` SysV Cap<T> class 章节
- 8 字节 Cap<T> layout:`project_cap_abi_layout.md`
