# v3.2.1 plan — 3j closures

## Context

**状态**: ⏳ **未启动**(`v3.x-language-expansion.md § Sprint 3j` 已写设计草案)。

**上游依赖**:
- ✅ V3-C `v3.1.0`..`v3.1.2` ship(借用检查 stub + Cap<T> + CapTable)
- ⏳ V3 `v3.2.0` (3i generics)ship — **D28 硬前置**(closure env 用 generic struct)
- ⏳ `v3.2.2` (3l.1 mem/fmt/string/arena)依赖本 sub-sprint ship(per D28 顺序)

**触发前置 / OS 关系**:
- **M8d launch 硬前置**(per D-GUI-11 锁 2026-09-01):seat focus callback 是闭包式
- **M11 launch 硬前置**(per `v2.0.0-os-prep.md § 1` M11):本 sub-sprint + v3.2.0 + v3.2.2..v3.2.5 全 ship

**Worktree 约定**:per 2026-09-06 user → **不开新 worktree**,axis-v3 direct commit + tag `v3.2.1`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| 闭包语法 `\|x: i32\| { ... }` | `compiler/src0/parser.jhyy` | ~80 行 |
| 闭包 = `{fn_ptr, captured_env}` + 合成函数 `__closure_N(env, ...)` | `compiler/src0/codegen.jhyy` | ~250 行 |
| 按值捕获 + 多变量捕获(capture analysis) | `compiler/src0/sema.jhyy` | ~150 行 |

### 新建
- `tests/closures_basic.jhyy` + `closures_multi_capture.jhyy` + `closures_as_arg.jhyy`(~150 行 + SKIP)
- `docs/abis/jhyy-lang-spec-closures-supplement-v3.2.1.md`(~200 行)
- `docs/logs/v3/changelog-v3.2.md`(append 段)

## 关键决策

- **M0 简化:闭包按值捕获**,不做 `move` 关键字
- **M0 简化:闭包不能捕获借用**(借用捕获留 v3.x 中)
- **M0 简化:不做 async 闭包**(async 留 v3.x 中)
- **不开新 worktree**(per 2026-09-06 user)
- **3l 紧随其后**:std lib 依赖 generics + closures

## 验收 / 5/5 ship gate

- 3+ 闭包测试 5/5 PASS(closures_basic / closures_multi_capture / closures_as_arg)
- D43 re-baseline → **N11**(闭包改 codegen,极可能漂移)
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` → N10 → N11 byte-equal hold

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 闭包 + 借用检查交互(捕获借用) | sema 复杂度 | M0 简化:闭包不能捕获借用;借用捕获留 v3.x 中 |
| 闭包 + 泛型交互(generic closure) | codegen 复杂度 | M0 简化:闭包非泛型(泛型闭包留 v3.x 中) |
| 合成函数 codegen 漂移 | regress 退步 | 合成函数 `__closure_N` 走 codegen 主路径 + 测试覆盖;失败时 codegen 走 fallback |

## Out of scope

- 借用捕获闭包(留 v3.x 中)
- `move` 关键字(留 v3.x 中)
- async 闭包(留 v3.x 中)
- 泛型闭包(留 v3.x 中)
- 闭包 trait objects(留 v3.x 末)
- 闭包 + lifetime 标注(留 v3.x 中)
- 3l std lib(留 v3.2.2+)
- 3h 浮点(留 v3.x 中)

## Commit / tag 节奏

- **Commit 1**:`feat(closures): add 闭包语法 + capture analysis + 合成函数 (3j)`
- **Tag**:`v3.2.1`
- 写进 `docs/logs/v3/changelog-v3.2.md` 对应段
- 通知 user:**本 sub-sprint ship 后,M8d launch 可启动**(per D-GUI-11)— compositor surface / seat focus callback 可写
- 通知 user:本 sub-sprint ship 后,D28 锁下一步 = v3.2.2 (3l.1) 可启动

## Cross-ref

- 设计草案:`[../roadmap/v3.x-language-expansion.md § Sprint 3j](../roadmap/v3.x-language-expansion.md)`
- 上游:`v3.2.0-plan.md`(3i generics 闭包 env 用 generic struct)
- 下游:`v3.2.2-plan.md`(3l.1 mem/fmt/string/arena 依赖 generics + closures)
- D-GUI-11 锁(3i + 3j 升级 M8d):`[../../jhyy_OS/docs/coordination.md](../../jhyy_OS/docs/coordination.md)` § 3
- M8d launch gate:`[../v2/v2.0.0-os-prep.md § 1](../v2/v2.0.0-os-prep.md)`
