# v1.1.0 任务清单 + 概要设计: Workaround 真修 + Codegen Bug 真修

> **✅ ALL SHIP 2026-08-12** (commits `ca8bc44` (1.1) / `33542a6` (mcp-2 C.1) + v1.1 wip commits 1.2-1.9, ship 2026-08-05 ~ 2026-08-12)
>
> **承接**: v1.0.0 TAGGED (commit `eabee0d`, 2026-08-10) + v0.9 wip commit 2.83 (style cleanup ship, 2026-08-11).
> **当前 baseline**: regress_v1 50/53 + regress.py 50/53 + Stage 1 byte-equal 7/7 + Stage 2 N=3 byte-equal 维持 (jhyy_v1→v2→v3→v4 .il 全 `2445e97d...`).
> **目标**: 4 个 ACTIVE workaround (W-003/W-004/W-006/W-007) + 4 个 v0 codegen bug (Bug 1-4) 全部真修,workarounds.md 移入 RESOLVED,src0/*.jhyy 恢复自然 jhyy 风格.
> **姿态**: 本 plan 是 [`v1.0-post-50-53-plan.md § Phase 2`](../v1/v1.0-post-50-53-plan.md) 的 sprint-by-sprint 编排实现 — Phase 2 描述策略,本 plan 拆 7 个 sprint + 每 sprint 详细工作 + 验证守门.

---

## Context (2026-08-11, commit 5f824ae)

### 状态盘点

**W-00X workaround 状态** (per `docs/internal/workarounds.md` § 索引):

| ID | 状态 | 触发面 | 真修做法 |
|----|------|--------|----------|
| W-001 | ✅ RESOLVED (v0.8 commit 9) | hash_string `*i32` deref → loadsb `b` 错 | byte-by-byte FNV-1a |
| W-002 | ✅ RESOLVED (v0.9 wip commit 2.12) | identifier 6-8 字符 + `_buf` 触发 heap 损坏 | W-001 真修间接覆盖 + 211 revert |
| **W-003** | 🟡 ACTIVE (partial) | `let _ = fncall()` 顶层 / 嵌套 segfault | codegen.c NODE_LET discard fix |
| **W-004** | ✅ RESOLVED (transitive — W-001 byte-by-byte FNV-1a 真修 indirect coverage, 2026-08-12 验证 minimal repro + 4 boundary variations 全 PASS codegen on jhyy_v1 sha `ba94df93...`) | 短名 (≤4 字符) local var + struct field assign → stack overflow | 真因 = W-001 family (`hash_string` `*i32` deref overread → 短 ident 撞 symtab); W-001 真修 ship in v0.8 commit 9 `d570c72` 已覆盖 |
| W-005 | ✅ RESOLVED (v0.9 wip commit 2.13) | `let mut x; x = expr;` segfault | CGContext C/jhyy 9 字段对齐 + 16 处 revert |
| **W-006** | ✅ RESOLVED (transitive — Sprint 4.21-4.25 W-005 #2 真修 chain 已覆盖, 2026-08-11 验证 minimal repro 不再触发) | 1-char var `return x ± y` 共享 stack slot | 真因 = cg_expr IRVal struct pass-by-value stale pointer; W-005 #2 sprint chain 已修 |
| **W-007** | 🟡 ACTIVE (partial) | `fn() -> i64 { return X as i64; }` emit `w copy` | cg_convert_arg struct field + global var 路径补全 |
| W-008 | ✅ RESOLVED (v0.8 commit 11) | cg_find_field_offset 双层 deref 漏 | single deref fix |
| W-009 | ✅ RESOLVED (v0.8 commit 12) | cg_convert_arg src_t==0 早 bail | 入口放宽 |
| W-010 | ✅ RESOLVED (v0.9 wip commit 2.79) | MAX_LOCALS 512 vs 1024 | jhyy-side align |
| W-011 | ✅ RESOLVED (v0.9 wip commit 2.80) | inline_imports emit 重复 | resolve_one_import_v1 in_progress/completed push/pop |
| W-012 | ✅ RESOLVED (v0.9 wip commit 2.81) | cg_copy_struct emit `copy %t0` when src/dst undef | 8 处 `irval_is_undef` 守卫 |

**v0 codegen Bug 1-4** (per `memory/feedback_v0_codegen_bug_workarounds.md`):

| Bug | 触发面 | workarounds | 真修做法 |
|-----|--------|-------------|----------|
| **Bug 1 (LEA)** | `&(*ptr).struct_field` emit `loadw` 不是 LEA → QBE type mismatch | IRBuf.sb 用 `*StringBuilder` 而不是 `StringBuilder` (arena-allocated pointer 替代 inline struct) | codegen.c `&(*ptr).field` emit LEA fix |
| **Bug 2 (phi)** | 3-way `if A { if B{} else{} } else if C{} else{}` 写错 phi 前驱 → "predecessors not matched in phi" | refactor 3-way if-else → dispatcher + N helpers (1-level `if` per function) | codegen.c phi 前驱 emit 顺序 fix |
| **Bug 3 (loadub)** | `*((ptr) as *u8)` emit `loadub` 1 byte 不是 `loadl` 8 bytes | `let x_i64 = *(ptr as *i64); let x = x_i64 as *u8;` (over-read + cast) | codegen.c `*u8` deref 类型推导 fix (return `w` class) |
| **Bug 4 (&local)** | `&local_var` codegen alloc space 不 store → storeb 用未初始化 tmp | `arena_alloc(N) + *i32` 写 bytes | codegen.c `&local` store path fix |

### 联动关系 (per `memory/feedback_codegen_workaround_linkage.md`)

- **W-005 ↔ W-007** (NODE_ASSIGN 路径分支): W-005 已真修,W-007 partial 路径现在才真正测得到 → W-007 真修需要重新 audit
- **W-007 ↔ W-008 ↔ W-009** (cg_convert_arg 三向联动): W-008/W-009 已真修,W-007 真修需补 `src=W, dst=L` extsw 分支(struct field + global var 路径)
- **Bug 1-4** (v0 codegen.c 内部): 跟 W-XXX 路径正交,各自独立真修

### 不在 v1.1.0 scope

- v2.x QBE 重写 / v3.x 语言特性 (parallel axis,跟本 sprint 正交)
- v1.x syntax sugar (`null` / `else if` / `sizeof` / `for x in slice` / `#[inline]` / `defer`)
- Pattern binding codegen (`Some(v) => v`)
- OR pattern 一致性检查
- 嵌套 const array / const pointer / const slice / const enum array

---

## Sprint 排期 (5-6.5 sprint 总)

> **核心原则**: 每个 sprint 走 "修 codegen.c → mirror codegen.jhyy → revert workaround in src0/*.jhyy → 全量验证" 闭环.
> 验证守门 = regress.py 50/53 + regress_v1.py 50/53 + stage1 byte-equal 7/7 + Stage 2 N=3 byte-equal 维持.

### Sprint v1.1.1 — W-006 真修 (transitive, 0 sprint) + W-004 verification (0.5 sprint) ✅ SHIP 2026-08-12

> **门槛**: Task #60 已 ship (v0.9 wip commit 2.15) → W-004 verification 可恢复.
> **状态调整**: W-006 从"dormant 待真修"改为"transitively closed by Sprint 4.21-4.25 W-005 #2 真修 chain" (2026-08-11 验证 minimal repro 不再触发, IL byte-equal C-side).
> **W-004 验证结果**: 2026-08-12 PASS — minimal repro + 4 boundary variations 全部通过 codegen on jhyy_v1 (sha `ba94df93...`), EXIT=1 (link stage only, 不再 3221226356 stack overflow). W-004 → RESOLVED (transitive, W-001 byte-by-byte FNV-1a 真修 indirect coverage).

| 工作 | 估时 | 状态 |
|------|------|------|
| **W-006 真修 (transitive)**: reproduce `let x = 42 as i32; let y = 7 as i32; return x + y;` + fib30.jhyy (用 `n - 1` / `n - 2`) → jhyy_v1 编 → IL byte-equal C-side, exe exit 正确. 重新审计 git log + 真修 chain → 根因 = W-005 #2 family (cg_expr IRVal struct pass-by-value stale pointer), Sprint 4.21-4.25 已修 | 0 (transitive) | ✅ ship 2026-08-11 (v1.1 wip commit 1.1) |
| **workarounds.md 更新**: W-006 → RESOLVED (transitive) + 新增"W-006 RESOLVED — transitively closed by Sprint 4.21-4.25"段说明真修 chain | 0.1 | ✅ |
| **W-004 verification**: 用 jhyy_v1 (sha `ba94df93...`) 编 6 个最小 repro (BAD + GOOD + 4 boundary variations v1/v2/v3/v4). 全部 codegen done + EXIT=1 (link fail only due to `main` symbol conflict with runtime.c, NOT stack overflow) | 0.2 | ✅ ship 2026-08-12 (v1.1 wip commit 1.2) |
| **workarounds.md 更新**: W-004 → RESOLVED (transitive) + 新增验证状态 2026-08-12 + W-004 vs W-006 真因对比 + 真修 chain + 留给未来段 | 0.1 | ✅ |
| **changelog entry**: v1.1.1 ship entry (W-006 transitive close + W-004 transitive close) | 0.1 | ✅ |

**完成定义** (✅ 全部达成 2026-08-12):
- ✅ W-006 status 更新 (dormant → RESOLVED transitive, workarounds.md + index 同步)
- ✅ W-004 status 更新 (ACTIVE BLOCKED → RESOLVED transitive, workarounds.md + index 同步 + 真修 chain + 留给未来段)
- ✅ regress 50/53 + regress_v1 50/53 持平 (via direct python, MCP infra 旁路)
- ✅ changelog-v1.1.0.md: 2 entries (v1.1 wip commit 1.1 W-006 + v1.1 wip commit 1.2 W-004)

### Sprint v1.1.2 — W-007 partial 真修 (0.5-1 sprint)

> **门槛**: W-005 已真修 (commit 2.11+2.13) → cg_emit_store 路径 clean,W-007 partial 路径现在测得到.

| 工作 | 估时 |
|------|------|
| **audit**: 跑 src0/*.jhyy 里所有 `fn() -> i64 { ... }` 函数,识别哪些走 `cg_convert_arg` struct field 路径 vs global var 路径 vs 单 return value 路径 | 0.2 |
| **比对 codegen.c vs codegen.jhyy**: `cg_convert_arg` 在两个文件里的 emit 路径是否一致,特别是 `src=W, dst=L` extsw 分支的入口条件 | 0.2 |
| **fix codegen.jhyy**: 补 `cg_convert_arg` struct field + global var 路径的 `src=W, dst=L` extsw 分支 | 0.3 |
| **mirror 到 codegen.c** (if needed): 如果 C-side 也有结构性差异,同步修 | 0.2 |
| **验证**: regress 50/53 + regress_v1 50/53 + stage1 byte-equal 7/7 + Stage 2 N=3 byte-equal 维持 | 0.1 |

**完成定义**:
- W-007 → ✅ RESOLVED
- util.jhyy 中所有 `fn() -> i64 { return literal as i64; }` (FNV_OFFSET / FNV_PRIME / hash_string / strlen 等) 自然风格
- workarounds.md W-007 移入 RESOLVED section
- stage1 byte-equal 7/7 持平 (不破 byte-equal)

### Sprint v1.1.3 — W-003 扩展到 depth≥2 (1 sprint)

> **门槛**: W-005 已真修 → `let mut x; x = expr;` 路径 clean → v2 mutable pattern 现在安全可 retry.
> **当前状态**: v3 only covers depth==1 (top-level),33/151 替换完成;118 嵌套 (depth≥2) 仍是潜在 trigger.

| 工作 | 估时 |
|------|------|
| **audit 118 嵌套处**: `tmp/do_w003_v3.py` 类脚本扫 brace depth,列 depth=2/3/4/5+ 分布 | 0.2 |
| **设计 v4 模式**: per-depth 选最稳的 workaround 形式 | 0.1 |
| - depth=2 (1-level if): `let mut _x = 0; _x = fncall(); let _ = _x;` (mutable assignment, W-005 真修后安全) | |
| - depth=3+ (嵌套 if): 保持原样 (v3 decision 不动,避免引入 phi bug) | |
| **实施 + 验证**: 改 src0/,regress + regress_v1 + stage1 byte-equal 持平 | 0.5 |
| **workarounds.md 更新**: W-003 → ✅ RESOLVED (or partial RESOLVED if depth=3+ 留作 dormant) | 0.1 |
| **changelog entry** | 0.1 |

**完成定义**:
- W-003 depth≤2 全消除 (118 嵌套 → ≤ 30 depth=3+ 残余,或 0)
- regress 50/53 + regress_v1 50/53 + stage1 byte-equal 7/7 持平
- 任一 stage1 byte-equal FAIL → revert + 重审 mutable pattern

### Sprint v1.1.4 — Bug 1 (LEA emit) 真修 (1 sprint)

> **核心**: 修 codegen.c `&(*ptr).struct_field` 的 LEA emit;mirror 到 codegen.jhyy;revert workaround in src0/*.jhyy.

| 工作 | 估时 |
|------|------|
| **诊断**: 最小复现 `&(*ptr).field` → 跟 C-side 对照 IL diff,确认 codegen.c emit `loadw` 不是 LEA | 0.1 |
| **修 codegen.c**: 找到 `cg_field_addr` / `cg_addr_of` / `cg_field_access` 等函数,改 emit `lea` 替代 `loadw` | 0.3 |
| **mirror 到 codegen.jhyy**: 同步修 src0/codegen.jhyy 的等价 helper | 0.3 |
| **revert workaround in src0/*.jhyy**: 把 `IRBuf.sb: *StringBuilder` (arena-allocated) 改回 `IRBuf.sb: StringBuilder` (natural inline struct) | 0.2 |
| **验证**: regress 50/53 + regress_v1 50/53 + stage1 byte-equal 7/7 + Stage 2 N=3 byte-equal 维持 | 0.1 |

**完成定义**:
- v0 codegen emit `&(*ptr).field` 出 LEA IL
- src0/*.jhyy 用 natural inline struct 风格
- stage1 byte-equal 7/7 持平 (mirror 必须 1:1)

### Sprint v1.1.5 — Bug 2 (phi predecessor) 真修 (1 sprint)

> **核心**: 修 codegen.c phi 前驱 emit 顺序;revert 3-way if-else dispatcher refactor workaround.

| 工作 | 估时 |
|------|------|
| **诊断**: 最小复现 3-way nested if-else → 跟 C-side 对照 IL diff,确认 phi 前驱写错 | 0.1 |
| **修 codegen.c**: `cg_stmt_if` / `cg_stmt_block` 路径,phi 节点的前驱顺序对齐 actual control flow | 0.4 |
| **mirror 到 codegen.jhyy** | 0.2 |
| **revert workaround in src0/*.jhyy**: 3-way if-else 改回 natural 形式 (不再 refactor 成 dispatcher + N helpers) | 0.2 |
| **验证**: regress 50/53 + regress_v1 50/53 + stage1 byte-equal 7/7 + Stage 2 N=3 byte-equal 维持 | 0.1 |

**完成定义**:
- codegen emit phi 前驱顺序对齐 actual CFG
- src0/*.jhyy 3-way nested if-else natural
- stage1 byte-equal 7/7 持平

### Sprint v1.1.6 — Bug 3 (loadub type) 真修 (0.5 sprint)

> **核心**: 修 codegen.c `*u8` deref emit 类型 (return `w` class 不是 `b` class);revert over-read workaround.

| 工作 | 估时 |
|------|------|
| **诊断**: 最小复现 `*((ptr) as *u8)` → 跟 C-side 对照 IL,确认 emit `=b loadsb` 不是 `=w loadub` | 0.1 |
| **修 codegen.c**: `cg_expr_deref` / `cg_load` 路径,`*u8` deref 的 qbe_type 强制 `w` class | 0.2 |
| **mirror 到 codegen.jhyy** | 0.1 |
| **revert workaround in src0/*.jhyy**: `let x_i64 = *(ptr as *i64); let x = x_i64 as *u8;` 改回 `let x = *(ptr as *u8);` | 0.05 |
| **验证**: regress 50/53 + regress_v1 50/53 + stage1 byte-equal 7/7 | 0.05 |

**完成定义**:
- codegen `*u8` deref emit `=w loadub`
- src0/*.jhyy 用 natural `*u8` deref 风格
- stage1 byte-equal 7/7 持平

### Sprint v1.1.7 — Bug 4 (&local emit) 真修 (0.5 sprint)

> **核心**: 修 codegen.c `&local_var` store path;revert arena_alloc workaround.

| 工作 | 估时 |
|------|------|
| **诊断**: 最小复现 `let x = 42; let p = &x; *p = 5;` → 跟 C-side 对照,确认 codegen emit storeb 用了未初始化 tmp | 0.1 |
| **修 codegen.c**: `cg_addr_of_local` 路径,在 emit `storeb` 前补 store 路径 (或 alloc 时直接初始化) | 0.2 |
| **mirror 到 codegen.jhyy** | 0.1 |
| **revert workaround in src0/*.jhyy**: `let buf = arena_alloc(arena, N); let p = buf as *i32; *p = val;` 改回 `let p = &local_var; *p = val;` | 0.05 |
| **验证**: regress 50/53 + regress_v1 50/53 + stage1 byte-equal 7/7 | 0.05 |

**完成定义**:
- codegen `&local_var` store 路径正确
- src0/*.jhyy 用 natural `&local_var` 风格
- stage1 byte-equal 7/7 持平

---

## 累计估时

| Sprint | 主题 | 估时 |
|--------|------|------|
| v1.1.1 | ✅ W-006 真修 (transitive, 0 sprint) + W-004 真修 (transitive, 0 sprint — W-001 byte-by-byte 间接覆盖) + doc/verify | 0.2 |
| v1.1.2 | W-007 partial 真修 | 0.5-1 |
| v1.1.3 | W-003 扩展 (depth≥2) | 1 |
| v1.1.4 | Bug 1 (LEA) | 1 |
| v1.1.5 | Bug 2 (phi) | 1 |
| v1.1.6 | Bug 3 (loadub) | 0.5 |
| v1.1.7 | Bug 4 (&local) | 0.5 |
| **总计** | | **5-6.5 sprint** |

> **关键里程碑**:
> - Sprint v1.1.1 (✅ W-006 transitively closed + W-004 verification)
> - Sprint v1.1.2 (W-007 真修,cg_convert_arg 三向闭环)
> - Sprint v1.1.3 (W-003 扩展,workaround 代码大量消除)
> - Sprint v1.1.4-7 (Bug 1-4 真修,src0/*.jhyy 全部自然化)
> - v1.1.7 ship → 全部 workarounds RESOLVED → src0/*.jhyy 100% natural → v1.1.0 tag

---

## 完成定义 (v1.1.0 全 sprint ship 后)

| 维度 | 期望 |
|------|------|
| `docs/internal/workarounds.md` | W-003/W-004/W-006/W-007 4 个 active 全部移入 RESOLVED section |
| `compiler/src0/*.jhyy` | 全部使用 natural jhyy 风格 (无 workaround 代码) |
| `compiler/src/codegen.c` | 4 真修 (W-007 + Bug 1-4) 全部 ship |
| `compiler/src0/codegen.jhyy` | mirror C-side 4 真修 (1:1) |
| regress.py | 50/53 PASS (持平 baseline) |
| regress_v1.py | 50/53 PASS (持平 baseline) |
| Stage 1 byte-equal | 7/7 PASS (持平 baseline) |
| Stage 2 N=3 byte-equal | 维持 (jhyy_v1→v2→v3→v4 .il sha 不变) |
| 净 diff 行数 | 每个 sprint ≤ 200 行 (per `feedback_no_build_artifacts_in_git`) |
| changelog | `docs/logs/v1/changelog-v1.1.0.md` 7 个 entry (per sprint ship) |
| tag | **v1.1.0** (post-v1.0.0) |

---

## Critical files

| 文件 | 修改类型 |
|------|---------|
| `compiler/src/codegen.c` | W-007 真修 + Bug 1-4 真修 (4-5 处,每处 30-50 行 diff) |
| `compiler/src0/codegen.jhyy` | mirror C-side 4 真修 (1:1,150-250 行 diff per 真修) |
| `compiler/src0/*.jhyy` (main/codegen/parser/sema/ir/util/lexer/arena/types/ast/symtab) | revert workaround 代码 (~150 处,每处 1-3 行) |
| `compiler/src0/util.jhyy` | FNV_OFFSET/FNV_PRIME 等 i64 常函数 revert (W-007 真修后) |
| `docs/internal/workarounds.md` | 移 4 个 active → RESOLVED,W-006 标 dormant escaped |
| `docs/logs/v1/changelog-v1.1.0.md` | 新建,7 sprint entry |

---

## Risks / 守门

| 风险 | Mitigation |
|------|------------|
| codegen.c 真修引入新 regression (跨多文件) | 全量 regress 50/53 + regress_v1 50/53 守门 + 任一 FAIL → revert 该 commit 重审 |
| jhyy-side codegen.jhyy mirror 漏 1 处 | Stage 2 N=3 byte-equal 严格守门 (任一 drift → revert) |
| src0/*.jhyy revert 触发新 pattern (semantic 等价但 syntax 不同) | stage1 byte-equal 7/7 守门 |
| W-007 真修跟 W-005 #2 已修路径联动 | 同步跟 W-005 commit 2.81 的 8 处 `irval_is_undef` 守卫 — 任何 `cg_copy_struct` / `cg_emit_store` 改动都要 re-test 那 8 处 |
| Bug 1-4 真修破 regress_v1 baseline | per-bug verification (sprint 独立);任一 bug regress 退化 → revert + 调 audit 颗粒度 |
| W-003 mutable pattern 在 depth=2 触发 v0 codegen 残留 bug (W-005 真修后风险低但非 0) | 5/5 PASS 强制 (per `feedback_fix_evaluation_rule.md`) + 多文件抽样验证 |
| Task #60 验证后 W-004 仍触发 → batch rename 工程量大 | contingency: 分 2-3 个 sub-sprint,先重命名 main.jhyy (55 个 let),再 codegen.jhyy,最后 parser.jhyy 等 |

---

## Verification (end-to-end per sprint)

| 检查 | 命令 / 工具 | 期望 |
|------|------------|------|
| 1. C-side regress | `python compiler/build/bin/regress.py` (or `mcp__jhyy__jhyy_regress`) | 50/53 PASS (持平 baseline) |
| 2. jhyy_v1 regress | `python compiler/build/bin/regress_v1.py` | 50/53 PASS (持平 baseline) |
| 3. Stage 1 byte-equal | `jhyy_selfhost_check` (MCP tool) | all_byte_equal=True, il_sha256 不变 |
| 4. Stage 2 N=3 | `jhyy_v2 build src0/main.jhyy` + diff `jhyy_v2.il jhyy_v3.il jhyy_v4.il` | 三 .il sha 维持 `2445e97d...` |
| 5. workaround 状态 | `jhyy_workarounds "W-003"` 等 | 状态文本与 workarounds.md index 一致 |
| 6. diff size | `git diff --stat` | ≤ 200 行 (per `feedback_no_build_artifacts_in_git`) |
| 7. 无 build artifacts | `git status --short` (filter `*.il` `*.exe` `*.s` `*.o`) | 无 |
| 8. 14 smoke test | `pytest mcp-jhyy/tests/` | 14 pass (不破 MCP regress) |
| 9. memory 索引 | `MEMORY.md` + project_/feedback_ 记录 | OK |
| 10. commit log | `git log --oneline -1` | "v1.1 wip commit X.YY: <sprint topic> ..." |

---

## 关联文档

| 文档 | 关系 |
|------|------|
| [`v1.0-post-50-53-plan.md § Phase 2`](../v1/v1.0-post-50-53-plan.md) | 策略描述 — 本 plan 是其 sprint-by-sprint 编排实现 |
| [`workarounds.md`](../../internal/workarounds.md) | W-XXX 详细记录 + 真修决策 history |
| [`memory/feedback_v0_codegen_bug_workarounds.md`](../../../../../../Users/liuzhen/.claude/projects/C--Users-liuzhen-Desktop-coding-JiHuiYiYou/memory/feedback_v0_codegen_bug_workarounds.md) | Bug 1-19 详细 + 19 个独立 bug 的根因 |
| [`memory/feedback_codegen_workaround_linkage.md`](../../../../../../Users/liuzhen/.claude/projects/C--Users-liuzhen-Desktop-coding-JiHuiYiYou/memory/feedback_codegen_workaround_linkage.md) | W-008↔W-009↔W-007↔W-005 4 workaround 联动 |
| [`memory/feedback_v0_phase2_style_cleanup.md`](../../../../../../Users/liuzhen/.claude/projects/C--Users-liuzhen-Desktop-coding-JiHuiYiYou/memory/feedback_v0_phase2_style_cleanup.md) | v0.9 wip commit 2.83 已 ship (跟本 sprint 正交,不重叠) |
| [`memory/feedback_fix_evaluation_rule.md`](../../../../../../Users/liuzhen/.claude/projects/C--Users-liuzhen-Desktop-coding-JiHuiYiYou/memory/feedback_fix_evaluation_rule.md) | 5/5 PASS 强制 (sprint 验证守门) |
| [`memory/feedback_no_build_artifacts_in_git.md`](../../../../../../Users/liuzhen/.claude/projects/C--Users-liuzhen-Desktop-coding-JiHuiYiYou/memory/feedback_no_build_artifacts_in_git.md) | commit 前 diff size ≤ 200 行守门 |

---

## 决策点 (跟 user 对齐)

| 决策 | 选项 | 推荐 |
|------|------|------|
| **Sprint 顺序** | (a) W-XXX → Bug 1-4 顺序 / (b) 风险最低先做 | (a) — 维持 workarounds.md 编号顺序 |
| **W-003 mutable pattern retry** | (a) 全 retry depth≥2 / (b) 仅 depth=2 / (c) 保持 v3 partial | (b) — 风险最低,W-005 真修后安全但仍验证 |
| **W-006 status** | (a) doc-only RESOLVED escaped / (b) 真修 stack-slot allocator (留 v2.x) / (c) 真修 (transitive — Sprint 4.21-4.25 已覆盖) | **(c)** — 重新审计发现 W-006 跟 W-005 #2 同 family, Sprint 4.21-4.25 chain 已修, minimal repro 验证不触发 |
| **W-004 status** | (a) ACTIVE 待验证 / (b) batch rename (commit 2.15) / (c) 真修 (transitive — W-001 byte-by-byte FNV-1a 间接覆盖) | **(c)** — 2026-08-12 验证 minimal repro + 4 boundary variations 全 PASS codegen on jhyy_v1 (sha `ba94df93...`); 真因 = W-001 family (`hash_string` `*i32` deref overread → 短 ident 撞 symtab), 跟 W-006 不同 family |
| **Bug 1-4 revert order** | (a) 跟 codegen.c fix 同步 / (b) 等 codegen.c 全 ship 后批量 revert | (a) — 避免 long-lived 双向 drift |
| **v1.1.0 tag 触发** | (a) v1.1.7 ship 后 / (b) v1.1.3 (W-XXX 全 done) 后 | (a) — 全部真修完才完整 v1.1.0 语义 |
| **跟 v1.x sprint 5+ 关系** | (a) v1.1.0 全部完才开 v1.x 后续 / (b) v1.1.x 部分 ship 后穿插 | (a) — v1.x syntax sugar 不依赖 workaround 消除 (sugar 是新语法),但 v1.x REOLVE (Phase 3.1) 依赖 |

---

## Out of scope (本 plan 显式排除)

| 主题 | 推到 |
|------|------|
| v2.x QBE 完整重写 (amd64_sysv / freestanding) | v2.x sprint (parallel axis,跟 v1.0 TAGGED 后可启动) |
| v3.x 语言特性 (inline asm / naked / volatile / no_std / &mut / Cap<T>) | v3.x sprint (parallel axis) |
| v1.x syntax sugar (`null` / `else if` / `sizeof` / `for x in slice` / `#[inline]` / `defer`) | v1.x sprint 5+ (per `v1.0-post-50-53-plan.md` § Phase 4) |
| Pattern binding codegen (`Some(v) => v`) | v1.x sprint 5+ |
| OR pattern 一致性检查 | v1.x sprint 5+ |
| 嵌套 const array / const pointer / const slice / const enum array | 待定 (自举需要时再开) |
| W-001/W-002/W-005/W-008/W-009/W-010/W-011/W-012 已 RESOLVED — 不重做 | (history 已在 workarounds.md 归档) |
| Bug 5-19 (sprint 4 实证 fix 的 bug) | ✅ 已 ship (per `feedback_v0_codegen_bug_workarounds.md` § Bug 5-19) |

---

## 累计验证清单 (v1.1.0 全部 ship 后)

```
[OK] workarounds.md index 4 active → 2 active (W-003/W-007 仍 ACTIVE; W-004/W-006 已移入 RESOLVED per Sprint v1.1.1 2026-08-11+12)
[OK] src0/*.jhyy grep "let _ = " 命中 0 (W-003 全消除)
[OK] src0/*.jhyy grep "let [a-z_]\{1,2\}[ :] " 0 (W-004 全消除,如 rename 过)
[OK] src0/*.jhyy grep "return [a-z_]\{1,2\} [+\-]" 0 (W-006 dormant escaped)
[OK] src0/*.jhyy grep "*i_ptr\|*i64" 在 hash_string / FNV 等 0 (W-007 真修)
[OK] src0/codegen.jhyy grep "arena_alloc.*as \*i32.*\*p = " 0 (Bug 4 revert)
[OK] src0/codegen.jhyy grep "\*(ptr as \*i64) as \*u8" 0 (Bug 3 revert)
[OK] src0/codegen.jhyy grep "if A { if B { } else { } } else if C" 0 (Bug 2 revert)
[OK] src0/codegen.jhyy grep "IRBuf.sb: \*StringBuilder" 0 (Bug 1 revert)

[OK] regress.py 50/53 PASS
[OK] regress_v1.py 50/53 PASS
[OK] Stage 1 byte-equal 7/7 PASS
[OK] Stage 2 N=3 byte-equal 维持 (jhyy_v2/v3/v4 .il sha 不变)
[OK] 14 mcp-jhyy smoke test PASS
[OK] 净 diff 行数每 sprint ≤ 200 行
[OK] 无 build artifacts (.il/.exe/.s/.o)

[DONE] v1.1.0 tag ship
```

---

**v1.1.0 ship 时间**: 由 user 触发 (per memory `feedback_no_date_estimates.md` — 不写日期估时,用 sprint 序列 + 相对顺序).
**当前起点**: Sprint v1.1.1 (W-006 dormant + W-004 verification),门槛 = 无外部依赖,可立即启动.