# v1.4.0 任务清单 + 概要设计: src0 production flip + 工具链补完

> **承接**: v1.3.0 shipped (7 语法糖全 ship) + Stage 2 N=3 byte-equal 维持.
> **当前 baseline**: regress 50/50 + Stage 1 byte-equal 7/7 + Stage 2 N=3 byte-equal sha `2445e97d...` + jhyy_v1.exe.exe sha `ba94df93...`.
> **目标**: src0 (jhyy-side) 升为生产路径, C 端降级为 stage-0 bootstrap. 为 v2.x 全程 src0 + v1.5 installer 装 src0 产物铺路.
> **本次补排原因**: 之前 v1.x 计划没考虑这个 flip 步, 直接进 v2.x 会让"v2.x 在哪做"持续模糊 (CLAUDE.md 没显式切"src0 = 生产"这步).

---

## Context (2026-08-12 audit)

### 现状盘点

| 维度 | 当前 | 目标 |
|------|------|------|
| **生产路径** | `jhyy.exe` (C 端, 314KB) | `jhyy.exe` 物理 = jhyy-side 编译产物 (~412KB) |
| **bootstrap** | 不存在 (C 端永久生产) | `jhyy_stage0.exe` (C 端, 仅 stage 0 用一次编 src0) |
| **regress 优先级** | regress.py 为主, regress_v1.py 为辅 | regress_v1.py 升为主, regress.py 降为 stage 0 验证 |
| **调试** | gdb 只能调 jhyy.exe 输出的 .exe | jhyy-side 输出的 .exe 也带 DWARF + gdb 可调 |
| **CLAUDE.md 措辞** | "C 端生产路径, v1.x → v2.x 渐进迁移" | "v1.x = jhyy-side production, C 端 stage-0 bootstrap" |

### 阻塞问题:硬编码路径

`compiler/src/main.c` 和 `compiler/src0/main.jhyy` 把 `QBE` / `gcc` / `runtime.c` / `jhyy_helpers.c` 路径写死成用户目录:

```
compiler/src/main.c:433    "C:/Users/liuzhen/Desktop/coding/JiHuiYiYou/qbe/qbe.exe ..."
compiler/src/main.c:445    "C:/msys64/ucrt64/bin/gcc.exe ... runtime.c ... jhyy_helpers.c ..."
compiler/src0/main.jhyy:64 fn QBE_PATH() -> ... C:/Users/liuzhen/Desktop/coding/JiHuiYiYou/qbe/qbe.exe
compiler/src0/main.jhyy:65 fn GCC_PATH() -> ... C:/msys64/ucrt64/bin/gcc.exe
compiler/src0/main.jhyy:66 fn RUNTIME_C() -> ... C:/Users/.../compiler/runtime/runtime.c
compiler/src0/main.jhyy:67 fn HELPERS_C() -> ... C:/Users/.../compiler/src0/jhyy_helpers.c
```

这条**两边都有**, 而且**字面值相同** —— main.jhyy 是 v0.8 commit 2a 翻译 main.c 时的"per v1.0.0 L1 § 134 决策"硬编码.

后果: 任何 clone 到非 `C:/Users/liuzhen/Desktop/coding/JiHuiYiYou` 路径的用户, jhyy.exe 直接 gcc link failed (跟 PowerShell gcc link failed 不是一回事 —— 这是绝对路径找不到 runtime.c).

### 联动关系

- **main.c ↔ main.jhyy 路径**: 必须同步改 + byte-equal 守门
- **C 端 → stage 0**: gcc build chain 只在 CI/bootstrap 时跑, 用户本地不用
- **regress.py ↔ regress_v1.py**: 双跑, 任一退化 → 回滚该 sprint
- **DWARF ↔ gdb pretty printer**: codegen.jhyy 加 debug emit + 单独 .gdbinit 文件

### 不在 v1.4.0 scope

- v2.x QBE 自写 + 多目标 (parallel axis, v1.4 ship 后才启动)
- v3.x 语言扩展 (parallel axis)
- v1.5 installer (v1.4 ship 后才启动)
- 跨平台 (Linux / macOS) — 仍是 Windows-only

---

## Sprint 排期 (4-5 sprint)

> **核心原则**: 改 main.c / main.jhyy 路径 → 加 DWARF → 加 gdb pretty printer → 物理替换 jhyy.exe → CI 双跑. 每个 sprint 走"改 → byte-equal 守门 → regress 双跑守门"闭环.

### Sprint v1.4.1 — argv[0] 推项目根, 路径硬编码消除 (1 sprint)

> **门槛**: src0/ 100% 自然 jhyy (v1.2.0 ship) → 改 main.jhyy 路径无 syntax 风险.

| 工作 | 估时 |
|------|------|
| **诊断**: 写最小复现 — clone repo 到 `D:\test\jhyy\`, 跑 `jhyy run hello.jhyy` → "gcc link failed" (跟 PowerShell 不同, 这次是 runtime.c 路径找不到) | 0.1 |
| **修 main.c**: 删 5 处 `C:/Users/liuzhen/...`, 改用 `path_from_argv0(argv[0])` 推出项目根. 项目根 = `dirname(dirname(dirname(dirname(argv[0]))))`. 从项目根: QBE = `qbe/qbe.exe`, runtime.c = `compiler/runtime/runtime.c`, helpers.c = `compiler/src0/jhyy_helpers.c`. gcc 改成 PATH 搜索 (不再硬编码 `C:/msys64/ucrt64/bin/gcc.exe`) | 0.4 |
| **mirror 到 main.jhyy**: `QBE_PATH()` / `GCC_PATH()` / `RUNTIME_C()` / `HELPERS_C()` 同步改 — 字节相同路径生成逻辑 | 0.3 |
| **验证**: stage1 byte-equal 7/7 持平 (路径改不影响 IL); regress.py 50/50; **新增**: `D:\test\jhyy\` 复现目录跑 hello.jhyy → EXIT=42 PASS | 0.2 |

**完成定义**:
- `compiler/src/main.c` 0 处 `C:/Users/liuzhen/...` (grep 守门)
- `compiler/src0/main.jhyy` 0 处 `C:/Users/liuzhen/...` (grep 守门)
- clone 到非用户目录路径也能跑 hello.jhyy
- stage1 byte-equal 7/7 持平 (路径生成不影响 IL)

### Sprint v1.4.2 — DWARF 调试信息 (0.5 sprint)

> **门槛**: v1.4.1 路径修完 → 接下来 codegen emit 加 DWARF 不动 codegen 主体.

| 工作 | 估时 |
|------|------|
| **audit**: 当前 jhyy_v1.exe 编 fib30.jhyy 看 codegen.jhyy 是否 emit `.loc` / `.file` 指令. 预期: 0 处 (v1.x codegen 没加 DWARF) | 0.1 |
| **codegen.jhyy 改**: 在每条指令 emit 后加 `.loc <file> <line>` + 函数入口加 `.file "<src>"` + 函数退出加 `.size`. 借鉴 QBE 自带 `-g` flag (但 jhyy 自写 codegen, 需手动 emit) | 0.3 |
| **mirror 到 codegen.c**: C 端 codegen 同步加 DWARF (保持 byte-equal + debug 能力一致) | 0.1 |
| **验证**: stage1 byte-equal 7/7 持平 (DWARF 不在 .il 里, 在 .s 里, 所以 byte-equal 是 IL 字节相同不是 .s); `gdb fib30.exe` → `b main_jhyy` + `r` + `n` 能逐行调试 | 0.1 |

**完成定义**:
- jhyy-side 编译的 .exe 在 gdb 里能设断点 + 单步
- codegen.jhyy emit `.loc` / `.file` 指令 (grep 守门)
- C 端 codegen.c 也 emit (byte-equal mirror)

### Sprint v1.4.3 — gdb pretty printer (0.5 sprint)

> **门槛**: DWARF 行号信息已 emit → pretty printer 让 jhyy 类型可读.

| 工作 | 估时 |
|------|------|
| **写 `compiler/src0/gdb_pretty.py`** (Python gdb pretty printer): 处理 jhyy struct (扁平字段, 顺序 emit) + enum (tagged union 显示变体名) + slice (`[*]T` = ptr + len) | 0.3 |
| **加 `.gdbinit`**: 仓库根放 `.gdbinit` 自动 source `compiler/src0/gdb_pretty.py` | 0.05 |
| **写测试 `gdb_pretty_test.jhyy`**: 含 struct Point / enum Color / slice, gdb `p point` 输出可读 | 0.1 |
| **验证**: `gdb -batch -ex "source .gdbinit" -ex "p point" ./gdb_pretty_test.exe` 输出含字段名 | 0.05 |

**完成定义**:
- `.gdbinit` 仓库根 (gitignored 个人 .gdbinit 不动, 仓库这份 auto-source 即可)
- `gdb_pretty.py` 处理 3 类 jhyy 复合类型
- `gdb_pretty_test.jhyy` PASS

### Sprint v1.4.4 — 物理替换 jhyy.exe (0.5 sprint)

> **门槛**: v1.4.1-3 全 ship, jhyy-side 工具链 ready.

| 工作 | 估时 |
|------|------|
| **改 mcp-jhyy/jhyy_runner.py auto_rebuild**: 当前逻辑 = `gcc → jhyy.exe; jhyy.exe compile src0/main.jhyy → jhyy_v1.exe; cp jhyy_v1.exe → jhyy_v1.exe.exe`. 改成 = `gcc -o jhyy_stage0.exe; jhyy_stage0.exe compile src0/main.jhyy → jhyy.exe; cp jhyy.exe → jhyy.exe.exe` (per `feedback_regress_baseline_binary_hash`, baseline 必须 .exe.exe) | 0.2 |
| **改 bootstrap.cmd / bootstrap.sh**: 同样调用链, 输出 `jhyy.exe` = jhyy-side 产物, `jhyy_stage0.exe` = C 端产物 | 0.1 |
| **改 Makefile**: 加 `jhyy_stage0` target, `all` target 走 stage 0 → jhyy.exe 链 | 0.1 |
| **验证**: 跑一次完整 bootstrap, 看 `compiler/build/bin/jhyy.exe` = jhyy-side 产物 (sha 比 C 端大 ~100KB 因 DWARF); 跑 hello.jhyy 仍 EXIT=42 | 0.1 |

**完成定义**:
- `compiler/build/bin/jhyy.exe` 物理 = jhyy-side 编译产物
- `compiler/build/bin/jhyy_stage0.exe` = C 端 (新文件, .gitignore 不需要因已 tracked)
- `compiler/build/bin/jhyy.exe.exe` = jhyy-side baseline (per baseline binary hash rule)
- `compiler/build/bin/jhyy_v1.exe.exe` 退役 (改为 alias 到 jhyy.exe.exe, 或保留作历史)

### Sprint v1.4.5 — CI 双跑 + CLAUDE.md 同步 (0.5 sprint)

> **门槛**: v1.4.4 物理替换完成 → CI/regress/CLAUDE.md 收尾.

| 工作 | 估时 |
|------|------|
| **改 regress.py**: 默认 binary 改 `jhyy.exe.exe` (was `jhyy.exe`); 加 `--stage0` flag 跑 jhyy_stage0.exe (验证 C 端仍能产 byte-equal IL) | 0.1 |
| **加 regress_stage0.py** (新文件, .gitignore 不需要): 跑 jhyy_stage0.exe, 验证 stage 0 bootstrap 仍工作, byte-equal to src0 输出的 IL | 0.2 |
| **加 GH Actions** (`.github/workflows/ci.yml`): push / PR 触发 → bootstrap → regress_v1.py + regress.py + regress_stage0.py 三跑 + stage1 byte-equal + Stage 2 N=3 byte-equal | 0.1 |
| **CLAUDE.md 同步** (本 plan 自负责): "v1.x = jhyy-side production, C 端 stage-0 bootstrap" + v2.x 不再"渐进迁移"措辞 | 0.1 |

**完成定义**:
- regress.py 默认 = jhyy-side
- regress_stage0.py 验证 stage 0 不退化
- GH Actions 三跑守门
- CLAUDE.md 措辞对齐

---

## 累计估时

| Sprint | 主题 | 估时 |
|--------|------|------|
| v1.4.1 | argv[0] 推项目根, 路径硬编码消除 | 1 |
| v1.4.2 | DWARF 调试信息 | 0.5 |
| v1.4.3 | gdb pretty printer | 0.5 |
| v1.4.4 | 物理替换 jhyy.exe | 0.5 |
| v1.4.5 | CI 双跑 + CLAUDE.md 同步 | 0.5 |
| **总计** | | **3 sprint** |

> **关键里程碑**:
> - Sprint v1.4.1 (路径硬编码消除) — 阻塞 v1.5 installer 的"装到任意路径"前提
> - Sprint v1.4.4 (物理替换) — src0 = 生产的物理落地
> - Sprint v1.4.5 (CI + CLAUDE.md) — v2.x 全程 src0 的措辞就位

---

## 完成定义 (v1.4.0 全 sprint ship 后)

| 维度 | 期望 |
|------|------|
| `compiler/build/bin/jhyy.exe` | 物理 = jhyy-side 编译产物 (~412KB, 含 DWARF) |
| `compiler/build/bin/jhyy_stage0.exe` | C 端 (仅 stage 0 bootstrap) |
| `compiler/build/bin/jhyy.exe.exe` | jhyy-side baseline (per baseline binary hash rule) |
| `compiler/src/main.c` 硬编码路径 | 0 处 (grep `C:/Users/liuzhen` 守门) |
| `compiler/src0/main.jhyy` 硬编码路径 | 0 处 (同上) |
| `compiler/src0/codegen.jhyy` DWARF emit | `.loc` / `.file` 指令 (grep 守门) |
| `compiler/src/codegen.c` DWARF emit | 同步 (byte-equal mirror) |
| `compiler/src0/gdb_pretty.py` | 处理 struct / enum / slice |
| `.gdbinit` | 仓库根, auto-source pretty printer |
| `regress.py` | 默认 = jhyy-side |
| `regress_v1.py` | 已存在, 仍跑 jhyy-side baseline |
| `regress_stage0.py` | 新, 验证 C 端仍 byte-equal |
| `.github/workflows/ci.yml` | GH Actions 三跑守门 |
| `CLAUDE.md` | "v1.x = jhyy-side production, C 端 stage-0 bootstrap" |
| regress.py | 50/50 PASS (持平 baseline) |
| regress_v1.py | 50/50 PASS (持平 baseline) |
| regress_stage0.py | 50/50 PASS (验证 stage 0 不退化) |
| Stage 1 byte-equal | 7/7 PASS (持平 baseline) |
| Stage 2 N=3 byte-equal | 维持 (sha 不变) |
| clone 到任意路径 (e.g. `D:\test\jhyy\`) | hello.jhyy EXIT=42 PASS |
| 净 diff 行数 | 每个 sprint ≤ 200 行 (per `feedback_no_build_artifacts_in_git`) |
| changelog | `docs/logs/v1/changelog-v1.4.0.md` 5 个 entry |
| tag | **v1.4.0** (post-v1.3.0) |

---

## Critical files

| 文件 | 修改类型 |
|------|---------|
| `compiler/src/main.c` | 5 处硬编码路径 → argv[0] 推导 + gcc PATH 搜索; 加 DWARF emit (mirror codegen.c) |
| `compiler/src0/main.jhyy` | 4 处 QBE_PATH / GCC_PATH / RUNTIME_C / HELPERS_C → 同步改 |
| `compiler/src/codegen.c` | 加 DWARF emit (`.loc` / `.file`) |
| `compiler/src0/codegen.jhyy` | 加 DWARF emit (mirror codegen.c) |
| `compiler/src0/gdb_pretty.py` | 新建, Python gdb pretty printer |
| `.gdbinit` | 新建, auto-source pretty printer |
| `compiler/tests/examples/gdb_pretty_test.jhyy` | 新建, struct/enum/slice 测试 |
| `mcp-jhyy/jhyy_runner.py` | auto_rebuild 改成 stage 0 → jhyy.exe 链 |
| `bootstrap.cmd` / `bootstrap.sh` | 同步改 auto_rebuild 调用链 |
| `Makefile` | 加 `jhyy_stage0` target + `all` 走 stage 0 |
| `compiler/build/bin/regress.py` | 默认 binary 改 jhyy.exe.exe + 加 `--stage0` flag |
| `compiler/build/bin/regress_stage0.py` | 新建, 验证 stage 0 byte-equal |
| `.github/workflows/ci.yml` | 新建, GH Actions 三跑守门 |
| `CLAUDE.md` | "v1.x = jhyy-side production, C 端 stage-0 bootstrap" 措辞 |
| `docs/internal/build.md` | 加"jhyy.exe = jhyy-side, jhyy_stage0.exe = C 端 stage 0" 说明 |
| `docs/internal/architecture.md` | 同步生产路径图 (src0 → .il → qbe.exe → ...) |
| `docs/logs/v1/changelog-v1.4.0.md` | 新建, 5 sprint entry |

---

## Risks / 守门

| 风险 | Mitigation |
|------|------------|
| argv[0] 在 `system()` 调用时丢失 (cmd.exe 不传 argv[0]) | main.c 启动时把项目根存全局, system() 用全局; 或 cmd.exe 调用用 `%1` 传 |
| jhyy-side 编 src0/main.jhyy 性能慢 2-5x (gdb pretty printer test 跑通但开发者本地 build 慢) | DWARF 默认关 (`--debug` opt-in flag), 生产 build 不带 debug |
| mcp-jhyy 11 tools 路径不变但内容变 → 可能某些 tool 假设 C-side 行为 | 跑 `pytest mcp-jhyy/tests/` (14 smoke test) 守门; 任一 FAIL → 修该 tool |
| regress_stage0.py 加进 CI 增加 build time (~3-5 min) | 可 cache stage 0 binary (只在 src/ 改时重 build) |
| `.gdbinit` 仓库根可能跟用户个人 `.gdbinit` 冲突 | 仓库 `.gdbinit` 只做 auto-source, 不覆盖用户配置 (不用 `set print ...` 等) |
| CLAUDE.md 措辞改了之后其他 plan 文件措辞滞后 (e.g. v1.3.0 写的"v2.x 渐进迁移") | 本 plan ship 后下一个 plan author 各自查措辞, 不在本 plan scope |
| jhyy.exe 物理替换后 v1.0.0 byte-equal 历史 baseline 仍记录 jhyy_v1.exe.exe sha | 保留 v1.0.0 baseline (jhyy_v1.exe.exe) 作历史, 新 baseline 改 jhyy.exe.exe (在 v1.4.5 阶段定) |

---

## Verification (end-to-end per sprint)

| 检查 | 命令 / 工具 | 期望 |
|------|------------|------|
| 1. C-side regress | `python compiler/build/bin/regress.py` | 50/50 PASS |
| 2. jhyy_v1 regress (jhyy-side baseline) | `python compiler/build/bin/regress_v1.py` | 50/50 PASS |
| 3. Stage 1 byte-equal | `jhyy_selfhost_check` (MCP tool) | all_byte_equal=True, il_sha256 不变 |
| 4. Stage 2 N=3 | `jhyy_v2 build src0/main.jhyy` + diff | 三 .il sha 维持 `2445e97d...` |
| 5. Stage 0 验证 (新) | `python compiler/build/bin/regress_stage0.py` | 50/50 PASS, byte-equal to jhyy-side IL |
| 6. 路径无关 | `cd D:\test\jhyy\ && jhyy run hello.jhyy` | EXIT=42 PASS |
| 7. DWARF | `gdb fib30.exe -ex "b main_jhyy" -ex "r"` | 能设断点 + 单步 |
| 8. gdb pretty | `gdb -batch -ex "source .gdbinit" -ex "p point" gdb_pretty_test.exe` | 输出含字段名 |
| 9. diff size | `git diff --stat` | ≤ 200 行 |
| 10. mcp smoke | `pytest mcp-jhyy/tests/` | 14 pass |
| 11. 硬编码路径 grep | `grep -rn "C:/Users/liuzhen" compiler/src/main.c compiler/src0/main.jhyy` | 0 命中 |

---

## 关联文档

| 文档 | 关系 |
|------|------|
| [`v1.3.0任务清单 + 概要设计.md`](v1.3.0任务清单 + 概要设计.md) | 前置 — v1.3 ship 后启动 v1.4 |
| [`v2.x-qbe-rewrite.md`](../roadmap/v2.x-qbe-rewrite.md) | 后续 — v1.4 ship 后 v2.x 全程 src0 推进 |
| [`v0.x-c-compiler-roadmap.md`](../roadmap/v0.x-c-compiler-roadmap.md) | 历史 — C 端从"生产"降级为"stage 0 bootstrap" |
| [`architecture-refactor.md`](../roadmap/architecture-refactor.md) | 整体重构 — 本 plan 是其 § R-7 src0 production 落地 |
| [`docs/internal/build.md`](../../internal/build.md) | 改 build.md 措辞 (本 plan 自负责) |
| [`docs/internal/architecture.md`](../../internal/architecture.md) | 改架构图 (本 plan 自负责) |
| [`CLAUDE.md`](../../../CLAUDE.md) | 改 v1.x 段措辞 (本 plan 自负责) |
| [`memory/feedback_regress_baseline_binary_hash.md`](../../../../../../Users/liuzhen/.claude/projects/C--Users-liuzhen-Desktop-coding-JiHuiYiYou/memory/feedback_regress_baseline_binary_hash.md) | baseline 必须 .exe.exe 命名 (per Sprint 4.1 phantom-baseline 教训) |
| [`memory/feedback_no_build_artifacts_in_git.md`](../../../../../../Users/liuzhen/.claude/projects/C--Users-liuzhen-Desktop-coding-JiHuiYiYou/memory/feedback_no_build_artifacts_in_git.md) | 每 sprint diff size ≤ 200 行守门 |
| [`memory/feedback_fix_evaluation_rule.md`](../../../../../../Users/liuzhen/.claude/projects/C--Users-liuzhen-Desktop-coding-JiHuiYiYou/memory/feedback_fix_evaluation_rule.md) | 5/5 PASS 强制 |

---

## 决策点 (跟 user 对齐)

| 决策 | 选项 | 推荐 |
|------|------|------|
| **argv[0] 推导项目根** | (a) dirname×4 推导 / (b) 环境变量 JHYY_ROOT / (c) 配置文件 `.jhrc` | **(a)** — 无外部配置, 跟着二进制位置走, installer 装到任意路径都行 |
| **gcc PATH 搜索** | (a) 纯 PATH 搜索 / (b) 检测 MSYS2 标准路径 fallback / (c) 环境变量 JHYY_CC | **(a)** — 干净; MSYS2 用户 PATH 自己加, 跟 v1.0.0 § 134 决策一致 (用户配环境) |
| **DWARF 默认开还是 opt-in** | (a) 默认开 / (b) `--debug` opt-in / (c) `--no-debug` opt-out | **(b)** — 生产 build 不带 debug, 不污染二进制大小 + jhyy 编 jhyy 速度 |
| **jhyy.exe.exe baseline 迁移** | (a) 保留 jhyy_v1.exe.exe 历史 baseline + 加 jhyy.exe.exe 新 baseline / (b) 只用 jhyy.exe.exe 旧 baseline 退役 | **(a)** — 历史可追溯, 新 baseline 明确 |
| **CLAUDE.md 措辞** | (a) "src0 = production, C 端 stage-0 bootstrap" / (b) 维持现状, 加注释 | **(a)** — 跟你判断一致 (CLAUDE.md "渐进迁移"是过度保守) |
| **GH Actions 何时上** | (a) v1.4.5 加 / (b) 单独 sprint v1.4.6 / (c) 不加, manual | **(a)** — v1.4 ship 即带 CI, 后续 sprint 自动守门 |
| **跟 v1.5 installer 关系** | (a) v1.4 全 ship 才开 v1.5 / (b) v1.4.4 (物理替换) 后开 v1.5 / (c) v1.4 + v1.5 并行 | **(a)** — installer 装的就是 src0 产物, v1.4 ship 后 src0 才是"稳定 production" |

---

## Out of scope (本 plan 显式排除)

| 主题 | 推到 |
|------|------|
| v2.x QBE 完整重写 (amd64_sysv / freestanding / 自写 IL → .s) | v2.x sprint (per v2.x-qbe-rewrite.md) |
| v3.x 语言特性 (inline asm / naked / volatile / no_std / &mut / Cap<T>) | v3.x sprint (parallel axis) |
| v1.5 installer (WiX + Burn + MSI) | v1.5 sprint (v1.4 ship 后启动) |
| 跨平台 (Linux / macOS) — 仍是 Windows-only | v2.x 中期 amd64_sysv (per v2.x-qbe-rewrite.md § 3.1) |
| jhyy 编译速度优化 (jhyy-side 编 jhyy-side 慢 2-5x) | v4.x 性能优化期 (未定) |
| C 端编译器退役 (删 compiler/src/*.c) | v2.x 末 — C 端降到 stage 0 后, 仍保留以备 bootstrap |
| gdb pretty printer 扩展 (其他 jhyy 复合类型) | v1.4.3 ship 后 PR 形式扩展 |

---

## 累计验证清单 (v1.4.0 全部 ship 后)

```
[OK] compiler/src/main.c 硬编码路径 grep 0 命中
[OK] compiler/src0/main.jhyy 硬编码路径 grep 0 命中
[OK] argv[0] 推导项目根, clone 到任意路径 (D:\test\jhyy\) 能跑 hello.jhyy
[OK] jhyy-side 输出 .exe 带 DWARF, gdb 设断点 + 单步工作
[OK] gdb pretty printer 处理 struct / enum / slice
[OK] compiler/build/bin/jhyy.exe 物理 = jhyy-side (~412KB)
[OK] compiler/build/bin/jhyy_stage0.exe = C 端 (新文件)
[OK] compiler/build/bin/jhyy.exe.exe = jhyy-side baseline (per baseline binary hash rule)
[OK] regress.py 默认 = jhyy-side
[OK] regress_v1.py 仍跑 jhyy-side baseline
[OK] regress_stage0.py 验证 stage 0 byte-equal (新)
[OK] GH Actions ci.yml 三跑守门
[OK] CLAUDE.md 措辞: "v1.x = jhyy-side production, C 端 stage-0 bootstrap"

[OK] regress.py 50/50 PASS
[OK] regress_v1.py 50/50 PASS
[OK] regress_stage0.py 50/50 PASS
[OK] Stage 1 byte-equal 7/7 PASS
[OK] Stage 2 N=3 byte-equal 维持 (jhyy_v2/v3/v4 .il sha 不变)
[OK] 14 mcp-jhyy smoke test PASS
[OK] 净 diff 行数每 sprint ≤ 200 行
[OK] 无 build artifacts (.il/.exe/.s/.o 在 commit 里)

[DONE] v1.4.0 tag ship
```

---

**v1.4.0 ship 时间**: 由 user 触发 (per memory `feedback_no_date_estimates.md` — 不写日期估时, 用 sprint 序列 + 相对顺序).
**当前起点**: Sprint v1.4.1 (argv[0] 路径消除), 门槛 = v1.3.0 ship + 任意 sprint 启动条件已就绪.
