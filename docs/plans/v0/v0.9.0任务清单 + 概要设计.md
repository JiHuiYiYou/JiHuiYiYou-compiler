# v0.9.0 任务清单 + 概要设计

## Context

v0.8 wip commit 12 收尾（[5820793](../logs/v0/changelog-v0.8.0.md)）：Stage 0 closure 解锁，jhyy_v1 跑 regress 12 OK / 47 总（持平 baseline）。9 个 codegen workaround（W-001~W-009）已用 jhyy 端 workaround 绕开，但**未真修**。

v0.9.0 定位：**清理自举路径剩余卡点**，让 v1.0.0 sprint 5 收尾时能跑 Stage 1 byte-equal 验证。**main.c 翻译 + codegen bug 真修**两件事在 v0.9 集中做（v0.8 计划把它们都塞 v0.8 收尾，但实际跑出 12 commit 才到 Stage 0 closure，main.c 翻译自然推迟）。

按 v1.0.0 L1 计划：v0.x patch 只修 bug，**不引入新特性**。v0.9.0 同样不引入新语法。

---

## v0.9 wip ship 总结 (commit 2.17, 7691457, 2026-08-05)

> **AUDIT + C' 双 audit 全 PASS, A 段 closure 总结**

### 完成 commit 全清单

| commit | 主题 | 状态 |
|--------|------|------|
| commit 1 (ce93f64) | 29-extsw hypothesis 验证（未命中） | ✅ SHIPPED |
| commit 2.5 | Stage 1 byte-equal 7 测试集 baseline + B-let2 codegen 差异修 | ✅ SHIPPED |
| commit 2.6 | B-φ1 真修 — phi emit 路径对齐 v0 | ✅ SHIPPED |
| commit 2.7 | B-data 真修 — .data 段 emit 顺序对齐 v0 | ✅ SHIPPED |
| commit 2.8 | B-struct 真修 — cg_copy_struct offset==0 加 copy | ✅ SHIPPED |
| commit 2.9 | B-match 真修 — NODE_MATCH codegen + cg_match_pattern helper | ✅ SHIPPED |
| commit 2.10 | W-005 根因重诊断 (CGContext 不匹配 → AUDIT 立项) | ✅ SHIPPED |
| commit 2.11 | W-005 真修 phase 2 — CGContext C/jhyy 布局对齐 | ✅ SHIPPED |
| commit 2.12a | B-match-sema 真修 — match_exhaustive byte-equal 5/7 → 6/7 | ✅ SHIPPED |
| commit 2.12b | B-match-codegen 真修 — NODE_ENUM_VARIANT 翻译 | ✅ SHIPPED |
| commit 2.13 | W-005 加固 revert 16 处 *pos_ptr_vN → let mut | ✅ SHIPPED |
| commit 2.14 | W-004 BLOCKED verification + W-002 archive + W-006 dormant + cross-ref | ✅ SHIPPED |
| commit 2.15 | Task #60 真修 — parse_if body inline parse_while 嵌套 TOKEN_WHILE | ✅ SHIPPED |
| **commit 2.16** | **AUDIT 真修 — VariantDesc 加 payload 字段 + VARIANT_DESC_SIZE 16→24 (heap overflow 修)** | ✅ SHIPPED |
| **commit 2.17** | **C' codegen 确定性 audit — 5 维度全 PASS, 0 真修, 3 stage1 实证 byte-equal** | ✅ SHIPPED |

### Ship 时回归基线

| 指标 | 状态 | 备注 |
|------|------|------|
| regress (v0) | **50/53 PASS** | 持平 baseline |
| regress (v1) | 12/47 OK | v0.8 wip commit 12 baseline 持平 |
| stage1 byte-equal | **6/7 PASS** | 持平 baseline; const_array = pre-existing Task #52 |
| AUDIT 真修 | **1 真修 (VariantDesc)** | 12 struct 全列出 (5 主审 + 7 附带) |
| C' 真修 | **0 真修** | 5 维度 by-construction deterministic |
| v0 build | clean | -Wall -Wextra 0 warnings |

### A 段 closure 总结 (per user plan "v0.9 wip 收尾 = docs cleanup + A 段 closure 总结")

**A 段定义**: AUDIT 段 = codegen 内部状态 struct layout 正确性 (commit 2.16) + C' 段 = codegen 外部输出 .il 字节确定性 (commit 2.17) = 两个 audit 维度互补。

**Stage 1 closure 三基础齐** (commit 2.17 ship 后):
1. **跑得对** (AUDIT, commit 2.16): 修 VariantDesc heap overflow + 验证 12 struct layout 一致
2. **跑得稳** (C', commit 2.17): 5 维度 by-construction deterministic + 7 类风险源 0 命中 + 3 stage1 实证
3. **跑得对齐** (6/7 stage1 byte-equal baseline): v0↔v1 6 测试 byte-equal (match_exhaustive + arith + struct_val_pass + fib_renamed + control_flow + hello)

### 不在 v0.9 wip scope (per user plan, 推到 v1.0 sprint 3)

| 项 | 推到 | 理由 |
|----|------|------|
| Task #52 (parser NODE_CONST_DECL → 7/7) | v1.0 sprint 3 task #1 | 独立可开, 是 sprint 3 启动条件 |
| B (resolve_imports 翻译 ~229 行) | v1.0 sprint 3 task #2 (∥ #1) | 跟 Task #52 并行 |
| Task #61 (jhyy_v1 编 src0/main.jhyy 真闭环) | v1.0 sprint 3 task #3 | 依赖 Task #52 |
| W-004 verification (codegen 路径 stack overflow) | v1.0 sprint 3 task #4 | 依赖 B' (codegen 跑通) |
| D (N=3 byte-equal) | v1.0 sprint 3 task #5 | 依赖 B' |

→ 见 [docs/plans/v1/v1.0-sprint-3-*.md](../v1/v1.0-sprint-3-*.md) (新 doc, 5 task 粗粒度)

### 不在 v0.9 wip scope (延后到 v1.0 sprint 5 收尾或更后)

| workaround / bug | 状态 | 推后 |
|------------------|------|------|
| W-006 (1-char local var stack spill) | ACTIVE dormant (per commit 2.14 scan) | codegen stack-slot allocator 真修 (v2.x?) |
| W-007 (cg_convert_arg struct field + global var path) | ACTIVE partial | v1.0 sprint 3+ 二次 audit |
| v0 codegen bug 1/2/3/4 (LEA / phi / loadub / &local) | workaround 在 jhyy 源码 | v1.0 sprint 5 收尾 |
| Pattern binding codegen (`Some(v) => v`) | 缺 | v1.0 sprint 3 末 patch |
| OR pattern 一致性检查 | 缺 | 待定 |
| 嵌套 const array / const pointer / const slice / const enum array | 缺 (sema 拒绝) | 自举需要时再开 |

---

## 目标

让 `jhyy_0 (C 编译) 编 main.jhyy → jhyy_1` 跑通，且 `jhyy_0` 和 `jhyy_1` 对相同 .jhyy 测试集产出 **byte-equal .il**。

### 当前状态（v0.8 wip 末 → v0.9 启动前）

| 项 | 状态 |
|----|------|
| Stage 0 closure（jhyy_v1 编 src0/arena.jhyy 通过 QBE typecheck） | ✅ 已解锁 |
| jhyy_v1 跑 regress | 12 OK / 47 总（8 CERR + 8 AV + 25 STK）|
| 9 个 codegen workaround（W-001~W-009） | 🟡 已绕开，未真修 |
| main.c 翻译 | 🔴 未做 |
| 29-extsw hypothesis 验证 | ✅ SHIPPED (commit 1, ce93f64, 未命中,extsw 是 .il 体积噪音) |
| Stage 1 byte-equal (hello.jhyy) | ✅ SHIPPED (commit 13, 5ce5782, jhyy_v1 编 hello.jhyy .il byte-equal jhyy_0) |

---

## v0.8 wip 末过渡 commit（在 v0.9 启动前追加,1-2 commit）

> **触发事实**: v0.8 wip commit 12 收尾时 Stage 0 closure 只验证 typecheck 通过,**没做端到端**;W-007 是 jhyy_v1 端孤立 fix(跟 W-001 根因解耦),提前做掉可以减少 src0/ workaround 注释 + 让 v0.9 commit 2 跳过 W-007。

### commit 13: arena_test 端到端验证

**目标**：让 `jhyy_v1` 编 `arena_test.jhyy` 编出 exe 跑出 step 1/2/3,**不只是 typecheck 通过**

**做法**：
1. 跑 `jhyy_v1 compile compiler/tests/examples/arena_test/arena.jhyy -o arena_test_v1.exe`
2. 跑 `arena_test_v1.exe`,验证输出 step 1/2/3 跟 `jhyy_0` 编的 `arena_test.exe` byte-equal 或行为一致
3. **撞 "unknown type"** 是 parser/sema 端 bug(跟 codegen workaround 解耦)→ 修 parser/sema 后重跑
4. **真闭环验证**：jhyy_v1 编 jhyy_v1 编 arena_test.jhyy(多层链)行为正确

**风险**：
- parser/sema 端 bug 路径未知 → 可能需要先 bisect
- end-to-end 多层链时间不可控(单层几分钟)

**完成定义**：
- `arena_test_v1.exe` 跑通 + step 1/2/3 输出对得上 jhyy_0 版
- 撞的 parser/sema bug 全部修完并 RESOLVED
- regress 持平 12 OK

### commit 14: W-007 真修（jhyy_v1 端）

**目标**：在 `jhyy_v1` 的 `codegen.jhyy` 端 `cg_convert_arg` 加 extsw,跟 v0 端 commit 1 对齐

**事实依据**：
- v0.8 commit 1 bug 11 修了 `codegen.c:222`(v0 端)加 extsw 分支
- jhyy_v1 端 `codegen.jhyy` 的 `cg_convert_arg` 路径**没同步修**,src0/ 里所有 `fn() -> i64 { return literal as i64; }` 还得带 workaround 注释
- W-007 状态 = ACTIVE(jhyy_v1 端),**跟 W-001 根因无关**

**做法**：
1. 改 `compiler/src0/codegen.jhyy` 的 `cg_convert_arg`,加 `if src_qt == 'w' && dst_qt == 'l' { conv = "extsw"; }` 分支(跟 v0 端对齐)
2. 删 src0/ 里 `fn() -> i64 { return literal as i64; }` 的 workaround 注释 + 恢复原代码结构
3. 跑 regress.py 持平 12 OK

**完成定义**：
- `workarounds.md` § W-007 ACTIVE → RESOLVED
- src0/ workaround 注释全删
- regress 持平 12 OK

---

## 范围（4 commit）

### commit 1: 29-extsw hypothesis 验证 — ✅ SHIPPED (ce93f64, 2026-08-05)

**50/50 赌结果：未命中**。`extsw` 计数 = .il 体积噪音,非功能 bug。

**验证结论**(per `docs/internal/codegen-pitfalls.md` § 1.1):
- arena.jhyy 翻译稿 ptr 算术上下文**全部用 `as i64`**(L50 `ptr_add_v1`, L97 `cur_int_v1`, L100 `end_int_v1`, L156/L162 `b_i64`),**无 i32 误用**
- 实际 29 extsw 来源: i32 字面量(0/1/8/16/1048576)在 i64 上下文经 `NODE_CAST`(codegen.c L776)或 `cg_convert_arg`(L222)合法 emit
- QBE 设计事实: `w`(32-bit) ↔ `l`(64-bit) 必须显式 extsw,不可隐式 widen — `cg_convert_arg` 的 extsw 分支是 QBE ABI 必需,非新坑
- 真 bug 未发现;**不影响 W-001 根因真修路径**(W-001 根因 = hash 算法,跟 extsw 现象解耦)

**事实依据**: memory `project_bootstrap_closure_state.md` 第 2 段 50/50 赌假设

**完成定义**: ✅ 全达成
- ✅ grep 报告(全 ptr 算术 + 字面量 cast 分类)
- ✅ 新建 `docs/internal/codegen-pitfalls.md`(§ 1.1 extsw 伪坑 + § 2 待验证占位 + § 3 历史 bug 索引)
- ✅ regress 47/50 passed(持平 baseline)
- ✅ 未改 codegen.c(不在 commit 1 范畴)

### commit 2: codegen workaround 真修（W 真修清单,**跳过已修**）

**目标**：把 v0.8 期间绕开的 workaround 全部真修 + 删 jhyy 端 workaround 注释

**事实依据**(per v0.8 wip commit 11/12 实测):
- **W-008**(v0.8 commit 11, 2d4c319)**已 RESOLVED** — codegen.jhyy 三层 deref 漏修已 ship,**v0 端 (codegen.c) 不需要再修**(v0 codegen 走另一条路径,本身 emit 正确 IL)
- **W-009**(v0.8 commit 12, 5820793)**已 RESOLVED** — cg_convert_arg src_t==0 兜底 + dst.kind 放宽已 ship,**v0 端不需要再修**
- **W-002**(211 个 identifier rename)跟 **W-004**(短局部变量)都是 **W-001 根因衍生** — W-001 真修(byte-by-byte hash)后**自动失效**,不需要单独真修
- **W-007**(extsw)**已在 v0.8 wip commit 14 真修**(jhyy_v1 codegen.jhyy 加 extsw + src0/ workaround 注释全删)— **v0.9 commit 2 跳过**

**真修清单**(按依赖顺序逐个 commit):

| 修次 | workaround | 改动文件 | 状态 |
|------|-----------|---------|------|
| 2.1 | **W-001** byte-by-byte hash(**根因**)| `compiler/src0/symtab.jhyy` hash_string 真改 + `compiler/src/codegen.c` 对应 | 🔴 真修(高风险,放最前) |
| 2.2 | **W-002 + W-004 自动失效验证** | 验证 W-001 修完后 src0/ 里 211 个 identifier 改名 + 短局部变量改名可全 revert | 🟢 跟 2.1 同步验证 |
| 2.3 | ~~**W-007** extsw(jhyy_v1 端)~~ | ~~`compiler/src0/codegen.jhyy` cg_convert_arg 加 extsw~~ | ✅ **已 RESOLVED**(v0.8 wip commit 14) |
| 2.4 | **W-005** let-mut 全 src0/ | src0/ 翻译稿里 let mut 遗漏补齐 + codegen.c `codegen_stmt` 增强 | 🔴 真修 |
| 2.5 | **W-006** 1-char local var | codegen 路径加固(sprint 4 已部分加固,验证完整性) | 🟡 验证完整 |
| 2.6 | **W-003** let _ = fncall() 顶层 | `compiler/src/codegen.c` `codegen_stmt` 处理 NODE_ASSIGN 时检查 LHS 是 `_` + 顶层 fn → 强制 emit call(不优化掉) | 🔴 真修 |
| 2.7 | **W-008 + W-009 完整性 check + 文档同步** | `workarounds.md` 已 RESOLVED 行确认 + src0/ workaround 注释清理(若有) | 🟢 文档同步 |

**事实依据**:`[`workarounds.md`](../../internal/workarounds.md)` 详细记录每条 workaround + 触发场景

**风险**:
- W-001 改 *u8 deref 类型推导 → 风险高(可能 regression 一片)— **修复前 audit 所有 regress 的 codegen 输出 diff**
- W-001 真修后 src0/ identifier 恢复 211 个 → codegen.jhyy 大改,需重测
- W-007 jhyy_v1 端 extsw 加完 → 删 src0/ workaround 注释,需端到端验证

**完成定义**:
- 3 条真修(W-001/003/005)全 ship + RESOLVED + W-006 验证完整
- W-002 + W-004 自动失效验证通过(W-001 根因修后衍生失效)
- W-007 已在 v0.8 wip commit 14 提前 RESOLVED(本 commit 跳过)
- W-008 + W-009 完整性 check + 文档同步完成
- src0/ 翻译稿里 workaround 注释全删
- `regress.py` 持平 12 OK
- `jhyy (C 版) 编 src0/*.jhyy` 全通过

### commit 3: 翻译 main.c → main.jhyy

**目标**：`compiler/src/main.c` (523 行) → `compiler/src0/main.jhyy`

**挑战**：
1. main.c 是 CLI 入口（不是数据/算法模块）：`main(int argc, char **argv)` + `cmd_compile` / `cmd_build` / `cmd_run` / `cmd_dump`
2. 大量 FFI 依赖：`fopen` / `fwrite` / `fread` / `fclose` / `system` / `exit` / `snprintf` / `malloc` / `free` / `sprintf_lld` / `jh_fputs_*`
3. QBE 绝对路径调用：`main.c:433` 写死 `C:/Users/liuzhen/Desktop/coding/JiHuiYiYou/qbe/qbe.exe`
4. qbe 跟 gcc 命令构造：sprintf 拼 cmd 字符串
5. link 三个 .c 文件：`runtime.c` + `jhyy_helpers.c` + 编译产出的 .s

**策略**：
- **不重翻译** v0.6 验证过的 `jhyy_helpers.c`（C 端唯一 FFI 桥，sprint 1 已就绪）
- **直接 link `main.jhyy → main.s` 进 self-hosted jhyy.exe**
- **重写 QBE/gcc 路径调用**：jhyy extern `system(cmd)` + jhyy 拼 cmd 字符串
- **MUST keep QBE 绝对路径**：v1.0.0 L1 计划明确 "qbe 二进制在 qbe/qbe.exe，不是 PATH 里，靠 jhyy main.c 写绝对路径调用"

**翻译顺序**（3 子 commit）：
- **3a**: jhyy extern 列表 + cmd_run（最简路径：编 + 跑 jhyy 程序）
- **3b**: cmd_compile / cmd_build（QBE 调用 + gcc 链接）
- **3c**: cmd_dump（AST 打印）

**事实依据**：旧 v0.8.0 任务清单 § commit 2（已推迟到 v0.9）

**风险**：main.c 含 fopen/system/qbe 调用 + linker 路径，FFI 声明要全

**完成定义**：`jhyy (C 版) 编 src0/main.jhyy` 成功 + 链接 jhyy_helpers.o + runtime.c 成功

### commit 4: Stage 1 byte-equal 验证 + regress.py JHYY_CC 支持

**目标**：
```bash
# 1. C 编译器编 jhyy → jhyy_0
gcc compiler/src/*.c -o jhyy_0 -I compiler/src

# 2. jhyy_0 编 main.jhyy → jhyy_1
jhyy_0 compile compiler/src0/main.jhyy -o jhyy_1

# 3. jhyy_0 vs jhyy_1 对相同 .jhyy 测试集产 byte-equal .il
mkdir ref
for test in regress_set; do
    jhyy_0 build $test -o ref/$test.il
    jhyy_1 build $test -o sh/$test.il
    diff -q ref/$test.il sh/$test.il   # 必须空
done

# 4. jhyy_1 跑 regress.py 全过
JHYY_CC=jhyy_1 python compiler/build/bin/regress.py
```

**做法**：
1. **改 regress.py**：第 8 行 `JHYY = "compiler/build/bin/jhyy.exe"` → `JHYY = os.environ.get("JHYY_CC", "compiler/build/bin/jhyy.exe")`（os 已 import）
2. 跑 Stage 1 验证脚本（v1.0.0 详细实现方案 § 5.3 已写好 stage1.sh 结构）
3. 验证 diff 全空 + regress 持平（12 OK 持平即可，**不是 47/47**，见 [architecture-refactor.md § 1.1.1](../roadmap/architecture-refactor.md) 失败分类 + [v1.0-self-hosting.md § 自举定义](../roadmap/v1.0-self-hosting.md) "12 OK 持平"）

**风险**：
- 临时变量编号差异 → codegen.jhyy 强制 tmp 编号单调 + 固定 emit 顺序（sprint 4 翻译时已保证）
- 注释 / 空白差异 → 不允许（.il byte-equal 是硬性要求）
- data 段 emit 顺序 → 同样需保证稳定

**完成定义**：
- diff `ref/*.il` `sh/*.il` 全空（所有 regress 测试）
- `JHYY_CC=jhyy_1 python regress.py` 持平 12 OK
- v0.9.0 changelog 写完

---

## 依赖

```
v0.8 wip commit 1-12 (已完成)
  ↓
v0.9.0 commit 1 (29-extsw 验证)
  ↓
v0.9.0 commit 2 (W-001~W-009 真修, 8 个子修)
  ↓
v0.9.0 commit 3 (main.c 翻译, 3 个子翻译)
  ↓
v0.9.0 commit 4 (Stage 1 验证)
  ↓
v1.0.0 启动
```

---

## 验收

| commit | 标准 |
|--------|------|
| 1 | grep 报告 + 修 arena.jhyy（若命中）+ regress 持平 |
| 2.1-2.7 | 3 真修(W-001/003/005) + 1 验证(W-006) + W-002/W-004 衍生失效 + W-008/W-009 完整性 check + 注释全删 + regress 持平 |
| 3a-3c | main.jhyy 编出 + 链接成功 + 自检 4 子命令全过 |
| 4 | diff 全空 + regress 持平 + changelog 写完 |

**v0.9.0 整体完成定义 = Stage 1 byte-equal 闭环（v1.0.0 真闭环的前置）**

---

## 不在 v0.9.0 范围

| 项 | 延后到 | 理由 |
|----|-------|------|
| 修 v0.6 codegen 坑清单 7 条（let mut / qbe_type_of / loadub / 变参 FFI / string dangle 等）| v1.0.0 末（v1.0 详细实现方案 § 0 codegen 坑清单）| 这些坑跟 v0.9 真修的 W-001~W-009 不同（v0.6 时期发现，sprint 4 翻译时已绕开）|
| 切分 parser.c 翻译（缺 match-expr / const-array / import）| v1.0.0 sprint 3 | CERR 8 个 = parser 翻译层缺功能（不在 v0.9）|
| QBE 完整重写 | v2.0 / v2.x | 跟 OS 走 UEFI + PE/COFF 对齐 |
| 性能优化 | v2.x 末 | 跟 v0.9 不相关 |

---

## 实施顺序

```
v0.9 commit 1: 29-extsw hypothesis 验证
v0.9 commit 2: W-001~W-009 真修 (8 个子修, sprint 内多 commit)
v0.9 commit 3: 翻译 main.c → main.jhyy (3 个子翻译)
v0.9 commit 4: Stage 1 byte-equal 验证
  → v1.0.0 启动
```

---

## 关联文档

- [architecture-refactor.md § 1.1](../roadmap/architecture-refactor.md) — 当前状态 + W-001~W-009 详情
- [architecture-refactor.md § 16](../roadmap/architecture-refactor.md) — v0.8 wip → OS M11 完整实施路线图
- [v0.8.0任务清单 + 概要设计.md](v0.8.0任务清单 + 概要设计.md) — 上一阶段（wip 收尾）
- [v1.0.0任务清单 + 概要设计.md](../v1/v1.0.0任务清单 + 概要设计.md) — 下一阶段
- [v1.0.0详细实现方案.md](../v1/v1.0.0详细实现方案.md) — codegen 坑清单（含 W-001~W-009）
- [workarounds.md](../../internal/workarounds.md) — W 详细记录
- [docs/logs/v0/changelog-v0.9.0.md](../../logs/v0/changelog-v0.9.0.md) — v0.9 changelog（待写）
