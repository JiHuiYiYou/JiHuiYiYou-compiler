# docs/plans/v0/ — C 编译器 sprint 计划（C 端编译器自身演进）

本目录放 **C 端编译器**（`compiler/src/*.c`）的 sprint 级计划。每个 sprint 一份任务清单 + 详细实现方案。

版本号对应 C 编译器版本（当前 v0.6.3）：
- `v0.4.0任务清单 + 概要设计.md` — v0.4 sprint 计划
- `v0.4.0详细实现方案.md`
- `v0.5.0任务清单 + 概要设计.md`
- `v0.5.0详细实现方案.md`
- `v0.6.0任务清单 + 概要设计.md`
- `v0.6.0详细实现方案.md`

远期 phase-N 路线图（phase-0 / phase-1 / phase-2 / phase-3）→ [`../phase/`](../phase/)

v1.0.0 phase-2 自举 sprint 计划 → [`../v1/`](../v1/)

**C 端编译器的 changelog** 在 [`../../logs/`](../../logs/)（也是 v0 时代 + v0.6.2/3 patch，未来视情况决定是否拆 `logs/v0/` `logs/v1/`）。
