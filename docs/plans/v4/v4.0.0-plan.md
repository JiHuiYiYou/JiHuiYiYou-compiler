# v4.0.0 plan — v2 + v3 axes merge → main axis push

## Context

**状态**: ⏳ **未启动**(concept only — per 2026-09-06 user:"v4 现在还没出文档，但是其实有这个概念")。

**触发前置 / 合并条件**:
- ✅ V3-A + V3-B + V3-C `v3.1.0` ship(M1-M4 launch 硬前置)
- ✅ V2-A `v2.5.0` ship
- ✅ V2-B `v2.6.0` + `v2.7.0` ship(M2 sysv target 全)
- ✅ V3 `v3.2.0`..`v3.2.5` ship(M8d + M11 硬前置)
- ✅ V3 `v3.3.0` (3h 浮点)ship(数学 std lib 完整)
- ⏳ V2-C `v2.8.0` ship(QBE 移除 + N 代 fixed point)— **merge 强前置**
- ⏳ user 决定 merge 时机 — **本 sprint 不抢,等 user 启动**

**Worktree 约定**:
- **merge 本身不开新 worktree** — `axis-v2` + `axis-v3` → `main` 是 1 个 integration commit
- **merge 后主版本轴推 v4**(`main` 第一个 tag = `v4.0.0`)
- **post-merge 串行**(per `project_v4_post_merge_axis`):v2.x + v3.x 并行轴结束,主版本轴 v4.x 单线串行

**M5 deferral 升级**(per 2026-09-06 user):原"v2/v3 后独立 sprint" → "v4.x 独立 sprint";`v1.x-phase-4-m5-boot-from-scratch.md` 仍保留为 deferral 决策 doc,**实现**走 `v4.1.0-plan.md`。

## Scope

### Merge 动作(机械,无新代码)
1. **分支合并**:
   - `git checkout main`(主轴 baseline)
   - `git merge --no-ff axis-v2`(rebase 过冲突则手动解)
   - `git merge --no-ff axis-v3`
   - 处理可能的合并冲突(预期:docs/* 路径冲突多,源码 `src0/*` 冲突少)
2. **Baseline 锁定**:
   - D43 重新 baseline(M11 launch 完 + QBE 移除完 = 主轴最终 baseline)
   - 写进 `docs/logs/v4/changelog-v4.0.0.md`(umbrella,~150 行:N 代 fixed point 终态 sha + merge commit + baseline freeze + tag 节奏)
3. **worktree 清理**:
   - `axis-v2` worktree remove + `branch -D axis-v2` + `push origin --delete axis-v2`(per `feedback_batch_worktree_cleanup`)
   - `axis-v3` worktree remove + `branch -D axis-v3` + `push origin --delete axis-v3`
4. **Tag push**:
   - `git tag -a v4.0.0 -m "v4.0.0: v2+v3 axes merge + main axis push (post-M11)"`
   - `git push origin v4.0.0`
5. **README / doc 更新**:
   - 根 `CLAUDE.md` 版本轴段更新:v2.x / v3.x 并行轴 → "v2.x + v3.x merge 完成,v4.x 串行"
   - `docs/internal/architecture.md` 模块边界图更新(去掉 V2 / V3 备注)
   - `docs/logs/v4/` 目录创建(新)

### 不在 v4.0.0 scope
- **删 `src/*.c`** → 留 `v4.1.0-plan.md`(M5)
- **untrack QBE** → V2-C v2.8.0 已做(v4.0.0 merge 时已 ship)
- **删 `runtime.c`** → 留 `v4.1.0-plan.md`
- **新功能 / bugfix** → 全部留 v4.x (1+)
- **3k 错误恢复 / 3m 优化 / 3n 包管理器** → 留 `v4.7.0` / `v4.8.0` / `v4.9.0`
- **v3.x 中/末 features**(async / lifetime full / closure 增强 / const generic / trait objects / HKT / specialization / 并发 cap / memory model)→ 留 `v4.2.0`..`v4.11.0`

## 关键决策

- **merge commit 用 `--no-ff`**:保留 v2 / v3 axes 平行史(per `feedback_auto_push_after_commit` 不要 force,标准 3-way merge)
- **不开新 worktree**(per 2026-09-06 user)— merge 是 1 个 commit
- **M5 留 v4.1.0** 不在本 sprint
- **user 决定 merge 时机** — 不主动推

## 验收 / ship gate

- `main` 分支 = v2.x + v3.x 全 worktree merge + 冲突解完
- `axis-v2` + `axis-v3` worktree + branch 删除(per `feedback_batch_worktree_cleanup`)
- D43 最终 baseline freeze + 写进 `changelog-v4.0.0.md`
- tag `v4.0.0` push 到 origin
- regress.py 104/104 + 新 v3.x test 全 PASS(v2.8.0 已 ship 后)
- `jhyy_selfhost_check` N=3+ byte-equal(V2-C v2.8.0 已 ship)

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| `axis-v2` + `axis-v3` 长期分支合并冲突 | merge fail | V2-C v2.8.0 + V3 v3.3.0 期间主轴已多次同步(每周 sync);merge 时用户协助解冲突 |
| merge 后 baseline 漂移 | N=3 byte-equal fail | merge 后重新 N 代 fixed point 验算;v4.0.0 ship gate 含 |
| `axis-v2` / `axis-v3` 删除后用户访问丢失 | 误删 | merge commit 永久在 main 历史;tag `v2.8.0` + `v3.3.0` 永久 |

## Out of scope

- M5 删 src/*.c(`v4.1.0-plan.md`)
- 性能优化(留 post-v4.x)
- CI/CD 升级(留 v4.x 中)

## Commit / tag 节奏

- **Commit 1**:`chore(merge): merge axis-v2 + axis-v3 into main, push v4.0.0`(merge commit)
- **Commit 2**:`chore(cleanup): remove axis-v2 + axis-v3 worktree/branch`(worktree/branch cleanup)
- **Commit 3**:`docs: v4.0.0 umbrella changelog + v4 post-merge serial concept`(changelog-v4.0.0.md + doc 同步)
- **Tag**:`v4.0.0`
- **写进** `docs/logs/v4/changelog-v4.0.0.md`

## Cross-ref

- v4 concept:`project_v4_post_merge_axis` memory + 2026-09-06 user 决定
- 触发前置:`[../v2/batch-V2-C-plan.md](../v2/batch-V2-C-plan.md)`(v2.8.0 N 代 fixed point)+ `[v3.3.0-plan.md](../v3/v3.3.0-plan.md)`(3h 浮点)
- M5 deferral:`[../v1/v1.x-phase-4-m5-boot-from-scratch.md](../v1/v1.x-phase-4-m5-boot-from-scratch.md)` → v4.1.0 实现
- worktree cleanup:`feedback_batch_worktree_cleanup` memory
- 后续 v4.x:`v4.1.0-plan.md`..`v4.11.0-plan.md`(本目录)
