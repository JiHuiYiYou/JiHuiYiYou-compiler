# v4.6.0 plan — trait objects (dyn Trait)

## Context

**状态**: ⏳ **未启动**(per `v3.x-language-expansion.md` "v3.x 末" trait objects,per 2026-09-06 决定推到 v4.x)。

**上游依赖**:
- ✅ v3.2.0 (3i generics)ship — dyn Trait 是反向(动态分发)
- ✅ v4.0.0 (v2+v3 merge)ship
- ✅ v4.4.0 (closure trait object)ship — 闭包 trait object 配套

**触发前置 / OS 关系**:
- UX 改进(动态分发,允许 heterogeneous collection)
- 不阻塞 MVP

**Worktree 约定**:v4.x 串行主轴,`main` direct commit + tag `v4.6.0`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| trait 语法 `trait Foo { ... }` + `impl Foo for Bar { ... }` | `compiler/src0/parser.jhyy` + `sema.jhyy` | ~150 行 |
| vtable emit(per type impl vtable struct)| `compiler/src0/codegen.jhyy` | ~200 行 |
| `dyn Trait` 类型 + dynamic dispatch via vtable | `compiler/src0/types.jhyy` + `sema.jhyy` | ~200 行 |
| `Box<dyn Trait>` heap-allocated trait object | `std/core.jhyy` + `compiler/src0/codegen.jhyy` | ~80 行 |
| Upcast / downcast(`as dyn SuperTrait`)| `compiler/src0/sema.jhyy` | ~100 行 |

### 新建
- `tests/trait_basic.jhyy` + `trait_object_dyn.jhyy` + `trait_upcast.jhyy`(~200 行 + SKIP)
- `docs/abis/jhyy-lang-spec-trait-supplement-v4.6.0.md`(~250 行)
- `docs/logs/v4/changelog-v4.6.md`(append 段)

## 关键决策

- **M0 简化:trait 不支持默认方法**(留 v4.x 中)
- **M0 简化:trait 不支持关联类型**(留 v4.x 中)— 仅支持 trait method dispatch
- **M0 简化:dyn Trait 仅 Box<dyn>(不做 `&dyn`)**
- **不开新 worktree**

## 验收 / 5/5 ship gate

- 3+ trait / dyn 测试 5/5 PASS(trait_basic / trait_object_dyn / trait_upcast)
- D43 re-baseline → **N21**
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` byte-equal hold

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| vtable emit 跟 codegen 主路径交互 | regress 退步 | vtable 走独立 codegen 路径 |
| upcast / downcast 类型规则复杂度 | sema 复杂度 | M0 简化:仅支持 upcast |
| trait + generic 交互(trait bound on generic)| sema 复杂度 | M0 简化:trait bound 仅 `T: Trait` 形式;高阶 trait bound 留 v4.x 中 |

## Out of scope

- 默认方法(留 v4.x 中)
- 关联类型(留 v4.x 中)
- `&dyn Trait`(留 v4.x 中)
- HRTB(留 v4.x 中)
- trait + specialization(留 `v4.10.0-plan.md`)
- object-safe 完整规则(留 v4.x 中)
- async trait(留 v4.x 中)

## Commit / tag 节奏

- **Commit 1**:`feat(trait): add trait + impl + dyn Trait + vtable dispatch`
- **Tag**:`v4.6.0`

## Cross-ref

- 上游:`v3.2.0-plan.md`(3i generics)+ `v4.4.0-plan.md`(closure trait object)
- 设计草案:`[../v3/roadmap/v3.x-language-expansion.md § "v3.x 末 features"](../../roadmap/v3.x-language-expansion.md)`
- v4 概念:`project_v4_post_merge_axis` memory
