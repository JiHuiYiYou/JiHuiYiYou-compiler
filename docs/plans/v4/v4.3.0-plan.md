# v4.3.0 plan — lifetime 完整版 + Polonius borrow check

## Context

**状态**: ⏳ **未启动**(per `v3.x-language-expansion.md` "v3.x 中" lifetime + Polonius,per 2026-09-06 决定推到 v4.x)。

**上游依赖**:
- ✅ v3.x 全 ship(v3.0.0..v3.3.0)
- ✅ v4.0.0 (v2+v3 merge)ship
- ✅ v4.1.0 (M5)ship
- ✅ v3.1.0 (3g `&mut` + lifetime stub)ship — 本 sprint 是 3g 完整版升级

**触发前置 / OS 关系**:
- 完整 lifetime 让 OS 代码更安全(避免 use-after-free 在编译期拒绝)
- M11 launch 之后 polish — **不阻塞**

**Worktree 约定**:v4.x 串行主轴,`main` direct commit + tag `v4.3.0`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| 完整 lifetime 标注语法 `'a` `'b` | `compiler/src0/parser.jhyy` | ~50 行 |
| lifetime 子类型(协变 / 逆变 / 不变)| `compiler/src0/sema.jhyy` | ~200 行 |
| Polonius borrow check(replaces NLL stub from v3.1.0)| `compiler/src0/sema.jhyy` + `borrow_check.jhyy`(新) | ~600 行 |
| 完整 reborrow 规则 | `compiler/src0/sema.jhyy` | ~150 行 |
| 借用 string view | `std/string.jhyy` 扩展 | ~50 行 |

### 新建
- `compiler/src0/borrow_check.jhyy`(Polonius 算法,~600 行)
- `tests/lifetime_advanced.jhyy` + `lifetime_subtype.jhyy` + `lifetime_reborrow.jhyy`(~200 行 + SKIP)
- `docs/abis/jhyy-lang-spec-lifetime-supplement-v4.3.0.md`(~300 行)
- `docs/logs/v4/changelog-v4.3.md`(append 段)

## 关键决策

- **M0 简化:Polonius 走 "subset-based" 算法**(per Polonius 论文)— 不做 full datafrog 优化,实现简单
- **M0 简化:lifetime 子类型仅协变 + 不变**(逆变留 v4.x 中)
- **M0 简化:不做 higher-ranked trait bounds (HRTB)**(留 v4.x 中)
- **不开新 worktree**

## 验收 / ship gate

- 3+ lifetime 测试 5/5 PASS(lifetime_advanced / lifetime_subtype / lifetime_reborrow)
- D43 re-baseline → **N18**
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` byte-equal hold
- v3.1.0 stub 借用检查全部 case 仍过(regress 不退步)

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| Polonius 算法复杂度 | ship 推迟 | subset-based 简化;perf 留 v4.x 中 |
| lifetime 子类型跟泛型交互(generic + lifetime)| sema 复杂度 | M0 仅在 generic fn 不带 lifetime 时完整 |
| 完整 reborrow 触发已有 regress 退步 | regress 退步 | 3g ship 时已 ship stub 版本;完整版跟 stub 兼容;regress 必跑 |

## Out of scope

- 逆变(留 v4.x 中)
- HRTB(留 v4.x 中)
- 完全 datafrog 优化版 Polonius(留 v4.x 中)
- ghost / 幽灵借用(留 v4.x 末)
- Polonius + 闭包交互复杂 case(留 v4.x 中)
- NLL v3 → Polonius 自动迁移工具(留 v4.x 中)

## Commit / tag 节奏

- **Commit 1**:`feat(lifetime): add 完整 lifetime 标注 + 子类型(协变 / 不变)`
- **Commit 2**:`feat(borrow-check): add Polonius borrow check (replaces NLL stub)`
- **Commit 3**:`feat(reborrow): add 完整 reborrow 规则 + string view`
- **Tag**:`v4.3.0`

## Cross-ref

- 上游:`v3.1.0-plan.md` / `batch-V3-C-plan.md`(3g stub ship)
- 设计草案:`[../v3/roadmap/v3.x-language-expansion.md § Sprint 3g lifetime](../../roadmap/v3.x-language-expansion.md)`
- v4 概念:`project_v4_post_merge_axis` memory
