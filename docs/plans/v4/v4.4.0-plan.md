# v4.4.0 plan — closure 增强 (move / borrow capture / generic / trait object)

## Context

**状态**: ⏳ **未启动**(per `v3.x-language-expansion.md` "v3.x 中/末",per 2026-09-06 决定推到 v4.x)。

**上游依赖**:
- ✅ v3.2.1 (3j closures ship)— 本 sprint 是 3j 增强版
- ✅ v3.2.0 (3i generics)ship — generic closure 依赖
- ✅ v4.3.0 (lifetime 完整版 + Polonius)ship — borrow capture 依赖

**触发前置 / OS 关系**:
- UX 改进,不阻塞 MVP

**Worktree 约定**:v4.x 串行主轴,`main` direct commit + tag `v4.4.0`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| `move` 关键字(显式 move 捕获)| `compiler/src0/parser.jhyy` + `sema.jhyy` | ~80 行 |
| 借用捕获(`&T` / `&mut T` 捕获)| `compiler/src0/sema.jhyy` | ~120 行 |
| 泛型闭包 `\|x: T\| -> T` | `compiler/src0/monomorphize.jhyy` (v3.2.0 创建)扩展 | ~150 行 |
| 闭包 trait objects `Box<dyn Fn(i32) -> i32>` | `compiler/src0/sema.jhyy` + `codegen.jhyy` | ~200 行 |
| Fn / FnMut / FnOnce 三 trait 区分 | `std/core.jhyy` + `compiler/src0/sema.jhyy` | ~150 行 |

### 新建
- `tests/closure_move.jhyy` + `closure_borrow_capture.jhyy` + `closure_generic.jhyy` + `closure_trait_object.jhyy`(~200 行 + SKIP)
- `docs/abis/jhyy-lang-spec-closures-advanced-supplement-v4.4.0.md`(~200 行)
- `docs/logs/v4/changelog-v4.4.md`(append 段)

## 关键决策

- **M0 简化:`move` 仅强制 move 捕获**(per-value 默认;borrow capture 由 lifetime 推断触发)
- **M0 简化:闭包 trait object 仅 `Box<dyn Fn>`**(不做 `&dyn Fn`)
- **不开新 worktree**

## 验收 / 5/5 ship gate

- 4+ closure 增强测试 5/5 PASS(closure_move / closure_borrow_capture / closure_generic / closure_trait_object)
- D43 re-baseline → **N19**
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` byte-equal hold
- v3.2.1 基础闭包测试仍过(regress 不退步)

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| borrow capture + lifetime 交互 | sema 复杂度 | 复用 v4.3.0 Polonius;sema 加 capture kind 标注 |
| 泛型闭包 + monomorphize pass | codegen 复杂度 | monomorphize 加 closure support;失败时 fallback |
| 闭包 trait object 跟 dyn Trait 集成 | v4.6.0 trait objects 强前置 | 本 sprint 留 FnMut/FnOnce 接口,完整 dyn Trait 集成留 `v4.6.0-plan.md` |

## Out of scope

- async 闭包(留 `v4.2.0-plan.md` 后 polish)
- HRTB 闭包(留 v4.x 中)
- 闭包 + 高阶类型(留 v4.x 末)
- 闭包 + specialization(留 `v4.10.0-plan.md`)

## Commit / tag 节奏

- **Commit 1**:`feat(closure): add move + borrow capture + generic + trait object (3j+ advanced)`
- **Tag**:`v4.4.0`

## Cross-ref

- 上游:`v3.2.1-plan.md`(3j closures)+ `v4.3.0-plan.md`(lifetime 完整版)+ `v4.6.0-plan.md`(trait objects 配套)
- 设计草案:`[../v3/roadmap/v3.x-language-expansion.md § Sprint 3j advanced](../../roadmap/v3.x-language-expansion.md)`
- v4 概念:`project_v4_post_merge_axis` memory
