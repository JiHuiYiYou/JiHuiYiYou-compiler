# v4.7.0 plan — 3k 错误恢复 (parser / sema 多错误报告)

## Context

**状态**: ⏳ **未启动**(per `v3.x-language-expansion.md § Sprint 3k`,per 2026-09-06 决定推到 v4.x)。

**上游依赖**:
- ✅ v3.x 全 ship
- ✅ v4.0.0 (v2+v3 merge)ship

**触发前置 / OS 关系**:
- UX 改进(不阻塞 MVP)
- v4.x polish 第一波(M5 之后)

**Worktree 约定**:v4.x 串行主轴,`main` direct commit + tag `v4.7.0`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| Parser 同步点恢复:遇错误跳到下一个 `;` 或 `}` | `compiler/src0/parser.jhyy` | ~100 行 |
| Sema 多错误收集:不遇第一个错误就 bail | `compiler/src0/sema.jhyy` | ~150 行 |
| 错误聚合:`Vec<Diag>` 收集 + 一次报告 | `compiler/src0/diag.jhyy`(新) | ~150 行 |
| Source location + span 信息增强(行/列 + 字符区间)| `compiler/src0/lexer.jhyy` + `parser.jhyy` | ~80 行 |
| CLI 错误输出:彩色 + 来源片段(caret 指向)| `compiler/src0/main.jhyy` | ~100 行 |

### 新建
- `tests/error_recovery_parser.jhyy` + `error_recovery_sema.jhyy`(~100 行 + SKIP)— 多错误 + 来源片段
- `docs/abis/jhyy-lang-spec-diagnostics-supplement-v4.7.0.md`(~150 行)— 错误恢复 spec
- `docs/logs/v4/changelog-v4.7.md`(append 段)

## 关键决策

- **M0 简化:Parser 同步点仅 `;` 和 `}`**(不引入复杂 resync 策略)
- **M0 简化:不实现 IDE 集成 LSP**(留 v4.x 末)— 只做 CLI 错误输出
- **不开新 worktree**

## 验收 / 5/5 ship gate

- 2+ 错误恢复测试 5/5 PASS(error_recovery_parser / error_recovery_sema)— 单文件 5+ 错误全部报告
- 不出现级联错误(同一 bug 不报 5+ 次)
- D43 re-baseline → **N22**
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` byte-equal hold

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 错误恢复引入 false positive | 漏报真错误 | 不恢复的 case 走原 bail;恢复路径保守 |
| Source span 字符区间 vs 行/列不一致 | 错误指向错 | lexer 统一记录 byte offset → span mapping |

## Out of scope

- 复杂 resync 策略(recovery 到语句 / 表达式块)
- LSP / IDE 集成(留 v4.x 末)
- 增量编译错误恢复(留 v4.x 末)
- 错误代码(error code, E0001 / E0002)(留 v4.x 中)— M0 仅文字描述
- fix-it hint(自动建议修复,留 v4.x 中)

## Commit / tag 节奏

- **Commit 1**:`feat(diag): add parser + sema 错误恢复 + diag 聚合`
- **Commit 2**:`feat(diag-cli): add 来源片段 + caret 指向输出`
- **Tag**:`v4.7.0`

## Cross-ref

- 设计草案:`[../v3/roadmap/v3.x-language-expansion.md § Sprint 3k](../../roadmap/v3.x-language-expansion.md)`
- v4 概念:`project_v4_post_merge_axis` memory
