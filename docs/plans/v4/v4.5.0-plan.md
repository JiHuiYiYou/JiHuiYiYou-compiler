# v4.5.0 plan — const generic

## Context

**状态**: ⏳ **未启动**(per `v3.x-language-expansion.md` "v3.x 末" const generic,per 2026-09-06 决定推到 v4.x)。

**上游依赖**:
- ✅ v3.2.0 (3i generics monomorphize)ship
- ✅ v4.0.0 (v2+v3 merge)ship

**触发前置 / OS 关系**:
- UX 改进(允许 `[T; N]` 类型,N 是 const 参数)
- 不阻塞 MVP

**Worktree 约定**:v4.x 串行主轴,`main` direct commit + tag `v4.5.0`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| 语法 `fn foo<const N: usize>(arr: [T; N])` | `compiler/src0/parser.jhyy` | ~60 行 |
| const 泛型参数挂在 fn / struct 节点 | `compiler/src0/ast.jhyy` | ~30 行 |
| 单态化时 const 参数求值(eval at monomorphize)| `compiler/src0/monomorphize.jhyy` | ~150 行 |
| `[T; N]` 数组类型用 const N 表达 | `compiler/src0/types.jhyy` + `codegen.jhyy` | ~80 行 |

### 新建
- `tests/const_generic_basic.jhyy` + `const_generic_array.jhyy` + `const_generic_struct.jhyy`(~150 行 + SKIP)
- `docs/abis/jhyy-lang-spec-const-generic-supplement-v4.5.0.md`(~200 行)
- `docs/logs/v4/changelog-v4.5.md`(append 段)

## 关键决策

- **M0 简化:const 泛型参数仅支持整数 + bool + char**(string / struct const 留 v4.x 末)
- **M0 简化:不做 const generic 表达式求值**(complex const expr 留 v4.x 末)
- **不开新 worktree**

## 验收 / 5/5 ship gate

- 3+ const generic 测试 5/5 PASS(const_generic_basic / const_generic_array / const_generic_struct)
- D43 re-baseline → **N20**
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` byte-equal hold

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| const 求值 monomorphize 复杂度 | codegen 复杂度 | M0 简化:仅支持常量;复杂表达式留 v4.x 末 |
| `[T; N]` 数组类型 + const generic 交互 | types 复杂度 | v1.0 已有 `[T; N]` 固定数组;const generic 升级为参数化 |

## Out of scope

- string / struct const 参数(留 v4.x 末)
- 复杂 const 表达式(留 v4.x 末)
- const generic + specialization 集成(留 `v4.10.0-plan.md`)
- const generic + HRTB(留 v4.x 中)
- `[T; N]` runtime 边界检查优化(留 v4.x 中)

## Commit / tag 节奏

- **Commit 1**:`feat(const-generic): add const 泛型参数 + 单态化 + 数组类型`
- **Tag**:`v4.5.0`

## Cross-ref

- 上游:`v3.2.0-plan.md`(3i generics)
- 设计草案:`[../v3/roadmap/v3.x-language-expansion.md § "v3.x 末 features"](../../roadmap/v3.x-language-expansion.md)`
- v4 概念:`project_v4_post_merge_axis` memory
