# v4.10.0 plan — HKT (Higher-Kinded Types) + specialization

## Context

**状态**: ⏳ **未启动**(per `v3.x-language-expansion.md` "v3.x 末",per 2026-09-06 决定推到 v4.x)。

**上游依赖**:
- ✅ v3.2.0 (3i generics monomorphize)ship
- ✅ v4.0.0 (v2+v3 merge)ship
- ✅ v4.5.0 (const generic)ship — const generic + specialization 配套
- ✅ v4.6.0 (trait objects / dyn Trait)ship — specialization + trait 配套

**触发前置 / OS 关系**:
- HKT 是高级类型系统 feature(允许 `F<T>` 作为参数,`Functor<F>` 等)
- 不阻塞 MVP / OS 启动
- v4.x 末(specialization + HKT 一对)

**Worktree 约定**:v4.x 串行主轴,`main` direct commit + tag `v4.10.0`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| HKT 语法 `trait Functor<F<*>> { ... }` | `compiler/src0/parser.jhyy` + `sema.jhyy` | ~200 行 |
| `*` 表示"类型构造器位置"(高阶) | `compiler/src0/types.jhyy` | ~80 行 |
| Specialization `impl Foo for Bar { ... }` 覆盖默认 | `compiler/src0/sema.jhyy` | ~150 行 |
| Specialization 解析规则(优先最具体 impl)| `compiler/src0/monomorphize.jhyy` | ~100 行 |

### 新建
- `tests/hkt_functor.jhyy` + `hkt_monad.jhyy` + `specialization_basic.jhyy`(~200 行 + SKIP)
- `docs/abis/jhyy-lang-spec-hkt-specialization-supplement-v4.10.0.md`(~250 行)
- `docs/logs/v4/changelog-v4.10.md`(append 段)

## 关键决策

- **M0 简化:HKT 仅支持 1 阶(`F<*>` 不支持 `F<G<*>>`)**
- **M0 简化:specialization 仅支持单层覆盖**(不做 negative reasoning / overlap detection 完整版)
- **M0 简化:不做 associated type constructors**(留 v4.x 末)
- **不开新 worktree**

## 验收 / 5/5 ship gate

- 3+ HKT/specialization 测试 5/5 PASS(hkt_functor / hkt_monad / specialization_basic)
- D43 re-baseline → **N25**
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` byte-equal hold

## 风险 + 缓解

| 风险 | 影响 | 修复 |
|------|------|------|
| HKT 类型推导复杂度 | sema 复杂度 | M0 简化:仅 1 阶;复杂推导留 v4.x 末 |
| specialization overlap 错误检测 | sema 复杂度 | M0 仅单层覆盖,overlap 报错清晰 |

## Out of scope

- 多阶 HKT(留 v4.x 末)
- associated type constructors(留 v4.x 末)
- specialization negative reasoning(留 v4.x 末)
- HKT + lifetime 集成(留 v4.x 末)
- HKT + const generic 集成(留 v4.x 末)
- type class coherence 完整规则(留 v4.x 末)

## Commit / tag 节奏

- **Commit 1**:`feat(hkt): add Higher-Kinded Types (1 阶 F<*>)`
- **Commit 2**:`feat(specialization): add impl 覆盖 + overlap 检测`
- **Tag**:`v4.10.0`

## Cross-ref

- 设计草案:`[../v3/roadmap/v3.x-language-expansion.md § "v3.x 末 features"](../../roadmap/v3.x-language-expansion.md)`
- 上游:`v3.2.0-plan.md`(generics)+ `v4.5.0-plan.md`(const generic)+ `v4.6.0-plan.md`(trait objects)
- v4 概念:`project_v4_post_merge_axis` memory
