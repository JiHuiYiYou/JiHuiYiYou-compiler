# v4.12.0 plan — `.jhyynb` native binary format 实装

## Context

**状态**: ⏳ **未启动**(per 2026-09-06 user:v4.12 做 .jhyynb)。

**Spec 锚点**:`[../../abis/jhyy-binary-format-v1.0.0.md](../../abis/jhyy-binary-format-v1.0.0.md)`(2026-09-04 锁,设计完成,实装未排)。

**上游依赖**:
- ✅ v3.x 全 ship(v3.0.0..v3.3.0)— `Cap<T>` 8B / `#[ipc_handler]` / `Cap::provenance()` / Debug ABI
- ✅ V2-C `v2.8.0` ship(自写后端完整 + QBE 移除)— 自写 emit 路径必备
- ✅ V3-C `v3.1.0`..`v3.1.2` ship — cap_init / ipc_dispatch / debug_group 必备
- ✅ v4.0.0 (v2+v3 merge)ship
- ✅ v4.1.0 (M5 删 `src/*.c` + untrack QBE + 删 `runtime.c`)ship — jhyy 编 jhyy 0 C 依赖闭环
- ⏳ jhyy_OS loader 配套(**不在本仓**)— per spec § 19 跨仓

**触发前置 / OS 关系**:
- **OS 真闭环(M11 之后,jhyy 编 jhyy 在 jhyy_OS 上跑)** — jhyy 编译器在 jhyy_OS 上自举用 `.jhyynb`(per spec § 18 验证 #5)
- M11 launch 自身不阻塞(用 v2/v3 + N=3 byte-equal 闭包已达成)
- 本 sub-sprint ship 后 = **OS 真闭环前提就位**

**Worktree 约定**:
- v4.x 串行主轴,`main` direct commit + tag `v4.12.0`
- **不开新 worktree**(per 2026-09-06 user)

## Scope

### Part 1:DWARF emitter(核心,新模块)
| 模块 | 功能 |
|------|------|
| `compiler/src0/dwarf_emit.jhyy`(新,~400 行)| DWARF v5 `.debug_info` / `.debug_line` / `.debug_abbrev` / `.debug_str` 子节 emit |
| `compiler/src0/dwarf_expr.jhyy`(新,~200 行)| DWARF expression(byte-level op encoding)|
| `compiler/src0/jhyynb_sections.jhyy`(新,~300 行)| section builder(Header / text / data / rodata / cap_init / ipc_dispatch / region_table / debug_group / symtab / strtab)|

### Part 2:`--target=jhyy-os` 后端集成
| 改动 | 文件 | 估算 |
|------|------|------|
| `target_dispatch.jhyy`:加 `TARGET_JHYY_OS = 4` | `compiler/src0/target_dispatch.jhyy` | ~10 行 |
| `codegen_amd64.jhyy`:加 `jhyy_os_emit` 函数 | `compiler/src0/codegen_amd64_emit.jhyy` | ~200 行 |
| `codegen_amd64_emit_call.jhyy`:集成 ipc_dispatch 表 emit | `compiler/src0/codegen_amd64_emit_call.jhyy` | ~80 行 |
| `codegen.jhyy`:cap_init section emit(per `Cap<T>` constructor 白名单)| `compiler/src0/codegen.jhyy` | ~120 行 |
| `main.jhyy`:`--target=jhyy-os` CLI flag + jhyynb emit cmd line | `compiler/src0/main.jhyy` | ~80 行 |

### Part 3:`jhyy-inspect` CLI 工具
- `compiler/src0/jhyy_inspect.jhyy`(新,~200 行)— 解析 .jhyynb section table / cap_init / ipc_dispatch / dump 字节流
- 集成到 `main.jhyy` 子命令:`jhyy inspect <file.jhyynb>`
- per spec § 18 验证 #3

### Part 4:测试
| 测试 | 内容 |
|------|------|
| `tests/jhyynb_smoke.jhyy` + SKIP directive | loader smoke test(最小 .jhyynb,仅 .text + .data) |
| `tests/jhyynb_dwarf.jhyy` + SKIP | DWARF 子节 emit + GDB attach 验证 |
| `tests/jhyynb_ipc.jhyy` + SKIP | ipc_dispatch section emit + cap_offset 表 byte-equal |
| `tests/jhyynb_cap.jhyy` + SKIP | cap_init section emit + OS cnode byte-equal 验证 |
| `tests/jhyynb_selfhost.jhyy` + SKIP | `--target=jhyy-os` 编 `src0/main.jhyy` → .jhyynb(集成测试)|

### Part 5:文档同步
- `docs/abis/jhyy-binary-format-v1.0.0.md`(spec 已锁,**不动**;实装后 § 18 验证逐项打勾)
- `docs/internal/architecture.md` 模块边界图加 jhyynb emit 模块
- `docs/internal/build.md` `--target=jhyy-os` 使用
- `docs/logs/v4/changelog-v4.12.md`(新 umbrella)

## 关键决策

- **DWARF v5 子集**(per spec § 11)— 不实现完整 DWARF(spec 已锁"完整 DWARF 子集")
- **Cap<T> phantom 字段不 emit 到 .jhyynb**(per spec § 16 D23 + `v3.1.1-plan.md`)
- **`#[ipc_handler]` 不进 spec**(per spec § 16 D13/D20)— 但 codegen emit cap_offset 表
- **wire-format 表达规则 per D40**(per spec § 16):链表 = `*T`,数组 = `[*]T`
- **M0 简化:`--target=jhyy-os` 仅 64-bit**(per spec § 19 aarch64/riscv64 留 future)
- **M0 简化:`.tls` section 留空(M11 后用,per spec § 7 kind=8)**
- **不开新 worktree**

## 验收 / 5/5 ship gate

- 5 个测试 5/5 PASS(jhyynb_smoke / jhyynb_dwarf / jhyynb_ipc / jhyynb_cap / jhyynb_selfhost)
- D43 re-baseline → **N27**(jhyynb emit 改 codegen 主路径)
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` byte-equal hold
- spec § 18 验证逐项打勾:
  - loader smoke test ✅
  - GDB attach 闭环(跨仓跟 jhyy_OS `jhyy-gdbstub` 联调)
  - `jhyy-inspect foo.jhyynb` ✅(本仓)
  - CodeGraph 摄入 ✅
  - jhyy 自举:`--target=jhyy-os` 编出 .jhyynb,在 jhyy_OS 上跑通(跨仓跟 jhyy_OS 联调)— **本 sub-sprint ship gate 不阻塞 OS 真闭环**
- `compiler/src0/dwarf_emit.jhyy` 测试覆盖率 > 80%

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| DWARF emit 字节级正确性 | spec § 11 不达标 | 严格按 DWARF v5 spec 字节序写;cross-test 跟 GCC/LLVM emit 比对 |
| 自写 emit 跟 .jhyynb spec 不一致(emit 字段偏移错)| spec § 16 对位表 fail | 单元测试逐字段对位 D-lock |
| ipc_dispatch 表跟 OS cnode byte-equal fail | spec § 18 验证 #4 失败 | 跟 jhyy_OS 端 cnode 内存 cross-test;失败时回退 + 修 offset 算术 |
| GDB attach 跨仓联调失败 | spec § 18 验证 #2 失败 | jhyy_OS 端 jhyy-gdbstub 必须先 ship;本 sub-sprite 不阻塞 |
| CodeGraph 摄入 .jhyynb debug_group | spec § 18 验证 #4 | CodeGraph MCP 后端需升级;本 sub-sprint ship gate 不阻塞 |

## Out of scope

- **jhyy_OS loader 实现**(跨仓,不属本仓 scope)— per spec § 19
- aarch64 / riscv64 .jhyynb 字节序 / calling convention 差异(per spec § 19)— 留 v4.x 末
- 完整 DWARF 实现(仅 v5 子集,per spec § 11)— 完整版留 v4.x 末
- QBE .s → post-process 包装路径(per spec § 19)— V2-C v2.8.0 之前过渡;本 sprint 自写 emit 为主路径
- 完整 8-class SysV 分类器(留 v4.x backlog)— 不影响 .jhyynb section emit
- `.tls` section 实际内容(M11 后用,per spec § 7)— 本 sprint 仅占位
- `jhyy-gdbstub` daemon 实现(跨仓)— 不属本仓 scope
- `jhyy-inspect` 图形化 UI(本 sprint 仅 CLI)— 图形化留 v4.x 末

## Commit / tag 节奏

- **Commit 1**:`feat(jhyynb): add DWARF emitter + section builder`(Part 1 主体)
- **Commit 2**:`feat(jhyynb): add --target=jhyy-os 后端集成`(Part 2)
- **Commit 3**:`feat(jhyy-inspect): add jhyy inspect CLI 工具`(Part 3)
- **Commit 4**:`test(jhyynb): add 5 个 .jhyynb emit + DWARF + IPC + cap_init 测试`(Part 4)
- **Commit 5**:`docs: v4.12.0 .jhyynb 实装 ship + spec § 18 验证打勾`(Part 5)
- **Tag**:`v4.12.0`
- **跨仓通知**:ship 后通知 jhyy_OS 仓:`.jhyynb` 实装就位,可启动 loader + jhyy-gdbstub 配套

## Cross-ref

- Spec:`[../../abis/jhyy-binary-format-v1.0.0.md](../../abis/jhyy-binary-format-v1.0.0.md)`(2026-09-04 锁)
- 上游:`v4.1.0-plan.md`(M5 ship)+ V2-C v2.8.0(自写后端)+ V3-C v3.1.0..v3.1.2(Cap<T> + ipc_handler + Debug ABI)
- 跨仓 jhyy_OS:`[../../../jhyy_OS/docs/v0.0.5-binary-format.md](../../../jhyy_OS/docs/v0.0.5-binary-format.md)`(OS loader)
- v4 概念:`project_v4_post_merge_axis` memory
- D-locks:D13/D16/D20/D22/D23/D40/D41/D-GUI-12/D42/D43(per spec § 16)
- 设计决策:`[../../roadmap/v2-v3-parallel-sprint-plan.md § 6](../../roadmap/v2-v3-parallel-sprint-plan.md)`(自写后端 vs QBE post-process 决策)
