# v4.9.0 plan — 3n 包管理器 (jhyy new / build / test)

## Context

**状态**: ⏳ **未启动**(per `v3.x-language-expansion.md § Sprint 3n`,per 2026-09-06 决定推到 v4.x)。

**上游依赖**:
- ✅ v3.2.2 (3l.1 std lib 基础)ship
- ✅ v3.2.3 (3l.2 std lib IO)ship
- ✅ v4.0.0 (v2+v3 merge)ship

**触发前置 / OS 关系**:
- UX 改进(让用户写项目更方便)
- 不阻塞 MVP / OS 启动

**Worktree 约定**:v4.x 串行主轴,`main` direct commit + tag `v4.9.0`。

## Scope

### 改动
| 改动 | 文件 | 估算 |
|------|------|------|
| 新 CLI 子命令 `jhyy new <name>` | `compiler/src0/main.jhyy` | ~80 行 |
| 新 CLI 子命令 `jhyy build`(读 `jhyy.toml`)| `compiler/src0/main.jhyy` | ~150 行 |
| 新 CLI 子命令 `jhyy test` | `compiler/src0/main.jhyy` | ~100 行 |
| `jhyy.toml` TOML 解析 | `compiler/src0/toml.jhyy`(新,依赖 std lib)| ~200 行 |
| 依赖管理:`[dependencies]` 解析 + 路径/版本解析 | `compiler/src0/manifest.jhyy`(新) | ~200 行 |
| 项目骨架模板(`jhyy new` 创建目录 + jhyy.toml + src/main.jhyy) | `compiler/templates/`(新目录)| ~50 行 |

### 新建
- `tests/pkg_new.jhyy` + `pkg_build.jhyy` + `pkg_test.jhyy` + `pkg_deps.jhyy`(~200 行 + SKIP)
- `docs/abis/jhyy-pkg-manager-spec-v4.9.0.md`(~250 行)— jhyy.toml spec + 依赖解析规则
- `docs/logs/v4/changelog-v4.9.md`(append 段)

## 关键决策

- **M0 简化:依赖仅 git URL + 版本 tag(不做 registry)** — 自建 registry 留 v4.x 末
- **M0 简化:不做 lockfile**(留 v4.x 中)
- **M0 简化:不做 virtual workspace / multi-package**(留 v4.x 中)
- **M0 简化:不做 publish / package 上传**(留 v4.x 末)
- **不开新 worktree**

## 验收 / 5/5 ship gate

- 4+ 包管理测试 5/5 PASS(pkg_new / pkg_build / pkg_test / pkg_deps)
- D43 re-baseline → **N24**
- `regress.py --all` → 2/2 gated PASS
- `jhyy_selfhost_check` byte-equal hold

## 风险 + 缓解

| 风险 | 影响 | 修复 |
|------|------|------|
| TOML 解析复杂度 | ship 推迟 | 复用 std lib 实现 TOML;不引入第三方 TOML 库 |
| git 依赖 clone 网络问题 | 离线 fail | 支持 path 依赖(本地目录);git 依赖失败给清晰错误 |
| 多版本依赖冲突 | dep resolve 复杂度 | M0 仅支持单一版本(latest tag);冲突留 v4.x 中 |

## Out of scope

- 自建 registry(留 v4.x 末)
- lockfile(留 v4.x 中)
- virtual workspace(留 v4.x 中)
- publish(留 v4.x 末)
- binary distribution(留 v4.x 末)
- build profiles / dev-deps(留 v4.x 中)
- workspace-level CLI flags(留 v4.x 中)

## Commit / tag 节奏

- **Commit 1**:`feat(pkg): add jhyy new/build/test CLI + jhyy.toml manifest (3n)`
- **Tag**:`v4.9.0`

## Cross-ref

- 设计草案:`[../v3/roadmap/v3.x-language-expansion.md § Sprint 3n](../../roadmap/v3.x-language-expansion.md)`
- 上游:`v3.2.2-plan.md`(std lib mem/fmt/string)+ `v3.2.3-plan.md`(std lib io/os)
- v4 概念:`project_v4_post_merge_axis` memory
