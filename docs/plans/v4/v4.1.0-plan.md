# v4.1.0 plan — M5 (删 src/*.c + untrack QBE + 删 runtime.c)

## Context

**状态**: ⏳ **未启动**(per 2026-09-06 user 决定:从 v2/v3 后 → v4.x)。

**M5 deferral 历史**(per `[../v1/v1.x-phase-4-m5-boot-from-scratch.md](../v1/v1.x-phase-4-m5-boot-from-scratch.md)`):
- 2026-08-14 user 决定:v1.5 installer ship 不触发 M5,M5 推迟
- 原计划 "v2/v3 后 sprint" → **升级为 v4.x sprint**(per 2026-09-06 user)
- v2.x 末 QBE 自写 + v3.x 末 runtime 重写完成后,统一砍 QBE + runtime.c

**上游依赖**:
- ✅ v2.x 末(V2-C `v2.8.0` ship)— QBE 自写后端全 target 覆盖 + QBE 工具链移除
- ✅ v3.x 末(`v3.3.0` ship)— language features 完整 + std lib 完整
- ✅ v4.0.0 (v2+v3 merge)ship — **强前置**

**触发前置 / OS 关系**:
- v4.0.0 merge 完毕后立刻推(per 2026-09-06 user)
- M5 = "jhyy 编 jhyy" 0 C 依赖闭环;自举 OS 真闭环前提

**Worktree 约定**:
- **v4.x 主轴串行**,post-merge 在 `main` 上直接 commit + tag
- **不开新 worktree**(per `project_v4_post_merge_axis` + 2026-09-06 user)

## Scope

### Part 1:删 `compiler/src/*.c`(C 端编译器源码)
- 文件清单(per `architecture.md`):
  - `compiler/src/main.c` + `compiler/src/lexer.c` + `compiler/src/parser.c` + `compiler/src/codegen.c` 等(具体清单 `git ls-files compiler/src/`)
- `git rm -r compiler/src/`
- 影响:`stage0` bootstrap 路径移除(纯 jhyy 编 jhyy 闭环)
- 更新:
  - `scripts/dev/build/build-jhyy.sh`:去 stage0 C 编译阶段
  - `docs/internal/build.md`:全 jhyy bootstrap 路径
  - `docs/internal/architecture.md`:模块边界图删 C 端

### Part 2:Untrack QBE 源码 + binary
- `compiler/qbe/`(如果还存在 source mirror)→ `git rm -r compiler/qbe/`
- `compiler/build/bin/qbe.exe`(V2-C v2.8.0 已删本地文件,本 part 确认 git tree 也没了)

### Part 3:删 `compiler/runtime/runtime.c` + 配套 C runtime
- C runtime helpers(memcpy / memset / printf / exit / etc.)
- 替换:`std/*.jhyy`(v3.2.2..v3.2.5 std lib)+ `unsafe` block + FFI(必要时)
- 注意:std lib 已 ship(v3.2.2 / v3.2.3 ship 后);M5 仅做删除

### Part 4:工具链自含闭环验证
- `jhyy.exe` 编 `src0/*.jhyy` → `.il` → `as` → `gcc` → `.exe`,**无 C runtime 依赖**
- `jhyy 编 jhyy` 全闭环:用 v4.0.0 编出的 jhyy.exe 编 src0/ 产出 v4.1.0;再用 v4.1.0 编同样 → `.il` byte-equal
- `tools/check_dangling.py` 0 dangling

### Key files
- `compiler/src/*.c`(删,~5000+ 行)
- `compiler/qbe/`(删,如果有)
- `compiler/runtime/runtime.c` + `compiler/runtime/crt0*.{c,S}`(删/转 jhyy)
- `scripts/dev/build/build-jhyy.sh`(改:去 stage0 C 编译)
- `docs/internal/build.md`(改:全 jhyy bootstrap 路径,~80 行)
- `docs/internal/architecture.md`(改:模块边界图,~50 行)
- `docs/logs/v4/changelog-v4.1.0.md`(新 umbrella,~200 行)

## 关键决策

- **M5 是"删除"而非"重写"**:实现代码已迁移到 jhyy(v3 std lib)或自写后端(v2 self-written);M5 仅删遗留 C
- **工具链自含依赖 MinGW `as` + `gcc`**:**不**重打包 MSYS2(用户已装)
- **`unsafe` block 必要时用于 FFI**:std lib `os.jhyy` / `math.jhyy` 已 ship;运行时 helpers 走 unsafe + FFI Win32 / POSIX
- **不开新 worktree**

## 验收 / ship gate

- `compiler/src/` + `compiler/qbe/` + `compiler/runtime/` C 端全删(grep `\.c$` in compiler/ → 0 hit,排除 std lib 自身 jhyy 实现)
- `jhyy 编 jhyy` 全闭环:`v4.0.0 编 src0 → v4.1.0 编 src0 → .il byte-equal`
- regress.py 104/104 PASS + 新 std lib test 全 PASS
- `jhyy_selfhost_check` N=3+ byte-equal hold
- `tools/check_dangling.py` 0 dangling
- 工具链 procmon / strace 验证 `jhyy.exe` 不 spawn `qbe.exe` + 不 link `runtime.c`(V2-C v2.8.0 已验证,本 sprint 复测)
- umbrella `changelog-v4.1.0.md` 写 N=3 sha + 删除步骤 + 工具链闭环

## 风险 + 缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 删 `compiler/src/*.c` 触发 build 系统崩溃 | ship fail | 先 dry-run 跑 `make -n` 验证无引用;保留 1 commit 恢复能力(per `feedback_revert_uncommitted_only_if_confirmed` 不可逆操作前必列清单确认)|
| 删 `runtime.c` 触发 std lib 漏覆盖 | runtime crash | regress 104/104 + std lib test 全过;失败时 hotfix 进 v4.1.x patch |
| `unsafe` FFI 漏写某个 C runtime helper | runtime 缺失 | std lib 已 ship 覆盖核心 helper;**不**在 M5 scope 补 std lib,留 v4.x 中 |
| `jhyy 编 jhyy` byte-equal fail | N 代 fixed point fail | revert 删 C action,回滚 commit;逐文件删逐步验算 |

## Out of scope

- 性能优化(留 post-v4.x)
- ARM / RISC-V target(留 v4.x 中)
- 跨平台 std lib 增强(留 v4.x 中)
- M5 deferral 决策 doc `v1.x-phase-4-m5-boot-from-scratch.md`(保留为历史决策文件,不动)

## Commit / tag 节奏

- **Commit 1**:`refactor(toolchain): delete compiler/src/*.c (C 端编译器源码)`(Part 1)
- **Commit 2**:`chore(qbe): untrack compiler/qbe/(如果存在)`(Part 2)
- **Commit 3**:`refactor(runtime): delete compiler/runtime/runtime.c + C runtime helpers`(Part 3)
- **Commit 4**:`chore(verify): full jhyy-selfhost closure + tools/check_dangling 0 hit`(Part 4 验证)
- **Commit 5**:`docs: v4.1.0 M5 ship + jhyy 编 jhyy 0 C 依赖闭环`(umbrella changelog)
- **Tag**:`v4.1.0`

## Cross-ref

- M5 deferral 决策:`[../v1/v1.x-phase-4-m5-boot-from-scratch.md](../v1/v1.x-phase-4-m5-boot-from-scratch.md)`(2026-08-14 推迟)
- v4 concept:`project_v4_post_merge_axis` memory
- 上游:`v4.0.0-plan.md`(v2+v3 merge ship)
- QBE 移除:`[../v2/batch-V2-C-plan.md § Part 2](../v2/batch-V2-C-plan.md)`
- std lib:`[../v3/v3.2.2-plan.md](../v3/v3.2.2-plan.md)`..`v3.2.5-plan.md`
- 安全 revert:`feedback_revert_uncommitted_only_if_confirmed` memory
